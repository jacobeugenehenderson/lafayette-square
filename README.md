# Lafayette Square

3D neighborhood visualization of Lafayette Square, St. Louis.

**Live site:** https://lafayette-square.com

---

## ⭐ START HERE — what's worked out, by topic (read this FIRST)

> **The settled-state index.** Before diagnosing, deciding, or building *anything*, read this so you build on what's already worked out instead of re-deriving it (re-deriving settled doctrine is the recurring, expensive mistake). Each row = the **settled conclusion** + its **one authoritative home**. The full doc landscape is the "Documentation map" further down; this is the topic-first orientation. *(Agents: the **universal first read is [`ORIENTATION.md`](ORIENTATION.md)** (root) — the plain-language mental model + settled doctrine; THEN this topic index, THEN the topic canon it names. That one order, every session. `BOZ.md` is the coordinator's doc — summoned only when you're Boz, never a step in the universal path.)*

> ▶ **What's *left* (as opposed to what's *settled*): [`ROADMAP.md`](ROADMAP.md)** — the one cross-domain master of all remaining work, ranked and pruned to zero (the home-stretch instrument, 2026-07-17). This START-HERE index is settled-state *per topic*; ROADMAP is the *whole-board punch-list* we prune against. Frame: two columns to done (the SHAPE finish + the tree impostor/placement scheme = "works on a phone") + a security close-out, then the cleanup tail toward human-developer engagement, then the approaching horizon.

> ▶ **What's *checkable* (as opposed to what's settled or what's left): [`PIPELINE-CLAIMS.md`](PIPELINE-CLAIMS.md)** — the **dead-end → corner → sidewalk-arrangement** chain stated as claims that can be **shown false**, each carrying the method that produced it and an *earned* confidence mark. Read it before proposing anything on that defect family; it separates **`[REQ]`** (what must be true) from **`[OUR]`** (the answer we happen to have chosen), and §9 re-runs every number. *(Narrow by design; the frozen whole-pipeline baseline is `cartograph/_archive/PIPELINE-CLAIMS-full-manifest-2026-08-02.md` — a drift-diff target, ⛔ never cited as true.)*

**Pipeline order (Jacob's):** `intake → skeleton → prebake → survey → ⟦WALL⟧ → section → bake → stage`. The open **SHAPE frontier is skeleton + survey**; **intake now has its operator front** — the Extent tool: author the **inclusion polygon** + one-click Bake (2026-07-14, `ExtentApp.jsx` + `BezierPen.jsx`; boundary model corrected 2026-07-20; committed on trunk, not yet in prod; see the row below + the feature index). **Altadena is the first hood poured fully end-to-end through it.** Recent sessions have also run on the downstream **look / stage / publish** side (TOD day-cycle, per-shot look cascade, the Preview publish gate). Trunk = `curb-offset-draw` (→ staging via `staging.yml`; the old `cartograph-looks-pass-ab` trunk was retired 2026-07-08).

> ⭐ **Strategic (2026-06-13, Jacob):** the remaining fixes are most likely **in the skeleton**, not the render. The drawing/offset rules are largely straightforward; the sophistication we still owe is in **how we interpret OSM and build the frame**. So the default suspicion on any defect is *"the fix is upstream, in the skeleton"* — confirm that before patching downstream. (The 2026-06-13 day was lost patching the polygon for a curve whose real fix was a frame-level corner-round.)

| Topic | Settled conclusion (don't re-derive) | Authoritative home | ⭐ Owns — knobs · data keys · code (route by SYMPTOM to here) |
|---|---|---|---|
| **Extent / intake tool + the Pour** | ⭐ **What the Extent tool MAKES is the SKELETON, served** — not the boundary disc. And the skeleton is really a **labeled point cloud**: points each carrying a stamp (their node kind), joined by segments only so a human can see it and so we have something to stroke. Identity lives on the points. Flow: `◎ Extent` (`ExtentApp.jsx`) — **search a PLACE** (sizes the fetch envelope, +1000 m and squared, because a circle can't fit a rectangle narrower than its diameter) → **SOFT fetch** (generous *because* it's cheap) → **author the BOUNDARY** (gazetteer ring · click boundary streets · draw) → exclusions + per-building overrides as *correction* → **HARD fetch = the SEAL** (freezes the soft fetch, bakes the skeleton) → **Bake**. ⭐ **Membership = `((polygon − exclusions) ∪ activate) − hide`** — the polygon DECIDES, the disc RENDERS. ⭐ **Sizes nest hood < disc < bb, on TWO independent centers:** the bb/frame origin is frozen at the seal and may **grow or destructively shrink but NEVER move** (moving it re-orders identity — the content-death mode); the disc centroid is a separate **draggable** value. bb = radius + ~20–25% = the "forever zone." ⭐ **The seal IS the identity registry** — after it, a fetch ordinal is a legitimate permanent key and augmentation is strictly append. ⛔ The 2026-07-14 "excluder pen / the circle IS the boundary, settled, do not re-open" model is **RETRACTED** (it cost two agent passes and 147 Księży Młyn buildings). ⛔ **Pouring a 2nd scene CLOBBERS production LS** until the `src/data/*` name-imports are retired — LS *is* the fallback (`ARCHITECTURE §Extent`). | ⭐ **`EXTENT-DESIGN.md`** (design of record) · `EXTENT-EXCAVATION.md` (evidence) · `cartograph/INTAKE.md §0.5` (as-built) · `cartograph/OPERATIONS.md §Extent` (manual) · `cartograph/ARCHITECTURE.md §Extent` | knobs: search place · SOFT fetch radius · boundary polygon · exclusion loops · per-building activate/hide · Bake. data: `data/<scene>/neighborhood_boundary.json` (`boundary`, `polygon`, `exclusions`). code: `src/cartograph/ExtentApp.jsx` · `BezierPen.jsx` · membership applied in `cartograph/pipeline.js` |
| **Altadena / terrain + landscape** ⭐NEW | **A CDP is 69× LS's area, and almost every constant in the kit is LS-tuned.** ▶ **Load: ~180 s → ~18 s** (figure-ground deleted at T4 — it was 95% of the CPU and drew *nothing*; plus a `buildTileGround` race worth ~80 s of discarded work, and a 4× rebuild). ▶ **Terrain doctrine CHANGED (Jacob, 2026-07-15): a neighborhood must NOT be poured flat** — elevation bakes between the Design tools and Stage (which is where `bake-terrain` already runs; Altadena was flat only because nobody had fetched its DEM — now fetched: USGS `n35w119` via the S3 mirror). ⛔ **BLOCKED:** baking Altadena's ground *with* terrain = **26.3M tris / 457 MB / 88 min** — the adaptive refine is right, its constants (`TOL=0.50`, `MIN_EDGE=6`) are LS-tuned. ▶ **Landscape = a STAGE set-piece**, opted into by `design.landscape.source`; it must never be auto-detected from files, defaults, *or* the bake (all three leaked; all three closed). The Stage upload flow that SETS `source` is **unbuilt**. | **`HANDOFF-altadena-pour.md`** (live state + the open tail) · `DESIGNER-LOAD-FORENSIC.md` (the method; superseded in part) | knobs: `design.landscape.source` (opt-in ONLY — never auto-detected) · adaptive-refine `TOL` / `MIN_EDGE` (LS-tuned) · DEM fetch. data: `data/<scene>/clean/elevation.json`. code: `bake-terrain` |
| **Building membership + render ledger** | Membership = **`((polygon − exclusions) ∪ activate) − hide`** (2026-07-20) — the **polygon decides, the disc renders**, and the disc is only the fallback when a scene has no polygon (so excluder-era hoods bake byte-identical). *(Supersedes the 2026-07-14 excluder model; see `INTAKE §0.5`, retracted.)* The pen carves strays as a **correction**; **"Edit buildings"** toggle-ghosts a single footprint IN/OUT (`{activate,hide}` per scene), all applied in `pipeline.js` so **`map.json` (2D Designer) + the bake both inherit it** — one filtered source. Buildings load from a per-scene **render ledger** (`data/<scene>/buildings.json`), retiring the LS `bake-buildings.js` source hardwire (LS bakes byte-identical; "LS is a look at the render level"). | **`NEIGHBORHOOD-INPUTS.md §5.2` / `§5.1`** (+ OPERATIONS §Extent · PIPELINE/BAKE wiring); brief archived → `cartograph/_archive/handoffs/HANDOFF-building-roster-editor-2026-07-05.md` | data: render ledger `data/<scene>/buildings.json` · `nb.polygon` / `nb.exclusions` · per-scene `{activate, hide}`. code: filtered once in `cartograph/pipeline.js` so `map.json` + the bake inherit one source · `bake-buildings.js` |
| **Skeleton** | "The Skeleton is The First Bake" → polygon-ready frame. Centerline over-densification **FIXED** (junction-protected RDP, `smooth=0`). Remaining debt is the **missing across-intersection organ** (doglegs + degenerate corners) — at the intersections, not the lines. ⛔ never conflate clean-lines with clean-corners; junction-protected always; two-carriageway model locked. | **`SKELETON.md`** (deep) · `PIPELINE.md §skeleton` | knobs: `RDP_EPS` / `RDP_EPS_LOOP` (junction-protected) · carriageway gates · `continuesAs`. data: `data/<scene>/clean/skeleton.json`. code: `cartograph/skeleton.js` |
| **Prebake** | `skeleton → pipeline.js → promote-ribbons.js → ribbons.json` (First Bake). **Survey** renders **live** from `ribbons.json` via `buildTileGround`; ⛔ **Section, Measure and the neutral Design view do NOT** — they consume the baked `shape.json`, so a re-bake *does* change what those three show. | `PIPELINE.md §prebake` | data: **`src/data/ribbons.json`** — `streets[]` · `tiles[]` · `junctionMap` · `medians` · `faces` · `corridors`. per-street: `measure` · `segmentMeasures` · `capEnds` · `anchor` · `innerSign` · `pairId` · `phase` · `gradeSeparated`. code: `cartograph/derive.js` → `promote-ribbons.js` |
| **Survey** | ✅ **WORKS AS EXPECTED — and ALL CURBS WORK (Jacob, 2026-08-06).** ⛔ When a ped/sidewalk defect appears, it is **not** this row — go to Section. **SHAPE only — NO ped depth** (ped = Section). Smoothing/caps/anchor/corner-R/curb. live==bake. ⚠️ **Two revert scopes, two panels, and no "revert everything":** Survey's *Revert to Default* is **disabled on all seven scenes** (`surveyDefault` is `null` everywhere — ★ Set Default has never been pressed), so the working gesture is **"Revert to Skeleton" (⤓)**; Section's *Revert to Default* clears only the ped fields. Clearing one leaves the other live — verify with `node scratch/claims-scene-at-default.mjs`, never by belief. | `PIPELINE.md §survey` + `ARCHITECTURE.md §2.1` | ⭐ **ROAD WIDTH IS AUTHORED HERE** — a curb kink on a straight street is THIS row, not geometry. knobs: asphalt-edge drag (pavement half-width) · corner-R · caps · anchor · **edit scope whole-street ↔ per-block** (`measureMode`) · **symmetric ↔ asymmetric** (`editSidesSeparately`). data: `blockCustoms[skelId][side][segOrd].{pavementHW, treelawn, sidewalk, terminal, materials}` in `public/looks/<look>/design.json`. code: `SurveyorOverlay.jsx` (the drag + the WRITE) · `SurveyorPanel.jsx` (scope buttons) · `measureModel.js` |
| **WALL** | The freeze after Survey — chains die here; downstream is a pure consumer. *Should* be at P2 (standing debt). First diagnostic: "is this chains again?" ⭐**NEW 2026-07-25 — the wall we have is a HANDLE rule, not a CONTENT rule.** `sectionOpen` takes no chain reference, so nothing can *reach back* — but that never checked whether the artifact **IS** a chain, and at a dead end it is: the face freeze walks the spur out and back over the same vertices, so **ALL 50 LS dead-end tips are zero-width slits** (40 look right only because a FILL-layer patch pries them apart). ⛔ **Read the tip off the frozen `cap.vertexIdx`, never a FILL run's span end** — they are different vertices, hundreds of metres apart on a long block, and the span-end probe under-counts. Jacob's rule: **POLYGONS ONLY by the Section tools** — content checks at the freeze, failing the bake. ⭐ `detectTileCaps` is a **slit detector** wearing a cap detector's name; `tile.caps` is a registry of the places the freeze failed to close. ⛔ Do NOT address around it (a walk-ordinal key was built and retired). ⭐**NEW 2026-07-30 — HALF BUILT, FLAG OFF: assert the spur BEFORE polygonization** (`spurOutline.js`; Jacob: *"the slit needs to be asserted before the polygonization"*). Lay the dead end down as a open U (two curbs + a cap) (curb·cap·curb), THEN walk the faces — nothing doubles back, the block closes around a real notch, and the two curbs land road-width apart = **the second mouth corner, created not detected**. Measured: slit rings **50→9**, caps **50→7**, 41 notch faces, **blocks stay 101** (whole-map punch-out moves 25). **FILL half DONE 2026-07-31** — the frozen edge carries `atCurb` and the block's asphalt inset is zero there, so the notch is the road and nothing paints it twice: spurs at full width **43/52 → 45/52** (A/B, same probe), two fixed, none lost; a spur whose notch fails to close is **rolled back** rather than shipped (that is the 2026-06 failure mode, and it bit 2 spurs). ⭐ **Junction band FIXED and net BETTER than baseline: 101 → 110 clean, 66 → 58 flagged** — the regression was a **datum change read as a street corner** (curb edge meets centreline edge ⇒ `a !== b` bid a fillet and shattered the ped band); suppress the corner + zero the fillet at that seam. 2 junctions still lose clean status, both marginal. ⛔⛔ **TRIED AND REVERTED 2026-07-31 (`7b5b87a3`); `SPUR_OUTLINE` is gone from the code — BUT THE "WORSE" VERDICT IS NOT SAFE TO LEAN ON.** ⭐⭐ **THE MECHANISM (named 2026-08-06, Jacob): Jacob was looking at `lafayette-square` while the work was on `lafayette-square-staging`, and neither party knew for the whole day.** Not a camera angle — **a different map**: overlay-authored streets **52 vs 177**, tiles 101 vs 116. ⛔ **A0 is UNDECIDED — the revert is not proof the construction fails, and this is not licence to re-land it. Look again, on the right scene.** ⚠️ The older reading — *"these gates do not predict the eye"* — **over-read a verdict taken on the other map**; the probes are neither vindicated nor discredited. ⭐⭐ **The rule that generalizes: AN EYE VERDICT MUST RECORD THE SCENE IT WAS TAKEN ON**, as strictly as a measurement records its authoring state — and ⛔ **the deliverable is not discipline: the tool must make the live scene unmissable**, because this is a Layer 0 silent substitution in the authoring surface itself (`ROADMAP A0`). ⭐ The arc surfaced a bigger defect: the committed `ribbons.json` does not reproduce from a fresh `pipeline.js` run (233 vs 228 junction nodes) — **the pipeline is not reproducible against its own committed output.** → `PREBAKE §4.0a` | `PIPELINE.md §Wall` · **rule: `POLYGON-FIRST.md §2.1`** · **live task: `_handoffs/HANDOFF-deadend-face-resolution.md`** · `PREBAKE.md §4.0` | data: `tiles[].caps` (a registry of places the freeze FAILED to close) · `public/baked/<look>/shape.json` (frozen — invisible until `scratch/rebake-shape.mjs`). code: the `sectionOpen` signature (chain-free by construction) · the D2 freeze in `derive.js`. gate: `POLYGON-FIRST §2.1` Checks 1–5 |
| **Corners / thorns** | Tile corner = band *bent*, never a constructed primitive (`RIBBONS §1` invariants). Roots, by layer: **divided false corner** = corner-builder pairs the carriageway *stubs* not the corridor outer-edge legs → fixed by cornering the corridor as one road (LANDED `9c275ce`, `SKELETON §5e`) · **width-step dogleg** = a per-fe `pavementHW` step at a through-node, a **datum-data** defect (drop the deviating override; `SKELETON §5a/§5g`) · **through-node ribbon break (the tabled "T-artifact"** — McKay×Hickory ≡ Kennett×S18th**)** = at a straight deg-3 T the mouth splits the through-frontage across 2 tiles, tile-local `isThrough` can't fire, so the through-run **bids a FALSE ADA corner against the stem** → the corner machinery shatters the treelawn/sidewalk into slivers. **CURE ATTEMPTED, NOT fixed on the eye (2026-07-16):** a frozen `thruNodeEnds` marker suppresses the false corner, but it keys the **wrong run** — marks the *interior-vertex* street as "through," which picks the **side street** when the through-street is split into 2 skelIds at the node (Mackay), and misses Kennett (node-coord mismatch). Proxy (live sliver-count) masked it; the defect is on the frozen `sectionOpen` render (`proxy ≠ eye`). Re-fix owed: correct through-street ID incl. split-skelId + coord match; verify on the frozen render (`scratch/thrunode-frozen-verify.mjs`). FRONT A1; detail `scratch/THRUNODE-GATE-LANDING.md` · **thin-tile thorn (G12)** = inward offset folds past the medial axis → the **LOCAL** capacity clamp (`HANDOFF-band-fold-fix.md`, still open; a *different* defect from the through-node break). ⛔ intersection-consolidation / `osm2streets` was the *wrong task* (retired 2026-06-13); ⛔ the through-node break is NOT a FILL patch (that class was tried + reverted — `THROAT-JUNCTION-FINDINGS.md`). | **`RIBBONS.md §1/§4`** · `SKELETON.md §5` · `THRUNODE-GATE-FINDINGS.md` · `HANDOFF-band-fold-fix.md` | knobs: corner-R (`vertR`) · `curbWidth`. data: `junctionMap.nodes[].corners.{outer, apex, stub}` — divided-construction bookkeeping, and ⛔ **the only registry `tileGround.js` sees**. ⛔ `corners.all` does **not** exist — it went out with `7b5b87a3` and has 0 occurrences repo-wide (`POLYGON-FIRST §1`). code: `tileGround.js` `cornerAt` · `filletRing` · `offsetRingVariable` · E3.3 corner construction. debug: `CORNER_DUMP=1` |
| **Divided roads** | Carriageways stay 2 chains; **D1 longitudinal weld + station-overlap gate LANDED**; median = emergent geometric face (`extractFaces`), tagging is downstream. **Truman south-of-Park = separate D3/D8** (one-sided cross-streets). | `RIBBONS.md §3.1` · forensic (archived): `cartograph/_archive/TRUMAN-FORENSICS.md` | data: `phase.{role, pairKey, spineAtStart, spineAtEnd}` · `pairId` · `innerSign` (⛔ UNRELIABLE — use `inboardSideOf`) · `anchor:'inner-edge'` · `medians[]`. code: `skeleton.js` carriageway gates · `derive.js` E2 · `inboardSideOf` in `tileGround.js` |
| **Grade separation** | LANDED — `gradeSeparated` excluded from faces, stroked as flat asphalt, own layer, renders behind local. | `PIPELINE.md §Wall` | data: `gradeSeparated` per street. code: excluded from the face-walk input; stroked flat on its own layer |
| **The construction model** | **TILE model** (`src/lib/tileGround.js`): tiles = faces of the centerline graph; the centerlines are the grout; strips painted *inward*; curb = concentric offset; corner = band bent. **It is now the ONLY construction** — figure-ground's geometry was **deleted at T4** (2026-07-15): the unreachable render branch, `buildChainBandsLive`, `emitOneBlockRingBands`, and every band emitter (−1,900 lines). It had cost 285 s of Altadena's 320 s Designer load while drawing nothing. What survives of `buildBlockGeometryV2` is **only the frontage-edge identity builder** (`feCustomKey` = the blockCustoms key) that Survey/Measure read; the name is vestigial. **Open: T3** — migrate that fe identity onto the tile `runs` (which already carry the same `[skelId, side, segOrd]` triple, `tileGround.js:935`), then the file dies. ⚠️ T3 must prove key parity first or authored customs silently orphan. | **`RIBBONS.md`** (tile model, v1.0) · `DESIGNER-LOAD-FORENSIC.md` (the budget) · `cartograph/_archive/RIBBONS-figureground-emitter-2026-06-15.md` (retired) | code: **`src/lib/tileGround.js` `buildTileGround`** — the ONLY construction. `buildBlockGeometryV2.js` survives as the frontage-edge IDENTITY builder only (`feCustomKey`), name vestigial |
| **The derivation chain** ⭐ | **The centerline is the ROOT source.** centerline → polygon → ribbon. The polygon is BOTH the geometry source AND the **identity** source (leg / corner / treelawn-vs-sidewalk all read off it), so a rough centerline corrupts *identity*, not just shape. **Fix at the centerline first, at its source** — patching the polygon/ribbon on a rough centerline is editing a shadow. Corollary: polygon moves but centerline doesn't ⇒ wrong layer. | **`RIBBONS.md §1`** (the Derivation Chain doctrine) · **`SKELETON.md §3.5`** (the concrete frame→render flow + where the curve-fit lives) · `HANDOFF-curve-primitive-skeleton.md` Law 1 | no knobs — a diagnostic. Order: centerline → polygon → ribbon. ⛔ polygon moves but centerline doesn't ⇒ you are at the wrong layer, go up |
| **Section** | ⛔⛔ **THE BROKEN TOOL (Jacob): "Survey works as expected; it is the Measure/Section customs tool that doesn't." ALL CURBS WORK.** ⭐ **ROOT — the V1 keystone working as DESIGNED, with one step not honoured** (`SECTION §7`). ⛔ **The construction is not new and the corner doctrine is settled — read the design record first:** `_archive/RIBBONS-figureground-emitter-2026-06-15.md` (`emitOneBlockRingBands`, 2026-05-29 — *"three inward Clipper offsets… 2 strips + sector slicing; the corner is the `fullBand` slice"*, the same construction `sectionPassTile` builds today) · `_archive/RIBBONS-history-2026-06-12.md §6.9` (AASHTO/ADA corner doctrine) + **`§7`** (the 13-month corner saga, every attempt with its lesson). **Mechanism:** `fullBand` is one continuous ribbon off the frozen `iA` (`tileGround.js:1301`); every strip boundary is a concentric offset of `iA`; the per-run sector (`:1450`) does its designed job — **slicing for material tags** — so it carries a **label, not a geometry**. ⭐⭐ **The deviation:** doctrine `§6.9`.4 says *"both legs stop at tA/tB; the corner ribbon **takes over**"* — the pull-back (`:1416`) is intended, but the takeover is gated (`:1566`) and needs to **find a fillet** (`:1575`). **Step over is unconditional; step back is not** — and band the legs released that no corner claims falls to `luRemainder` (`:1642`) and **renders as land use.** ⇒ the gap is **ribbon painted green**, not missing geometry. ⛔ **The FILL-PATCH class is CLOSED** — clamp, wrap, re-key and snap were each built and reverted. **Cure:** honour doctrine 4 — ring ownership is a **partition**; a corner with no fillet still owns its arc (a square corner is a band slice too). ⛔ **Width adjustment already maintains the mono-width band** — the envelope is `cw + max(TL) + max(SW)` per TILE (`:1286`), so the partition runs purely *along the ring* and never touches the envelope. ⭐ **Clamps are settled, don't re-open:** `§6.9`.5 (*self-intersection is signal, not error*) governs **meaningful** degeneracy (authored R shrinking the pad); `W` past the medial axis inverts the offset and floods the block, and the live `:1198` guard is a **topological capacity guard, not a doctrine clamp** — `[[feedback_no_corner_radius_clamps_in_emit]]`. ▶ Rest: per-edge FILL, bent corner, caps, revert UI all built; perf/D6d + Measure→Section rename remain. **Freeze the silhouette, author the FILL live.** ⛔ **The dead-end tail is NOT a Section defect:** unresponsive legs, the leg-flip swap and the mouth co-claim are ONE **upstream** fault — the freeze never closes a polygon at a dead end. Fix at prebake; do not re-key, re-fan, or re-patch in Section. | **`SECTION.md`** (deep, the SSOT) · `_handoffs/HANDOFF-deadend-face-resolution.md` | knobs: treelawn / sidewalk depths · materials `{outer, inner}` · ADA pad · cap wrap · revert. data: the ped side of the SAME `blockCustoms` keys as Survey. code: `sectionPass` / `sectionOpen` in `tileGround.js` · `MeasurePanel.jsx` · `MeasureOverlay.jsx` |
| **Preview** | The **publish-confidence gate**: renders production's exact tree (parity) + the inspection toolkit. **Mirrors the Look; AUTHORS deployment policy** (per-platform inclusion manifest) at the gate — scoped reversal of "Preview never writes" (2026-06-17, "we've grown"). ▶ In flight: the v0.2 measurement regime — virtual-device emulator + device-budget gauges + thermal/memory/transition axes (real GPU downclock is impossible in-browser; emulate via mobile-render-path + supersample strain). | **`PREVIEW.md`** (Reference) · `HANDOFF-preview-measurement.md` (arc) | knobs: per-platform inclusion manifest (Preview AUTHORS it) · device budget gauges. code: the Preview app; renders production's exact tree |
| **Density — lamps & trees** ⭐NEW | ⭐ **A census is the UNION of its wells**, deduped by proximity — never whichever well was found first. Jacob: *"I'd rather have more lamps and trees, since they're vibes."* A source-selection `if/else` is the bug: it makes every other well invisible and nothing errors. **Lamps** = OSM fetch ∪ authored well (dedupe 4 m) — LS's 80 authored **park** lamps had silently stopped baking the day LS gained an OSM fetch (unreachable branch), so the park ran on ~35 incidental lamps while the total *rose* 80→536. **Trees** are thinned by two gates of very different size: **hardscape paint** 1707 (25%, not a knob — surveyed trees get *nudged*) vs **the LU allow-model** 637 (9.4% — `parking`/`commercial`/`unknown`), which IS the dial: per-scene `data/<scene>/lu-policy.json`, no kit edit. | **`cartograph/BAKE.md §4.5/§4.6`** · `cartograph/OPERATIONS.md §Density` · harness `scratch/tree-lu-exclusion-census.mjs` · `cartograph/lu-policy.mjs` | knobs: **`data/<scene>/lu-policy.json`** (the tree dial — per scene, no kit edit). data: lamp + tree wells, unioned then deduped. code: `cartograph/lu-policy.mjs` · `BAKE.md §4.5/§4.6`. harness: `scratch/tree-lu-exclusion-census.mjs` |
| **Doc process** | three kinds (Reference/State/Diary), one-kind-per-doc, content flows downstream as it ages; pack-up/pick-up day cycle. | `BOZ.md §4` (live) · `cartograph/_archive/BOZ-full-2026-08-06.md` (the full three-kinds detail) | no code. three kinds (Reference / State / Diary), one kind per doc; corrections REPLACE the line, the old one goes to the Diary |

---

## Local development

```bash
npm install
npm run dev
```

`npm run dev` launches **everything in one terminal** with prefixed colored logs:

| Prefix | Process | Port | Role |
|---|---|---|---|
| `web`   | `vite`                  | 5173 | Main neighborhood app + helper UIs (Cartograph, Stage, Arborist) |
| `carto` | `cartograph/serve.js`   | 3333 | Cartograph backend: skeleton/overlay I/O, Looks API, bake CLI runner |
| `arb`   | `arborist/serve.js`     | 3334 | Arborist backend: species library, specimen browser API, tree bake CLI runner |
| `met`   | `meteorologist/serve.js`| 3335 | Meteorologist backend: Teapot (clouds) + Almanac (weather rules) I/O, almanac evaluator endpoint |

`Ctrl-C` kills all four. Escape hatches if you want to run one in isolation:

```bash
npm run dev:web         # vite only
npm run dev:cartograph  # cartograph backend only
npm run dev:arborist    # arborist backend only
```

The dev server reads environment variables from `.env` (gitignored):

```
VITE_API_URL=https://script.google.com/macros/s/AKfycbxv3JihCx0U7JfGqle6ZpsLamkRS5PAEGRn6_NaM0Nc7r5zdY7kyctDioScGy8nVcAqWQ/exec
VITE_SUPABASE_URL=https://ngbvgjzrpnfrqmzkqvch.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key>
```

If `VITE_API_URL` is missing, the dev server falls back to in-memory mocks (no real data).
If `VITE_SUPABASE_URL` is missing, the Supabase client returns a safe stub (Cary features inert).

---

## Architecture at a glance

The project is organized as a **public-facing runtime app** plus a small set of **standalone helper apps** that produce the assets the runtime consumes. Each helper publishes one canonical artifact; the runtime composes them.

| Helper | Built in | Publishes | Consumed by |
|---|---|---|---|
| **Cartograph** (`/cartograph`) | `src/cartograph/` + `cartograph/` | `public/baked/<id>/ground.{json,bin}` (+ `shape.json`) | Stage / Preview / The Ward |
| **Stage** (`/stage`)             | `src/stage/` + `src/cartograph/Stage*` | `public/baked/<id>/scene.json` per Look | Runtime scene environment |
| **Arborist** (`/arborist`) — the kit-matcher tree builder ([README §START HERE](arborist/README.md)) | `src/arborist/` + `arborist/` | `public/trees/<id>/…` + per-Look `public/baked/<look>/trees/…` (the slab) | Runtime `InstancedTrees` |
| **Meteorologist** (in Stage)     | `meteorologist/` + `src/cartograph/` (UI inline) + `src/components/Atmosphere.jsx` (planned) | `public/clouds/{presets,almanac}.json` | Runtime `<Atmosphere />` (planned) |

### Data sources (intake → the model)

The neighborhood is **assembled from many open + measured sources**, not one — much of the model is already built on them. Full provenance (endpoints, fetch scripts, what we transform, what's authoritative vs. OSM-default): **[`cartograph/INTAKE.md`](cartograph/INTAKE.md)**.

| Source | Gives us | Authoritative? |
|---|---|---|
| **OpenStreetMap** (Overpass) | street centerlines (geometry), buildings, POIs, ground features, park paths/water, lamps | base — geometry is OSM digitization |
| **Microsoft Global ML Footprints** / **Overture** | building footprints | — |
| **City of St. Louis Open Data** (ArcGIS Assessor) | official **parcels + right-of-way + land-use** | ✅ municipal |
| **`survey.json`** (operator-measured) | custom **street widths** (ROW, pavement half-width, lanes) | ✅ measured |
| **35 curated centerlines** | hand-corrected geometry where OSM was wrong | ✅ ours |
| **Mapillary** | street-level imagery (facade matching) | — |
| **Elevation DEM** · **Park trees / NPS / Wikimedia** | ground elevation; tree inventory, building enrichment, historic narrative | — |

> ⚠️ **Architecture intent:** today many of these are **hardwired into the LS runtime**; ultimately they **all route through the Slab** (`SLAB-CONTRACT.md`) — `sources → Cartograph (intake → skeleton → bake) → ⟦Slab⟧ → LS runtime`. The Slab is the boundary; LS should consume the *Slab*, never the raw sources.

---

## Documentation map

> **This section is the index — find any doc from here without being told where to look.** Docs live *next to their code* and follow **three kinds**, separated by tense (the architecture is codified in [`BOZ.md`](BOZ.md)): **Reference** (how it works / why — eternal-present) · **State** (where we are / what's next — the `BACKLOG` + the `HANDOFF-*.md` future-looking to-do layer) · **Diary** (how we got here — `NOTES` + git). *One kind per doc; content flows downstream as it ages.*
>
> **⭐ Audience note — three readers within Reference:** **FEATURES** = user/investor (*what it is, why it's special* — the brochure). **OPERATIONS** = operator (*here's the panel, here's the knob, here's when to turn it* — the manual; the engineering/"actuarial" counterpoint to FEATURES, **paired with it per domain**). **README · ARCHITECTURE · PIPELINE · RIBBONS** = developer (contract / build+rationale / execution / geometry). So **FEATURES is never subsumed** — distinct audience; engineer-internals migrate FEATURES→ARCHITECTURE, operator-knobs migrate FEATURES→OPERATIONS, keeping FEATURES the clean pitch. *(OPERATIONS is seeded in cartograph; other domains get one as they're touched.)*

**Start here, any session (in this order):**
- **[`ORIENTATION.md`](ORIENTATION.md)** — the **universal first read**: what we're building · the dependency chain · the settled doctrine in plain language. The mental model everything else hangs off (and the read a tech-DD pass needs of *our* understanding).
- **This `README.md`** — the doc index (you're in it): §⭐ START HERE (settled-state by topic), the Documentation map, the cross-cutting feature index ("where does X live"), + dev setup.
- → then the **topic canon** the index names.
- *(**[`BOZ.md`](BOZ.md)** is the **coordinator's doc** — the Boz identity, the librarian Process, the day-cycle. It loads **only when you're summoned as Boz**; it is not a step in the universal path.)*

### Per-domain docs

The project is **four domains** + the runtime, each documented beside its code:

| Domain | Reference | State | Diary |
|---|---|---|---|
| **Cartograph** — map-making toolkit (Designer / Stage / Preview / bake) | [README](cartograph/README.md) · [FEATURES](cartograph/FEATURES.md) *(user/pitch)* · [OPERATIONS](cartograph/OPERATIONS.md) *(operator manual — every knob)* · [ARCHITECTURE](cartograph/ARCHITECTURE.md) · [PIPELINE](cartograph/PIPELINE.md) · **[INTAKE](cartograph/INTAKE.md)** (data provenance — where it all comes from) · **[SKELETON](cartograph/SKELETON.md)** (the frame) · **[PREBAKE](cartograph/PREBAKE.md)** (chains → frozen polygon substrate) · **[SURVEY](cartograph/SURVEY.md)** (the SHAPE tool) · **[WALL](cartograph/WALL.md)** (the freeze; frozen-wrong-data is odious) · **[SECTION](cartograph/SECTION.md)** (the FILL tool) · **[BAKE](cartograph/BAKE.md)** (pour the slab) · **[STAGE](cartograph/STAGE.md)** (the Look tool) · **[PREVIEW](cartograph/PREVIEW.md)** (the publish-confidence gate) · **[POLYGON-FIRST](cartograph/POLYGON-FIRST.md)** (⭐ the doctrine made enforceable — checks, not adjectives) · **[RIBBONS](cartograph/RIBBONS.md)** (geometry canon — read before any ribbon/corner work) · **[LOOP-STREETS](cartograph/LOOP-STREETS.md)** (Benton/Waverly loop canon) | [BACKLOG](cartograph/BACKLOG.md) · [DOC-CODE-COHERENCE](cartograph/DOC-CODE-COHERENCE.md) (the corpse-lie ledger) + the root `HANDOFF-*.md` briefs | [NOTES](cartograph/NOTES.md) · [OSM-FORENSICS](cartograph/OSM-FORENSICS.md) · [RENDER-PATH-CENSUS](cartograph/_archive/RENDER-PATH-CENSUS.md) *(archived)* |
| **LS app** — the consumer surface (place cards, residence, guardians, Cary) | [FEATURES](ls/FEATURES.md) *(user/pitch)* · [OPERATIONS](ls/OPERATIONS.md) *(operator manual)* · [ARCHITECTURE](ls/ARCHITECTURE.md) · **[PLACE-CARDS](ls/PLACE-CARDS.md)** (the Society Pages + enhanced-card data model — every field, the menu/delivery model) · the trust-role specs **[TOWNIES](ls/TOWNIES.md)** (check-in ladder) · **[GUARDIANS](ls/GUARDIANS.md)** (claim + staff perms) · **[RESIDENTS](ls/RESIDENTS.md)** (residence + lobby) · **[QR-CODES](ls/QR-CODES.md)** (the 4 QR types, claim-secret model, the QR Studio) · **[BULLETIN](ls/BULLETIN.md)** (the neighborhood board: posts · comments · DM threads) · **[CARY](ls/CARY.md)** (courier system — app integration; program/legal in `_handoffs/CARY-BRIEF.md` (gitignored, local-only); **submit pipeline in [cary/ORDER-PIPELINE.md](cary/ORDER-PIPELINE.md)**; order→kitchen POS injection in [cary/pos/](cary/pos/README.md)) · **[IDENTITY](ls/IDENTITY.md)** (device hash · handle · avatar) · **[DEVICE-LINK](ls/DEVICE-LINK.md)** (desktop handoff + disconnect) · **[PUBLIC-PAGES](ls/PUBLIC-PAGES.md)** (the hard-wired privacy/terms pages) · **[STREET-VIEW](ls/STREET-VIEW.md)** (the eye-level mode: camera · sky/constellations · trees) · [reference/INVENTORY-DATA](ls/reference/INVENTORY-DATA.md) (every data source) · [INVENTORY-API](ls/reference/INVENTORY-API.md) (every endpoint) · [RUNTIME-DELTA](ls/reference/RUNTIME-DELTA.md) | [STATUS](ls/STATUS.md) (whole-picture section×state map) · [BACKLOG](ls/BACKLOG.md) | — |
| **Arborist** — tree library + bake | ⭐ **[SALON-INTERFACE](SALON-INTERFACE.md)** (the Salon **plate-rack** — the realized rubric-forward front, 2026-06-25) · [FOREST-BUILDER-KIT-MATCHER](scratch/FOREST-BUILDER-KIT-MATCHER.md) (the ratified architecture — **no-cull**; Authored-only, LiDAR/Procedural kept as peers; hero-LOD/DoF **parked**) · [README](arborist/README.md) · [SPEC](arborist/SPEC.md) · [FEATURES](arborist/FEATURES.md) · [ARCHITECTURE](arborist/ARCHITECTURE.md) · [ROSTER-COVERAGE](arborist/ROSTER-COVERAGE.md) | [BACKLOG](arborist/BACKLOG.md) | [NOTES](arborist/NOTES.md) |
| **Meteorologist** — clouds + weather (standalone app at `/meteorologist.html`; the staging area for the slab) | [README](meteorologist/README.md) · [FEATURES](meteorologist/FEATURES.md) *(user/pitch)* · [OPERATIONS](meteorologist/OPERATIONS.md) *(operator manual)* · [ARCHITECTURE](meteorologist/ARCHITECTURE.md) · [INTERFACE](meteorologist/INTERFACE.md) · **[WEATHER-MODEL](meteorologist/WEATHER-MODEL.md)** (nomenclature + model SSOT) · [SPEC](meteorologist/SPEC.md) · [CANON](meteorologist/CANON.md) | [STATUS](meteorologist/STATUS.md) (wiring matrix) · [BACKLOG](meteorologist/BACKLOG.md) · [STAGE_MIGRATION](meteorologist/STAGE_MIGRATION.md) · [CLOUD-PHASE0](meteorologist/CLOUD-PHASE0.md) | [NOTES](meteorologist/NOTES.md) |

Cartograph and LS are **standalone yet completely overlapped** — cartograph could pour slabs for other neighborhoods; LS could surface other operators' slabs. Read the domain relevant to your session; flag mid-session contradictions; update at session end.

#### The cartograph tools — Survey · Section · Stage (first-class; not incidental)

The cartograph authoring app is **three distinct tools**, each its own tab/area with its own toolset, each freezing the artifact the next consumes. They are the spine of the whole map pipeline — canon table in **[`cartograph/ARCHITECTURE.md §2.1`](cartograph/ARCHITECTURE.md)**:

- **Survey** — the hardscape **SHAPE**: centerlines, smoothing, caps, anchor, road metadata, hero-pick *(target: + corner radius + curb)*. Strokes chains outward; **freezes wall #1 (chains die).** *(⭐ **Corrected 2026-07-31 — the wall is BUILT and USED; the earlier "the curb is still live-stroked" understated it badly.** **Consumer side ✅:** every non-Survey view — Section *and* the neutral Design view — renders from the frozen `shape.json` (frozen `iA` on 93/101 tiles); `sectionOpen` has no chain in scope and physically cannot re-derive. **Survey live-strokes** — it edits the SHAPE, so that is not a wall violation; ⚠️ but the current WHOLE-MAP scope of the re-stroke is a known perf defect (D6c), not a blessed design. **Producer side 🔴:** `shape.json` is minted by the live `buildTileGround` and snapshotted, so it is a photograph of a chain-stroke rather than a function of the frozen frame — **Check C, RED**. Accurate SSOT: **`cartograph/WALL.md §31`** → `cartograph/PIPELINE.md §Wall`; enforced by `cartograph/POLYGON-FIRST.md`.)* Tab: the `surveyor` pill in `src/cartograph/Panel.jsx` → `SurveyorPanel.jsx`. Docs: `ARCHITECTURE.md §2.1`, `FEATURES.md §"Toolbar = views, Panel = tools"`.
- **Section** — the pedestrian **CROSS-SECTION + ribbon corner fills**: treelawn/sidewalk widths, the bent-rectangle corner fills, ADA pads, stroked inward off the frozen curb (LU = remainder). *Currently the `measure` pill → `MeasurePanel.jsx`; "Measure" is the vestigial name.* Geometry canon: **`RIBBONS.md`**.
- **Stage** — the **LOOK / slab**: materials, color, visibility, shaders, sky, post-FX, neon, camera; **bakes the slab (wall #2, store dies).** Docs: `FEATURES.md §"Stage"`, `ARCHITECTURE.md`. *(The Designer's `design` pill is the 2D look-editing default; Stage is the 3D look environment + the slab pour.)*

(`Preview` inherits Stage's render tree — a Slab Player, not a fourth tool for authoring the *Look*. But as of 2026-06-17 it **authors *deployment policy*** — the per-platform inclusion manifest, decided at the publish gate beside the cost instrument. Keystone: `cartograph/PREVIEW.md` §0.2; arc: `HANDOFF-preview-measurement.md`.)

#### The cartograph pipeline stages — where each is authoritatively documented

The build pipeline (Jacob's order: **intake → skeleton → prebake → survey → ⟦WALL⟧ → section → bake → stage**) has ONE authoritative home per stage in **[`cartograph/PIPELINE.md` → "The stages, in order"](cartograph/PIPELINE.md)** (read that first — it carries what each stage is, its current STATUS, and its doctrine; deep-dives below). *Diffuse-doctrine across many files is what caused repeated mistakes — this is the de-diffusion index.*

| Stage | Authoritative home | Deep-dive / detail |
|---|---|---|
| **intake** (sources → cleaned frame; now an **operator tool** — `ExtentApp`: author the inclusion polygon + one-click Bake, no longer hand-edited JSON) | ⭐ **`/EXTENT-DESIGN.md`** (design of record) · **`INTAKE.md §0.5`** (the as-built Extent flow + the Pour; data provenance — every source, fetch, transform; authoritative vs. OSM-default) · `PIPELINE.md §intake` | `scripts/` (the fetch pipeline) · `OSM-FORENSICS.md` · `/EXTENT-EXCAVATION.md` (evidence) |
| **skeleton** (the frame) | **`SKELETON.md`** (the keystone artifact reference — schema + build stages + affordances + gaps) · `PIPELINE.md §skeleton` + `§Wall` + ladder `P1` | `OSM-FORENSICS.md` · `[[project_skeleton_is_the_first_bake]]` · the "better bones" 4 prongs (BACKLOG NOW) |
| **prebake** (First Bake → `ribbons.json`) | **`PREBAKE.md`** (the keystone — current compile + the polygon-ization target) · `PIPELINE.md §prebake` + ladder `P3` | `[[project_two_bakes_two_walls]]` · `[[feedback_skeleton_pipeline_two_step]]` |
| **survey** (SHAPE tool — *no ped depth*) | **`SURVEY.md`** (the keystone — tile construction + authoring + the wall) · `PIPELINE.md §survey` + `ARCHITECTURE.md §2.1` | `FEATURES.md §Toolbar` · `SECTION.md §7.1` (SHAPE/FILL split) |
| **⟦WALL⟧** (the freeze; chains die) | **`WALL.md`** · `PIPELINE.md §Wall` | `[[project_skeleton_is_the_first_bake]]` · `[[project_two_bakes_two_walls]]` |
| **section** (ped FILL → ground bake) | **`SECTION.md`** (the FILL SSOT) · `PIPELINE.md` · `FEATURES.md` | `RIBBONS.md` (FILL geometry) |
| **bake** (pour the slab → `public/baked/<id>/*`) | **`BAKE.md`** (the keystone — the chain) · `SLAB-CONTRACT.md` (slab format) | `ARCHITECTURE.md §3/§7` |
| **stage** (the Look tool → `scene.json`) | **`STAGE.md`** (the keystone — SC.1–SC.7; §1.5 per-shot look overrides; §1 panel-by-intent reorg) · `SLAB-CONTRACT.md §4` · `scratch/LOOK-PANEL-TAXONOMY.md` (panel taxonomy: current map + Phase B) | `FEATURES.md §Stage` |

#### The cross-cutting feature index — where each CONSTRUCTION lives ("where is X")

> The tables above are keyed by *pipeline stage*. Several **constructions cut across stages** (a loop touches skeleton + prebake + section) and live in a **dedicated topic doc or a State HANDOFF** — index them here so they're *found before re-derived*. ⚠️ **The recurring failure is hunting topic docs when the plan is a HANDOFF** — plans live in `BACKLOG.md` + the `HANDOFF-*.md` it indexes; grep BACKLOG for the feature name first, then read its HANDOFF.

| Construction / feature | Home doc(s) | One-line |
|---|---|---|
| **Extent tool + the Pour** (intake step-0 — search a place → fetch generously → author the boundary from the street names that come back → correct the margin → one-click Bake) | ⭐ **`/EXTENT-DESIGN.md`** (design of record: what it makes = the served skeleton / labeled point cloud · hood<disc<bb · two centers · the seal = the identity registry · the v1 worklist) · **`cartograph/ARCHITECTURE.md §Extent`** (THE PROCEDURE, as-built + D4 + the identity lock + ⛔ the LS-clobber warning) · `/EXTENT-EXCAVATION.md` (evidence) · `cartograph/INTAKE.md §0.5` · `NEIGHBORHOOD-INPUTS.md §5.2/§11` · `FEATURES.md` / `OPERATIONS.md §Extent` | `ExtentApp.jsx` + `BezierPen.jsx` (`◎ Extent`). ⭐ **Membership = `((polygon − exclusions) ∪ activate) − hide` — the polygon DECIDES, the disc RENDERS** (the disc is only the fallback when a scene has no polygon). ⛔ **The 2026-07-14 "the circle IS the boundary / excluder pen supersedes every trace-the-perimeter model" claim is RETRACTED** (2026-07-20) — it made subtraction the only gesture and silently cost 147 Księży Młyn buildings. ⛔ **Street names come FROM the fetch**, so the boundary can only be authored after step 3 — by click, pulldown, or text entry, all equivalent. ⛔ **The gazetteer is a hint, never a requirement**: an *invented/composite* hood (Hi-Pointe–DeMun) gets no usable ring and is a first-class case, so street selection must work from zero. **Two centerpoints** — the frozen fetch center (frame origin) vs the derived kept-buildings hood center (drives the disc); collapsing them is what leaves a correctly-isolated hood off-center. **Bake** ⛔ **does NOT re-center and does NOT run `reproject-raw`** (changed 2026-07-23 — the frame origin is frozen at the fetch center and the hood centre is stored off-origin as the disc's `discCenter`); it writes `neighborhood_boundary.json`, then pours scene-generically (pipeline→promote→bake), Data-Wall clip neutering overshooting arterials. ⛔ **The "re-center guard" was REMOVED in the same change** — there is no `recenter-blocked`; the protection is the frozen origin itself. **Divided-road weld** fixed in-skeleton 2026-07-11. |
| **Intake manifest** (the Extent **Intake** panel · the Stage **Materials** panel · what a town must obtain) | **`INTAKE-CATALOGUE.md`** (all four domains, every input + where to get it) · `_handoffs/BRIEF-intake-manifest.md` (⚠️ **untracked** — the `_handoffs/` layer is gitignored; treat it as State, not canon) · `cartograph/intake-rows.mjs` (the schema) | Row **definitions are kit-global**, **provenance is per-town**; status is computed from disk on every read and **never stored**. **Intake** answers *"what does THIS town still need"* (every row shown, filled or not — the panel is the checklist, the RENDER is what hides missing things); **Materials** answers *"what must YOU obtain"* — 13 inputs in three tiers by **who supplies it** (automatic · public records · local knowledge), each tier copyable as a delegatable brief. Operator-added sources are shared by **jurisdiction**, not scene, and accumulate. ⛔ Anything the kit DERIVES or you author in a panel is not on the list (centrelines, street widths); the tree **materials** are the platform's library, only regional **knowledge** is the operator's. |
| **Loop streets** (Benton teardrop · Waverly couplet · Park/Saint-Vincent bulbs · 18th U) | **`LOOP-STREETS.md`** · `RIBBONS §3.2` (the endpoint-weld) | median = the **emergent enclosed face**; the endpoint-weld closes near-coincident loop bodies |
| **Dead-ends** (cul-de-sac · stub · spike) | ⭐ **`POLYGON-FIRST.md §2.1` Check 5** (the diagnosis + the enforceable gate) · **`SECTION.md §6.3`** (the class, the scoreboard, the cap-as-end-COUPLER LANDED 2026-07-22) · `_handoffs/HANDOFF-deadend-face-resolution.md §C0` (the live task) · `OSM-FORENSICS §1` | ⭐⭐ **THE MISSING PIECE IS THE CORNER** (2026-07-30). A corner is built where **two different streets meet** (`cornerAt(a,b)` iff `a !== b`). The doubled-back ring visits a spur's mouth vertex **twice** — 1st pass `cross-street → spur` **is** a corner, 2nd pass `spur → spur` **is not** — so **a dead-end spur gets a corner on ONE side of its mouth and NONE on the other**: one leg is bounded corner-to-corner, the other runs through unbounded. That is the whole triad: **edit lands on a SEGMENT not the LEG** (no far corner to fan to) · **partner flips** (same line; sides labelled from walk direction ⇒ inverted 34/34) · **neighbouring corner + cap move** (the mouth *patch* is defining them). ⭐ **Naming ≠ bounding** — the walk-ordinal coupler named the two legs correctly and still could not finish, because there was no second mouth corner to couple to. ⛔ **Test any proposal with: does it CREATE THE SECOND MOUTH CORNER?** A notch (road-with-width subtracted from the disc) does. ⚠️ **NOT "nothing to click"** — 98 of 107 dead-end leg slots *are* clickable, 9 are not (`coupler-fe-coverage.mjs`, ported to trunk 2026-07-30; the branch counted Slice-1 walk slots, 191 of 198 — same shape, different denominator); the defect is **BOUNDING, not EXISTENCE**. ⭐ **And Check 5 is NOT universal: the corner test fires on a minority of the 50 spurs.** ⛔⛔ **RE-MEASURED 2026-08-05 — there is no "6 vs 9" split.** Spurs missing a mouth corner and spurs with a leg running through the mouth are **SET-IDENTICAL, 9 and 9**, from two independent predicates (edge-identity at the vertex vs leg-span endpoints) — `A ∩ B = 9`, neither has a member the other lacks. ⭐ That co-extensiveness **supports** A0's one-fault thesis; it does **not** mean cause and symptom are one concept. The **"6 of 50" is UNREPRODUCIBLE, not merely wrong** — `stamp-mouth-audit.mjs`, the probe that produced it, was **deleted by `7b5b87a3`** (whose message claimed *"the probes are kept"*), violating this section's own rule: *quote the number with the probe that produced it, or don't quote it.* Of the 9, **5** are a genuine two-pass missing corner and **4** present only one mouth pass — a different condition sharing a counter. The **no-mouth-disc** set is genuinely a third set (9 chains / 10 folds, 3 members unique to it). ⭐ `SECTION §6.3` had this right all along while four docs contradicted it. → `/PIPELINE-CLAIMS.md` `C4`/`C5`, `POLYGON-FIRST §2.1`. Those are the spurs — those whose chain CONTINUES past the mouth, so both sides of the second pass carry the same `skelId`; the other 41 do get two mouth corners (`POLYGON-FIRST §2.1`). Also settled: render **clean WOVEN** with their authored cap, **the ribbon folds, not the chain**, **the bulb has no halves**; 3 defects fixed at source (cap slope · leg flip · width collapse); legs 56→93/100; 10 asymmetric caps ACCEPTED. ⛔ The **MOUTH** is a **co-claim, not a gap** — never FILL-patch it. ⛔ The pendant-prune was tried **twice** and reverted (`dd4ddb6d`; asphalt is tile-sourced → pruning deletes the road) — this is the **third** pass at the class. ⭐ **2026-07-31: the corner is now CREATED** — assert the spur as an **OPEN U (two curbs + an END COUPLER)** *before* polygonization; the FACE WALK closes it (`PREBAKE §4.0a`). Built behind `SPUR_OUTLINE=1` (slits 50→9, blocks 101→101, junction band 101→**110** clean) — ⚠️ **then run past the eye and REVERTED (`7b5b87a3`) as WORSE** — ⛔⛔ **but that verdict is UNRELIABLE — the eye was on `lafayette-square` while the work was on `lafayette-square-staging`, unnoticed all day (named 2026-08-06).** Two different maps (52 vs 177 authored streets), not two views. The flag no longer exists. ⛔ **A0 is UNDECIDED — the revert is not proof the construction fails, and this is not licence to re-land it. Look again, on the right scene.** ⭐⭐ **The rule that generalizes: an eye verdict must record the SCENE it was taken on, exactly as a measurement must record its authoring state** — and the fix is the tool making scene identity unmissable, not operator vigilance. The old line *"the gates said better, the eye said no"* over-read a verdict taken on the other map. Construction preserved at `152e7734`; diagnosis + crops on branch `polygon-asks-stamp`. ⛔ The end coupler averaged its two radii, which the asymmetric-cap ruling forbids (Nicholson 2.50/6.70) |
| **Divided roads / carriageways / medians** | **`RIBBONS.md §3.1`** (the live model — inner-edge anchor LOCKED) · `SKELETON.md §2/§3` (frame topology) · `_archive/TRUMAN-FORENSICS.md` | median = emergent geometric face; two-carriageway model locked |
| **Divided↔undivided transition** (the false corner · the "d" bulge · the through-edge needle) | **`SKELETON.md §5e`** (corner the corridor legs, not the stubs — LANDED `9c275ce`) · **`§5h`** (through-edge stays straight at a divided-into-through-road node — prevailing-direction overlay, LANDED 2026-07-22) · `HANDOFF-freeze-the-curb-in-the-first-bake.md` D6b/c (the "d" bulge = the PRODUCER tracing chains) | corner the corridor outer-edge legs; a divided road T-ing into a through-road straightens ONLY its kinked leg through the junction box so the median opens clean (§5h, a construction overlay — frame untouched); the residual bulge is **minted into the frozen artifact** — `shape.json` is snapshotted from a live chain-stroke, so freezing harder downstream can't cure it (**Check C RED**; ⚠️ *not* "the curb is unfrozen" — consumers already read the freeze, `WALL.md §31`) |
| **Junction / corner construction** | **`RIBBONS.md §1/§4`** (the invariants + the corner's homes) · `SKELETON.md §5` (the across-intersection organ) | corner = band bent, never a primitive; legs are clean, the bug is leg-choice or a width datum |
| **Band-fold / thorns** (thin-tile degeneracy, G12) | **`RIBBONS.md §6.1`** · `SECTION-CAP-CLAMP-FORENSIC.md` · `HANDOFF-band-fold-fix.md` · ledger row **G12** | the LOCAL capacity clamp (engage on partial-degeneracy without over-clamping); two subclasses, both open |
| **Polygon-first / the Data Wall** | **`POLYGON-FIRST.md`** · `PREBAKE.md §4–5` · `WALL.md` | freeze topology (✅) + the curb (open); intersection-everywhere sub-thread retired 2026-06-13 |
| **Correctness suite** (one RED-until-true invariant per bug-class) | **`POLYGON-FIRST.md §5`** · `SIEVE-DETECTOR-FINDINGS.md` · `LOOM-TOPO-FINDINGS.md` · `THROAT-JUNCTION-FINDINGS.md` · harness `scratch/correctness-detector.mjs` | the detector is the deliverable, not the fixes; validated against the 35 curated |
| **Frame forensics** (the evidence base) | `OSM-FORENSICS.md` + `-EVAL.md` · `OSM2STREETS-GROUNDING.md` | "OSM or us" = 100% us; the standard gets us correct |
| **Save → ship lifecycle** (dirty tree · source-vs-derived · how to commit/discard a slab · why staging is stale · the symptom→door table) | **`cartograph/OPERATIONS.md §Save → ship`** · `BAKE.md` (the bake mechanism) · `SLAB-CONTRACT.md` (byte format) · `PUBLISH.md` (deploy procedures) | derived files are deterministic — a dirty diff is **timestamp noise** (discard) or **real geometry** (decide); CI serves the **committed** slab as-is (no bake in CI) |
| **Street view** (the eye-level mode · `planetarium` in code) — camera rig · sky/constellations · trees-at-1.7m | **`ls/STREET-VIEW.md`** · `ls/FEATURES.md §Planetarium` · `cartograph/STAGE.md` (SC.1 sky / SC.5 camera) · `arborist/ARCHITECTURE.md` (tier-2 bark) | **street view == planetarium** (one mode, two names); in-place eye-level orbit (no free-walk); constellation overlay IS mounted but `constellations` channel defaults to 0 |
| **Ground-contact effects** (lamp light-pools · tree/lamp contact shadows · trunk-base ground blend) | **`SLAB-CONTRACT.md §3.1/§3.2`** · `cartograph/BAKE.md §2` (step 8, `ground-ao` emits 3 textures) · `cartograph/STAGE.md` (Lamps `lantern` / Arch `archLight` cards) | baked **into the ground**, contour-correct (not floating discs); the pool follows the Lantern Brightness × dusk→night ramp, colored by `layerColors.lamp`; rides the Ground layer (no separate toggle); the trunk-base now takes the **combined effective** ground colour (albedo × baked contact-shadow + lamp pool), 2026-06-29 |
| **Ground conformance** (everything sits on the terrain · objects don't float) | **`cartograph/ARCHITECTURE.md §8 "Ground conformance"`** · `cartograph/groundSampler.js` · `bake-lamps.js` / `bake-tree-anchors.js` | one DRAWN surface: CPU↔GPU sample reconciled (`_terrainUV`, was ~2 m off); lamps + trees rigid-lift by a **baked per-object anchor** onto the rendered ground (`patchTerrainInstancedBaked`) — the buildings `aCentroidY` regime generalized; `park_path` contour-refined; **bake-anchors run AFTER bake-ground** (2026-06-29) |
| **Neon** (rooftop tubes · open-by-hours · TOD intensity) | **`cartograph/ARCHITECTURE.md §8 "Neon renderer"`** · `NeonBands.jsx` · `SceneNeon.jsx` | two-stage gate: WHICH tubes = listing `hours` ∩ `currentTime` (`_isWithinHours`); HOW bright = the `animated:"tod"` slot **resolved** (`resolveGroupAtMinute`) — production must resolve, not flat-read (the 2026-06-29 invisible-neon regression); `forceNeonOn` = Stage master, omitted = production hours-gated |
| **Per-shot look overrides** (channel-variant cascade — a shot like Browse overriding exposure/bloom/mist independently of Hero) | **`HANDOFF-channel-variant-cascade.md`** · `cartograph/STAGE.md §1.5` · `SLAB-CONTRACT.md §4` (`shotLooks`) · `ARCHITECTURE.md §7` | implicit per-channel override recorded on edit (inherit Hero otherwise), `shotLooks[shot][channel]`; resolved `{...base, ...shotLooks[shot]}` at ONE point — `useSceneJson`/`shotScene.js` (production), `activeChannel` (Stage). Shot axis LANDED 2026-06-29; **platform axis** (mobile/desktop) unbuilt. Building material stays global (`SlabBuildings` bypasses the adapter). **Panel reorganized by operator INTENT (Phase A, 2026-06-30, UI-only):** cards `Light & Sky` / `Image` (was Post) / `Light Sources` (was Lamps); relabels Fill→Shadow lift · Ambient→Fill light · Hemisphere→Sky fill · AO→Occlusion; new Grade **Brightness** (additive lift, the "B" of HSB) + dead Grade Toe removed — `scratch/LOOK-PANEL-TAXONOMY.md` (Phase B open: lamp fixture/pool/canopy · Bloom+Neon into Light Sources · cascade per-ToD-per-shot) |

### Cross-domain / strategic (repo root)

- **[`SHOW-BIBLE.md`](SHOW-BIBLE.md)** — the master package: the whole project for three audiences (Marketing / Fundraising / Engineering) + the productization roadmap. The synthesis the audit campaign produced; points into the corpus.
- **[`SLAB-CONTRACT.md`](SLAB-CONTRACT.md)** — the formal cartograph↔LS boundary (the slab format; owned by neither app).
- **[`AGENT-VALIDATION-SURFACES.md`](AGENT-VALIDATION-SURFACES.md)** — where to validate (toy vs LS); the guardrails.
- **[`AUDIT-MATRIX.md`](AUDIT-MATRIX.md)** (the instrument) + the filed findings `scratch/audit-{cartograph,arborist,ls-app,docs}.md` — the forensic-audit campaign (the four walks landed; the dispatch briefs retired).
- **[`plans/`](plans/)** — forward/strategic: productization, basemap-swap, pre-public-cleanout, kit-couplers, **[clean-for-handoff](plans/clean-for-handoff.md)** (the human-dev handoff campaign — leads with render-pipeline-install; DoD = the zero-open corpse-lie ledger + one render pipeline).
- **[`PUBLISH.md`](PUBLISH.md)** — deploy procedures · `BUSINESS_LISTINGS.md` · `_handoffs/CARY-BRIEF.md`.

### State layer & working dirs

- **`_handoffs/HANDOFF-*.md`** — dispatch-ready briefs = the **future-looking to-do**; live ones are indexed from `cartograph/BACKLOG.md §LIVE HANDOFFS`. ⚠️ **This folder is gitignored** (local-only), so a HANDOFF is *not* archived by git — don't rely on `git log` to recover one.
- **`BRIEF-*.md`** (repo root) — git-tracked **campaign** briefs (a level up from a HANDOFF). When one lands, its outcome is captured in `cartograph/BACKLOG.md §"Retired BRIEFs"` and the file is **deleted** — git is its archive.
- **`scratch/`** — git-tracked working files (briefs, audits, journals); throwaway-ish, *not* canonical.
- **`inventory/`** — the LS content corpus (narrative + page); data, not docs.
- **`_archive/`** — retired docs (git keeps the rest).
- **`memory/`** — the coordinator's continuity (auto-loaded; `MEMORY.md` is its index, and it points back here).

> *Incomplete? This index is incorporated **a bit at a time** — if a doc isn't mapped here yet, add it to its domain×kind cell (or the right root bucket) when you touch it. The index is canonical; keep it honest.*

URL routes during development:

| Route        | What it is |
|---|---|
| `/`          | Public neighborhood viewer (the runtime) |
| `/cartograph`| Cartograph helper app (Designer + Stage + Surfaces) |
| `/stage`     | Standalone Stage page (camera/lighting authoring, no cartograph data) |
| `/arborist`  | Arborist helper app (species library + specimen workstage; scaffold) |

---

## Stack

React Three Fiber, Three.js, Zustand, Tailwind CSS, Vite, Supabase (Cary courier system).

## Backend

- **Apps Script** (`apps-script/Code.js`) — listings, reviews, events, check-ins, residence, guardian claims, QR designs.
- **Supabase** — Cary courier system (requests, sessions, auth). Not yet live.
- **Cloudflare Worker** (`worker.js`) — OG meta tags for social link previews.
- **Cartograph backend** (`cartograph/serve.js`) — local-only Node service for the authoring helpers (Looks API, bake CLI runner, overlay I/O). Not deployed; helpers are dev-time tools.

## Admin access

Append `?admin` to any URL to trigger the admin login prompt. The passphrase is validated server-side and a session token is issued (valid 6 hours, stored in `localStorage` as `lsq_admin_token`). Use `?logout` to end the session.

Set the passphrase in Apps Script: `PropertiesService.getScriptProperties().setProperty('ADMIN_PASSPHRASE', 'your-secret')`

## Publishing

See [PUBLISH.md](PUBLISH.md) for deployment procedures (frontend, backend, DNS, worker).
