# HANDOFF — osm2streets specialist: ground our road model in the standard

**Why you're here:** we've been inventing a private vocabulary and a private geometry engine ("loop streets," "divided pairs," "perpendicular-join artifacts," "junction maps") for problems the field already has names and standard solutions for. **osm2streets** (the A/B Street project — OSM → street geometry: lanes, intersections, road casings) is the reference implementation for *exactly* our problem. Your job: **learn how it's actually done, map our pipeline onto it, and tell us what to adopt** — and how many of our "bugs" are just us doing it the non-standard way.

**You are an osm2streets specialist.** Name yourself. You need **web access** (`WebSearch`/`WebFetch`) — osm2streets is open-source (`github.com/a-b-street/osm2streets`), with design docs, a `StreetExplorer` demo, and Rust source. Read widely, then read our pipeline. **No code changes** — the deliverable is a grounding writeup. **Be critical, not reverent:** osm2streets serves a traffic *simulator*; we're a stylized map + 3D slab with operator *authoring*. Say what transfers and what doesn't.

**Read first:**
- **osm2streets:** the repo + its docs (the road/intersection/lane model — "StreetNetwork", `Road`, `Intersection`, `LaneSpec`), how it handles **(a) dual carriageways** (does it detect from tags/relations vs geometry? does it merge them? "collapse short roads"/"merge dual carriageways"), **(b) intersections** (the intersection *polygon* / casing dissolve / how junctions render cleanly — the thing we call the "perpendicular-join artifact"), **(c) dead-ends & service roads**, **(d) the OSM ingestion** (which tags decide width, lanes, divided). Also skim standard **road-casing cartography** (two-pass casing + intersection dissolve) and OSM's `dual_carriageway`/`oneway` conventions.
- **Ours:** `cartograph/SKELETON.md` (our OSM→chains "frame"), `RIBBONS.md` + `src/lib/tileGround.js` (our casing/intersection construction), `JUNCTION-CURE-PLAN.md` (our homegrown intersection model), `LOOP-STREETS.md` (the "loop street" concept — *is this even real, or our artifact?*), `PREBAKE.md`. Memory: `[[project_skeleton_is_the_first_bake]]`, `[[project_the_palimpsest_code_path_multiplicity]]`.

**Deliver `cartograph/OSM2STREETS-GROUNDING.md`:**
1. **The standard model, plainly** — how osm2streets represents roads, intersections, lanes; how it decides a road is **divided** (the key one — we infer it *geometrically* from parallelism; we suspect they use the **data model**, which is why **18th** — a normal block, not a divided road — got mis-paired); how it **dissolves casings at intersections**.
2. **Concept map: ours → standard** — a table. `chain`/`frame` → ? · `tileGround` casing → road casing · "junction map"/"perpendicular-join" → intersection dissolve · "divided pair (geometric)" → dual-carriageway (data) · **"loop street" → (does this concept exist at all, or is 18th just "a block bounded by streets"?)**. Flag every place we reinvented or diverged.
3. **Our live bugs in standard terms** — for **18th** (mis-paired parallel streets), the **perpendicular-join artifact** (casing not dissolved at plain Ts), and the **divided mis-detection**: how does the standard approach handle each, and do they **dissolve** under it? Be concrete.
4. **Recommendation** — **adopt the library** (Rust→WASM), **port the methods** (reimplement the intersection-dissolve / dual-carriageway logic in our JS), or **align concepts** only? Assess fit for *our* needs (operator authoring on top, the frozen-polygon Wall, the 3D slab, the kit). Give a migration sketch + what it costs vs what it dissolves.

**Tone:** honest about where we went wrong, honest about where our constraints differ. The win is *correct vocabulary + standard methods*, not a religious rewrite. Sync to trunk, commit the writeup, report. ⛔ No other canonical-doc edits (Boz conforms).
