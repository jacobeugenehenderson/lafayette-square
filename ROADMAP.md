# ROADMAP — the whole remaining board, pruned to zero

> **What this is.** The one **cross-domain** master of everything left to do — cartograph, arborist,
> meteorologist, the `ls/` public app, security, kit-productization. It exists because we're in the
> **home stretch**: the whole backlog (not just MEMORY's immediate task) must be *constantly surfaced
> and pruned until it is gone*. This is that surface.
>
> **How it's built (one home per fact).** This is a prioritized **index**, NOT a restatement. Each
> line is a one-liner + a size + a **→ pointer to its detail home** (a domain `BACKLOG` section or a
> `HANDOFF-*.md`). The pointer is the truth; this file is the *ordering*.
>
> ▶ **Detail-home relocation (2026-07-21).** The `HANDOFF-*.md` files these lines point to were moved
> out of git into the local, **gitignored `_handoffs/`** folder (Jacob: declutter root + no git backup).
> Pointers below name the file by **basename** — find it in **`_handoffs/`**. The 6 *active* dispatch
> briefs stay tracked at root (`BRIEF-land-use-derivation`, `-terminal-node-sweep`, `-street-labels`,
> `-ls-bleed-excision`, `-arborist-slab-weight`, `-extent-excavation-and-design`). Landed/superseded
> items from the 2026-07-18/21 retirement batches are in `cartograph/_archive/` (Diary, tracked). Git
> history is the permanent archive of every version.
>
> **The pruning discipline.** When an item lands, **strike it from ROADMAP *and* its detail home in
> the same breath** (the accord sweep — `BOZ.md §3`). Never let a done item sit here. Tiny aesthetic
> bugs get *added* here as they surface (or vanish on their own) — the list breathes until it empties.
>
> **Sizes:** S = hours · M = a session · L = multi-session.
> **DoD everywhere = Jacob's eye on the real render, never a proxy** (`feedback_proxy_render_is_not_the_operator_eye`).
>
> *Established 2026-07-17 (Boz + Jacob, the whole-corpus appraisal). The frame is Jacob's: two
> columns to done + close security, then the tail is process-cleanup ahead of human-developer
> engagement. Reachable from `ORIENTATION.md` + `README §⭐ START HERE`.*

---

## ⚠️ Live / blocking defects — do on contact
- **Prod: 88 trees 404 on lafayette-square.com** — `origin/main` census requests `platanus/skeleton-4` ×88, no such GLB ships. Fixed by the re-bake at the tree eye-gate; **do NOT `git restore public/baked/**`** (working tree is more correct). S · → folds into **B6**.
- **Build blocker: dangling symlink** `public/photos/lafayette-square/other` fails `npm run build` (vite publicDir copy). Blocks every prod build → blocks baking. S · → `arborist/BACKLOG.md`.
- **Latent prod crash: `MountainBackdrop.jsx:68`** white-screens any Look that opts into landscape without overrides — one-line guard (`.values ?? LANDSCAPE_FLAT_DEFAULTS`) drafted, unapplied. S · → `HANDOFF-altadena-pour.md:77`.
- **⭐ Land use is INVENTED for most of the map — live on prod. ▶ DISPATCH-READY BRIEF PREPPED → `BRIEF-land-use-derivation.md` (fresh agent, phased).** `luForRing` decides a whole block's LU from **one point sample**; on a miss it falls to `pickLuFromHash`, a *weighted random* palette ("tuned to read as a real neighborhood with anomalies"). `blockLandUse` (the authored override) is **0 entries in both scenes**, so nothing ever pre-empts it. Measured: **LS 62% of blocks / 42% of area · HPDM 57% / 62%** are invented. 25% of unmatched blocks roll hardscape → bald grey (Jacob: *"the verdant greenbelt is not 'Vacant'"*). **Root for HPDM: `derive.js:1020` reads only `stl_parcels.json`; the 14,597 County parcels are fetched, reprojected, read by `bake-content.js` — and ignored by the land-use join.** DeMun is County. ⚠️ Taxonomies differ (City 4-digit, County 3-digit) so unioning naively maps the County half wrong. **Contradicts the headline "the inputs are real, not guessed"** (`ORIENTATION`, `FEATURES`) → also an `ACCORDANCE-REVIEW` item. Phased: County parcels (M, may be most of it) → delete the hash + area-weighted join (M) → building-derived LU for assessor-less towns (L, the portable rung; `centrum` tags validated). M–L · → **`BRIEF-land-use-derivation.md`**. *(Downstream policy layer `cartograph/lu-policy.mjs` already landed 2026-07-21 — do not widen it to paper over the join.)*

---

# THE NEAR-TERM PUSH — two columns + a close-out (run in parallel)

> The two columns are independent (A = cartograph geometry, B = arborist/render) and dispatch
> concurrently; security is a bounded third track. Jacob's thesis: **land both columns + close
> security and what's left is scant** — at which point the work becomes the cleanup tail below.

## Column A — the SHAPE finish (Front A)  ·  *the final aesthetic of a premier product, not optional polish*
> The governing rule is locked (one uninterrupted frontage chain real-corner→real-corner; the ADA
> transition slope fires ONLY on arrangement-*difference*) — `SECTION.md` canon (`0f0a6473`).

- **A00 · ⛔⛔ RIP OUT THE LS FALLBACKS — a failed pour must FAIL, not show Lafayette Square** *(NEW 2026-07-31, Jacob: "there should be no fallback; it would help exactly 0")* — a fallback converts a failure into a plausible-looking success, which for a kit is the worst outcome available: the operator sees a map and trusts it. **Measured, live:** `measureModel.js` seeds street widths in EVERY scene from LS's `ribbons.json` **keyed by street NAME** → **24 Altadena streets silently inherit St. Louis measurements** (Allen Ave, Iowa Ave, + every auto-named `motorway_link N`); Hi-Pointe 6; both Polish pours **0** — so it is invisible in exactly the scenes you'd use to prove the kit travels. Other silent substitutions found the same day: `config.js:26` `SCENE = env || DEFAULT_SCENE` (no env ⇒ silently builds LS — this cost a full day), `serve.js:902` (no scene in the route ⇒ serves LS), `serve.js:765` (a Look with no scene is *assigned* LS), `serve.js:1906` (a bake with no scene ⇒ **bakes over** LS). ⭐ Every one should be a loud failure. S · → `CLAUDE.md` Layer 0, `ORIENTATION`.
- **A02 · ✅ BUILT 2026-07-31, ⚠️ NOT YET EYE-VERIFIED — the wall no longer falls back silently** — the consumer boundary is otherwise real and defended (every non-Survey view renders from the frozen `shape.json`; `sectionOpen` is chain-free by signature), but it **degrades without failing** by two doors: a failed `shape.json` fetch (`BlockGeometryV2Debug.jsx:589`, a `console.warn`) and a scene with no freeze (`:595`). On a town where the freeze failed or never ran the operator sees a plausible map and never learns the wall didn't hold — and because LS always has a freeze, it is **invisible in the scene you'd use to prove the wall works.** Same family as `BRIEF-ls-bleed-excision`. **FIX (built):** the store carries `shapeFreezeMissing` (a *reason*, not a bool — "never frozen" and "fetch FAILED" read differently), set at both doors and cleared in Survey (where live-stroking is the point, so the banner must not cry wolf); `StatusBar` renders it as the same non-dismissable red banner `overlaySaveBlocked` already used. ⭐ **The live build still DRAWS** — a fresh pour with no freeze is legitimate and refusing to draw would make onboarding town #2 impossible. **The defect was the silence, not the draw**, so the cure is that the view is now *labelled* a live re-derivation instead of masquerading as the frozen shape. ⚠️ **Eye-gate owed:** `vite build` passes and the wiring is verified, but the banner **cannot fire on any of the 7 scenes that have a freeze** — only `default` lacks one. Test: point the tool at a scene with no `shape.json` (or rename one) and confirm the banner. **S** · `WALL.md §2`, `PIPELINE §Wall`.
- **A03 · 🔴 CHECK C IS RED — the wall's PRODUCER still traces chains** *(rescoped 2026-07-31)* — `shape.json` is minted by the live `buildTileGround(liveRibbons,…)` and snapshotted, so the frozen artifact is **a photograph of a chain-stroke, not a pure function of the frozen frame** — which is why the divided-transition bow is frozen *into* it and cannot be cured downstream. ⚠️ **This is the whole remaining "freeze the curb" program — D6b/c only, NOT "the curb is unfrozen."** The consumer half landed 2026-07-14/15; several docs (`PIPELINE`, `README`, `PREBAKE §4.1`, `SKELETON §5f`) still said otherwise until corrected 2026-07-31, and that staleness cost a session. **M** · `WALL.md §31` (the accurate SSOT) · `HANDOFF-freeze-the-curb-in-the-first-bake.md`.
- **A04 · ⛔ COLLAPSED CURB RINGS — 28 of 92 tiles, and the detector was hiding them** *(NEW 2026-07-31)* — **28 of 92** LS tiles carry a shrunken curb ring; tile **37** is a **2058 m²** block whose ring is an **85 m² fragment**. This is a **collapse, not a bow**: the same street is a perfect parallel offset on six tiles and wild on two, so it is **specific tiles, not the offset math.** ⛔ **Deliberately NOT folded into A03** — A03's contract is byte-identical output and repairing rings changes geometry. ⭐ **The kit prize is the CHECK, not the repair:** *"this block's curb ring collapsed"* is computable **from the frozen artifact alone** (ring area vs tile area) — no operator, no eye — and fails the bake loudly on a town nobody has inspected. ⚠️ It was invisible because `litmus-curb-parallel` **skips tiles with no curb ring**, so a total failure printed as a modest bow. **M** · `POLYGON-FIRST §2` Check A · `§5` Rules 1–3.
- **A05 · ⛔ CHECK A IS MIS-SPECIFIED — it scores the operator's authoring as damage** *(NEW 2026-07-31)* — `litmus-curb-parallel.mjs:77` runs `blockCustoms: null`, i.e. **authoring OFF**, so it compares an authored curb against un-authored widths: Mississippi is parallel to the centimetre at its authored 8.70 m and was reported as a 3.13 m bow against the corridor's 11.83 m. ⭐⭐ **Layer-0 shape, not a mere bug: the check fails WORST on the most heavily authored town (LS, the mould) and would look CLEAN on a fresh pour** — blind exactly where the map is most worked-on. Also `:86` skips unmeasurable tiles (→ A04). **Fix:** run with the scene's authored `blockCustoms`; emit `UNMEASURABLE` as its own failing class. **S** · rides A03's landing · `POLYGON-FIRST §2`/`§5`.
- **A01 · ⚠️ THE PIPELINE DOES NOT REPRODUCE ITS OWN COMMITTED OUTPUT** *(NEW 2026-07-31)* — re-running `pipeline.js` on unchanged inputs produced a materially different LS map than the committed artifact (233 vs 228 junction nodes; 75 vs 71 asphalt rings; different curb + sidewalk). The fresh derivation was **worse on the eye**. Until this is understood, **no "I measured it and it's better" claim is trustworthy**, and re-promoting over a committed artifact without the operator seeing the delta is destructive (it was, this day). S/M · evidence in the revert commit.
- **A0 · ⭐⭐ CLOSE THE POLYGON AT DEAD ENDS** *(NEW 2026-07-25 — takes priority over A1; A1's remainder is a symptom of it)* — the face freeze never closes a polygon at a degree-1 chain: `extractFaces` walks a spur out and back over the same vertices, so **ALL 50 LS dead-end tips are zero-width slits** (corrected 2026-07-30; ⭐ but the missing-mouth-corner test fires on **6 of 50**, not all — and the "9 of 50" quoted elsewhere is a *different* measurement (legs running through the mouth); `POLYGON-FIRST §2.1` separates the three sets), and the **40** that look right are held by the FILL-layer mouth-wrap snap. A leg with no interior on one side **has nothing to click** — that IS A1's "Δ=0.0 / unresponsive legs", the leg-flip swap, and the mouth co-claim, all one upstream fault. ⛔ **Not fixable in Section**: a side-free walk-ordinal key was built, gated, and **retired** for re-deriving identity from a chain trace after the Wall. **Construction (Jacob):** the SSoT radius as the **outer polygon**, everything inside **punched out** — blocks = boundary − stroked roads (precedent: V2's `differenceRings([stencil], asphaltSharp)`; all 9 slit chains already have both sides there). Also lands **Jacob's data-wall rule — POLYGONS ONLY by the Section tools** (`POLYGON-FIRST §2.1`), and plausibly subsumes the junction/mouth co-claim (Slice 2). ⚠️ Re-founds the tile substrate (`RIBBONS §1`) — **spike + standup before build**; risks = identity attribution and width-dependent topology. ⛔⛔ **TRIED AND REVERTED 2026-07-31 (`7b5b87a3`) — read this before re-opening.** The construction below was built, measured green on every gate, run past Jacob's eye on both scenes, and judged **WORSE**. `SPUR_OUTLINE` is gone from the code; the construction lives at `152e7734`. ⭐ **The transferable lesson: these probes do not predict the eye** — do not re-derive this table and read it as success. ⭐ **And it exposed a larger, still-open kit defect: the committed `ribbons.json` does not reproduce from a fresh `pipeline.js` run (233 vs 228 junction nodes; asphalt 75 vs 71 rings).** The pipeline is not reproducible against its own committed output — on town #2 there is no known-good file to catch that. That now outranks this item. *(Historical description follows.)* ⭐ **WAS: IN FLIGHT 2026-07-30 — half built, flag off (`SPUR_OUTLINE=1`).** Assert the spur as a open U (two curbs + a cap) *before* polygonization rather than punching the whole map: slit rings **50→9**, caps **50→7**, 41 notch faces, and **blocks stay 101** (whole-map punch-out moves 25 of 101). FILL half landed 2026-07-31 (`atCurb` ⇒ zero asphalt inset on an edge already at the curb): spurs at full road width **43/52 → 45/52**, no losses, and unclosed spurs roll back instead of losing their road. ⭐ Junction band **fixed and net better: 101 → 110 clean** (a datum change was being read as a street corner). ⛔ Remaining before default-on: **Jacob's eye in Survey — never run** (needs `rebake-shape.mjs`), 2 marginal junctions still off-clean, and the 9 one-sided-mouth tips. `PREBAKE §4.0a`. L · → **`_handoffs/HANDOFF-deadend-face-resolution.md`**, `PREBAKE §4.0`, `PIPELINE §Wall`.
- **A1 · Flippable cul-de-sac LEGS** *(⚠️ superseded in root-cause by A0 — do not chase the leg tail in Section)* — the dead-end **cap** flip + dip-in slope **LANDED & eye-confirmed 2026-07-18** (`5a0eecc8`→`330004ec`): toggle-swap cap, semicircle swap, legs bend so the sidewalk stays contiguous. The live remainder is the **legs**: a leg material flip currently renders **Δ=0.0** — the terminal-leg custom isn't reaching the strip, and/or the new crossing-band transition zone overrides it. M · → `HANDOFF-dead-end-cap-flip.md` (status block + next-task). *(To ship the caps: a real prod bake regenerates `shape.json` + `design.json` — the current re-bake/test-flips are uncommitted working-copy only.)*
- **A2 · Through-node T-artifact re-fix** — ✅ **SOLUTION LANDED (terminal-node sweep, 2026-07-21).** Replaced the failed `thruTNode` marker (which keyed the wrong run — side-street on split-skelId at Mackay, coord-miss at Kennett) with the **identity+terminal sweep**: group roads by identity (name + near-continuous), a node is TERMINAL for identity X iff it's a degree-1 tip of X's graph, else THROUGH; corners mint only on terminals; a 0-tip identity is a ring (no corner). Stamped in the frame, frozen, read post-Wall — no inference, no coord-match. The removal pass excises the guessers it subsumes. → `BRIEF-terminal-node-sweep.md` (spec), `scratch/THRUNODE-GATE-FINDINGS.md`.
- **A2b · Hold the through-road's EDGE straight through the junction** *(a STROKE fix — NOT a frame edit)* — the through-road's stroked **curb edge bends/bulges** where a road terminates against it (Clayton × De Mun HPDM `[-4.4,931.3]`: Clayton's north `iA` collapses to a degenerate needle-cluster on its own edge — the median-needle pinch bleeding onto the through-edge). ⛔ **Chains are IMMUTABLE** — a frame straighten (reproject the terminal tip) was **TRIED & REVERTED tonight** (dead-ended the carriageway, corrupted the survey). `[[feedback_survey_chains_immutable_corner_is_stroke]]`. The through-road's chain is straight (Clayton ≤1.7°); the bend is purely the STROKE. Fix: hold ANY through-edge as a straight offset of its straight chain across the through-node (consume the frozen `through`/`throughId`/`thruNodeEnds` identity the ped band already reads); the terminating road corners INTO it. ⛔ **divided-vs-single is IRRELEVANT — do not special-case.** ~~`BRIEF-prevailing-direction-projection.md`~~ **MOOT** (frame edit; tip absorbed by widths). ✅ **LANDED `cd062388`** ("hold the through-road edge straight at a divided-into-through-road node"); brief retired on landing.
- **A3 · Freeze the curb in prebake** *(structural — closes the centerline leak)* — build curb+junction ONCE from the frozen frame; Survey consumes it; only the active element re-strokes → Check C green, chains can't leak. **Rebuild-gated.** L · → `HANDOFF-freeze-the-curb-in-the-first-bake.md` (D6b/c).
- **A4 · Robust bezier offset → curves ON → re-bake HPDM** *(this IS "HPDM chunky")* — `CURVE_FIT` is built + eye-approved but **OFF**; `STREET_SMOOTH` is pinned 0 because the offset isn't robust on tight bends (turning it on sprays needles). **Strictly downstream of A3.** L · → `HANDOFF-curve-primitive-skeleton.md`, `smoothCenterline.js:150`.
- **A5 · band-fold G12 thorn clamp** — the local capacity clamp; built but stranded on `8e1e414`, unlanded. Eye-gate + canon fold. S–M · → `HANDOFF-band-fold-fix.md`.
- **A6 · junction-construction geometry** — build the osm2streets trim-back/assemble polygon at *every* node (metadata spans all nodes; geometry is divided-only today). L · → `HANDOFF-junction-construction.md`.
- **A7 · Fortify corner identity + the ADA-regime trigger** — **some real corners aren't getting ADA treatment.** A dedicated pass on *what is a corner* (identity, not angle — `feedback_walker_corner_detection_is_identity_not_angle`) and *when/how to force the ADA regime* on one. Jacob held it to see if the through-node + cap fixes would shake it out; they didn't fully → its own pass. The **inverse of A2** (through-node = a FALSE corner wrongly firing ADA; this = REAL corners NOT firing) — same root question, corner identity. M–L · → `SECTION` canon (the governing rule) · `RIBBONS §1/§4`.
- **A8 · Sidewalk proximity-join** — when two sidewalk runs come within proximity and leave a **thin treelawn sliver** between them (seen at a cul-de-sac bulb ↔ leg), **join them into one contiguous sidewalk** instead of leaving the gap. Consistent with the frontage-contiguity governing rule, applied *between* runs. Likely **FILL/Section** (the inward ped stroke arrangement) — ⚠️ confirm the layer (FILL join vs a curb-proximity SHAPE thing) with the canon open before touching. M · → `SECTION` canon · *(surfaced 2026-07-18 from the cul-de-sac render)*.
- **A9 · Pair-free edge-anchor (fix overlap-holes on divided/frontage roads)** — where two same-material road strokes run **parallel and overlap** (divided carriageways, frontage roads), the bake's `ringsToHoledPolys` (`bake-ground.js:307`) reads the overlap as a **hole** (winding-sign classifies an overlapping opposite-wound ring as a void → punched-out asphalt; the nested-donut islands). ⭐ **Fix at source, not the bake union:** don't let them overlap — **project the ribbon from an edge, not the centerline** (the inner-edge anchor, `RIBBONS §3.1`). The anchor mode EXISTS but is **hard-coupled to a detected pair** (`derive.js:3554`; the Survey dropdown greys out "Inner-edge (no paired chain detected)") → **useless for frontage roads** (no mate). Generalize the anchor from `{Center, Inner-edge-needs-pair}` to **`{Center, Left-edge, Right-edge}`** — pair-free, operator-chosen side, kit auto-suggests from the nearest parallel same-material neighbor; the offset already does one-sided projection. ⚠️ `innerSign` is documented UNRELIABLE (`tileGround.js:742`) — a second, smaller leak. M · → `BRIEF-pair-free-edge-anchor.md` (draft) · `RIBBONS §3.1`. *(surfaced 2026-07-21, HPDM divided/frontage overlap-holes)*
- *(Gated sibling: **D3 corner-identity freeze at prebake** — the false-corner cure is live; doctrine wants it decided once upstream. M · → `PREBAKE §5`. Do with A3, don't cleanup-refactor.)*

## Column B — tree impostor editing + placement  ·  *this IS "works on a phone" (trees are the OOM/perf culprit)*
> Column B makes the phone light. The instrument that *proves* it's light (the measurement regime) is
> the confidence-half, parked in the Horizon (**H1**) — pull it forward when you want publish-and-know.
>
> **▶ STATUS 2026-07-22 — BOTH impostors are landed and in the player; what's left is WEIGHT.**
> `hero-impostor-foundation` (21 commits, Phases 0–2) merged to trunk (`3e809a56`). The impostor is now
> the **foundation**: every tree paints, the tallest `heroGeomFraction` (0.15) keeps mesh as anchors, and
> the hero-pan **cull is retired** (it was dropping 69% of placements onto bare ground). Overhead/Browse
> landed 2026-07-10. **The player-load gap is closed** — the crux was indeed slab-carry: `heroImpostorBySpecies`
> is browser-authored and must be *carried*, never re-derived (it had to be grafted by hand through the merge).
> **B1/B2/B3 are done; the remainder is B6 + the two open defects below.** Full as-built + open work:
> **`arborist/ARCHITECTURE.md §"Tree-render reality at LS"` — the SSoT; don't restate render state on this board.**

- [x] **B1 · Salon hero-impostor editorial surface** *(2026-07-22)* — `HeroImpostorBaker` on Bake→Slab, Hero-view eye-gate in the Salon, baked `heroImpostorBySpecies`, `bake-look` carry. `Matrix4` aliasing prereq fixed (`5a06ed5d`). ⚠️ **It does NOT yet pay for itself:** the hero pool is ~70 MB of PNG against the ~39 MB of `lod1` it relieves → **B6 is now a prerequisite, not an adjacent nicety.**
- [x] **B2 · Overhead-impostor chain** *(2026-07-10, `a4458f4a`)* — 3-slice snapshot, runtime camera-height select, `overheadBySpecies` carried by `bake-look`. ⚠️ **Open defect:** 3 of 30 LS bands baked **blank** and shipped undetected (platanus canopy+branch, linden canopy); the baker now refuses blank bands, and the platanus needs a Salon composition so it has a `barkDetailBySpecies` record at all.
- [x] **B3 · impostor LOD render** *(superseded/absorbed)* — the capture-architecture question is answered (in-browser GPU RTT, both hemispheres); the whole-tree octahedral cross is **killed, not parked**; `occlusion→cull` is **withdrawn** (culling is no longer how density is managed).
- **B4 · density-impostor-swap** — HPDM's mesh trees to impostor. **Largely absorbed by the foundation** (the height split is scene-generic, so HPDM inherits it) — what remains is the **canopy-dims join fix** + confirming HPDM's own bake. Re-scope before dispatching; the per-scene `promThreshold` it was built around no longer exists. S · → `HANDOFF-density-impostor-swap.md`.
- **B5 · atlasKind `/stem/` classifier bug** — 11.3% of placements render **black** (foliage samples the bark atlas). Needs standup + a re-bake (stale atlas blanks the whole grove). M · → `HANDOFF-tree-spokes-and-census.md §3f`, `atlas-kind-classifier.js:52`. *(⚠️ Revised 2026-07-22: this no longer claims to also clear the overhead blank-bands. Those were measured — the platanus blanks because it has **no `barkDetailBySpecies` record at all** (never Salon-composed), which collapses `uBarkTileScale` to (0,0); a different bug from `/stem/` misclassification. `scratch/overhead-band-coverage.mjs` is the meter.)*
- **B6 · Placement / census correctness** — **the tree-spokes overhead artifact still UNIDENTIFIED** (next step is a scene-graph probe in Browse, not more theory — 9 theories already ruled out). ✅ *Closed 2026-07-22:* the **census-well union** (LS was baking 729 park-only placements while its own 2,635 + 3,376 street-tree wells sat unread — two entry points had drifted) and the **allowed-zone bypass** (the bare CLI placed with no zone test at all; it now resolves through `treeBakeInputsForScene` and refuses to run unzoned). LS: 729 → **4,992 placed**, 892 forbidden-surface drops, 1,405 surveyed trees nudged onto legal ground. ▶ **Still open:** LS has **no `neighborhood_boundary.json`**, so tree membership falls back to the disc (`⚠️ no boundary-street polygon` in every bake). S + M · → `HANDOFF-grove-neighborhood-axis.md`, `HANDOFF-tree-spokes-and-census.md`, [[project_tree_census_wells_must_union_all]].
- **B7 · 23a merged-mesh segmentation** — split "group-shot" merged-mesh forest GLBs into individual trees (the real root of the rows-of-trees / lost-willow artifact). Now dispatchable (gauntlet landed). M–L · → `HANDOFF-23a-merged-mesh-segmentation.md`.
- **B8 · Trees "demander" force-render** — explicit `invalidate()` when tree assets finish loading so trees appear unconditionally (async-load race on the on-demand loop). M · → `arborist/ARCHITECTURE.md §"Tree-render reality at LS"`, BACKLOG NOW.

## Close-out — Supabase / Cary security  ·  *"just be done with it"*
- **S1 · Apply migration `009`** — drafted, not pushed. Fixes **F-1 (CRITICAL: `sms_messages` has no RLS → anon reads/writes PII)**, F-7 (definer view leaks name/email/phone), F-8 (3 `security definer` fns without `search_path`). Needs Jacob's interactive `supabase db push` + re-run Advisor. S · → `HANDOFF-security-audit.md`, `SECURITY.md`.
- **S2 · Service-role auth + webhook signatures** *(the design conversation — hard prereq before any real Cary user/dollar)* — F-2/3/4/5: functions trust body-supplied ids with no caller auth; `requests` RLS is `USING(true)`; **no Stripe/Checkr/Twilio signature verification anywhere**. M · → `SECURITY.md` F-2…F-5.
- **S3 · Remaining Supabase findings** — F-6 IDOR · F-9 TwiML injection · F-10 GPS-read scope · F-11 cron fail-open · F-12 `serve.js` command-injection (dev) · F-13 CORS · the Auth-dashboard lints (OTP expiry / leaked-password / redirect URLs). S–M each · → `SECURITY.md`.
- *(Sibling, distinct arc — the **LS Apps-Script** side: admin-token ship-blocker + no rate-limiting, both 🔴. Ships-blocks the LS app, not Supabase. → `ls/STATUS.md §security`. Tracked in the Horizon LS-app cluster **H7**.)*

## Quick independent wins — cheap, public-visible, no column dependency
- ~~**Lamp census restore**~~ **LANDED `87fe26fb` (2026-07-18)** — scene-homed the raw OSM file + allowlisted + re-baked: 80 fallback → **536 real OSM lamps** (641 clipped to hood). Render path was already live. Pending final eye-confirm; retire `HANDOFF-lamp-census-restore.md` once confirmed.
- **Ground foundation-crack (mechanism B)** — on a slope the ground falls below the flat foundation top and exposes a concrete band. Cure = a `bake-terrain.js` footprint-flatten + apron sharing one `footprintMeanElevation()` helper with `bake-buildings.js`. Rebuild-gated. *(Mechanism A — the tessellation split — is already fixed.)* M · → `HANDOFF-ground-foundation-crack.md`, `scratch/GROUND-BUILDING-CRACK-FINDINGS.md`.
- **Forest Park census free win** — read `FORESTRY_TREES/MapServer/4` (4,297 trees, richer schema) we currently ignore; best species prior for the HPDM census gap. "Costs one integer." S · → `HANDOFF-tree-spokes-and-census.md:33`.
- **Milky Way re-enable** — ⚠️ NOT a one-liner (the `CelestialBodies.jsx:~1194` pointer is STALE; that file no longer exists and the runtime celestial renderer isn't grep-locatable — 2026-07-18). Channel plumbing is live (`skyLightChannels.js` `MILKYWAY_FLAT_DEFAULTS` → `bake-scene.js`); the *renderer* is missing/renamed. Needs a talk-out investigation: locate/revive the night-sky render, view across TOD, report what beautiful costs. M · → talk-out brief. · **building-dissolve UI sliders** · **landmark-label nudge**. S each · → `ls/STATUS.md`, `ls/BACKLOG.md`.
- *…tiny aesthetic bugs land here as they surface.*

---

# THE CLEANUP TAIL — process-cleanup ahead of human-developer engagement
> Code legibility + pristine docs + the kit-productization that makes the whole thing hand-off-able to
> a human developer. This is where the horizon's "make it legible" subset lives.

- **C1 · blank-app / Universal Reader** — Ph4 (`branding.copy` bundle + build-time `index.html` inject) + **the zero-hardcode gate** (grep the reader for LS literals → zero) + the per-instance content sidecar (off `src/data/buildings`) + the module manifest. The legibility spine for handoff. L · → `HANDOFF-blank-app-instance-decoupling.md`, `plans/front-front-end-and-productization.md`, `SLAB-CONTRACT §C2`, `NEIGHBORHOOD-INPUTS §5.1`.
- **C2 · HiPointe backend-tenancy — deploy + verify** — per-look Apps Script backend; code shipped on `sheet-tenancy`, Jacob runs `clasp push && clasp deploy` + the 5-step eye-gate (no cross-tenant leak). S · → `HANDOFF-neighborhood-backend-tenancy.md`, `DEPLOY-CHECKLIST-backend-tenancy.md`.
- **C3 · The correctness-detector campaign** — the real kit deliverable: one automatic check per bug-class → CI gate → onboarding loop (run-suite → fix-class → green). This is what lets a human work town #2..N without re-inspecting everything. L · → BACKLOG kit-correctness track, `POLYGON-FIRST §5`, `scratch/correctness-detector.mjs`.
- **C4 · figure-ground dead-path excision** — extract the still-live helpers from `buildBlockGeometryV2.js` (3,371 lines, ~13 importers) → migrate authoring onto tiles (T3) → delete the builder + `BlockGeometryV2Debug.jsx` + the `bake-ground.js:28` import (T4). L · → `HANDOFF-tile-T3-authoring.md`, `DOC-CODE-COHERENCE` C3/C4/B4, `plans/clean-for-handoff.md` W2.
- **C5 · Doc-pristineness to zero** — drive `DOC-CODE-COHERENCE` corpse-lies to ✅ (C5–C13, D1–D8); codify the doc-currency mechanism into `BOZ §3`; physical-archive the parked material (RUNTIME-DELTA, the misfiled cartograph sky-ADRs in `meteorologist/NOTES`); reconcile the three May-12 "marriage-leap" plans (partly OBE). M · → `cartograph/DOC-CODE-COHERENCE.md`, `plans/clean-for-handoff.md`.
- **C6 · Retire landed HANDOFFs** → **SUPERSEDED by the 2026-07-21 wholesale sweep (Jacob's call).** The per-item "keep done-stamped handoffs at root as detail-homes" policy is retired: **all 46 root `HANDOFF-*.md`/`BRIEF-*.md` except the 6 active briefs were moved to the local gitignored `_handoffs/` folder** (`git rm --cached` + `.gitignore`), out of git, kept on disk, history intact. Root clutter → one collapsed folder. *(The Diary `cartograph/_archive/` still holds the 8 items from the 2026-07-18/21 retirement batches, tracked — a follow-up call if those should also leave git.)* Historical detail of the batches below.<br>Original C6 (`cartograph/_archive/`, per CLAUDE.md; the 2026-07-18 + 2026-07-21 batches both landed there flat — that is now the settled home, not `_archive/handoffs/`). **✅ 5 archived 2026-07-18** (`DEAD-END-MOUTH-FORENSIC`, `HANDOFF-ls-planting-LANDED`, `HANDOFF-section-perf`, `SPAR-SKELETON-FORENSIC`, `SPLINE-18TH-FINDINGS`). **✅ 4 archived 2026-07-21** (`HANDOFF-hipointe-pour-step0-LANDED` + `HANDOFF-boundary-trio-SUPERSEDED` — both per `EXTENT-EXCAVATION.md §312`, with retraction banners; `HANDOFF-designer-load-forensic-DONE` — forensic delivered, README ref-note was stale, refs repointed; `HANDOFF-animator-unify-LANDED` — `0c39e8ce`, folded to `OPERATIONS §64`, zero refs). **Still pending:** `ls-statistical-planting` (checked 2026-07-21 — **NOT done**; future-tense "retire once landed" clause — stays); `thrunode-gate-fix` / `grove-neighborhood-axis` (LIVE A2/B6 detail-homes — archive when their lines land). The ~14 other done-stamped root handoffs stay put as live ROADMAP detail-homes and ride the pruning discipline (strike + archive when their line lands). S · → `ACCORDANCE-REVIEW.md` detritus survey.
- **C7 · LS pre-public cleanout** — mode-conditional `rollupOptions.input` (drops the ~4.5 MB cartograph chunk from prod), `copyPublicDir:false` allow-list, deployment-ID audit gate, two verified-orphan deletions. M · → `plans/pre_public_cleanout.md`.
- **C8 · render-pipeline-install Ph4/5** — the SCENE tree onto the one manifest + fold render-conformance/preview-measurement in as installer capabilities (makes the render path legible + parity structural). M · → `HANDOFF-render-pipeline-install.md`.
- **C9 · Detritus-survey code-legibility items** *(2026-07-18 survey — record in `ACCORDANCE-REVIEW.md`; Tier 1 archive sweep already done)*. Headline: the *source* is disciplined (7 `TODO/HACK` markers repo-wide, near-zero commented code) — the sediment is docs + `scratch/` + 2–3 god-files. Three tracked, none urgent:
  - **(a) Decompose `cartograph/derive.js#deriveLayers()`** — a single **~3,465-line function** (L1009→~4474, 75% of the 4,607-line file) — into named phases like its siblings (`detectDividedStreets`, `correctStreetWidths`, …). A deliberate SHAPE-core refactor, **eye-gated, NOT a delete**. L. *(Distinct from C4's `buildBlockGeometryV2.js` excision — different file.)*
  - **(b) De-barnacle the patch-over-prose docs** — `arborist/FEATURES.md` (40 dated + 31 `Brief` tags, incl. "read X below as Y" clauses) + `arborist/ARCHITECTURE.md` (713 ln) + `meteorologist/NOTES.md` (957 ln): pull the dated decision-log to `_archive`, rewrite the "read-as" clauses to plain current-state. Finishes the FEATURES voice sweep. M · folds with **C5**.
  - **(c) `scratch/` sediment triage** — 1,048 files (~459 images); archive/delete the pre-July render-dumps + stale `.md`. It's git-tracked, so cut deliberately. S.

---

# THE HORIZON — approaching, not far
> Bumped below the near-term push, but the horizon is coming up fast — these won't stay here long, and
> several are already part-planned or part-superseded. Re-tier into the push as a column empties.

- **H1 · Publish-confidence / mobile-measurement** *(the confidence-half of Column B's phone goal)* — `preview-measurement` (**real per-tier device numbers = the hard gate**; today `phone-hi==phone-lo`, both guessed) · render-conformance Ph4–7 · mobile-profile B/C/E · phone-slice frustum culling (trees + buildings) · hero-motion smoothness (spatial Arch-bounce → catmullRom cusp/FOV) · the 7 Browse-shot looks · heroTier wrong-camera-target (~1200 m, unblocks all tree-LOD). L · → `HANDOFF-preview-measurement.md`, `HANDOFF-render-conformance.md`, `HANDOFF-mobile-profile.md`, BACKLOG NOW.
- **H2 · Altadena / CDP pour** — ground-refine-cdp scale blocker (26.3M tris/88 min → per-hood knob) · the Stage landscape-upload flow (sets `landscape.source`, unbuilt) · mountain radial cut + shading pass · fix the wrong (LS) lamps. L · → `HANDOFF-altadena-pour.md`, `HANDOFF-altadena-mountain-hero.md`, `HANDOFF-ground-refine-cdp.md`.
- **H3 · HiPointe productization** — building-position-from-slab (dead clicks / missing pins) · content-join re-join (24 stale listings) · city-county-divide render · HPDM lamp coord bug + trees eye-gate. L · → respective HANDOFFs.
- **H4 · Meteorologist** — the **Tuner** (the `capture-render` primitive T-1 gates everything) · cloud-specialist morphology renderer · 8 weather-wiring gaps (W-1…W-8) · sky-pivot seasonal deviations · Preview Studio · wind-field · audiologist. L · → `meteorologist/TUNER.md`, `HANDOFF-cloud-specialist.md`, `meteorologist/STATUS.md`.
- **H5 · Streaming-slab spatial chunking** *(Jacob, 2026-07-17)* — chunk the monolithic merged-mesh slab at coarse region granularity + stream just-in-time along the fixed hero pan → attacks mobile OOM. **Composes with Column B** (impostor placeholder first, geometry streams in). L · → BACKLOG Front B, `SLAB-CONTRACT`.
- **H6 · Arborist depth** — leaf-base library gaps (~6) · phenology (season/color ramp + annual-cycle editor) · STAGE-2 Sugar-Maple vertical slice · full dossiers · green-light readiness gate · night-emissive foliage · birch-bare render. L · → `arborist/BACKLOG.md`, `arborist/README.md`.
- **H7 · LS app features** — Cary order pipeline + POS (L) · generic place-card templates · honorary-townie endpoint · TOD-aware OG preview · **LS Apps-Script security ship-blockers (🔴)** · ~12 completeness gaps (event edit/delete, community-stats hydration, courier-count stub…). S–L · → `ls/BACKLOG.md`, `ls/STATUS.md`.
- **H8 · Kit intake** — custom street-data import (generalize `survey.json`) · the chassis-tagging gauntlet (the manual 0/241 pass — surface built, tagging not) · productionize the scene-generic content-intake stage. S–L · → `SKELETON.md`, `[[project_chassis_tagging_gauntlet]]`, `NEIGHBORHOOD-INPUTS §11`.
