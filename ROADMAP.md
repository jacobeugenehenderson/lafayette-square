# ROADMAP — the whole remaining board, pruned to zero

> **What this is.** The one **cross-domain** master of everything left to do — cartograph, arborist,
> meteorologist, the `ls/` public app, security, kit-productization. It exists because we're in the
> **home stretch**: the whole backlog (not just MEMORY's immediate task) must be *constantly surfaced
> and pruned until it is gone*. This is that surface.
>
> ## ⛔⛔ THE ORDERING CONSTRAINT — read before re-prioritizing anything (Jacob, 2026-08-06)
>
> **LS is the current priority, and the point of fixing it is that it is the HARDEST CASE IN THE KIT.**
> It is historical, idiosyncratic and the most heavily authored scene we have — so a method that closes
> on LS closes on town #2 by construction, and one that needs LS-specific knowledge was never a method.
> ⭐ **"Fix it once and for all" is the bar**: not "make LS look right," but *"the class is closed and
> the check that proves it runs on a town nobody has looked at"* (`CLAUDE.md` Layer 0).
>
> ### ⛔⛔ TWO KINDS OF WORK, AND LS SITS AT OPPOSITE ENDS OF EACH. **[`EXTENT-DESIGN.md`](EXTENT-DESIGN.md)**
>
> **ALL OF THIS IS CO-EXISTENT WITH THE STILL-ONGOING MIGRATION TO THE EXTENT TOOL**, which was
> **bolted onto the front of the product** and has open issues of its own. Anything here must *fit*
> that migration rather than assume it has landed. ⭐ **It is a SEPARATE PROJECT with its own ruled
> ordering, and reading only the constraint above will send you the wrong way** *(it did, 2026-08-08)*.
> ⚠️ **Both get called "working on Lafayette Square." They are not the same job:**
>
> | | **FIXING THE GEOMETRY** — sidewalks, curbs, corners *(this board)* | **MOVING A TOWN ONTO THE EXTENT TOOL** — "conforming" it *(`EXTENT-DESIGN §6`)* |
> |---|---|---|
> | **LS goes** | **FIRST** | **LAST** |
> | **why** | it is the most authored, most irregular map we have ⇒ a method that survives LS travels anywhere | it is live production and has **never been poured** — every other town already lives in `clean/<scene>/`; LS still sits at the old shared paths, which is *why* it is the fallback. *"I can't abide HPDM getting off on the wrong foot since it's the only actual commercially requested map"* (Jacob, 2026-07-22) ⇒ HPDM hardened first, LS moved last, **never the night before a demo** |
>
> **In one line: fix LS's sidewalks first; move LS's plumbing last.**
>
> ⭐⭐ **AND THE SCOPE RULING BOUNDS WHAT COUNTS AS A DEFECT AT ALL** (`EXTENT-DESIGN §6`, Jacob
> 2026-08-08): **v1 hardens the MSBF path — LS + HPDM. Those two are the most CORRECT and the ones to
> MAINTAIN AND SAFEGUARD.** ⛔⛔ **ksi-y-m-yn + centrum ARE EXCISED (Jacob, 2026-08-13: *"we made them for a pitch, we made the pitch, it is over"*; escalated to deletion 2026-09-19).** Out of the roster, the app, the git and the disk. ⛔ **There is nothing left to skip and no name to write down:** a town leaves scope by leaving `public/looks/index.json`, and `checks/_scenes.mjs` enumerates DECLARED ∩ MEASURABLE. *(The older instruction "a check must report CHILLERED, not a number" is retired — it produced seven hardcoded name lists, three carrying `altadena`, a live town that consequently went unmeasured while announcing a ruling that never covered it.)* ⇒ **HPDM is now the ENTIRE town-#2 evidence base.** altadena *"stays working but unpolished"* — **we
> simply have not gotten to them.** ⛔ **So an LS-shaped value in one of those three is USUALLY "not
> gotten to," NOT a live blocking bug** — file it, size it small, and do **not** let it outrank the two
> maps that matter. **Deferred ≠ forked:** one code path, no per-town branch. *(Added 2026-08-08 after
> a standup proposed an LS-first, all-five-towns plan off this board alone.)*
>
> ⭐ **The LS-bleed class has a RATIFIED ROOT CURE — do not patch it site-by-site.** `EXTENT-DESIGN §2.1`:
> LS is not a scene. Every other hood is served from `clean/<scene>/`; **LS's artifacts live at the
> shared default paths** (`src/data/{ribbons,buildings,street_lamps,landmarks,park-feature-elev}.json`),
> name-imported across the app — so `src/data/*` is *simultaneously* "the shared fallback" **and** "LS's
> own render data." **That conflation is the root the whole bleed class grows from.** `§6` step 4
> retires those imports and closes the class at the root. ⛔ Before filing another bleed instance here,
> check whether step 4 already owns it.
> ⛔ **Consequence for the doc corpus: several docs cannot be brought to a settled state yet, and their
> "stale" banners are therefore BLOCKED-ON notices, not neglect** — `ls/STATUS.md`, `ls/BACKLOG.md`
> (bodies predate the slab-merge world) and `_archive/HIPOINTE-DEMUN-ROSTER-ROTTEN-2026-09-13.md` (stats predate the Extent
> re-pour + the 2026-07-08 parcel-frame fix). **Do not "tidy" them into a settled state, and do not
> quote their bodies as current.** They close when the migration does.
>
> **How it's built (one home per fact).** This is a prioritized **index**, NOT a restatement. Each
> line is a one-liner + a size + a **→ pointer to its detail home** (a domain `BACKLOG` section or a
> `HANDOFF-*.md`). The pointer is the truth; this file is the *ordering*.
>
> ▶ **Detail-home relocation (2026-07-21).** The `HANDOFF-*.md` files these lines point to were moved
> out of git into the local, **gitignored `_handoffs/`** folder (Jacob: declutter root + no git backup).
> Pointers below name the file by **basename** — find it in **`_handoffs/`**. Active dispatch briefs
> are tracked in **`docs/briefs/`** — **`ls docs/briefs/BRIEF-*.md` is the roster; do not maintain a
> copy of it here.** ⛔ Not the repo root: that returns nothing and reads as *"no open briefs"*
> (`docs/agents/BOZ.md §3`, which owns this fact). Landed/superseded
> items from the 2026-07-18/21 retirement batches are in `cartograph/_archive/` (Diary, tracked). Git
> history is the permanent archive of every version.
>
> **The pruning discipline.** When an item lands, **strike it from ROADMAP *and* its detail home in
> the same breath** (the accord sweep — `docs/agents/BOZ.md §4`). Never let a done item sit here. Tiny aesthetic
> bugs get *added* here as they surface (or vanish on their own) — the list breathes until it empties.
>
> **Sizes:** S = hours · M = a session · L = multi-session.
> **DoD everywhere = Jacob's eye on the real render, never a proxy** (`feedback_proxy_render_is_not_the_operator_eye`).
>
> *Established 2026-07-17 (Boz + Jacob, the whole-corpus appraisal). The frame is Jacob's: two
> columns to done + close security, then the tail is process-cleanup ahead of human-developer
> engagement. Reachable from `ORIENTATION.md` + `README §⭐ START HERE`.*

---

## ⚠️ Live / blocking defects — do on contact

### ⭐⭐ HURON — THE FIRST POUR OF A TOWN NOBODY HAD LOOKED AT (2026-09-19, Jacob's eye)
> **The protopolygon did "pretty good".** What follows is what the first genuinely
> unfamiliar town exposed — the evidence base `ORIENTATION` says the kit exists to
> produce, and it arrived in one evening. ⛔ These are OBSERVATIONS with causes NOT
> established; do not reason forward from them without measuring.

- **H-1 · ✅ CLOSED `67dbfcd6` — a fresh pour lands in SURVEY.** Root: `tool` initialises from `localStorage['cartograph-tool']`, **one global value, not per-scene**, so a town whose SHAPE nobody had looked at opened in Measure because of what the operator was doing on a *different* town. Set on both pour paths only — a return visit still lands where you left off. ⛔ Was: *"The sequence demands that things bake in a specific sequence."* Survey is SHAPE (step 4), Section is FILL (step 6); landing the operator in FILL on a town whose SHAPE has never been looked at inverts the chain. S · → `ORIENTATION` the dependency chain, `ExtentApp.jsx` the post-bake hand-off.
- **H-2 · ⭐⭐⭐ THE KIT HAS NO CONCEPT OF A NATURAL FEATURE, AND ON A WATERFRONT TOWN "HALF OUR MAP READS LIKE A MISTAKE."** The street index is built from `ground.highway` features ALONE, so a shoreline, a river and a lake are not merely unfilterable — they were never candidates. Consequences, all one root: the boundary picker cannot offer the water · `EXTENT-DESIGN §8 Q1` ("must the boundary close?") is unanswerable by the tool for any coastal hood · and **H-4**. ✅ **THE FETCH HALF LANDED — `e02fafe4`, 2026-09-19, "The lake was never in the fetch, because a lake is a relation."** `fetch.js` now issues `relation["<tag>"](bbox)` for the whole tag set with members scoped to the envelope (`way(r.rels)(bbox)`), and a ring that cannot close inside the envelope is marked `clipped` rather than papered over — closing it is the stencil's job. ⭐ **VERIFIED ON DISK 2026-09-21**, in the re-fetch of 2026-09-20: `cartograph/data/huron/raw/osm.json` carries **Lake Erie, `osmType: relation`, 1,329 points, `clipped: true`**, alongside 28 other relation-sourced features. ▶ re-derive with the one-liner in the consumer half below. ⛔ **WHAT IS STILL OPEN IS THE CONSUMER HALF, AND IT IS THIS ROW'S HEAD:** `pipeline.js` (`deriveLayers(snapped.ground.highway)`) and `skeleton.js` still read `ground.highway` ALONE, so the water is acquired and then not a candidate for anything downstream.
  - ▶ **MEASURED: 35.5% of Huron's disc is Lake Erie — 13.96 km² of 39.35 — and 41 of the municipal ring's 82 vertices are offshore.** That is *"half our map reads like a mistake"* with a number on it.
  - ⭐ **The vocabulary half's root, confirmed:** `skeleton.js:1587` `const highwaysRaw = osm.ground?.highway || []` — a single source. Huron's `street-index.json` carries 422 chains, **zero** waterway or railway; `streetNamesFor` reads `skel.streets` and inherits it.
  - ⚠️ **And `railway` is fetched but has no `tagPriority` entry**, so 43 features — including the NS Chicago Line — land in the wrong bucket. The other half of `§0.7` item 4.
  - ▶ **THREE INDEPENDENT GAPS, NOT ONE — sizing H-2 as "the skeleton reads more than `ground.highway`" understates it by two thirds** *(Strand, 2026-09-19)*: **① ACQUIRE the water** — ✅ `e02fafe4` · **② let the picker OFFER it** — ⭕ blocked on a ruling: the boundary picker reads `skeleton.json`, **not** the `street-index.json` two emphatic comments say it reads. Measured ASPIRATION, not regression: at `fe1bb3a1`, *the commit that introduced the index*, the lookup was already skeleton-first. Only huron has an index at all; LS/HPDM/altadena have none. · **③ let the map DRAW it** — 🔵 **OUT OF SCOPE** (`H-8` demoted: *"water doesn't matter; it's the shoreline"*).
  - ⭐⭐ **SO THE ARC IS `H-4` ALONE, AND ② MAY BE MOOT:** a shoreline is not a street the operator picks, it is an edge the map HAS — like the disc rim, which nobody picks. The ruled construction is a **COMPOUND `boundaryPolyXZ` — disc MINUS water**, changing no owner class: shore edges carry `__boundary__`, no measure, `depthAt → 0`, land-use floods to the edge, no curb at the water. ⭐ Its one cost — *it cannot tell shore from rim* — is close to free once the water never draws, and `ORIENTATION` already rules that shape: **"the rim is an EDGE OF THE DRAWING, never an absence."**
  - ⚠️ **Premise still to check before building:** Erie is a 19-way relation whose members carry **no tags**, assembled by us. A ring that self-intersects, gaps at member joins, or carries islands as separate outer rings would **poison** the compound boundary rather than bound it. Measure the ring before subtracting with it. M–L · → `SKELETON.md`, `serve.js streetNamesFor`.
- **H-8 · ✅ MOSTLY CLOSED 2026-09-20 — ⭐ THE CLAIM THAT DEFINED THIS ROW IS NOW FALSE.** It read *"NO TOWN BUT LAFAYETTE SQUARE CAN DRAW WATER"*, on the strength of a one-line `natural=water` skip in `MapLayers.jsx`. ⛔ **That skip is still there and is now CORRECT and explained:** water has its own producer — `derive.js` `layers.water` ← `mintProtopolygon`'s `waterRings` ← the coast/relation path — and **a second producer for one feature class is the bug, not the fix**. Both renderers route to it (`bake-ground.js` NATURAL_KEYS, `MapLayers.jsx` `groundByKind`), `natural:water` is declared `none` in `OSM_LU_DECLARED` **with that reason**, and huron's slab carries `water:lake` at renderOrder 34. The old text is retired rather than kept beside its replacement.
  - ⛔ **WHAT IS ACTUALLY OPEN IS PRODUCER COVERAGE, NOT VOCABULARY, AND IT IS MEASURED.** huron's own pour prints it, every run: *"40 water bodies lie wholly inside the disc — a pond is not a coast, not applied"* and *"15 closed water bodies cross the rim but are held WHOLE by the fetch — not applied"*. ⇒ **55 water bodies on one town are acquired, classified, and drawn by nothing.** ⭐ And the finding came out of the POUR'S PRINTED OUTPUT, read by someone running it for another reason — the producer has been saying what it does not do all along and nobody had read the line.
  - ⚠️ **It changes what the water-shader work is.** A beautiful lake shader that leaves 40 ponds invisible is **worse** than today, because the lake will look finished. ▶ Establish which water a town actually DRAWS before designing anything on top of it. → `docs/briefs/BRIEF-water-shader.md`, `H-2` ③.
  - ⭐ **The compound-face problem was already solved here by hand** and is the shape the general capability should reach: `park_water.json` carries `lake.outer` + `lake.island` — LS's lake has a HOLE, hand-authored, in a bespoke file, for one pond. ⇒ let that file become **one town's authored override of a general capability**, which is `ORIENTATION`'s *"the override IS the product"* exactly. M · → `H-2`, `EXTENT-DESIGN §2.1`.

- **H-3 · The highway / non-street handling is "problematic."** Cause not established; distinct from H-2 and not yet separated from it. ⛔ **ITS "related and measured" SUB-CLAIM IS NOW FALSE and is evicted, 2026-09-21:** it read *"the intake carries zero `ref` tags, so a road known by its shield (US 6, SR 2) is invisible even when the road itself is fetched."* Re-measured on `cartograph/data/huron/raw/osm.json`: **242 features carry a `ref`, 195 of them on `ground.highway`** — `US 6`, `CR 132`, `TR 1307`. The shields are in the intake. ⚠️ That says nothing about whether anything downstream READS them, which is `A09`'s question and is unmeasured. S to measure · → `A09`.
- **H-4 · ⭐⭐ THE PROTOPOLYGON WILL FAIL AT THE WATER'S EDGE — "because it itself is a polygon boundary."** *(Jacob, 2026-09-19.)* ⭐ **And his reading of the difficulty is the part to keep: this should be EASIER than a man-made feature, because a shoreline is absolute and permanent(ish) where a curb is authored and negotiable.** ⛔ Do not file this as a hard case; file it as the case ① was always going to have to meet. Gated on H-2 — but ⛔ **NOT where H-2 said any more: the water IS on disk** (Lake Erie, as a clipped relation ring, verified 2026-09-21). The remaining gate is H-2's CONSUMER half: nothing downstream of `ground.highway` can see a natural feature yet.
  - ⛔⛔ **MY BRIEF WAS WRONG ABOUT THE SEAM AND STRAND SETTLED IT AGAINST ME.** I wrote that `mintProtopolygon`'s `boundary` param means ① already models a non-street boundary edge. It does not: `derive.js:5113` passes the **DISC**, and `tileGround.js:547` rules *"⭐⭐⭐ THE CIRCLE IS THE STENCIL, NOT A CHAIN"*, citing `RIBBONS §1`'s *"THE RIM BOUNDS, IT DOES NOT OWN."* ⚠️ Also: the **live** path passes no boundary at all; only prebake does.
  - ✅ **RULED AND BUILT 2026-09-20 (Jacob), and it is NOT the disc-minus-water construction this slot reserved** — that is retired rather than kept beside its replacement. *"Stroke the coast as a two-sided chain"* · *"the lake is a discrete polygon made of shoreline and bb. The OTHER SIDE of the shoreline isn't necessarily a continuous ring; it is separate, via the protopoly, from the lake."* ⇒ **TWO objects:** the closed ring is the lake face (shoreline + bb); the OPEN arc is expanded at ε like any chain, so the land closes against ①'s own edge carrying a per-vertex `left`/`right` stamp. ⭐ **That articulation is the point** — a slipway T's into the shoreline exactly as a street T's into a street, so water access needs no rule of its own. ⛔ The earlier *"not stroked as a chain"* ruling was measured on the CLOSED ring and still stands for it; `clipToRect` yields the OPEN arc, which the same note names as the safe case.
  - ⭐⭐ **AND THE FRAME IS THE bb, not the ink bbox** — the change that actually moved it. Overpass returns whole ways, so the ink bbox is 20.6 × 16.4 km on Huron while the shoreline spans the bb's 9.9 km: the coast ended **4.2 km short of the frame**, the land flowed around both of its ends, and no amount of stroking could close it. ⛔ The bb is the FROZEN DATA EXTENT, not the disc — the circle still stamps LAST. ▶ Measured after: coastal streets bounded only by the exterior **43 → 0**, largest block **341.87 km² (the frame itself) → 5.04 km² (the coastal zone, 1,189 `__water__` edges)**, asphalt rings **20 → 410**. Eye-gated by Jacob on huron.
  - ⚠️ **OPEN — the non-coastal regression check is UNRUN, and it is not coast-only.** The frame change affects any town whose ink spills past its bb, and **every town's does**: LS 7.9% of ways · staging 8.4% · altadena 4.5% · HPDM 2.9% · huron 1.7%, overshooting up to 7.7 km. The claim that it is harmless elsewhere rests on the disc stamping well inside the bb, which is **reasoning, not a measurement**. ▶ Re-pour one non-coastal town and diff its block count + in-disc tile count: `CARTOGRAPH_SCENE=altadena node cartograph/pipeline.js --skip-elevation`. ⛔ Do this before the next altadena or LS pour, not before huron ships. S · → `RIBBONS §1`, `A21`.
- **H-5 · ⭐ THE ARBORIST BELONGS IN THE INITIAL GRAB — it is part of the INPUTS sequence.** The **USDA foliage survey** is a fetch, not a later step; given a current arborist library the trees should be **placeable from Section onward** rather than bolted on at bake. ⭐ This is `EXTENT-DESIGN §3.1`'s Inputs-tab north star reaching a real consumer. M · → `arborist/README`, `EXTENT-DESIGN §3.1`, `INTAKE-CATALOGUE`.
- **H-7 · ⭕⭐⭐ RULING OWED — DOES THE HERO-OBJECT PROGRAM BELONG IN THE KIT AT ALL?** *(Jacob, 2026-09-19, opened by the first real pour: "worth delving into now because we're really pouring real hoods now.")*
  > *"We added the hero object so that scenes could have a hero subject. But I guess fundamentally, we don't **require** one; we set it up so the camera was always looking at the hero object… but is that the right idea? … I wonder if that's actually a St. Louis specific concern? Like, if we were doing the Hollywood sign we wouldn't even try to do it thru the kit, that would just be a custom install. I think we need to either flesh out this part of the hero program or rethink including it in the kit."*
  - ⭐ **The code already treats "no hero" as first-class** — `heroSubject.js`'s undesignated path resolves to the hood centroid and calls it, in its own comment, *"the answer for every installation that doesn't own a Gateway Arch."* So the kit is not built on the assumption Jacob is questioning; the assumption lives in the **default Look**, which is a St. Louis town.
  - ⭐⭐ **A framing that may dissolve the question: TWO THINGS WEAR ONE NAME.** A **set-piece** is a modelled object the whole map orients to — the Arch, the Hollywood sign — i.e. *geometry the kit did not pour*, which is what makes it smell like a custom install. A **framing subject** is merely a point the camera favours — Huron's jetty, the water. ⛔ The first needs an asset pipeline; the second needs **a pin**. `heroSubject` is already `{kind, id}`, so a pin is a new `kind`, not a new concept.
  - ▶ Jacob's own candidate for Huron is *"the water or the jetty that's sort of in the middle of the coastline… maybe we can drop a pin?"* — which is the framing-subject case, and it is the one every town can satisfy.
  - ⛔ **Not a coordinator call.** ⚠️ Gates nothing today: the kit-default Look stores no camera (his ruling, same night), so a fresh pour needs no hero to open correctly. → `A11`, `src/lib/heroSubject.js`, **H-1**.
- **H-6 · ✅ THE GATEWAY ARCH WAS HURON'S HERO, SITTING IN LAKE ERIE — cleared for this town 2026-09-19.** `heroSubject {kind:'arch'}` + `arch.values` arrived **byte-identical to LS's** via the Look seed. Removed from `public/looks/huron/design.json`; it now falls to the hood centroid, which `heroSubject.js` itself calls *"the answer for every installation that doesn't own a Gateway Arch."* ⛔ **The ROOT IS UNFIXED and is `A11`** — `createLook({name, scene})` passes no `fromLookId` (`ExtentApp.jsx:1731`/`:1788`), so the next town inherits it too. ⛔ **CORRECTION, same night (agent KILN):** I wrote that Huron was *"spared by luck — no street-name collisions."* **False.** `seedDesignForScene` (`serve.js`) STRIPS `SCENE_KEYED_DESIGN_FIELDS` on any cross-scene seed and 409s on undeclared residue; ▶ `node checks/claims-look-seed-scene-clean.mjs` (7/0, incl. *"the Pour's seed is scrubbed for a new scene"*). Huron's `blockCustoms` differ from LS's **because they were stripped.** The geometry half is CLOSED and `A11` already said so (🟡 *mechanism closed, artifacts not cleaned*). ⚠️ I read `serve.js:805` — the fix's own header comment, narrating the damage it repaired — as a live hazard.
  - ⭐ **THE LIVE DEFECT IS THE LOOK CHANNELS, AND IT IS NOT THE SEED.** Huron is **46 of 54 channels byte-identical to LS**; the 8 that differ are exactly the ones the stripper removes. ⛔ **And stripping alone cannot fix it: `useCartographStore` hydrates `absence` as `inherit`** (`heroKeyframes`, `buildingPalette`, `labels` — `d.X || get().X`), so open LS in Stage, open a stripped town, save, and LS's values land on its disk. **A kit-default Look is cosmetic until that inherit is killed.** [[feedback_absence_means_inherit_in_authored_blocks]] · `A11`.

- **H-23 · A PERIODIC STUTTER IN CAMERA MOVES — "1, 2, click. 1, 2, click." ⛔ MECHANISM READ FROM SOURCE, CAUSE NOT ESTABLISHED.** *(Jacob, 2026-09-21; and he is "not totally sure" the mechanism below is it — do not treat the rhythm as a diagnosis.)* ⭐ **What IS established, by reading `CelestialBodies`:** the shadow map re-renders only when the focus has slid past ONE TEXEL (`_focus.distanceToSquared(_prevFocus) > texel*texel`), and that re-render is a full scene draw into a **4096²** map. So a gliding camera holds for N frames and then pays a whole extra scene pass — a periodic cost whose PERIOD SCALES WITH THE SHOT, because `texel = 2·half/4096`: **0.13 m close · 0.50 m mid · 1.73 m town-wide on huron** (4× LS's 0.44 m). ▶ **THE DISCRIMINATOR, ten seconds and no instrument:** pan at the same speed CLOSE, then WIDE. Clicks getting rarer as the shot widens ⇒ this gate. Identical rhythm ⇒ not this, and stop here. ⚠️ **AND THERE IS A REASON TO DOUBT IT:** `H-9` records that removing ALL 144 instanced meshes moved 8.5→8.8 FPS — that also empties the shadow pass, so if this re-render were the expensive thing, that experiment should have shown it. ⭐ Which sharpens H-9 rather than this row: **the unaccounted ~130 ms survives with no geometry in EITHER pass, so it is not drawing triangles.** → `src/components/CelestialBodies.jsx`, `H-9`.
- **H-9 · ⛔⛔ THE FRAME RATE. 8.5 FPS in Preview, 6.3 in Stage, SCENE-WIDE — AND IT IS NEITHER TRIANGLE- NOR FILL-BOUND.** *(huron, 2026-09-21.)* ⛔ **CAUSE NOT ESTABLISHED, and the eliminations are the value of this row — do not re-run them.** Removing **ALL 144 instanced meshes (~138M tris)** only reached **8.8**. A **36× pixel-ratio cut** only reached **7.6**. ~130 ms/frame is unaccounted for. ⚠️ Distinct from the shadow-box flash, which WAS found and fixed (`ff8cf53a`); the parity probe is what separated them — it measured the camera as smooth and monotone *at 8.5 FPS*, so the flash was not judder.
  - ⭐ **AN UNEXAMINED LEAD THAT FITS THE PER-FRAME-CPU CANDIDATE, AND NOBODY OWNS IT:** measured on huron, **`heroTier: 2375 mesh / 0 impostor`** — the tree impostor path is **not engaging**, so 18,616 trees render as full meshes rather than cards. ⚠️ A lead, not a diagnosis.
  - Two candidates stand and **neither is measured**: per-frame CPU (huron carries **111,696 instances** against LS's 15,327) and the **20-pass post stack** stalling on GPU sync.
  - ⛔⛔ **AND THE INSTRUMENT RULE THIS ROW EXISTS TO CARRY: `gl.info.render.frame` COUNTS RENDER PASSES, NOT FRAMES** — 20 per frame here. Two instruments on one page read **88–99 "fps" and 8.5 fps at the same moment.** It is a valid **LIVENESS** gate (zero means nothing draws) and **is not an FPS meter**. ⛔ Do not open this row with a number taken from it. M–L · → `Column B`, `ARCHITECTURE §8 Cast shadows`.
- **H-10 · `asphalt ∩ stripe` — 194 m² SURVIVES THE BAKE-TIME FLATTEN, CAUSE NOT ESTABLISHED.** Measured at 10 cm cells in a 500 m window, after cutting: 386 → **194 m²**, 39% of stripe's own area. ⇒ cutting **halved it and did not finish it**. Candidates: clipper's **mixed winding** in the accumulator (29 of 392 stripe rings are CW, and mixed winding under `pftNonZero` cancels regions while leaving the paths in place) and **holes touching the asphalt boundary** rather than sitting inside it. ⛔ **Neither is measured, so neither is the answer.** ⚠️ `stripe` therefore **cuts but keeps its own slot** — a guard against an unexplained residual, not a design choice, because coplanar that 194 m² would z-fight and ship flickering road markings. ⭐ **Remove the guard the day the residual measures zero, not on the grounds that it should be.** ▶ **JACOB'S READ, 2026-09-21, UNTESTED AND RECORDED AS A LEAD:** *"This smells like Skeleton and the protopolygon."* ⛔ Neither of the two candidates above is a skeleton/① story, so this points somewhere they do not — test it BEFORE spending on them. ⭐ It is also checkable cheaply: the residual is measured per scene, and huron carries a frozen ① while three towns do not, so the two populations can be compared rather than argued about. S–M · → `bake-ground.js` `flattenPaintStack`, `ARCHITECTURE §8`.
- **H-11 · ⛔ THREE TOWNS CANNOT RE-BAKE UNTIL THEY ARE RE-POURED — altadena · hipointe-demun · lafayette-square-staging.** They refuse with *"carries NO frozen protopolygon: it has not been poured since ① landed"*. ⇒ **they do not get the single-plane ground, and `claims-coplanar-groups-do-not-overlap` stays RED on them, on purpose** — a green check there would be a lie. ⭐ **VERIFIED PER SCENE 2026-09-21** — `clean/ribbons.json#protopolygon`: huron has one (rings 214 · blocks 220 · boundaryRing 256); hipointe-demun, altadena and lafayette-square-staging have **none**. ⭐ **A SECOND CHECK IS NOW RED ON THE SAME THREE TOWNS BY A DIFFERENT ROUTE** — `claims-every-baked-species-has-an-impostor` reports hipointe-demun shipping **4,476 of 8,346 placements (54%) as mesh at every distance**, and lafayette-square-staging (plus `toy`) with no atlas at all. ⛔ Same cause, same remedy: **do not weaken either check to make the suite green** — both are honest reports of a town that has not been re-poured. ⚠️ Pre-existing; not caused by the flatten. A **full re-pour is Jacob's call**, and it is the same gate `H-4`'s non-coastal regression check wants. S to decide, M to run.
- **H-12 · `bake look failed: 500` ON THE SAVE PATH, LEAVING THE DESIGNER ON LAFAYETTE SQUARE.** Seen by the parity auditor, 2026-09-21. ⛔ **UNPROVEN AND UNDIAGNOSED.** ⚠️ It is adjacent to a second unproven observation from the same night — **whether Stage keyframes PERSIST at all**: `design.json`'s mtime showed that none of that evening's authoring reached disk. Two symptoms, one save path, **and the link between them is not established.** ⭐ **A CAUSE THAT COVERS THIS ENDPOINT WAS FOUND AND FIXED 2026-09-21** and it is why the first move is now cheap: `api.js#bakeLook` posts `/looks/<id>/bake` on the cartograph server, and that server was SIGTERMing itself mid-request — the bake's last act stamps `bakedAt` into `public/looks/index.json`, which `src/instance.js` statically imports, so `node --watch` restarted the server ~200 ms later, during the R2 upload (`claims-the-dev-servers-do-not-import-the-looks-index`). The endpoint now returns 200, twice, verified. ⛔ **NOT CLOSED, AND THE MISMATCH IS THE REASON:** that failure presented as an EMPTY REPLY, not a 500, so `res.ok` was never reached and the message would not have been "bake look failed: 500". Either a second failure exists or the report's status was approximate. ⭐ Its other half — whether any of that evening's authoring reached disk — rides the SAME server and the same kill window, so re-test it before diagnosing it. ▶ First move is unchanged: reproduce, and read what the bake actually returned. S to measure.
- **H-13 · A PREVIEW SUBTREE COLLAPSED TO 10 GEOMETRIES WITH NO CONSOLE ERROR, AND DID NOT REPRODUCE.** Observed once, 2026-09-21. ⛔ **Recorded because it did not reproduce, not in spite of it** — a silent collapse with a clean console is exactly the signature of a non-linking GLSL program (`docs/agents/AGENT-VALIDATION-SURFACES.md §0`), and the one instrument that names the failing line was three clicks away for six rounds on the night this was seen. ⚠️ **Nothing here is diagnosed.** If it recurs: **browser console first**, then a second differently-shaped measurement — network or causation, never another counter. S · → `AGENT-VALIDATION-SURFACES §0`.
- **H-14 · 👁️ AWAITING JACOB'S EYE — huron's cast shadows.** The pass was fixed and measured (`3dcb5dd3`), and the flash the fix itself introduced was found and fixed (`ff8cf53a`). ⛔ **No eye verdict is recorded anywhere**, and the landing commit said in terms *"FEATURES unchanged until this is eye-gated"* — FEATURES has since been written on the strength of the measurement, not of a verdict. ⭐ **Boarded rather than left in a commit body**, the same call as the LS park eye-gate (`c6d42d75`). **The gate:** do the buildings throw shadows that land where the buildings are, at Hero and at Street, and is the edge softness sane at the authored Penumbra? ⚠️ **A17's rule applies — the eye verdict must record its scene.** S · → `cartograph/_archive/BRIEF-shadows-are-ls-sized-DELIVERED-2026-09-21.md`, `ARCHITECTURE §8 Cast shadows`.
- **H-15 · 👁️ AWAITING JACOB'S EYE — the two-body light rig, AT NIGHT, IN TWO TOWNS.** The sun→moon lerp is deleted and the fills are hemispheres, so ⚠️ **night will look different and that is the change, not a side effect**: `dirMoon`'s authored night value now lands on the real moon instead of on a blend. **The gate this asks for is the CROSSOVER HOUR** — low sun, risen moon, both up, two paths on two real azimuths, moving. ⛔ **Not a still.** S · → `cartograph/_archive/BRIEF-two-bodies-two-lights-DELIVERED-2026-09-21.md`, `ls/FEATURES.md`.
- **H-18 · ⛔ ONE STAGING SITE, N TOWNS — AND THE BLOCKER IS A RULING, NOT CODE.** *"Once we have this whole pipeline together I can build neighborhoods and send the working links to prospective partners"* (Jacob, 2026-09-21) ⇒ **this is the last mile of the product, not infrastructure polish.** ⭐⭐ **THE PREMISE THAT THE KIT CANNOT SERVE A SECOND TOWN IS FALSE — measured 2026-09-21:** `src/instance.js` boots any town from **`?look=`** and already holds a `huron` module; `curl` returns **200** for `assets.theward.online/staging/baked/huron/scene.json` AND `…/ground.json`; the staging site returns **200**. ⇒ **`…/lafayette-square-staging/?look=huron` very likely already renders Huron.** ⛔ **NOT VERIFIED IN A BROWSER — and that ten-second check gates everything else**, because it decides whether this is a NAMING problem (hours) or a ROUTING one (days). **What is actually broken:** ① the URL carries another town's name (repo, Pages path, Vite `--base`) — *this is the complaint* · ② `DEFAULT_LOOK = 'lafayette-square'`, correct for prod, wrong for a preview site · ③ ⛔ **Publish prints `STAGING_SITE_URL`, a module constant, whatever look you shipped — a successful publish handing back the wrong address** · ④ one trunk, one deploy, so publishing any town reships every town. ⛔ **DO NOT TOUCH the slab layer or the staging/prod split — both are already per-town and already guarded** (`bakedUrl.js`: *"one prefix per look… pouring town #2 needs no code change"*; `staging.yml` refuses to fall back to the prod asset base). ### ✅ RULED 2026-09-21 (Jacob): **ONE STAGING SITE PER MAP.** *"One for each separate 'publish to staging' button; so practically, one for each Map or Scene."* ⭐ **THE BUTTON IS THE UNIT, and that is the part to keep** — the ruling is stated in terms of the gesture the operator makes, not of the hosting. One Publish-to-Staging press ⇒ one site with its own address. ⇒ **④ is settled with it**: publishing a town must ship THAT town, not reship every town, because one deploy per button is what "one site each" means. ### ✅ AND THE FOURTH QUESTION IS ANSWERED, 2026-09-21 (Jacob): **UNLISTED, BUT IT PERSISTS.** *"The partner link is 'permanent' insofar as it survives alongside the pushed site (their own custom URLs), but we won't 'publicize' it and nobody will go to it except in the case of updates."* ⭐ **So it is NOT a throwaway preview and NOT a public site — it is a durable address with no audience.** What that buys: the URL may stay ugly-ish and need no SEO, no index, no share card; what it costs: it may **never break**, because a partner holds it and returns to it months later. ⛔ **A staging site is therefore not disposable** — do not design the regime around tearing one down and standing it back up under a new name. ### ✅ RULED 2026-09-21 (Jacob): **THE ADDRESS IS `staging.theward.online/<map>/`, ON CLOUDFLARE.**
  ⭐⭐ **AND THE REASONING IS THE PART TO KEEP, because I got it wrong first and he caught it:** I proposed `huron-staging`, i.e. town #1's artifact name with the town swapped. ⛔ `lafayette-square-staging` was never a pattern — it is what one extra site got called once, and generalising it is Layer 0's mould trap. **LS is not a kit-level term; The Ward is** (`project_the_ward_is_the_player_not_the_neighborhood`).
  ⭐ **THE SAME CRITIQUE KILLS THE MECHANISM, NOT JUST THE NAME.** "A repo per town + a deploy key per town" needs a human in a settings page before a poured town can be seen — a ceremony per instance, which fails town #2 as surely as a skip list. ⛔ Do not replicate the GitHub-Pages-external-repo shape N times.
  ⭐ **THE LAYER BELOW ALREADY SOLVED THIS AND THE SITE LAYER SHOULD MIRROR IT:** `assets.theward.online/staging/baked/<look>/…` — *"one prefix per look under one bucket, so pouring town #2 needs no code change and no entry in any table"* (`bakedUrl.js`).
  ⭐ **THE DISTINCTION THAT FALLS OUT, and it settles prod too: THE STAGING ADDRESS BELONGS TO THE WARD; THE PRODUCTION ADDRESS BELONGS TO THE TOWN.** Staging is our preview of our product, so it lives under our roof. Production is the town's own front door — already modelled as the authored `src/instances/<map>.js#domain`, which ③ now reads and which is `null` for huron. ⇒ `jacobeugenehenderson.github.io/lafayette-square-staging/` is wrong twice: it carries a person's account name AND a town's name, and neither is the product.
  ▶ Now buildable: ① the URL carries its own town's name · ② `DEFAULT_LOOK` per site · ③ below · ④ per-town deploy · **⑤ THE LOAD SCREEN IS LS-BRANDED ON EVERY TOWN** *(Jacob, 2026-09-21: "the current load screen is LS branded, so if we're going to need a local item for load screen we should note that")* — `App.jsx`'s splash renders `BASE_URL + 'favicon.svg'`, one shared file, with `alt={INSTANCE.name}`, so Huron's partner sees **Lafayette Square's mark for the whole load**. ⭐ **The knob already exists and nothing reads it:** every instance module declares `branding.faviconUrl` (LS sets it; huron and hipointe-demun are `null`) and a grep for the field finds only its three declarations — it is honestly labelled *"Phase 4 (index.html inject) — not read at runtime"*, so this is ASPIRATION, not rot. ⛔ A `null` must render the town's NAME or nothing, never another town's logo. ⚠️ First thing a partner sees, and on this site it is seen for a long time. ⭐ **③ is a lie, is the smallest piece, and can be killed today.** ⛔ Deliberately uncosted. → `docs/briefs/BRIEF-a-link-per-town.md`.
- **H-20 · ⛔⛔ A TOWN CANNOT WITHHOLD A TREE SPECIES — THE POUR PLACES SPECIES THE LOOK CANNOT RENDER, AND NO OPERATOR LEVER STOPS IT.** *(Measured 2026-09-21, acting on Jacob's "withhold birch and platanus".)* ⭐ **The Grove's own banner offers withholding as the remedy** — *"no impostor: birch — these render as mesh at every distance. **Withhold them**, or fix the capture"* — so this is ASPIRATION: the UI names a remedy the pour cannot deliver. **What was tried, in order, each followed by a full re-pour:** ① removed `birch` + `platanus_acerifolia` from the roster (`public/looks/huron/design.json#trees`, 13 → 8 entries) → **birch unchanged at 1,679**; ② removed the routing rows (`cartograph/data/huron/tree-species-map.json`: `Birch, River → birch`, `Sycamore, American → platanus_acerifolia`) → **1,679 → 1,041, still not zero.** ⇒ **THE ROOT IS THE CANDIDATE POOL.** `bake-trees#pickVariant` honours the routing first; failing that it falls through to `CATEGORY_FALLBACK` and runs a hash lottery over **every library species of that category**, and the library contains species this Look's atlas will never carry. ⛔ **The roster is a PREFERENCE, not a filter** — `preferComposedTwin` uses it only to swap a twin for a roster sibling with the same canonical, and its own header says so ("an empty roster disables the rule rather than guessing"). So a species with no roster twin is placed regardless of what the operator chose. ⚠️ **The obvious fix — restrict the pool to species the atlas carries — changes the census of EVERY town, so it is not a tidy-up**, and it is exactly the kind of quiet selection change that must be eye-gated rather than shipped at midnight. ⭐ **No longer fatal:** `InstancedTrees` now DROPS an un-atlased species loudly instead of 404-ing its GLB and taking the whole canopy down, so today this costs 1,041 trees rather than all 18,616. ▶ `node checks/claims-every-baked-species-has-an-impostor.mjs`. → `arborist/bake-trees.js#pickVariant`.
- **H-21 · ⛔ THE DRAWN GROUND BREAKS INSTEAD OF STRETCHING — cross-polygon T-junctions, and the gap is ALREADY DOCUMENTED AS UNBUILT.** *(Jacob, 2026-09-21: "the ground layer is stretched over terrain but it is visibly split in places. It really must stretch and not break; it is built the way it is for exactly this reason.")* ⭐ **Not an investigation.** `bake-ground.js` triangulates and refines **each polygon independently** and concatenates with vertex offsets; the red-green conforming pass and its edge-keyed midpoint cache live inside ONE call, so two polygons sharing a boundary refine without reference to each other and a vertex on one side has no partner on the other. On a GPU-displaced mesh that T-junction is a hole. `ARCHITECTURE §8` says it outright: *"the conforming guarantee is WITHIN a polygon; cross-polygon (group-boundary) conformity is not yet handled."* ⇒ Class **ASPIRATION**. ⭐ **Signature kit shape — invisible on town #1:** crack height is bounded by how far the two sides disagree about the terrain between them (≥ `GROUND_REFINE_TOL_M` 0.50 m, more where it bends), so LS's ~35 m of relief hides it and huron's shore — 859 steps over 1 m within 40 m — does not. ⛔ **Do not merge with the OTHER shore seam** — huron's terrain sitting 1.13–4.77 m above the lake plane at 100% of 1,894 vertices was a systematic offset, not a crack, and it was **fixed the same day** (`cdd41f6e`, the datum is now derived from the water surface). ⛔⛔ **That fix changed a contract: `terrain.bin` NOW GOES NEGATIVE below the waterline** — 814,882 of 2,064,969 huron samples (39.5%), min −1.20 m — so nothing may assume the heightfield starts at 0. ▶ Deliverable is a check — no ground vertex in the interior of another ground triangle's edge, over every slab on disk — plus a STREET-LEVEL eye gate, because the canon says these are "invisible from overhead, glaring at street level". → `docs/briefs/BRIEF-ground-cross-polygon-conformity.md`.
- **H-22 · huron's TICKER AND SOCIETY PAGES ARE EMPTY, AND THEY ARE TWO DIFFERENT DEFECTS.** *(Jacob, 2026-09-21, on the working staging site: "there is no ticker and the society pages aren't wired.")* ⛔ **Do not fix them together.** ① **THE TICKER WAS A CONTENT GAP — ✅ HALF CLOSED 2026-09-22, and closing it exposed `H-30`.** `EventTicker` builds "one entry per listing" from hours/schedules/descriptions plus `seedEvents`, and huron's 285 listings carried **0 with `hours` and 0 with `description`** — so the ticker had nothing to say. **Three research beats then landed 24 places (`62287231`): hours 0 → 10, description 0 → 15.** ⭐ It runs now. ⛔ **But only THREE listings can ever reach it**, because `TICKER_EXCLUDED` throws out 7 of the 10 that have hours — ▶ `H-30`, and that is the real remaining half. ⭐ The original diagnosis holds: the kit behaved as designed ("an activated feature with empty assets must degrade to an empty state"), and the work was INTAKE. ### ⭐ AND THE CHEAPEST REMAINING INTAKE IS THE OVERNIGHT SET — the candidates are ALREADY IN THE DIRECTORY *(Jacob, 2026-09-22: "hopefully in the future we can add at least 1 24-hour place to carry the ticker")* — **15 overnight-plausible listings are already there and every one is ticker-ELIGIBLE** (four hotels: Comfort Inn River’s Edge, Quality Inn, Microtel, Motel 6 · gas/convenience: Hy-Miler ×3, Mickey Mart, Shell ×2, Mobil ×2, bp · Huron Market, Vermillion). ⛔ **None carries hours or a description**, and the ticker needs BOTH — so this is one small research beat, not a feature. ⭐ A hotel front desk is genuinely 24-hour, which makes it the honest way to keep a night ticker alive; ⛔ **the hours must be REAL** — padding a close time to fill the bar is exactly the plausible-looking success Layer 0 q2 forbids, on a page that presents itself as a record of a real place. ▶ `node -e` over `content/listings.json` re-derives the candidate list; do not quote the 15. ② **SOCIETY IS A WIRING GAP AND THE DATA IS THERE.** `MANIFESTS.huron` in `loadInstanceData.js` declares only `landmarks`, so `buildings` resolves to `[]` and the bare-building half of the Society Pages is absent; huron's `content/roster.json` holds **3,678 buildings, 3,573 (97.1%) with an address**. ⚠️ The in-code note that huron is "~3,627 of 3,678" address-less is STALE — that was before its parcel well was declared; re-derive, never quote. ⛔⛔ **BUT DO NOT JUST ADD THE MANIFEST ENTRY.** The synthetic bare-building listings are categorised by **St. Louis zoning letters**, and huron's roster has **`zoning` on 0 of 3,678 and an empty `zoning_code_format`** — and `useLandmarkFilter` warns that an unmatched tag "would quietly leave the Society Pages". ⇒ wiring it as-is may add 3,573 entries that are then silently filtered out, which is a worse state than empty. ▶ Answer the category question first; the manifest's own comment already demands it ("Add an entry when you have checked the thing that reads it"). → `src/data/loadInstanceData.js`, `src/hooks/useListings.js`, `src/components/EventTicker.jsx`.
- **H-25 · ⛔ THE SOFT-SHADOW PENUMBRA IS CONVERTED AGAINST A TEXEL SIZE THE RENDERER STOPPED USING — up to 28.9× wrong, TODAY, and independent of cascades.** *(Measured by the CSM session, 2026-09-21, while confirming H-24's premises; verified independently here.)* `PostProcessing.jsx:214` computes the PCSS radius as `resolved.size / shadowMetresPerTexel(stencil)` — and that function returns the **TOWN-WIDE** half extent (`radius + hypot(center) + 100`). But `CelestialBodies`' `useFrame` loads a **FITTED** box into the shadow camera, and has done since the frustum became camera-fitted (`3dcb5dd3`). ⇒ the metres→texels conversion reads a denominator the renderer abandoned: on huron the published figure is 1.806 m while a close shot's real texel is 0.063 m — **28.9× too large a denominator, so a penumbra 28.9× too NARROW.** Measured spread: 1.8× at half 2048 · 3.6× at 1024 · 7.2× at 512 · 14.4× at 256 · 28.9× at 128. ⭐⭐ **AND IT GETS WORSE THE BETTER THE FIT GETS** — the closer the shot, the larger the error — which is the opposite of how a rendering bug is expected to behave and is why nobody caught it by looking at wide shots. ⚠️ **The file's own comment at `:217` describes the situation exactly** — *"Once the shadow frustum follows the camera, a texel can be 0.03 m"* — and the code never sees it; the comment was written about the hazard and the denominator was left pointing at the old value. ⭐ **THE FIX IS A PUBLISH, NOT A DERIVATION:** `CelestialBodies` already computes `const texel = (2 * half) / SHADOW_MAP_SIZE` every frame and writes `light.shadow.normalBias = 1.0 * texel` from it — the correct value exists and is simply never exposed to `sceneStencilState`'s consumers. ⛔ **DO NOT PARK THIS BEHIND H-24.** It is a defect in the metres→texels contract, not in the frustum; it would still be wrong if cascades were never built, and riding inside the cascade row it reads as cascade fallout and dies with it. ⚠️ Related and unmeasured: that same fitted texel drives `normalBias = 1.0 × texel`, i.e. **1.806 m of world-space normal offset** on a near-level huron shot, which is a candidate for the peter-panning/"teeth" Jacob reports — ⛔ not established, and `H-24`'s first move is the discriminator. → `src/components/PostProcessing.jsx:214`, `src/components/sceneStencilState.js`, `src/components/CelestialBodies.jsx`.
- **H-24 · ⛔ ONE SHADOW MAP CANNOT SERVE A 7 km TOWN — the teeth are ARITHMETIC, and the flash fix pinned the camera to the worst row.** *(Jacob, 2026-09-21: "flashing with the jagged shadow pyramid teeth all over the buildings… Let's hit the cascaded shadow maps now.")* **`SHADOW_MAP_SIZE = 4096`, one caster, one fitted box ⇒ `2·half/4096` m per texel: huron **1.806 m**, altadena 2.081, hipointe-demun 0.660, LS **0.495**.** ⛔ **`half` IS NOT THE DISC RADIUS** — `shadowHalfExtent` is `radius + hypot(center) + 100`; this row first published 1.73/0.44 by putting the radius in the half column, and `CelestialBodies.jsx:186,199` already had it right. ⭐ Re-derive, never quote. A building's shadow edge can only land on that grid, so at 1.73 m it is a staircase you can count. ⛔⛔ **AND `c8bb79d9` — the same night's flash fix — MAKES THE NEAR-LEVEL CAMERA ALWAYS TAKE THE LARGEST BOX**, which is the coarsest texel: correct, continuous, and it converted the flash into permanent teeth at exactly the angle the hero shot wants. ⛔ **Do not revert it** — it is right for a single map, and the single map is the defect. ⚠️ **Blast radius, so it is not discovered late:** `PostProcessing.jsx:214` derives the PCSS penumbra radius FROM `mPerTexel` and caps it against the sample budget (*"or the penumbra stops being soft and becomes NOISE"*) — cascades give several texel sizes at once and that consumer assumes exactly one; `sceneStencilState`'s `shadowHalfExtent`/`shadowMetresPerTexel` are the shared vocabulary and need a defined answer under cascades. ⭐ **Keep what the current fit earned:** texel-snapped focus (or shadows crawl) and power-of-two buckets with hysteresis (or the boundary flaps) — per cascade. ▶ Alternative NOT chosen and still available: one authored **max shadow distance** (512 m ⇒ 0.25 m texels), which is also the first cascade's far plane. → `docs/briefs/BRIEF-cascaded-shadow-maps.md`.
- **H-26 · huron's GROUND IS MISSING UNDER A PALE PLANE — at STREET, and (second observation) at BROWSE — four clean eliminations, ⛔ CAUSE NOT ESTABLISHED, and NOT being chased.** *(Found by the CSM session, 2026-09-21, looking for a near-level camera; Jacob: "we're not developing street view this second anyway" — parked deliberately, recorded so it is not re-discovered.)* At Street (`planetarium`, huron authors `{fov:75, eyeHeight:1.73}`), **a uniform pale blue-white plane occludes the ground and the lower ~2 m of every building across the whole view**, inland and toward the bay, cutting buildings at a constant world height. No ground is visible at all. **ELIMINATED, each one click and re-screenshotted:** Atmospheric Fog OFF → unchanged (not `StageFog`/`scene.mist`) · Park paths/water/canopy OFF → unchanged (not the park water) · Ground OFF → **unchanged, which says the ground was not drawing in this view to begin with** · `Terrain` is `visible={false}` in Preview by construction (`PreviewApp.jsx:1328`) · `WaterSurface` has no mount site in `PreviewApp` or `Scene`. ⛔ **Two tidy stories died on those toggles** — the `cdd41f6e` datum putting 39.5% of samples below the waterline, and the terrain mesh — so no third one is offered here. ⚠️ **IT MATTERS BEYOND STREET:** the near-level bay view is the frame Jacob's planned hero sweep is composed of (camera at the lighthouse, 360° from the water), so whatever this is sits in the acceptance shot for `H-24`. ⭐ **AND IT MAY BE OLD, NOT NEW:** `H-14`'s gate explicitly reads *"at Hero and at Street"* and **no eye verdict is recorded anywhere** — huron's Street has never been looked at. ### ⚠️⚠️ SECOND OBSERVATION, AND IT MAY UNDO THE REASON THIS IS PARKED — **the ground is missing in BROWSE too** *(same session, 2026-09-22, ~10:30am scene time)*: buildings and trees standing over a pale void, seen from overhead. ⛔ **Boarded as a SECOND OBSERVATION, not as a widening of the first** — at the finder's own insistence, because nobody established the two share a cause and merging them would assert one.
  ⛔⛔ **THE PARKING RATIONALE WAS "we're not developing street view this second" (Jacob). BROWSE IS NOT STREET VIEW** — it is a shipping shot, authored per town (`scene.json#shots.values.browse` carries huron's bounds), and it is one of the two framings a visitor actually gets. ⇒ **the decision to park this needs re-taking with the Browse sighting in hand.** It is Jacob's call, not a widening anyone should make unilaterally.
  ▶ Next probes, cheapest first, neither run: does the same plane appear on **Lafayette Square** — at Street, and now also at **Browse**? That is the town-vs-kit discriminator twice over. → `src/preview/PreviewApp.jsx`, `ls/STREET-VIEW.md`, `H-14`, `H-24`.
- **H-27 · ⛔ huron'S DIRECTORY LISTS ELEVEN PLACES TWICE, AND THE WRONG-ENTITY HALF HAS NO CHECK.** *(Found 2026-09-22 by a place-card research agent noticing two pizzerias sharing one phone; the class then fell out of two fields we already collect.)* ▶ `node checks/claims-two-listings-are-not-one-place.mjs` — **huron 11 collisions of 285; altadena 0 of 23; hipointe-demun 0 of 192; LS-staging 0 of 41.** ⭐ **FOUR DIFFERENT KINDS OF WRONG from one query:** the same place twice (Holiday Harbor Marina ↔ itself; Old Fish House ↔ "The Old Fish House (Huron, Ohio)") · **successive tenants** (Huron Iga ↔ Cornell's Foods; Emmie.Styled ↔ Corner Cuts; Wink's ↔ Bruno's — and that unit now trades as a THIRD name, so both listings are dead) · **a product as a business** (Shell ↔ Blue Rhino Propane; Blimpie ↔ Blue Rhino) · **a person beside their practice** (NOMS Family Practice ↔ Christopher Emery). ⚠️ **Normalisation is load-bearing** — "Rd E" vs "Rd East" and `+1`-prefixed phones hid four of the eleven from an exact compare. ⭐⭐ **THE `AND` IS LOAD-BEARING, and it was luck until it was measured:** three DISTINCT businesses share 100 Laguna Dr — Huron Lagoons Marina, the Viking's Den restaurant inside it, and North Coast Boating in suite C-32 — with three different phones, and the check correctly ignores all three. **Venue-inside-a-venue is the legitimate fifth case**, it is common (hit twice in one 8-listing sample), and matching on address ALONE would have condemned a marina's own restaurant. ⛔ Never relax the check to address-only. ⛔ **The check does NOT rule which is which and must not start** — two tenants versus a forecourt product is the operator's call, and `listings.overrides.json` already carries `drops` and `patches` for it. ⭐ A listing that closed in 2023 is still in the directory (Shawnee Elementary, closed by board vote, building now Huron Sports Academy — **both listings on `msbf-2521`**), so the map looks better stocked than the town is, which is Layer 0 q2 exactly.
  ### ⛔ THE OTHER HALF IS UNCHECKED: A URL THAT RESOLVES TO THE WRONG ORGANISATION.
  `huro-lst-0119` Salvation Army points at the **Indiana Division** (Huron OH is Northeast Ohio) and `huro-lst-0198` Sand Bar points at a bar in **Lawrence, Kansas**. ⭐⭐ **Both return 200 OK on plausible pages, so any "are our links alive?" check passes them clean — LIVENESS IS NOT IDENTITY**, and a reachability check would bless both. Meanwhile `christchurchhuron.com` and `winkspizza.com` are NXDOMAIN while search engines still index their old pages, so they *look* alive. ⛔⛔ **AND THE OBVIOUS CHECK — name/domain agreement — WOULD BREAK A WORKING LISTING.** `huro-lst-0157` Rippin Lips Lake Erie Charters points at a WATERFOWL domain and the URL is **correct**: one operator runs both businesses under one site whose contact page reads *"Tattered Wings Waterfowl | Rippin Lips Lake Erie Charters"*. A naive mismatch flag would have condemned it. ⇒ any wrong-entity check must agree on ADDRESS or PHONE found on the page, never on the name. ⚠️ And "broken link" is four failure modes wearing one symptom — NXDOMAIN · a truncated string · TLS verification failure · 403 to some user-agents and 200 to others (`huro-lst-0243`) — so a single boolean reachability test mislabels three of them. → `checks/claims-two-listings-are-not-one-place.mjs`, `INTAKE-CATALOGUE §3.2b`, `cartograph/data/<scene>/content/listings.overrides.json`.
- **H-28 · A CORRECTION SHEET FOR LOCAL HOSTS — the role exists, the editable surface exists, and the BULK one does not.** *(Jacob, 2026-09-22: "we should probably export spreadsheets at some point for local hosts to manually edit and correct.")* ⭐ **TWO DIFFERENT ROLES, and conflating them is the trap:** a **Guardian** claims ONE business and edits its own card, authenticated by the `claim_secret` printed on a physical QR card (`ls/PLACE-CARDS §2`), and writes to the Apps Script sheet — that exists and works. A **HOST** knows the whole town and corrects the DIRECTORY in bulk: duplicates, dead listings, wrong URLs, successive tenants. ⛔ A business owner will never delete a competitor's stale listing, so the guardian channel structurally cannot produce the corrections `H-27` just surfaced. The Host is already a named role in the product (`THEWARD-ONLINE`, the `/handbook` Host Handbook); what is missing is a bulk surface for it. ⛔⛔ **AND THE ANSWER IS NOT "GIVE THE HOST THE GOOGLE SHEET"** — that is the live production backend, per-look tabbed, with no diff, no review and no undo, and the corrections belong to the BASE layer rather than the guardian layer anyway. ▶ **The shape that fits what already exists:** export a town's baked listings to CSV (stable key column + the fields a host can fix + read-only provenance) → the host edits in Sheets or Excel, offline, no account → import DIFFS the returned CSV against the base and emits `content/listings.overrides.json` `patches`/`drops`/`adds`. ⭐ **The diff is the whole point:** a host's edit becomes a REVIEWABLE override in git rather than a silent overwrite, and because it lands in the overrides layer a re-intake (a fresh Overture pull) cannot destroy their work — which is precisely why that layer exists. ⚠️ Design hazards, named now: a deleted ROW must mean `drops` EXPLICITLY (a vanished row is ambiguous — use an `_action` column, never infer from absence) · the round-trip must be lossless for fields the host did not touch · ⛔ the importer may never write the base file.

  ### ⭐ THREE SURFACES, FOR THREE DIFFERENT JOBS — sequenced, and ⛔ the sheet is NOT first *(Jacob, 2026-09-22: "maybe we even create an attractive harness OR we make sample Place Cards that they can see and edit")*
  ⛔ **FIRST, THE CORRECTION I GOT WRONG AND JACOB FIXED:** `CodeDeskModal` is **not** an editor. It only ASSOCIATES A DEVICE WITH A LISTING — the QR/claim path (`ls/QR-CODES.md`). **The CARD ITSELF is the editable/overwritable surface**; a guardian edits their own card, and `api.js#updateListing(listingId, fields)` is the write, authenticated by **device hash — no account**, which is this product's existing answer to identity. ⚠️ Do not plan a host tool around CodeDesk.
  **① A REVIEW QUEUE over `H-27`'s findings — smallest, highest value, and build it first.** We do not have a vague "improve the directory" problem; we have a GENERATED LIST OF SPECIFIC DEFECTS WITH EVIDENCE. That wants triage, not an editor: one screen per finding — *"Wink's and Bruno's share an address and a phone, and that unit now trades as a third name — drop both? merge? keep?"* — with accept / reject / fix, each decision landing as a `drop` or `patch`. ⭐ It asks the host only what a local actually knows ("Bruno's closed years ago"), and it is a far smaller build than a general editor.
  **② THE CARD AS THE EDIT SURFACE, for ENRICHMENT** (hours, descriptions, menus). The card wins here decisively because a host looking at the real card SEES the gap and is moved by it; a spreadsheet row moves nobody. And it is WYSIWYG by construction — the edit surface IS the product, so the two cannot drift.
  **③ CSV EXPORT LAST, IF EVER.** Its one real advantage is BULK — sorting, find-replace, 285 rows at once, offline, no account. ⚠️ But once ① exists the bulk problem is mostly gone, and once ② exists the enrichment problem is. ⛔ Do not build the importer before finding out whether anyone still wants Excel.
  → `src/components/PlaceCard.jsx`, `src/lib/api.js#updateListing`, `cartograph/bake-content.js#applyListingOverrides`, `ls/PLACE-CARDS.md`, `H-27`.
- **H-29 · ⛔⛔ AN OVERRIDE THAT MATCHES NOTHING IS A SILENT NO-OP — the operator’s authoring vanishes and the bake reports success.** *(Found 2026-09-22 merging the place-card research.)* `applyListingOverrides` resolves each `patches` key with `byKey.get(key)`, falls back to `_match_name`, and then does `if (target) { … report.patched++ }` — **with no else.** A key that resolves to nothing is skipped, the count simply comes out lower, and nothing in the run says which key died. ⭐⭐ **THIS IS LAYER 0 q2 INSIDE THE MERGE PATH: a failure converted into a plausible-looking success**, and it is worse here than almost anywhere, because an override IS the operator’s authoring — the one thing the kit promises never to lose. ⭐ **Measured, and it was not hypothetical:** the three research beats came back keyed by `huro-lst-NNNN` (the **display** id, assigned by `assignDisplayIds` at bake time and deleted from the output at `bake-content.js:982`), while `_key` on an Overture base is `ovt-<GERS>`. Keyed as delivered, **0 of 24 would have hit `byKey`** and the `_match_name` fallback would have carried **19** — *“BGSU Firelands Library”* is not *“Bowling Green State University, Firelands College”*, and four more like it. **Five places of research would have gone silently missing from a run that printed `19 patches` and exited 0.** ⚠️ The merge that landed (`62287231`) sidesteps this by keying on `ovt-<GERS>`; **the defect is untouched and the next person to hand-write an override meets it.** ▶ Deliverable is a check, not a better key convention: **every `patches` key, every `drops` entry and every `adds` anchor must resolve, on every scene on disk, or the bake fails loudly** — and mutation-test it by renaming one key. ⭐ It generalises past listings: `roster.overrides.json` is merged the same way. → `cartograph/bake-content.js#applyListingOverrides`, `#assignDisplayIds`.
- **H-30 · ⛔ `TICKER_EXCLUDED` IS A CONSTANT THAT WAS RIGHT FOR TOWN #1 — it drops 1.6% of Lafayette Square and 70% of huron.** *(Measured 2026-09-22, the moment huron had hours to exclude.)* `EventTicker.jsx:147` hardcodes `new Set(['residential', 'community', 'parks', 'historic'])` and skips those listings before the open-now test. **Measured over listings that actually carry `hours`: LS 1 of 61 excluded; huron 7 of 10.** ⭐⭐ **CLASS D, TEXTBOOK: no fallback, no scene name, no `||`, so no grep finds it** — an un-unitised category list whose correctness depends on something else being fixed, namely which taxonomy the town’s listings were built from. ⭐ **The mechanism is measured, not guessed:** huron is an **Overture** base, and `OVERTURE_ROOTS` files `sports_and_recreation → ['parks', null]`, so **a marina is a `parks` listing** — Holiday Harbor and Huron Lagoons are commercial businesses with seven-day hours sitting in the bucket named for green space. Likewise `education` and `community_and_government → community` put the high school, the BGSU Firelands library, the food pantry and the sports academy there. ⚠️ On LS the same four words cost one listing, which is why nobody saw it. ⇒ the ticker on huron shows **3 entries** (Berardi’s, Marconi’s, the Vikings Den) out of 10 places with hours. ⛔ **The fix is not a better list** — a fifth category is the same defect with a longer constant. ▶ Ask the listing, not the bucket: a listing with published hours and a tagline has something to say regardless of category, so the exclusion wants to be a property of the RECORD (or the town’s authored choice with a neutral default), never a hardcoded taxonomy set. ⛔ **Not fixed — what belongs on a town’s ticker is a product decision and Jacob’s call**, and the row exists so the 3-of-10 is not mistaken for thin research. → `src/components/EventTicker.jsx:147`, `cartograph/bake-content.js#OVERTURE_ROOTS`, `H-22` ①.
- **H-31 · A TOWN CALENDAR THAT IS NOT CLOCK-DEPENDENT — ⭐ THE DATE MODEL IS ALREADY BUILT; THE HOME AND THE KEY ARE NOT.** *(Jacob, 2026-09-22: "a sub-schedule of ‘Huron things’ which aren’t clock dependent — like pumpkin festival oct 1 – 14 or whatever.")* ⛔ **Do not build a date model — `isActiveEvent` IS one.** It takes `start_date`/`end_date`, and its time-of-day test is **opt-in**: `if (timeStr && e.start_time && e.end_time)`. ⇒ an event with dates and **no times is active all day, every day, across the range** — exactly what was asked for, expressible today, and the ticker already renders it in amber and lets it override the open-now entries. ⭐ It also already does weekly recurrence (`type: 'recurring'`, `day_of_week`), which covers a farmers market. **THREE THINGS BLOCK IT, all measured 2026-09-22:** ⭐⭐ **① THE ENTRY MAP IS KEYED BY `listing_id`, AND A TOWN FESTIVAL HAS NO LISTING.** `entries.set(e.listing_id, entry)` — with no listing every town-wide event writes the key `undefined`, so they overwrite each other. **Measured: three events in (Pumpkin Festival, Harbor Fireworks, Farmers Market) → ONE entry out**, and the survivor is arbitrary, because the tie-break is `(e.start_time || '') > (existing._startTime || '')` and `'' > ''` is false, so first-in wins by accident. ⛔ This is the whole defect; the rest is plumbing. ② **`openEvent` no-ops without a `listing_id`** and `_venueName` comes out `''`, so a town event is an unclickable headline with no byline — wanted behaviour is arguably a town event pointing at a PLACE (the festival is on Main St) or at nothing, and that is a product call. ③ **huron has no seed-event channel at all:** `MANIFESTS` declares `seedEvents` for **lafayette-square only** (whose set is `[]`), so `loadInstanceData('huron', 'seedEvents')` returns null and the only other source is the Apps Script API — **the guardian layer, and a town festival has no guardian**, the same structural gap `H-28` names for the directory. ▶ **Recommended home, not yet ruled:** `cartograph/data/<scene>/content/events.json`, baked by `bake-content.js` beside `listings.json` — that is where a poured town’s content lives, and it inherits the overrides layer, the `_`-prefixed provenance convention and `stripMeta` for free. ⚠️ The alternative (`src/data/<look>/seedEvents.json`, where LS’s sits) would split huron’s content across two homes. ⭐ **And it is the honest answer to the night ticker** — a festival running Oct 1–14 carries the bar at 3am without anyone padding a restaurant’s closing time (`H-22` ①). ⚠️ A town calendar is also a NEW INPUT KIND and wants a line in `INTAKE-CATALOGUE` once its shape is ruled; the civic research already found the neighbouring case — a school’s **~180 named closure days**, which no weekly `hours` can express. → `src/hooks/useEvents.js#isActiveEvent`, `src/components/EventTicker.jsx#buildTickerEntries`, `src/data/loadInstanceData.js#MANIFESTS`, `H-22` ①, `H-28`.
- **H-19 · ⛔ ANTI-SCRAPING: THE SLAB IS BULK-DOWNLOADABLE BY ANYONE, AND THAT IS A NEW REQUIREMENT RATHER THAN A BREAKAGE.** *(Jacob, 2026-09-21: "we want the final product to be rather impenetrable for IP, secret sauce, security, etc. reasons (anti-scraping)".)* Measured from a shell with no browser: `curl https://assets.theward.online/baked/lafayette-square/scene.json` → **200**, and the same for any town under `staging/`. The bucket's custom domain is public and ungated, keys are a documented regular layout, and `public/looks/index.json` publishes the enumeration key. ⛔⛔ **THE CEILING IS HONEST AND LOAD-BEARING: a browser-delivered product cannot be made impenetrable** — the bytes reach the client or nothing renders. ⇒ the goal is removing **bulk, anonymous, scriptable** access, never secrecy, and any plan resting on the latter fails. ⭐ **The enforcement point exists already**: `workers/staging-sites` is in front of the sites as of tonight, so gating the slab there and closing the public bucket domain is mostly configuration. ⚠️ ⛔ **Both prefixes move together** — gating staging and leaving `baked/` open protects the preview and not the product. ⭐ **And the moat is the KIT, not the slab** (skeleton, derivation, Survey/Section, the authoring tools — none of it on the CDN). ⛔ Deliberately uncosted. → `SECURITY.md` F-18.
- **H-17 · ⛔ STREET LAMPS BAKE TO ZERO ON EVERY POURED TOWN — THE DATA IS ON DISK AND THE BAKE READS THE WRONG DRAWER.** Measured 2026-09-21: `public/baked/huron/lamps.json` is **`count: 0`**; lafayette-square's is **583**. ⭐ **huron HAS 30 usable `highway=street_lamp` nodes with lon/lat, in `cartograph/data/huron/raw/osm.json`, and it HAS `geography.json`** — so the census exists and the reprojection is possible. `bake-lamps.js` never looks there. Its three wells are `raw/osm_street_lamps.json` (⛔ **a hand export that NOTHING in the kit writes** — LS has one because a human made it), `authored_lamps.json` (per-scene, absent), and `src/data/street_lamps.json` (⛔ 80 lamps **all inside Lafayette Park**, LS-specific). ⇒ **No town the kit pours can ever populate any of the three**, so lamps are structurally an LS-only feature. ⚠️ **AND THE REFUSAL'S ADVICE IS FALSE.** `bake-lamps.js` warns *"This scene has no lamp census; acquire one via the Intake panel (OSM highway=street_lamp)"* — huron acquired one; it is sitting in `raw/osm.json`. ⭐ The refusal itself is CORRECT and Layer 0-shaped (it bakes zero rather than substituting another scene's lamps); it is the **remedy text** that sends the operator to re-fetch data they already have. ▶ **The fix is a method, not a file: read `highway=street_lamp` out of the town's own `raw/osm.json` POIs and reproject through `geography.json`** — the same well, and the same extraction, that `derive.js` already performs. ⛔ **`derive.js` consumes lamps for WIDTH CALIBRATION and block-constraint solving ONLY; it writes no lamp artifact**, which is why the 2026-09-21 derive change reached zero rendered lamps and I reported it as though it had. S–M. → `cartograph/bake-lamps.js`, `cartograph/derive.js`.

  - ### ⛔⛔ THE PRODUCT QUESTION, AND IT OUTRANKS THE WIRING FIX ABOVE *(Jacob, 2026-09-21)*
    **Wiring `bake-lamps.js` to the right drawer will not light Huron, and the measurement says why.** huron's 30 lamps span **431 m** at a **median nearest-neighbour spacing of 18 m** — ⭐ **that is one person mapping one street at real pole spacing and stopping**, not a town census, and it sits **2.6–2.9 km from centre**, nowhere near the hero path. Meanwhile the intake is NOT at fault: `LIGHT_NODES = ['highway']` queries `node["highway"]` over the whole envelope and returned 500 POI nodes across 8 kinds (`crossing=107 · street_lamp=30 · turning_circle=13 · traffic_signals=8 · …`). **We asked correctly; OSM has 30.**
    ⭐ **LAFAYETTE SQUARE IS THE OUTLIER, NOT HURON.** Its 641 came from a hand-made `raw/osm_street_lamps.json` — St. Louis has a **municipal lighting import** in OSM. Most places do not. ⇒ **For most towns there is no lamp census to acquire at all**, and this is therefore not a pipeline defect: the bake's refusal is correct and says so.
    ⛔ **MUNICIPAL DATA FOR HURON: LOOKED FOR, NOT FOUND — measured 2026-09-21, do not re-run this.** Huron OH is in **Erie County, Ohio** (⚠️ *not* Erie County NY, and *not* Huron County OH — both are different places that dominate the search results). Erie County OH's ArcGIS server is live (`arcgis.eriecounty.oh.gov/arcgis/rest/services`, v10.91) and its public folders — `Hosted · PictometryConnect · Utilities · WMAS · WMInspections` — expose **no lighting layer** (`Utilities` holds only Esri's `PrintingTools` GPServer). Its open-data hub lists **130 datasets and not one lighting/pole layer** — all six keyword matches are Esri basemaps ("Light Gray Canvas", "Streets"). The portal is basemaps, aerials and parcels. ⚠️ Huron's poles are presumably the utility's (Ohio Edison/FirstEnergy) and unpublished — **not established**.
    ### ✅ RULED 2026-09-21 (Jacob): **REAL WHERE REAL, EXTRAPOLATED WHERE NECESSARY — AND IT IS ONE RULE FOR LAMPS *AND* TREES.**
    *"We want both, as close to reality as possible, and extrapolated where necessary."*
    ⛔⛔ **THE 'LAMPS vs TREES DIVERGENCE' RECORDED HERE EARLIER WAS MY MISREADING AND IS EVICTED.** I read `bake-trees.js`'s *"acquire an inventory, or accept a uniform canopy"* as a ruling about **whether trees appear**. It is not — it is about **DBH**, trunk diameter: *"0 measured DBH across the whole census"*, so every tree renders at a flat 1:1 **within its size band**. ⭐ **"Uniform canopy" means one SIZE, not no trees.** There was never a contradiction to reconcile, and the paragraph telling future readers not to 'harmonise' the two was protecting a conflict that did not exist.
    ⭐⭐ **TREES ALREADY DO EXACTLY WHAT JACOB DESCRIBED, AND LAMPS SHOULD COPY THE PATTERN NEXT DOOR RATHER THAN INVENT ONE.** Measured on huron 2026-09-21: `clean/osm_trees.json` = **168 surveyed**, `clean/derived_trees.json` = **21,505 invented** (NLCD canopy fill) → 18,616 placed. The machinery that makes that honest is already built and is the spec for lamps:
    - **Two wells, and provenance is stamped per item** — `SOURCE_BY_BASENAME` (`bake-trees.js`) maps each well to a `source`: `osm` for real positions, `derived` for the invented fill. ⇒ Lamps want `osm_lamps.json` + `derived_lamps.json` in the same shape.
    - **Cross-well dedup keeps the RICHEST record** — a real inventory beats OSM's real-but-sampled position, which beats the fill. ⇒ A surveyed lamp must win over a derived one at the same spot.
    - **⭐ SURVEYED IS NUDGED, INVENTED IS DROPPED** — *"a real recorded tree is never deleted for taste, only invented ones thin."* A surveyed item landing on illegal ground is moved to the nearest legal ground (our strip widths are soft); an invented one there is simply wrong and goes. ⇒ Exactly the policy a lamp generator needs on sidewalks, driveways and hardscape.
    - **The dissolve** — invented items thin toward the rim over the ground's fade band so the greater circle dissolves instead of ending at a seam; surveyed items are exempt. ⇒ Lamps get this free.
    ⇒ **THE DELIVERABLE IS A `derived_lamps.json` PRODUCER PLUS THE SAME PROVENANCE CONTRACT** — not a new design. ⛔ The wiring fix in H-17's head is still needed (it is how the *surveyed* well reaches the bake at all) but it is now step one of two.
    ### ▶ DIRECTION 2026-09-21 (Jacob), on HOW these get answered: *"This is a heuristic concern; we need to avail ourselves to the data and think it through."* ⛔ **So they are not settled by picking a number — and not by asking him to pick one either.** Go to the towns' own data: the `highway=street_lamp` nodes already in every scene's `raw/osm.json`, across every scene on disk, and let that population say what the spacing and the street-selection are. ⛔ A heuristic derived from one street is the Class D constant this kit keeps shipping.
    ▶ **Open sub-questions, none ruled, do not invent answers:** the derived SPACING (⛔ **not** huron's 18 m — that is one street, not a population) and whether it varies by road class · which streets get lamps at all · whether lamps need an equivalent of the NLCD raster (trees had one; lamps have only the street graph) · the operator's override surface, since **the override IS the product**.
    ⚠️ **Still true and unrelated to the above:** huron has **no municipal lamp data** (searched 2026-09-21, receipts below), and its 30 OSM lamps are one 431 m street at 18 m spacing, 2.6 km from the hero path. **Derivation is what lights the town; the surveyed 30 are a seed, not a census.**
    ▶ **SUPERSEDED — kept only to show what was asked before the ruling:** does
 a poured town with no census get lamps **DERIVED** — placed procedurally along the street graph at an authored spacing, the way the canopy is filled — or does it get **NONE** until municipal data is found? ⭐ **The kit already answers this exact question for trees and answers it the second way**: *"For a town with no municipal census this is the HONEST output, not a defect… acquire an inventory, or accept a uniform canopy"* (`bake-trees.js`). ⛔ Whichever way this goes, it must go the same way for both, or the kit says two different things about the same situation. ⚠️ Lamps differ from trees in one respect that may change the answer: **a lamp is a LIGHT SOURCE**, so 'none' is not a cosmetic absence — it changes what the town looks like at night, and the two-body rig now makes night a first-class view.
    ✅ **DONE 2026-09-21, the one piece that needed no ruling:** the intake now NAMES the census it acquired. The POI tally is keyed on `category`, so every `highway=*` node landed in one line reading `highway: 500` — a town's entire lamp census was invisible at the only place anyone would look to learn it arrived. `fetch.js` now prints the per-kind breakdown, and prints a loud ⚠️ when `street_lamp=0` saying that zero is normal and honest. ⛔ It reports what was ACQUIRED and promises nothing about what the bake consumes.
- **H-16 · ✅ THE FLASH IS FOUND AND FIXED 2026-09-21 — A CLIFF IN THE FITTED SHADOW BOX AT THE HORIZON. ⚠️ THE CRAWL IS A SEPARATE, STILL-OPEN DEFECT; read both halves.**
  ### ✅ THE FLASH — closed, and Jacob called the layer before any of my measurements did.
  **The shadow box is fitted to what the camera sees, and its size sets the texel grid** (`texel = 2·half/SHADOW_MAP_SIZE`), so any change in `half` re-snaps EVERY shadow edge in the scene in one frame. The ground-hit ray fell back to the point straight BELOW the camera when it found no intersection — **the smallest extent at exactly the angle where the camera sees furthest.** ⇒ at camY 60 / fov 45 / townHalf 3539 the box went **3539 m → 64 m across 0.1° of pitch, a 55× step in texel size**; a near-level camera sits on that cliff and jitters across it every frame. ⭐ **Every measurement fits:** two exact luma values (67.2/73.5) alternating at 30 Hz · bright regions moving twice as far as dark ones (shadow coverage, not exposure) · only at *"certain angles and ranges"* (Jacob) · absent at night (no sun casting) · and **55× on huron against 14× on LS**, because the cliff's two sides are further apart the bigger the town. ⚠️ It only became possible when the frustum turned CAMERA-FITTED the same week (`3dcb5dd3`); a fixed ±900 box could not flap — which is what *"worse than ever"* was. ⛔ **The hysteresis was never the bug and must not be tuned to hide this** — it governs a continuous input and no band damps a 55× step. **Fix:** no ground hit ⇒ the horizon is in shot ⇒ the whole town is visible. ▶ `node checks/claims-the-shadow-box-has-no-cliff.mjs` (property test over two town sizes × three camera heights, plus the shape test; mutation-tested — restoring the old fallback reproduces 55.3× at pitch 0.00°). 👁️ **Eye gate owed: Jacob, at a near-level camera.**
  ### ⛔ THE CRAWL IS STILL OPEN and is NOT this. — one cause MEASURED, one fix TRIED AND REVERTED.** Jacob, 2026-09-21: *"flickering between ground plane and buildings/surfaces… motion related, interpolation and antialiasing."* ⭐ **MEASURED AND STILL TRUE: `anisotropy` is set at ten sites in `src/`** — impostors, the Milky Way panorama, `LafayettePark.jsx`, the tree atlases — **and at none of the three ground textures**, which run at three.js' default of **1**. The ground is the one surface always seen almost edge-on, so trilinear picks its mip from the worst-case axis and the level walks as the camera moves. ⛔ `antialias: true` is MSAA and cannot touch texture interiors, which is why this survives a canvas with AA on. ⛔⛔ **BUT THE OBVIOUS FIX IS NOT AVAILABLE AS WRITTEN — IT WAS TRIED AND IT BROKE THE SCENE** (`7780f888`, reverted `c48ba6ce`, ~20 minutes end to end). Setting `.anisotropy` on the ground textures made **every object in the scene flash individually** — buildings and trees included, which sample none of those textures. ⭐ **THAT IS THE REAL FINDING, AND IT IS BIGGER THAN THE CRAWL:** the attempt added `groundAniso` to the dependency arrays of the two effects that call `setGroundColorMap` / `setGroundFxMap`, **and each of those cleanups publishes `null`.** Those maps are consumed by the **tree-trunk and grass shaders**. ⇒ **The ground's texture-publication path is shared state that cannot tolerate re-entry, and touching it rebuilds materials across the whole scene.** ⛔ **CAUSE NOT ESTABLISHED — the dep-churn account is a hypothesis; what is MEASURED is only that the change flashed and the revert fixed it, both on Jacob's eye.** ▶ **Next move is NOT to re-try anisotropy** — it is to instrument `setGroundColorMap` / `setGroundFxMap` and find out what re-entry actually costs, because that path is live on every town and nobody has looked at it. ### ⭐⭐ MEASURED FROM A RECORDING, 2026-09-21 21:01 (Jacob: *"the flashing is worse than ever"*) — AND IT IS NOT THE CRAWL.
  ▶ `ffmpeg -i <clip> -vf signalstats,metadata=print:key=lavfi.signalstats.YAVG -f null -`
  **The WHOLE FRAME alternates between two discrete luma values — 67.2 and 73.5, a constant 6.2 step (~9%) — flipping every one or two frames for the length of the clip, stable to ±0.05 within each state.** ⛔ Two exact states repeated, not a gradient and not motion-dependent: that is a BINARY per-frame A/B, which is what two writers fighting over one thing looks like. A mip-level walk cannot produce it.
  ⭐ **AND IT IS NOT A GLOBAL EXPOSURE CHANGE EITHER — the swing is highlight-weighted:** a bright crop moves 95.8→81.8 (**−14.6%**) while a dark crop moves 49.1→45.6 (**−7.1%**), same frames. A uniform multiply would move both by the same ratio. ⇒ consistent with an ADDITIVE pass appearing and disappearing per frame — bloom is the obvious candidate, and `ARCHITECTURE §8` already records a composer-rebuild failure that "presents as 'bloom went dark'", with the key `fx-${smaaOn}-${dofOn}`. ⛔ **WHICH pass, and why, is NOT ESTABLISHED** — the two numbers above are all that is measured.
  ⛔⛔ **SO THIS ROW HAS BEEN CONFLATING TWO THINGS.** The anisotropy finding is still true as written (re-verified 2026-09-21: ten `anisotropy` sites in `src/`, none on the three ground textures) and it would explain a CRAWL in texture detail under motion. It cannot explain a 9% whole-frame flash at 30 Hz. ▶ Treat them as two defects and chase the flash FIRST — it is the one the operator is actually looking at, and it is measurable without an eye.
  ⚠️ **Plausibly upstream of `H-13`** (a Preview subtree that collapsed to 10 geometries with no console error) and worth holding next to `H-9`'s unexplained ~130 ms/frame; ⛔ **neither link is established.** ⚠️ **Process note, recorded because it cost Jacob a reload at 01:30:** the fix was built and shipped without asking, against the standing rule. The finding was sound and the remedy was not, and the gap between those two is exactly what the ask is for. S to instrument. → `src/components/BakedGround.jsx`, `src/components/groundColorState.js`.



- **⛔⛔ A17 · THE SIDEWALK/TREE-LAWN SWAP IS UNRELIABLE — and it is the ONLY reported symptom on this arc** *(Jacob's eye, 2026-08-21)* — *"Most of the sidewalks are visually working, but when I click the street to activate the ribbons to swap sidewalk and TL, it's not reliable."* **Cause not established. Nobody has measured which key the click writes, or whether that key resolves on the edge that was clicked.** ⛔ Do not fill this in from reasoning — that is what produced the last two sessions.
  - ⭐⭐ **JACOB'S STANDING RULING, said repeatedly and it is the frame for this whole board:** ***"If we were properly polygonized we wouldn't be having these conversations. There are numerous symptoms but they're irrelevant — it just means the polygons didn't get made correctly."*** ⇒ ⛔ **Do not work a symptom as its own ticket.** This is `MEMORY §B`'s **SHAPE KNOT** in the operator's words — *the partition is computed from chains instead of read off the polygon* — and `CLAUDE.md`'s routing gate says the same: **the polygon is NAMED; ask it, not the graph.**
  - ⛔ **NOT explained by slice 2.** The substrate walk is **default-off** (`tileGround.js:4709`; nothing passes `opts.substrateTiles`, `SUBSTRATE_TILES` absent in the browser) ⇒ production tiles are the frozen `shape.json` byte-for-byte. *(Was cited as `:3047`/`:3042` until 2026-09-08; both had drifted — re-derive with the grep, don't quote a line.)* `cornersAdjacent`'s only consumer is `substrateWalk.js`. **The clip/`junctionMap` root is real and reaches nothing on screen.** → `docs/briefs/BRIEF-slice2-walk-full-crop-last.md §SCOPE`, `PREBAKE §2.5` (⛔ the clip half is excised, `ec7dd3f4` — the `junctionMap` root it names is closed by removal; `_archive/PREBAKE-2.5-boundary-clip-EXCISED-2026-09-05.md`).
  - ▶ **The first measurement, and it is polygon-side:** click an edge whose swap does not take → which frozen tile ring contains it (**by geometry, ⛔ never tile index**) → which `skelId · side · segOrd` does `tile.runs` say owns that arc → is that the key the write produced (`readFeCustom`, `SurveyorOverlay.jsx:223`)? **No chain graph involved.**
  - ### ⛔⛔ THE `clipRun` DUPLICATE-VERTEX LEAD IS DEAD — struck 2026-09-08, and the mechanism is now GONE
    Jacob, 2026-08-21: *"the streets we manually clipped way back at the beginning are causing us trouble."*
    ▶ `node scratch/segord-duplicate-forensic.mjs` → **0 duplicate-carrying chains · 0 zero-length spans
    minted · 0 partitions that MOVE.** And `clipRun` itself was **excised 2026-09-05 (`ec7dd3f4`)**, so it
    cannot recur. ⛔ Do not re-derive it. Method + figures → `cartograph/_archive/PREBAKE-2.5-boundary-clip-EXCISED-2026-09-05.md` (appendix).
    - ⭐ **THE LIVE CONSEQUENCE, and it is good news for the operator:** the authoring key space is MORE
      stable than this ticket implied — **a re-authoring pass is not exposed to that class at all.**
    - ⭐ **THE LIVE LESSON: EXISTENCE IS NOT RUNNABILITY.** The probe was cited for weeks while
      unrunnable — it imported `resolveChainSegmentation` from `buildBlockGeometryV2.js` after the
      function moved to `chainSegmentation.js`, so nobody who tried could reproduce the evidence.
      Repaired 2026-09-08; this is why `scratch/claims-doc-pointers-resolve.mjs --run` exists.
  - ⚠️ **Two named candidates, and ⛔ neither is a diagnosis.** The `fe↔span` credit in `assignSegOrdsToFes` (`buildBlockGeometryV2.js:1026`) is **LS 0/3 — measured absent on the town Jacob is clicking**, so it cannot be this on LS. The `feCustomKey` re-key (`C4`/T3) is untested against this symptom. **S to measure** · → `A15`, `MEMORY §C` 🔑.

- **⛔⛔ A21 · ① IS NOT MANAGING THE INTERSECTION — it mints extra and degenerate HOLES there, and the corner the operator loses is downstream of that** *(NEW 2026-09-08, from Jacob's eye at Mississippi × Park and Mississippi × Lafayette: "one corner doesn't draw an eligible corner geometry so all the ADA/treelawn, etc. doesn't work")*
  - ⛔⛔ **READ THIS ITEM IN ①, NOT IN CHAINS.** The first write-up of this defect called it "a ② defect" and described the junction as *"bounded by park-avenue-4, mississippi-avenue…"* and *"Lafayette arriving as three chains"* — **chain-world nouns, and the conclusion drawn in them was wrong** (Jacob: *"you're using words that sound a lot like chains. I believe the initial polygon isn't managing the intersection correctly."*). ⭐ He was right, and the measurement below is what a polygon-side question returns. `CLAUDE.md`'s routing gate names this exact slip: **the polygon is NAMED — ask it, not the graph.**
  - ▶ **① MINTS MORE THAN FOUR HOLES AT A CROSSING — 5 nodes on LS, one of them SIX.** `166.5,221.9` (Jacob's own junction) · `121.2,526.0` · `424.4,-88.7` · `-426.4,134.1` · `-355.0,-253.9` (six).
  - ▶ **AND 26 OF ①'s HOLES ARE SLIVERS** (≤6 vertices, <2000 m²) — **three of them are 0 m², 1 m² and 1 m²** (tiles 103, 21, 24). ⛔ A zero-area block is not a block; ① asserted a hole where its own ink should have closed.
  - ### ⭐⭐⭐ THE CORNERS OFF THE PARK — the eye report this item exists for *(Jacob, 2026-09-08: "one corner doesn't draw an eligible corner geometry so all the ADA/treelawn, etc. doesn't work")*
    **THE PARK IS ONE OBJECT WITH THREE INDICES, AND THIS ITEM QUOTED THE WRONG ONE FOR AN HOUR.** ⛔ Address it by GEOMETRY — `lu: park`, **148,707 m²**, frontages on Park · Mississippi · Lafayette · Missouri — never by number:
    | space | index | count |
    |---|---|---|
    | live `protoShapeTiles` | **#79** | 148 |
    | frozen `shape.json.tiles` | **#80** | 147 |
    | `RIBBONS §6.2a` (2026-08-12) | **#8** | a third era |
    ⚠️ This item said "`shape.json` tile #79", which is the live index labelled with the frozen artifact's name — **the fourth index-space error of the session, in the line filed for Jacob's own priority.** ⚠️ And the canon's entry names the frontages as `lafayette-avenue-3 · missouri-avenue-2 · park-avenue-1` where the pour now reports `-4 · -3 · -4`: **the chain ordinals have moved too**, so neither the index nor the ordinal is a durable handle.
    ### ⛔⛔ AND ITS TWO CORNERS FAIL FOR **DIFFERENT** REASONS — measured per corner, 2026-09-08
    | junction | ① turns | ① stamp on the contour | arc built |
    |---|---|---|---|
    | Mississippi × Park `229.0,-158.9` | 109.4° | ✅ present | ✅ **YES** |
    | Mississippi × Lafayette `166.5,221.9` | 112.0° | ⛔ **absent** | ⛔ no |
    ⇒ **Miss × Park's arc IS built** — its visible defect is signature 1's near-reversal spike in ②, downstream of a corner that exists. **Miss × Lafayette has no ① stamp reaching the contour at all** — the UNREACHED class. ⛔ **Do not look for one cure.**
    ⭐ **NOT a loop interior:** the block carries **4 distinct owners** (`park-avenue-4 · mississippi-avenue · lafayette-avenue-4 · missouri-avenue-3`) over 11 runs, so `RIBBONS §1`'s "medians and loop interiors — one street, so the owner never changes" does **not** apply. Excluded by measurement, not by argument.
    ⛔ **AND ① IS NOT AT FAULT AT EITHER: its contour turns 109.4° and 112.0° there — clean corners, far past the 18° tolerance.** The curb built from them carries a **163–165° near-reversal spike** where the corner should be. ⇒ the park's missing ADA/treelawn is **not** an approach-geometry problem, so ⛔ **the mint straighten Jacob originally asked about is not its cure** — that was established before any of it was built.
    ▶ `node checks/claims-every-turn-in-the-protopolygon-gets-an-arc.mjs [scene] [--list]` — the gate for this class, RED: **99 of 862 ① corners genuinely lost · 48 of them to ① and the offset ring disagreeing about what turns · 376 a traceability gap.** ⛔ Its complement `claims-the-ease-is-the-corner` reads 100% on the same map — run BOTH or the class is invisible.
    ▶ `node checks/claims-a-kink-recovers-but-a-corner-does-not.mjs [scene] [--list]` — the discriminator that retired the straighten: on LS **one** near-junction vertex is a single street bending, and it is a real corner, not a kink.
  - ⭐ **AT JACOB'S JUNCTION, CONCRETELY:** ① makes five holes; the fifth is a **586 m², 5-vertex wedge** whose vertex at the node turns **150.3°** — a near-reversal in ①'s own contour, before any offset. The park's hole turns a clean 109–112° at both its Mississippi corners, so **the two failing corners do not share one cause and this item must not assume they do.**
  - ▶ **SIGNATURE 1 — near-reversal vertices, and ⭐ THE STAGE THEY BECOME SPURS AT IS THE BAND STRIKE, NOT THE CURB** *(stage attribution measured by agent `lafayette-square-nosync-73`, 2026-09-08, using the code's own `SPUR_COS` dot-product rather than an independent angle formula)*
    Map-wide the curb carries **35 vertices turning >140° across 31 tiles**, clustering immediately under **165° = `SPUR_COS`** (`tileGround.js:131`), the threshold `dropFoldSpurs` catches at. ⛔ **But at the park's two junctions the curb never crosses it** — and this item said "the curb built from them carries a spike", which is the wrong stage:
    | stage | Miss × Park | Miss × Lafayette |
    |---|---|---|
    | ① proto | 109.4° · **0 spurs** | 112.0° · **0 spurs** |
    | protoCurb | 162.7° · 0 spurs | 164.6° · 0 spurs |
    | band curb | 159.1° · 0 spurs | 164.6° · 0 spurs |
    | **band treelawn / sidewalk** | **173.3° · 1 spur** | **173.0° · 4 spurs** |
    | band lu | 98.7° · 0 | 170.8° · 1 |
    ⇒ **the curb is UNDER the threshold at both nodes, so `dropFoldSpurs` correctly leaves it; the spur appears when the TREELAWN and SIDEWALK are struck.** ⭐⭐ Which is exactly what the operator reported — *"all the ADA/treelawn, etc. doesn't work"*, **not** "the curb is wrong". ▶ `node scratch/_pk-park-corners.mjs [R=metres]`
    ⛔ **NOT established that the band strike CREATES the spur** — a 162.7° curb vertex is already nearly a reversal and the strike may only be revealing it. **The stage at which it crosses is measured; the causation is not.**
    ⚠️ **AND THIS IS ONE SIGNATURE, NOT THE CLASS** — near-reversals are 21% of the corners ② fails to build against 5–6% of the ones it builds: a 3–4× over-representation and **not** an explanation. **79% of the lost corners turn like ordinary corners.** ▶ `node checks/claims-every-turn-in-the-protopolygon-gets-an-arc.mjs` splits it: **99 of 862 ① corners genuinely lost · 48 to ① and the offset ring disagreeing about what turns · 376 a TRACEABILITY gap where the arc exists and the ① stamp does not.** ⛔ A traceability gap and a lost corner may not share a number.
    ⚠️ **A FOURTH INDEX FOR THE PARK:** it is **① ring #168** in the proto ring space. With `protoShapeTiles #79`, `shape.json #80` and the canon's `#8`, that is four numbers for one block — ⛔ reconcile by AREA (148,707 m²), never by index.
  - ⛔⛔ **THE GATE IS GREEN THROUGH ALL OF IT, BY CONSTRUCTION.** `claims-the-ease-is-the-corner` reports **100%** because it checks *arcs → ramps*. **A corner that never made an arc is invisible to it** — the false-negative blindness `RIBBONS §1` names when it retires the one-sided safety argument, live and costing the operator a corner. ⭐ **A new check is owed on the other side: every place ① turns, did an arc get made?**
  - ### ⛔⛔ READ BEFORE QUOTING ANY CROSS-TOWN NUMBER IN THIS ITEM — **ONLY LAFAYETTE SQUARE IS FROZEN**
    ▶ verified from the build, per scene, 2026-09-08: `protoSource` is **`frozen` on lafayette-square ONLY**; hipointe-demun · altadena · lafayette-square-staging all return *"live: this scene carries no frozen protopolygon — it has not been poured since ① landed"*. ⇒ **every HPDM / altadena / staging figure below is measured on the LIVE re-derivation**, which `A20` records is built from `streetsOrig` — the input `derive.js:5105` explicitly REFUSES, because it "silently produces a dense contour with phantom blocks".
    ⛔ **NOT void — a live row is still evidence, and suppressing it would be worse.** But it is weaker than a frozen row and must not be presented flat. ⭐⭐ **It bites the item's own portability argument hardest:** signature 2's *"altadena has zero authored slots and still fails 5/5, so it is the kit not authoring"* is the Layer 0 q3 answer this whole item leans on, and it rests on that path.
    ⭐⭐⭐ **⇒ A21 IS COUPLED TO A20, AND THIS IS THE CONCRETE COST.** Pouring any one of those three would turn the strongest cross-town claim here from live to frozen. **A20's pour half is not merely dormant housekeeping — it is what A21's portability evidence is waiting on.** That is A20's call to make, not A21's.
    ⭐ Both `claims-*` now print an `① SOURCE:` line on every scene row and say to read it before quoting; ⛔ deliberately NOT a refusal, for the reason above.
  - ### ⭐⭐⭐ SIGNATURE 2 — **A FRONTAGE CANNOT ASK FOR NO PEDESTRIAN REALM** *(agent `lafayette-square-nosync-73`; RE-FOUNDED 2026-09-08 after its first premise was retracted — see below)*
    ▶ `node checks/claims-a-frontage-can-ask-for-no-ped-band.mjs [scene]` — goes through the **exported `resolvePedDepths`** and `blockCustoms`, so it cannot drift from the shipped resolver.
    `resolvePedDepths` (`tileGround.js:2259`) is `tl = custom.treelawn ?? STD_TREELAWN(1.5)` · `sw = custom.sidewalk ?? ADA_SIDEWALK(1.5)`. **The base measure's own ped fields are never read**; `gleanTreelawn` sets only the arrangement.
    | scene | ① source | stamps | no ped depth in the data | painted anyway | reach zero |
    |---|---|---|---|---|---|
    | lafayette-square (82 slots) | **frozen** | 6,172 | 2,216 (35.9%) | **2,216** | **0** |
    | hipointe-demun (17 slots) | live | 136,654 | 40,058 (29.3%) | all | **0** |
    | altadena (0 slots) | live | 20,846 | 1,106 (5.3%) | all | **0** |
    ⭐⭐ **ZERO STAMPS REACH ZERO PED DEPTH IN ANY TOWN — including LS with 82 authored slots.** The only route to "no pedestrian realm here" is hand-authoring BOTH fields on one segment. ⇒ **on town #2 every motorway ramp gets an ADA sidewalk and nobody will know** (LS: 1,688 of the 2,216 are `material: highway`, 528 unclassified).
    ⛔ **THE FIX IS NOT PROPOSED HERE AND IS NOT OURS.** "Should `material: highway` suppress the ped default" is a design call, and the present behaviour is the deliberate cure for a worse bug the code records at `:6698`. Backlogged with the check as its receipt.
    ### ⛔⛔ RETRACTED, AND EXCISED RATHER THAN BANNERED — what this section said for three hours
    It said: *"every block whose every frontage asks for `sidewalk 0 AND treelawn 0` is handed one anyway — LS 17/18 · 65,900 m² · HPDM 54/59 · altadena 5/5."* ⛔ **The premise was false.** The classifier read `measure[side].treelawn/.sidewalk` — the RAW fields — which `:6698` warns against **four lines above the function it was replicating**: *"`measure.treelawn` is only the AUTHORED OVERRIDE; the depth the map actually paints comes from `resolvePedDepths`."* Those blocks were not dressed against their frontages' wishes; **the resolver dresses every frontage by design**, so the measurement was the standard, not a bug. Layer 0 q3, committed by an instrument. The check that produced it is **EXCISED, not bannered** (`claims-the-hairlines-are-on-blocks-that-take-no-stack.mjs` is gone).
    ⭐⭐⭐ **AND MY OWN "INDEPENDENT VERIFICATION" MADE THE IDENTICAL ERROR, WHICH IS THE LESSON WORTH MORE THAN THE FINDING.** This item said "verified independently here by a second route" — a per-STREET cross-check against `ribbons.streets[].measure`. **A different route reading the same wrong field.** It agreed, and I read agreement as corroboration. ⛔ **TWO INSTRUMENTS THAT SHARE AN ERROR AGREE. Corroboration requires a different ORACLE, not a different route to the same one** — here, going through the shipped resolver, which is what the replacement does.
    ⭐ **WHAT SURVIVES INTACT:** the geometry and areas are real; `brentwood-promenade-court-1/-2` (98,186 m², material NULL) still kills the "highway gores" framing and is now better explained — material `null` is 26,034 of HPDM's frontages, so it was never a highway story. ⚠️ **Signature 3's hairline COUNTS never read the ped fields; its `demand` DOES — see the label correction under signature 3. The hairline check itself is untouched.**
    ⭐ **AND ONE OBSERVATION SURVIVES THE FILTER because it is measured on OUTPUT, not on the ask:** **block 133 has 0.00 m² of ped band; block 101 has 19,962 m²; they carry the SAME tags** (`motorway_link`, `motorway`). Map-wide **only 6 of 256 blocks get zero ped area at all.** ⛔ The old framing — "the one that gets it right among the no-stack blocks" — is void with its filter. Why those 6 differ is still a live, small question.
  - ### ⭐⭐ SIGNATURE 3 — **A BLOCK NARROWER THAN THE STACK THE KIT WILL PAINT ON IT** *(agent `lafayette-square-nosync-73`, 2026-09-08; re-founded on the PAINTED depth after the raw-field retraction)*
    ▶ `node scratch/_mk-hairline-slack.mjs <scene>` — prints BOTH demands side by side, so the correction is visible rather than asserted.
    **reach** = `2|A|/P` of the block outline. **demand** = what the map actually PAINTS: `curbWidth + STD_TREELAWN + ADA_SIDEWALK`. ⭐⭐ **That demand is CONSTANT — nothing reaches zero (signature 2) — so this is a pure geometric threshold: a block whose mean half-width is under it is narrower than the pedestrian stack the kit will put on it.**
    | reach/demand | LS | altadena |
    |---|---|---|
    | 0–1 | **23.3%** | **38.5%** |
    | 1–2 | 14.3% | 14.0% |
    | 2–4 | 6.9% | 10.2% |
    | 4–8 | 13.8% | 2.1% |
    | 8–∞ | **2.9%** | **1.8%** |
    8× on LS, **21× on altadena** — sharper than the retracted version, not weaker.
    ### ⛔⛔ QUOTE THE FORMULA, NOT THE NUMBER — measured here 2026-09-08
    It evaluates to **3.381 m** on lafayette-square · hipointe-demun · altadena · lafayette-square-staging, and it is tempting to call that "one number, the same in every town". ⛔ **It is not a constant.** `toy` carries `curbWidth 0.3302` ⇒ **3.330 m**, and `STD_TREELAWN`/`ADA_SIDEWALK` are both marked **"tunable"** at `tileGround.js:2168-9` while `curbWidth` is **authored per Look**. ⭐ The four scenes agreeing is as likely to be `A11`'s Look-seed clone as a kit property. ⇒ **the portable object is `curbWidth + STD_TREELAWN + ADA_SIDEWALK`, evaluated per scene** — a formula derived from the resolver, never a number carried across towns. *(Quoting the number would be the LS-shaped claim this item killed twice tonight.)*
    ⛔ **RETIRED — the DATA-demand table** (`0–1 19.6% … 8+ 3.7%`, and the median-area figures 599 vs 10,517 m² etc.). It was built on the raw `measure` fields and its demand did **no work**: painted demand is constant, so the ratio was measuring reach alone. The probe keeps that row visible next to the painted one **to show the difference**, and A21 does not quote it.
    ⛔ **AND IT STILL DOES NOT CLOSE THE CLASS: 37 of LS's hairlines sit in the roomiest bucket**, on blocks with 8× the width they need. **Cause not established.**
  - ### ⛔ TWO MORE CANDIDATES TESTED AND RETIRED — 2026-09-08, so A21 does not carry them as open
    · **COMPOUND UNDER-COUNT — DEAD.** The worry was that `demand` computed from outer labels alone under-counts, since `WBnom` maxes over holes too. It cannot bite: ⭐ **LS carries exactly ONE compound block, holding ONE hole** — authoritative, read straight off the frozen ①'s `blockHoles`, not inferred. And **no** roomiest-bucket hairline block is compound (LS 0 of 14 blocks / 0 of 45 hairlines). ⇒ the mono-width envelope reaching over hole labels at `:7341` is real, correct, and irrelevant to the buckets. ⚠️ **HPDM and altadena cannot be answered this way at all — they carry no frozen ① (`A20`)**, so any compound count for them rests on the live re-derivation A20 says is built from the input the pour refuses. ⛔ Do not quote a compound figure for those two.
    · **THE CAPACITY GUARD — BOUNDED, NOT MEASURED, AND NOT WORTH A FIELD.** Capping fires only when a block cannot take `WBnom/0.9` at all, so **every capped block must have `reach/demand < 1`** — the top bucket, **30 blocks on LS**. ⇒ `capped ≤ 30`, against 174 hairlines on 61 blocks, so it is a SUBSET at most. The code comment's "LS 5" is consistent with that ceiling and remains ⛔ **unverified — bounded is not measured**, and neither is a licence to close the class.
  - ### ⭐⭐ THE RESIDUE, AND IT IS NAMED RATHER THAN A PERCENTAGE
    **45 hairlines on 14 LS blocks in the ROOMIEST bucket — 3.2 each, so CLUSTERED, not scattered.** ⛔ Not squeeze · not capping · not compound · not frontage variety · not material · not authoring. Six candidates tested and retired against it. **Cause not established**, and the clustering is the shape of the next question: whatever it is, it happens repeatedly on the same block rather than once anywhere.
  - ⛔ **VOID WITH ITS FILTER, NOT MERELY KILLED — 2026-09-08.** This carried a kill: *"the hairlines are NOT the stack struck on no-stack faces — 8%/9%/6%/14% against a 7%/4%/1%/10% base rate, so 88% are on ordinary blocks."* ⛔ **Every one of those percentages was computed with the retracted raw-field classifier, and the "no-stack" CLASS ITSELF does not exist** — no frontage can ask for nothing (signature 2). The hypothesis is not disproved, it is unaskable as posed. ⭐ **What stands is the plain fact it was reaching for: the hairlines' cause is NOT ESTABLISHED**, and the percentages that were doing the work are withdrawn rather than restated.
  - ⛔ **NOT A21's, AND DELIBERATELY NOT OPENED HERE:** the Park × Ohio median is dressed because ①'s labels for that face come back `park-avenue-5/-7 LEFT`, and left is the side carrying `sidewalk`. That is the **already-open divided-road side question** — `RIBBONS §3.5`, awaiting Jacob's eye since 2026-09-04, 128/133 pairs across 4 towns. It has a home and an owner; a second front on it would be the duplicate this item exists to avoid.
  - ▶ **THE QUESTION THIS LEAVES, unruled:** should ③ emit a band at all on a frontage whose measure is zero? ⛔ Not a threshold — an authored zero and an absent measure must not read the same (`feedback_partition_artifact_vs_authored_zero`). **Jacob's call; do not build it as a filter.**
  - **M** · → `RIBBONS §1` (① is sharp; the corner is where ① turns; identity rides the union), `SKELETON §0.1` (Law 3 — suspect the input).

- **⛔⛔ A20 · ONLY LAFAYETTE SQUARE HAS A FROZEN ①. EVERY OTHER SCENE RE-DERIVES IT LIVE, FROM THE INPUT THE POUR EXPLICITLY REFUSES** *(NEW 2026-09-08, found while sizing the mint straighten)* — ▶ re-derive, never quote: `node -e "const fs=require('fs');for(const d of fs.readdirSync('cartograph/data')){const p=d==='lafayette-square'?'src/data/ribbons.json':'cartograph/data/'+d+'/clean/ribbons.json';if(!fs.existsSync(p))continue;const P=JSON.parse(fs.readFileSync(p,'utf8')).protopolygon;console.log(d.padEnd(26),P?.rings?.length?'frozen ① '+P.rings.length:'⛔ NONE')}"` — measured today: **LS 283 rings; altadena · hipointe-demun · lafayette-square-staging NONE** ⇒ the pour half of this item is **HPDM + staging + altadena**, three scenes. *(It named five until the day it was written, the two extra being the Polish scenes — excised 2026-09-19, so the command above now enumerates the real roster on its own. The reach problem it recorded was real: a ruling a year older than the ticket still had not reached it, which is why scope now lives in the look manifest and not in prose.)*
  - ⭐⭐ **THE DEFECT IS THAT THE TWO PATHS HOLD OPPOSITE POLICIES ON ONE QUESTION.** `derive.js:5105` (frozen) **refuses** and says why — *"Refusing to mint ① from the DERIVED chains: that silently produces a dense contour with phantom blocks, which renders and cannot be seen to be wrong."* `tileGround.js:6587` (live) does **exactly that**, from `streetsOrig` (`:4443` — `ribbons.streets`, the derived chains), behind a `console.warn`. ⇒ **Layer 0 q2 in the shape the canon names worst: a plausible map, and the operator is never told.** `SKELETON §0.1`'s NO-FALLBACK ruling is enforced on one path only.
  - ⚠️ **DORMANT, NOT BURNING — say so honestly.** It bites when someone OPENS one of those scenes; LS carries its frozen ① and is correct. Nothing is at risk of being lost, and the old *"pouring a second scene clobbers production LS"* hazard is **CLOSED** for this path — `promote-ribbons.js` is scene-scoped (*"a non-default scene writes its OWN clean/ribbons.json so LS's bundle is never clobbered"*). ⛔ The other shared-default `src/data/*` name-imports are **not** verified here; do not read this as a general all-clear (`EXTENT-DESIGN §6` step 4).
  - ⛔⛔ **AND `lafayette-square-staging` IS IN THE LIST, WHICH IS THE SHARP END.** The canon records a full day lost to Jacob eye-gating staging while the work was on LS (`PREBAKE §4.0a`) — and those were only different AUTHORING STATES. They are now different **CONSTRUCTIONS**. ⭐ **An eye verdict taken on staging today is taken on a map built the way the ruling forbids** (`feedback_an_eye_verdict_must_record_the_scene`).
  - ⭐ **THE CURE IS ALREADY RULED — IT IS `A02`'s, APPLIED TO ①.** *"The live build still DRAWS — a fresh pour with no freeze is legitimate and refusing to draw would make onboarding town #2 impossible. **The defect was the silence, not the draw.**"* ⇒ carry a *reason* (not a bool) the way `shapeFreezeMissing` does, and surface it as the same non-dismissable banner. ⛔ **ORDERING, and it is not optional: POUR FIRST, THEN TIGHTEN.** Make the live path refuse before the scenes carry a frozen ① and every non-LS town stops drawing at once.
  - ### ⭐⭐⭐ RESIZED THE SAME DAY — **THE POUR HALF IS BLOCKING `A21`, SO "DORMANT" IS ONLY TRUE FOR THE OPERATOR**
    Filed above as "dormant, not burning" because it bites when someone OPENS one of those scenes. ⛔ **That is still right for the operator and is now WRONG for the investigation.** Every cross-town figure in `A21` — the hairline counts, the zero-frontage counts, and specifically **altadena's `5/5` with zero authored slots, which is A21's entire Layer 0 q3 answer** — is measured on the LIVE re-derivation, because only `lafayette-square` carries a frozen ①. ▶ verified per scene from the build 2026-09-08 (`protoSource`).
    ⇒ **Pouring ONE of hipointe-demun · altadena · lafayette-square-staging upgrades A21's strongest portability claim from live to frozen.** That is the cheapest thing on this board that makes another item's evidence sound. ⛔ Still ordered POUR FIRST, THEN TIGHTEN.
  - **S** (the banner, on A02's existing wiring) + **M** (the pours, one per scene, each needing its own eye). → `SKELETON §0.1`, `RIBBONS §1`, `A02`, `A00`, **`A21`** (which is waiting on the pour half).

- ⛔ **A12 · `?look=<unregistered>` falls back to LS — RULED CLOSED, NOT A DEFECT (Jacob, 2026-09-20, twice).** *"This feels like an alarm for a non-existent fire. If `?look` is unregistered that's basically tautological."* And, on being re-asked the same day: *"I am so tired of this question... When would/could this ever even happen? And who cares if it does?"* ⇒ **An unregistered look resolving to LS is accepted product behaviour.** ⛔ Do not re-open it, and ⛔ do not re-derive it from the general no-fallbacks doctrine — this is the operator's ruling on this specific case, and the doctrine does not overrule it.
  ⭐ **The code fact, kept so nobody re-measures it:** `src/instance.js#resolveInstance` falls back to `instanceForMap(DEFAULT_MAP)` for an unknown look, but it now `console.error`s loudly, keeps the **asked-for** `lookId` rather than LS's, and stamps `identityResolved: false` (written; no consumer). ⛔ **Was framed here as owed work — *"an unregistered look must fail loudly, not draw the mould"* — and that framing is what brought it back after the first ruling.** An `S`-sized item retired without being built. → `A00` (where it is also recorded as ruled-closed).
- **⛔ A13 · `browse.bounds` IS LAFAYETTE SQUARE'S FRAME, IN EVERY TOWN — ⭐ AND THE FIX IS CHEAP, BECAUSE THE HOOD'S REAL SIZE ALREADY EXISTS PER TOWN** *(NEW 2026-08-07; ruled + resized 2026-08-08)* — `{cx:95, cz:-158, w:1292, h:1025}` is **byte-identical across all 8 baked `scene.json`** (verify: read `shots.values.browse.bounds` per scene). Hardcoded in `skyLightChannels.js` and again in `StageApp.jsx`, **never derived, no authoring surface** ⇒ Browse is parked on LS's centre and altitude everywhere, and A11-c's derived hero standoff gets the right *place* at **LS's size** (an LS-sized standoff drops the camera inside downtown Łódź).
  - ⭐⭐ **JACOB'S RULING (2026-08-08) — the default framing, both shots:** **Browse = the neighborhood's outer ring (the soft gradient edge) + a little padding.** **Hero = always show *a* hero object**, camera **pulled back to the ring, up in the air**, with a **slow pan** — the pan angle is a **dial** (eye-gate 30 / 45 / 60; don't argue it in prose). ⛔ **Do NOT make the hero a required field** — mandatory-plus-a-default contradict, and a pour that can't complete without a hand-placed hero cannot pour town #2 without hand-work. Optional, with a derived default; the operator overrides when the town owns an Arch.
  - ✅ **NO DERIVATION TO BUILD — the number is already authored per town** in `cartograph/data/<scene>/neighborhood_boundary.json`: **LS `radius:892` · HPDM `1251` · ksi `1530`**, each with its own `fade{inner,outer}`. That *is* the soft gradient edge. So this is "read the radius, add padding, point the camera," not a new field. *(Reproduce: `node -e "console.log(require('./cartograph/data/<scene>/neighborhood_boundary.json'))"`.)*
  - ✅ **The undesignated-hero default is ALREADY kit-general** — `src/lib/heroSubject.js` resolves no-designation → the authored arch channel → **the hood centroid** when no arch exists, and says so in its own comment (*"the answer for every installation that doesn't own a Gateway Arch"*). ⇒ *"always show a hero object"* is half-shipped; what is missing is the **placement** (at the ring, in the air) and the **pan**.
  - ⚠️ **This revises A11-c's ruling** (*"no hero object ⇒ the pan defaults OFF, static"*, `fb6c4ab2`). Today's is *"always a hero, always a slow pan."* Compatible in spirit — neither inherits LS's path — but the deliverable moved; don't land it as if nothing changed.
  - ⛔⛔ **NAME COLLISION — TWO DIFFERENT THINGS ARE CALLED `radius` + `fade`. This burned a standup on 2026-08-08.** `scene.json`'s **`horizon {radius, fadeInner, fadeOuter}`** is a **SKY/atmosphere look channel** (`HORIZON_FLAT_DEFAULTS`, `src/cartograph/skyLightChannels.js:335`) — **it is NOT the neighborhood disc.** The disc is `neighborhood_boundary.json`'s `radius` + `fade{inner,outer}`, read by `src/cartograph/boundary.js`. ⭐ Grepping `scene.json` for a fade-shaped number finds the **sky** and looks like a smoking gun: the sky horizon *is* identical across the 6 real Looks — but that is the **A11 Look-seed clone** (a look channel copied by the seed), **not** the hood's edge, which is correctly per-town. **Check `neighborhood_boundary.json` before reporting a hood-size finding.**
  S · → `A11`, `EXTENT-DESIGN §3.3`, `project_camera_framing_slab_contract`.
- **~~A14~~ ⛔ WITHDRAWN 2026-08-08 — it was the operator troubleshooting.** A `layerVis` flip is Jacob switching layers off to see the sidewalks; **re-baking was never blocked.** Residue is one publish-checklist line, not an investigation. → `_archive/A14-withdrawn-and-ls-substitution-triage-2026-08-08.md`, `[[feedback_layer_toggles_are_troubleshooting_not_drift]]`

- **✅ A16 · CLOSED `43bab464` — a materials flip writes the material and nothing else, and the resolver now MERGES.** Verified 2026-08-13: Jacob's authoring session persisted **112 lines, every slot `materials`-only** — no fabricated `treelawn`/`sidewalk`. ⏳ **Open residue, separate:** the **pre-`43bab464` slots** already on disk carrying `treelawn`/`sidewalk` — ⛔ **not assumable as fabrication** (`SECTION §3.3` makes equal-width strips sacrosanct, so `1.5/1.5` may be correct). ▶ `node scratch/a16-write-site-identity.mjs`. *(The full filing + the two corrections it went through are in git at `7299bbeb`.)*
  - **What the write does.** A strip-material flip on `dolman-street-1 / left / segOrd 4` persists a full measure blob whose `treelawn` **and** `sidewalk` are the `STD_TREELAWN` / `ADA_SIDEWALK` constants rather than the edge's own values — `MeasureOverlay.jsx:745-751` builds the seed from the frame, then `:750`'s trailing `treelawn:` / `sidewalk:` keys clobber that spread. ▶ **Reproduce, don't quote: `node scratch/a16-write-site-identity.mjs`.**
  - ⛔⛔ **THE ORIGINAL FILING WAS WRONG TWICE. Both corrections are load-bearing.**
    **(1)** `sidewalk 1.52 → 1.5` was called a **rounding**. It is not — it is `ADA_SIDEWALK`, the same constant substitution as the treelawn. **Two fields are invented, not one.**
    **(2)** *"The sidewalk mended because the toggle gave the leg a 1.5 m treelawn"* is **REFUTED — the blob is INERT in the render**, in Section and in Survey: `resolvePedDepths` returns identical `(tl, sw, hasTL)` before and after, the owning tile's FILL is byte-identical, `runMeasure` copies only `pavementHW` (unchanged), and `sectionPassTile` never reads `terminal`. ⭐ **Do not cite "the map got BETTER" as evidence for this ticket** — nothing here drew it.
  - ⚠️ **∴ THE MEND IS UNEXPLAINED, and the better candidate is a TOOL HOP, not a toggle:** the Survey-exit re-freeze (`BlockGeometryV2Debug.jsx:849-868`, landed the same day in `a96f00c2`) rewrites what Section draws. Corroborating: `{outer:'SW', inner:'LU'}` **cannot** come from one flip off the no-custom default (`MeasureOverlay.jsx:728-736`), so the gesture was ≥2 clicks and the intermediate state *was* live. **Needs Jacob's account of the clicks — not answerable from disk.**
  - ⭐ **THE REAL HARM — IT DEFEATS REVERT. And it is a SECTION defect, not a Survey one** (`SURVEY §0`: *"there is no notion of pedestrian depth in Survey"*). `SECTION §3.1`/`§5.1`: ADA depths **are** the Revert state, and Default **is** the calculation — clearing the override re-seeds by construction, so **absence is the defined state for "not authored."** The write converts *unauthored, re-seeds* into *authored, pinned*: harmless while the constants match, wrong the moment the calculation moves or `A10` ③ lands, and it makes an untouched edge count as an override. *(⛔ The retired filing cited `feedback_partition_artifact_vs_authored_zero` — that invariant is about an **operator-authored** zero. The frame's `treelawn: 0` is the **gleaned gap** from `survey.json`, thresholded to Y/N by design, `SECTION §3.1`. Wrong citation; do not restore it.)*
  - ▶ **THE FORK HAS COLLAPSED TO ONE CANDIDATE.** ⛔ **(a) "seed from the resolved measure" is DEAD** — `resolvePedDepths` **is** the constants, so it re-writes the same invented values. ⛔ **Seeding from the FRAME is barred by an invariant, not by taste:** writing `treelawn: 0` makes the inert write live and collapses the strip — `SECTION §3.3`, *"the mono-width ribbon is SACROSANCT · two strips always, EQUAL width — swap, not collapse."* ⇒ **what remains is to OMIT the Section fields from a materials write** (the shape `SECTION §5`'s Revert already uses). The thing to clear first is the OR-REPLACE resolver — `buildBlockGeometryV2.js:1579`, `(customsResolver(…)) || m[sideKey] || {}` — so a partial blob must still carry `pavementHW` or that edge's asphalt collapses. **S** · → `SECTION §3.1`/`§3.3`/`§5.1`, `SURVEY §0`, `feedback_customs_resolver_wholesale_not_merge`.
  - ⏳ **HELD, NOT FILED:** `tileGround.js:2684` runs `gleanTreelawn ? STD_TREELAWN : 0` on dead-end tips. **This may be `SECTION §3` step 3's ruled round-dead-end exception rather than a stale survival** — route it to `SECTION §6.3` before anyone calls it a defect.
- **⛔ A11 · 🟡 THE ROOT IS CLOSED (2026-09-19) — ARTIFACTS STILL NOT CLEANED; three towns carry Lafayette Square's authoring in their own `design.json`** *(2026-08-07, agent Kern; root closed by agent Kiln)*
  - ✅ **THE MECHANISM IS NAMED TO A COMMIT, AND IT WAS A FIX.** `POST /looks` (`serve.js:2340`) seeds a new Look's **entire** `design.json` from a seed Look; `ExtentApp.jsx:1605`/`:1662` calls `createLook({name: scene, scene})` with **no `fromLookId`**, so the seed falls to the default Look — **Lafayette Square**. Introduced by **`69825d1a`, 2026-07-14, *"first bake out of Extent creates a Look for the scene"*** — the fix for Altadena's slab clobbering LS. ⭐⭐ **The fix for LS-substitution-in-the-BAKE created LS-substitution-in-the-ARTIFACT.** Proven, not inferred: each town's `blockCustoms` hashes byte-equal to LS's `design.json` at that town's own creation commit.
  - ✅ **RECURRENCE CLOSED** (`serve.js`): a cross-scene seed carries **style** and drops the four scene-keyed maps (`blockCustoms`, both corner-radius maps, `blockLandUse`), naming what it dropped in the log and the response; a **same-scene** clone keeps its authoring. ⭐ Because a strip list rots, it is paired with a **declare-or-refuse guard** — any surviving key naming a street in the seed scene's bake **409s with the field named**, so tomorrow's scene-keyed field fails loudly instead of bleeding. Gate: **`node checks/claims-look-seed-scene-clean.mjs`** (7/7, lifts the field list out of `serve.js` source rather than restating it; replays the 2026-07-14 regression; mutation-verified — emptying the strip list fails 5 of 7).
  - ✅ **DETECTOR: `node checks/claims-override-provenance.mjs`** — classifies every override slot in every Look by asking **which scene's bake contains the run it names**: RESOLVED / **BLED** (foreign scene named) / STALE (no bake at all) / UNCLASSIFIABLE. No LS knowledge, no street names in the source, Look→scene from `index.json` not the directory name. **63 RESOLVED · 142 BLED · 38 STALE · 0 unclassifiable · 0 not-checked.**
  - ⏳ **REMAINING — the artifacts, and it is Jacob's call, nothing removed:** **safe to remove, 32 slots** (altadena) — ⚠️ **this was 136 across three towns until 2026-09-19; the other 104 were on the two Polish scenes and went with them, so only altadena's remain.** ⛔ Re-derive, don't quote — every key names an LS street, **none names a street of its own town**, and 31/32 values are byte-identical to LS's `design.json.pre-reset`; **no local authoring is mixed in.** ⛔ **MUST NOT be removed:** `lafayette-square-staging`'s 90 (11 exist nowhere in LS's history — genuine post-reset authoring; its 33 STALE are a **re-skeleton artefact wanting a re-key, not a delete**) and `hipointe-demun`'s 17 (all local — the **clean control** that proves the classifier isn't just flagging everything).
  - ✅ **RULED + CLOSED AT THE ROOT 2026-09-19 — the 0-state is no longer a town.** The wider surface the key-based detector could not see was real and is measured: **Huron, the freshest pour, arrived 46 of 46 channels byte-identical to LS** (`heroKeyframes`, `shots`, `trees`, `labels`, `parkTitlePos`, `horizon`, `browseHeading` — an authored LS *sky* included, `HORIZON_FLAT_DEFAULTS` being 3750/900/3750). ⭐ The replacement question — *"strip to nothing, keep a generic camera, or derive from the scene's extent?"* — **is answered:** `index.json`'s `default` is now **`kit-default`**, a Look whose `design.json` is `{}` and whose entry binds **no scene**, so a new pour seeds from **nothing** and every channel resolves from the store's `DESIGN_FIELDS`. ⛔ **The kit stores no camera** (Jacob: *"the camera motion is not something that carries realistically from hood to hood"*); absent a hero object the opening view derives from the scene's own extent. ⚠️ **The store's own `heroKeyframes` default WAS a second LS-tuned pair**, so "use the store defaults" would have laundered LS — it is now `[]`. → `ARCHITECTURE §3`, `EXTENT-DESIGN §2.1`.
  - ✅ **AND THE DOOR IT WOULD HAVE COME BACK THROUGH IS SHUT.** Four hydrators read `d.X || get().X` — absence meant *"inherit whatever the last-opened Look left in the store"*, so a stripped Look re-acquired LS's values on screen and the next autosave wrote them to disk (this slot's own ⚠️ from 2026-08-08, unfixed for a year). `hydrateDesign()` **no longer forwards `get`**, so a hydrator cannot reach live state at all. The three client-side `DEFAULT_LOOK_ID = 'lafayette-square'` literals (store · `Toolbar.jsx` · and the server's) are gone; the 0-state id is **served**, not compiled in.
  - ▶ **Gate: `node checks/claims-look-default-has-no-town.mjs`** (9/9). Asserts a property of the **default itself** — never a comparison against LS, which passes trivially for a town with no colliding street names and is how this survived four pours. **Mutation-verified assertion by assertion**: pointing `default` back at `lafayette-square` reds 4 of 9; each of the other 5 reddened individually. ⚠️ *Fixing this check's own `HERO_KEYFRAMES_DEFAULT` regex — it stopped at the `]` inside a `position: [x,y,z]` and THREW where it should FAIL — is why every assertion gets mutated, not just the headline one.*
  - ⛔ **STILL OPEN, deliberately untouched:** the **existing** towns' `design.json`. Huron's `arch` was cleared by hand (`H-6`); altadena and HPDM carry the same clone. Scrubbing them is a separate evidence-led pass — some of those channels are values their operators now rely on, and **you cannot tell inherited from adopted without looking.** ⭐ Whether the hero-object program belongs in the kit at all is **`H-7`**, not this.

  - ⚠️ **THE BLEED CAN STILL RETURN THROUGH STAGE — `useCartographStore.js:341`:** `hydrate: (d, get) => d.heroKeyframes || get().heroKeyframes`. A Look whose field is **absent** (i.e. every Look A11-c just stripped) inherits **whatever the previously-opened Look left in the store**, and the next save writes it back to disk. The store's own default (`:715`) is another LS-tuned pair. **Production is safe today** — `bake-scene.js:126` reads `design.json` directly with `|| []` — **but Stage is a live re-bleed path**, and `absence-means-inherit` is a known trap (`feedback_absence_means_inherit_in_authored_blocks`). ⛔ Fix before anyone opens a stripped Look in Stage and saves. **S.**
  - ⚠️ **Parallel copy, unfixed:** `PreviewApp.jsx:157` falls back to its own `SHOTS.hero.position` literal — the same defect A11-c fixed in `Scene.jsx`, in a second file *(agent-reported, not independently verified)*.

  *(Original finding, kept for the evidence — two of its three scenes were excised 2026-09-19, so only the `altadena` half is still on disk:)* — `public/looks/{altadena,ksi-y-m-yn,centrum}/design.json` held `blockCustoms` keyed to **LS street names** — `lafayette-avenue-5` · `park-avenue-1` · `kennett-place` · `mississippi-avenue` — in **Altadena, California** and in **Łódź, Poland**. Verify: `node checks/claims-corner-takeover.mjs` (per-scene "readable by this bake" line), or key the design against the bake's own `runs[]`.
  - **Today it is inert — 0 of 5 / 0 of 9 / 0 of 9 keys resolve against their own bake** — which is exactly why nobody saw it. **It is not harmless.** ⭐ **`park-avenue` and `kennett-place` are ordinary American street names**: the day a poured town has one, **Lafayette Square's authored widths silently apply to it**, and the operator sees a plausible map. That is Layer 0 q2 in the authored artifact rather than in code.
  - ⚠️ **AND IT SILENTLY VOIDED OUR INSTRUMENTS.** Every "authored vs bare-defaults" comparison run on those three scenes has been **defaults vs defaults while reporting itself as a dual-state test** — the A03/A05/A07 harness pattern assumes the authored state is real. `claims-corner-takeover.mjs` now labels each row **LIVE / INERT / none-authored** so this cannot recur silently; the other harnesses do not.
  - **Mechanism (unconfirmed, one read away):** their Looks were **cloned from LS's** and the authoring travelled. Same family as `A00`'s LS-fallback class — but in the *artifact*, not the code path, so `A00`'s fixes do not reach it. **Do not simply delete the keys** until it is confirmed nothing authored-and-wanted is mixed in with the bled data.
  - ⚠️ **Related, different, also open:** `lafayette-square-staging` has **39 of 90 override slots keyed to runs its own bake does not contain** — stale within one town's own family, not bleed. **S/M** · → `A00`, `BRIEF-ls-bleed-excision`.
- **Prod: 184 trees 404 on lafayette-square.com** — `origin/main`'s `public/baked/default.json` requests `skeleton-4` **184×** (platanus_acerifolia 88 · betula_pendula 48 · acer_saccharum_procedural 48); `git ls-tree -r origin/main public/trees | grep skeleton-4` → **zero** files. *(Sized at 88 for weeks — that was one species of three, off by 2×. Re-counted 2026-08-04.)* Fixed by the re-bake at the tree eye-gate; **do NOT `git restore public/baked/**`** (working tree is more correct). S · → folds into **B6**.

- **⭐ Land use — 🟡 PHASE 1 LANDED 2026-08-01 (`cd8e70e6`+`40c7e2e5`), EYE-GATED AND FAILED 2026-08-02. The brief's headline was WRONG; the arc continues under a corrected diagnosis.** → `docs/briefs/BRIEF-land-use-derivation.md` (⚠️ read its corrected banner first).
  - ⛔ **RETRACTED — "LS 62% of blocks / HPDM 57%, invented by `pickLuFromHash`."** `pickLuFromHash` is effectively **dead**: 1/101 LS tiles, 2/196 HPDM. `ribbons.faces` and `ribbons.tiles` are walked from the SAME centreline graph, so `luForRing`'s interior probe essentially always hits. ⛔ Stop citing 62%/42%. Detector: `scratch/invented-lu-census.mjs`.
  - ✅ **What Phase 1 actually fixed.** The real invention was one stage upstream — `derive.js` read only `stl_parcels.json` (14,597 County parcels unread), its mapper was City-only with a `return 'residential'` catch-all (14,587 County codes flunked it), and the ladder's third rung was the bare constant `use = 'residential'`. Now: both files jurisdiction-tagged, `cartograph/parcel-landuse.mjs` (County via the assessor's own 189-row bucket table, **no catch-all**), and an honest `underived` class wired through all five vocabulary consumers. **Measured on HPDM: invented land use 121 faces / 17.6% of area → 55 / 3.3%, and labelled.** County parcels 99.8% classified.
  - ⚠️ **EYE-GATE OWED AT LS'S NEXT POUR (2026-09-20, `ea13e35e`) — LS gains FIVE parks it has never drawn.** `derive.js` dropped the whole `leisure=park` class from the leisure overlays on a line reading *"Lafayette Park rendered separately"* — true of Lafayette Park, applied to every park in every town. huron drew **0 of 11** named parks; **LS dropped 6, of which only 1 is rendered separately**, so Fox Park, Buder Park, Ray Leisure Park, Eads Square Park and Fountain Park Plaza were rendered by nothing. The skip now keys to the **authored polygon's own centroid** (one point is inside at most one ring ⇒ no threshold, and a town with no authored park skips nothing). ▶ Verified without pouring LS: containment run against LS's `park-polygon.json` + raw OSM says **skip 1, draw 5**; huron re-poured **0 → 11**. ⛔ **This moves LS's land-use output, and this row exists because a change here gates on Jacob's eye** — it was sitting in a commit body until Boz called it. Twin of `H-8`'s `natural=water` skip: same town, same shape, invisible to any aggregate taken on LS.
  - 🔴 **THE EYE SAID NO — the greenbelt is still grey, and the cause is elsewhere.** It is **Concordia Seminary** (`amenity=college`, 291,019 m²), typed `unknown` and painted `#666666`. Root: **`classify.js:57` types an OSM overlay `unknown` when its tags match none of four branches, then `:86` gives the FACE that type and `break`s** — so the face never reaches `:94`'s `absArea > 500 → block`, and `derive.js`'s `if (face.type === 'block')` skips the land-use ladder entirely. ⭐⭐ **An OSM polygon the kit cannot read is STRICTLY WORSE THAN NO POLYGON** — the richer the data, the worse the map. **17/17** `unknown` faces are hijacks (zero reached the fallback); **15/17** would classify if typed `block`. Detector: `scratch/unknown-face-forensic.mjs`.
  - ⛔ **"Make `unknown` soft" is NOT the fix — measured, not argued.** `LU_POLICY` soft/hard is consumed by exactly ONE thing, `forbidden-surface.mjs` (the TREE gate). It has zero influence on ground colour. Flipping it would plant trees on ground that stays `#666666`. **Jacob's ruling still owed** on whether `unknown` should be soft on its own (smaller, separate, tree-count question).
  - **Two more joins behind it, both kit-general and both unfixed:** (a) `OSM_TO_LU` is a **32-entry allow-list**; HPDM brings **54 subtypes it cannot read, 2,446,607 m²** (`amenity:college` 445k · `amenity:hospital` 112k · `landuse:forest` 48k · `natural:grassland` 58k) — same shape as the `lu-policy` vocabulary gap, one stage upstream at ingest. (b) **The OSM vote asks "is the POLYGON's centroid in the face?"**, which structurally favours small polygons: the seminary votes in **1 of the 7 faces it covers**, so mapped parking lots outvote the unmapped lawn in the other six. The **reverse** test is YES for all 7 — and is what `luForRing` already uses downstream, so **the two stages disagree about which direction containment runs.**
  - **∴ NEXT, in dependency order — they do not ship independently** (fixing only the first moves the seminary from grey `unknown` to grey `parking`): stop the hijack → make the ingest vocabulary fail loudly the way `lu-policy` does → settle the containment direction. **Then** Phase 2 (area-weighted join, delete `pickLuFromHash`) and Phase 3 (building-derived LU, the portable rung for assessor-less towns). ⚠️ **LS has NOT been re-poured or re-baked** — eye-gated production, ~19 of its 173 faces move. M–L · `DOC-CODE-COHERENCE` C15/C16/C17.

---

# THE NEAR-TERM PUSH — two columns + a close-out (run in parallel)

> The two columns are independent (A = cartograph geometry, B = arborist/render) and dispatch
> concurrently; security is a bounded third track. Jacob's thesis: **land both columns + close
> security and what's left is scant** — at which point the work becomes the cleanup tail below.

## Column A — the SHAPE finish (Front A)  ·  *the final aesthetic of a premier product, not optional polish*
> The governing rule is locked (one uninterrupted frontage chain real-corner→real-corner; the ADA
> transition slope fires ONLY on arrangement-*difference*) — `SECTION.md` canon (`0f0a6473`).

- **A00 · 🟢 MOSTLY CLOSED (`08d61ce1`, 2026-07-31) — one read path left. RESIZED 2026-08-02.** *(Jacob: "there should be no fallback; it would help exactly 0.")* A fallback converts a failure into a plausible-looking success, which for a kit is the worst outcome available: the operator sees a map and trusts it. **Re-verified site by site, 2026-08-02:**
  - ✅ **`measureModel.js` name-keyed LS width seed — EXCISED.** Now `src/cartograph/measureModel.js`, scene-scoped via `setSceneMeasureSource()` (`:43`), registered at `useCartographStore.js:2173`; unregistered ⇒ empty seed ⇒ **common reasonable defaults that belong to most towns** (the generic street-type widths). ⭐ **That distinction is the whole point and worth stating plainly** *(Jacob, 2026-08-04)*: a broad, honest default is **not** the Layer-0 defect. The defect was substituting **one specific other town's measured data** — St. Louis numbers presented as Altadena's, with nothing on screen saying so. Falling back to a generic is fine; falling back to *Lafayette Square* is the thing that must never happen. **The 24-Altadena-streets figure is HISTORY, not a live measurement** — ⛔ stop citing it as standing evidence (`CLAUDE.md` Layer 0's receipt is now **A05** — the curb check that runs with authoring off; A07 held the slot until it closed 2026-08-04).
  - ✅ **`config.js` `SCENE = env || DEFAULT_MAP`** — `SCENE_IS_EXPLICIT` (`:42`) + `requireExplicitMap()` (`:50`); every WRITE entry point refuses and prints the corrected command.
  - ✅ **`serve.js:765`** — a Look with no scene is no longer *assigned* LS; left unset.
  - ✅ **`serve.js:1906`** — a bake with no scene now **refuses with 409** instead of baking over LS. *(Its comment still said "fall back to the default" until 2026-08-02; corrected.)*
  - 🔴 **REMAINING — the read route.** `serve.js` `const scene = sceneRouteMatch[1] || DEFAULT_MAP`: a data request with no scene in the path still silently serves Lafayette Square. Non-destructive (a read), which is why it survived the pass — but it is the same silent substitution, and it is the one that makes a mis-wired client *look* like it works. **XS.**
  ⚠️ **The class is NOT closed** — see **A05**, a silent fallback inside the curb *detector* itself (the SHAPE-producer instance, A07, closed 2026-08-04). S→XS · → `CLAUDE.md` Layer 0, `ORIENTATION`.
- **A08 · ✅ CLOSED 2026-08-04 — the three membership sites are now ONE function** (`cartograph/membership.mjs`). *(Found while ruling the precedence; fixed on Jacob's go-ahead.)* `bake-buildings.js`'s comment claimed the cull *"must match `pipeline.js` exactly so 2D + slab agree on membership"* — it did not: for a footprint with <3 vertices, pipeline pre-clip **kept**, pipeline post-derive **centroid-tested**, the bake **dropped**. So the 2D Designer could show a building the slab does not.
  - **The fix is one function, not one rule pasted three times** — that is what let it drift. All three sites now call `createMembershipFilter().decide(id, ring)`; both hand-copied `pointInPolygon` helpers were excised.
  - **The degenerate case is now RULED and LOUD:** a <3-vertex ring has no trustworthy centroid, so membership is **UNDECIDABLE**. `hide`/`activate` still apply (id-keyed, need no geometry); past that the building is **KEPT and COUNTED**, and `report()` prints a ⛔ line naming ids. **Membership is not a data-quality culler** — a malformed footprint is an **intake** defect, and dropping it silently loses the building *and its content*. The bake's mesh loop separately reports what it cannot extrude (it already skipped them, silently).
  - **⚠️ A near-miss worth recording:** `nb.polygon`/`nb.exclusions` are `{x,z}` **objects**; both old helpers hard-coded that. Unifying on an `[x,z]` assumption would have read `undefined` for every coordinate and dropped **every** building in any scene with a polygon — rendering as an empty map, not an error. The shared helper is deliberately format-agnostic and says so.
  - **Proof, not assertion — `scratch/a08-membership-equivalence.mjs`** (read-only, no pour/bake): replays the three historical predicates *verbatim* against the new one over every scene's committed artifacts — **72,735 decisions, ZERO disagreements**, including both polygon scenes (hipointe-demun 4-pt, centrum 815-pt), so the ring-format hazard is proven closed rather than reasoned about. Plus two synthetic cases that **pairwise distinguish all three old rules**, and an assertion that `activate` beats an exclusion loop. ⭐ **Zero degenerate footprints exist in any scene today (raw or clean), so this changes no current output — it is armour for town #2.**

- **A02 · ✅ BUILT 2026-07-31, ⚠️ NOT YET EYE-VERIFIED — the wall no longer falls back silently** — the consumer boundary is otherwise real and defended (every non-Survey view renders from the frozen `shape.json`; `sectionOpen` is chain-free by signature), but it **degrades without failing** by two doors: a failed `shape.json` fetch (`BlockGeometryV2Debug.jsx:589`, a `console.warn`) and a scene with no freeze (`:595`). On a town where the freeze failed or never ran the operator sees a plausible map and never learns the wall didn't hold — and because LS always has a freeze, it is **invisible in the scene you'd use to prove the wall works.** Same family as `BRIEF-ls-bleed-excision`. **FIX (built):** the store carries `shapeFreezeMissing` (a *reason*, not a bool — "never frozen" and "fetch FAILED" read differently), set at both doors and cleared in Survey (where live-stroking is the point, so the banner must not cry wolf); `StatusBar` renders it as the same non-dismissable red banner `overlaySaveBlocked` already used. ⭐ **The live build still DRAWS** — a fresh pour with no freeze is legitimate and refusing to draw would make onboarding town #2 impossible. **The defect was the silence, not the draw**, so the cure is that the view is now *labelled* a live re-derivation instead of masquerading as the frozen shape. ⚠️ **Eye-gate owed:** `vite build` passes and the wiring is verified, but the banner **cannot fire on any of the 7 scenes that have a freeze** — only `default` lacks one. Test: point the tool at a scene with no `shape.json` (or rename one) and confirm the banner. **S** · `PIPELINE.md` §5 (the Wall), `PIPELINE §5 (the Wall)`.
- **A03 · 🟡 PARTIALLY LANDED 2026-07-31 (`4dd05303`+`aa40a7d5`) — the producer is split at the chain boundary; Check C is green for 58% of tiles and still RED overall.** The curb producer now divides into **`freezeCurbEdgeFacts()`** (chain-derived — reduces runs/streets/measures to one fact per ring edge) → **`buildCurbRings()`** (**chain-free**: ring + facts + authored widths, with no streets/runs/measures/ribbons in lexical scope, the `sectionPass` pattern). ⭐ **Verified BYTE-IDENTICAL on BOTH the authored state and bare defaults** — harness `scratch/a03-curb-identity.mjs --against baseline`, re-run independently at merge. ⭐ **The reframe that made it buildable: the goal is CHAIN-FREEDOM, not prebake-location** — prebake is authoring-blind, so freezing `iA` there would have frozen bare defaults (Layer 0 q3); see `POLYGON-FIRST §3`.
  - ⚠️ **What is NOT done, stated honestly: 42 of 101 tiles still route through the legacy carve** (`tile.ring − aFill`, and `aFill` is a chain-derived asphalt stroke) — **30 divided median · 3 loop-body median · 9 small · 0 degenerate**. **Check C is green for the offset path only.** ⭐ Zero degenerate fallbacks: every tile that *qualifies* for the chain-free path succeeds on it, so the 42 are structural, not failures. ⛔ The chain was deliberately **not** passed into `buildCurbRings` to cover them — that would have looked like 100% and quietly re-opened Check C. **The gap is bounded and named rather than hidden**, which is the whole difference from the six prior attempts.
  - **→ the remainder is `A06`.** `PIPELINE.md` §5 (the Wall) · `POLYGON-FIRST §3` (as-built) · `HANDOFF-freeze-the-curb-in-the-first-bake.md`.
- **⛔⛔ A18 · A GATED EXPERIMENT REACHED THE SHIPPED MAP THROUGH A SHARED HELPER — CLOSED 2026-09-06 (`cd873545`), and the LESSON is the ticket.**
  `0e879687` ("identity rides the boolean") changed **`booleanLabelled`** so the protopolygon could
  carry identity through the union — a real requirement (`RIBBONS §1`: identity must be carried
  THROUGH the boolean, never recovered from ring geometry afterward). ⛔ But that helper has **SIX
  callers and only ONE is the proto path**; the other five build the shipped map. So a capability
  only the experiment uses silently changed what the operator sees, for a day.
  - **MEASURED, bisected to the function — `node scratch/claims-band-reaches-lu.mjs` on LS:**
    ledger unconditional **34 tiles / 3669.6 m²** of band falling into land use · ledger opt-in
    **28 tiles / 0.2 m²**, which is production's number exactly. Splicing `origin/main`'s
    `booleanLabelled` into HEAD and changing nothing else reproduced the 0.2. ⭐ The probe names the
    symptom in its own verdict: *"This is the sidewalk the operator sees stop."*
  - **The mechanism:** the removed line was the shipped CONTRACT —
    `if (v < 0) return { rings, labels: null, refused: 'all-vertices-minted' }`. A wholly-minted ring
    has no owner the function can honestly name, so it refused, and `tileGround.js:4178` branches on
    exactly that. The new path GUESSES an owner from an edge ledger. Those labels are `iaEdge`,
    `iaEdge` is the ring partition, and the partition tells the FILL which arc belongs to which run.
  - **Fix: `carryEdges` is opt-in** — default false (shipped behaviour, byte-for-byte), the proto
    caller passes true. ⭐ Not reverted: the capability is kept, the blast radius is not.
  - ### ⭐⭐⭐ THE LESSON, AND IT GENERALISES PAST THIS FUNCTION
    **A FLAG ON A BLOCK IS NOT A WALL IF THE BLOCK EDITS SOMETHING THE REST OF THE MAP CALLS.**
    `grout === 'proto'` gates the protopolygon block correctly; this edit was **outside** it, in a
    shared helper. ⛔ Anything an experiment needs from shared code is **opt-in at the call site**,
    or it is not gated at all. ⚠️ Nothing in the suite caught it — `a03-curb-identity` compares the
    CURB, which never moved; the defect was in the FILL. **A gate that watches the wrong artifact is
    a gate that is not watching.**

- **⛔⛔ A19 · A NAMED FOOTPATH BECOMES A STREET AND BOUNDS CITY BLOCKS — the canon says it is pavement *inside* one.** *(NEW 2026-09-06, surfaced from the ① injection test; Jacob on the instance: "I have no idea what that is.")*
  **THE CANON IS NOT AMBIGUOUS.** `SKELETON §2` (`paths[]`): *"Non-vehicular **unnamed** ways
  (footway/cycleway/steps/service) — Render pavement-only, **no measure authoring**."* `§3 step 7`:
  *"Unnamed vehicular → streets with synthetic names. **Everything else → `paths[]`**."* And a path is
  stroked by `buildPathRibbons` **clipped to the parcel interior** (`ARCHITECTURE:142`, `BAKE §3`) —
  block − curb − treelawn − sidewalk. A footway is ink **inside** a block. It is not a block boundary
  and it takes no authoring.
  - **THE MECHANISM — the rule is keyed on the word "UNNAMED", and nothing downstream re-checks the
    CLASS.** `skeleton.js §3` step 1 buckets by **name**; every named group goes through `makeStreet`
    (step 6) whatever its `highway` tag says. Only the *unnamed* bucket ever reaches `paths[]`. ⇒ a
    **named** footway becomes a street chain, bounds blocks, and is **also** stroked as a path inside
    them. ⭐ The double carry is the tell, and it is measured, not inferred.
  - ▶ **`node checks/claims-named-way-becomes-street.mjs`** — reads the classes off the artifacts and
    derives the double carry by joining `streets[]` to `paths[]`. ⛔ Re-run it; do not quote it here.
    Today it reports **LS 0 · HPDM 51 · altadena 135**, every one of them NAMED and ~all double-carried.
  - ### ⭐⭐ THIS IS LAYER 0'S SIGNATURE SHAPE, ARRIVING UNPROMPTED — **LS IS STRUCTURALLY BLIND TO IT.**
    LS has **hundreds of footways and not one named**, so **zero** reach `streets[]`: the class cannot
    appear on the town the whole project is built against. It is not small elsewhere — HPDM's are Forest
    Park's trail network (`Heels Path` 2475 pts, `Wheels Path`, `Centennial Greenway`, `St. Louis Walk of
    Fame`) cutting the park into "blocks"; **altadena's are mountain hiking trails** (`Altadena Crest
    Trail`, `Lower Sam Merrill Trail`), and there they are **~15% of the whole chain set**. ⛔ So a
    green LS is not evidence, and never could have been.
  - **WHICH OF THE THREE CAUSES: ⭐⭐ ASPIRATION** (`CLAUDE.md`, the smell-detector) — **not ROT, not
    REGRESSION.** The doc describes `paths[]` correctly and nothing drifted; the intent *"non-vehicular
    ways are pavement-only with no measure authoring"* was **built for the unnamed case and never
    qualified by class for the named one.** An unbuilt decision filed as done. ⛔ Do not evict the doc
    and do not "correct" it — the doc is right and the code is short.
  - ### ✅ RULED 2026-09-20 (Jacob) — **CLASS DECIDES, NOT NAME.** *"Class (another thing where logic would have solved the issue)."*
    A way's **highway class** decides whether it is a street. A name does not make a service drive or a
    footway into a block-bounding street, and the ABSENCE of one does not stop a residential road being
    one — so the same rule closes both halves: the named footways here AND the unnamed public roads
    (huron: 25 `residential` · 5 `primary` · 2 `tertiary` · 1 `unclassified`) dropped for want of a name.
    ⭐⭐ **READ THE PARENTHESIS — IT LANDS ON THIS DOC.** This line used to read *"the fix is not mine to
    pick… it wants Jacob's ruling before a line is written."* **It was never owed.** Layer 0 answers it
    outright — it is a kit, handle the class — and a name gate is the instance-shaped answer by
    construction. ⛔ **A doc that files a DERIVABLE question as an open ruling manufactures a blocker**,
    and this one cost two agents a round trip on 2026-09-20 before anyone re-read it. The sentence is
    excised rather than bannered; the cost is recorded here so the shape is recognisable next time.
  - ⛔ **THE RULING IS NOT A LICENCE TO LAND IT ON A GREEN CHECK.** It moves 51 chains out of HPDM's
    `streets[]` and 135 out of altadena's — **it re-partitions the block grid of both safeguarded
    towns**, so acceptance is **Jacob's eye on LS and HPDM**, and ⭐ *the eye verdict must record the
    scene it was taken on* (`A0`). ⚠️ Size the population from **today's run** of
    `node checks/claims-intake-is-consumed.mjs` — ⛔ never from the two retired highway figures, which
    were taken on different predicates and must not be differenced.
    ⚠️ Still unruled, and narrower: a way that genuinely is both (a pedestrian **mall** that really does
    bound blocks), and whether the double carry is double *paint* on the slab today.
  - **SIDE-TRACKS SPOTTED, NEITHER CHASED:** HPDM carries 2 chains tagged `highway=proposed` — roads that
    do not exist yet, stroked as ink. · `SKELETON §2` documents `paths[]` as `{id, highway, tags, coords,
    osmId}`; the clean artifact carries `{name, kind, points, pavedWidth}`. Different shape, unverified.
  - **Blocks nothing currently in flight** — ① and the stencil are upstream-clean of it. **M** ·
    `SKELETON §2/§3`, `INTAKE`, `skeleton.js` step 1/6/7.

- **A06 · 🟡 CHECK C's REMAINDER — the last 42 tiles · ✅ UNBLOCKED 2026-08-08 — THE STAMP IS IN THE ARTIFACT.** A07 **closed 2026-08-04** and is this ticket's TEST INSTRUMENT — its DoD *"these tiles now take the offset path"* is a per-tile producer statement, unmeasurable without the stamp. ⭐ **The stamp has since been baked in, and every tile carries it** — ▶ `node -e "const t=require('./public/baked/lafayette-square/shape.json').tiles; const c={}; for(const x of t) c[x.producer??'none']=(c[x.producer??'none']||0)+1; console.log(t.length,'tiles',c)"` — which prints the total **and the carve/offset split**, so neither has to be written down here. *(⛔ **This line has now gone stale TWICE in the same way** — it read "101 tiles, 0 producer-stamped ⇒ blocked on the re-bake decision" until 2026-08-08, then "101/101 — carve 42 · offset 59" until 2026-09-13, by which point LS had been re-poured and the denominator was no longer 101. **The number is deleted on purpose; the command is the claim.**)* ⭐ **Its other stated blocker also dissolved the same day** — the *"don't re-bake LS"* fear was **A14**, now **withdrawn** (those were the operator's layer toggles; the poured overlays are disposable until the real republish). ⇒ **A06 is ready to scope; it is waiting on nothing.** **✅ Scope re-checked against the corrected prebake fact and unchanged — see A07.** *(NEW 2026-07-31, split out of A03; **scope CORRECTED the same evening after a trace — read the scope note before estimating**)* — 30 divided-median + 3 loop-body-median + 9 small tiles still build their curb as `tile.ring − aFill`. Closing this is what turns **Check C** green outright.
  - ### ⛔⛔ SCOPE IS UNVERIFIED AS OF 2026-09-13 — **RE-DERIVE BEFORE ESTIMATING.** The verify command above now prints **`147 tiles { proto: 147 }`**. There is no `carve`/`offset` split in the artifact any more; every tile reports a single producer, `proto`. ⇒ **The "42 tiles" scope in the line above was measured on the 101-tile, carve/offset-era bake and cannot be assumed to survive the re-pour.** ⛔ **Cause not established** — whether ① becoming the producer for Survey *and* Section (`A20`/`A21`) subsumed the legacy carve, renamed it, or merely hid it behind one label is **not measured**, and the three have very different consequences for this ticket. ▶ First move is to re-derive the remainder against the current artifact, not to scope the old 42.
  - ⛔ **PARK PLACE RATIONALE EXCISED 2026-08-13 (Wren, `52b5df18`) — the producer does NOT decide the partition; tile 99 is stamped and resolves 30 spans.** *"The carve path emits no labels"* was TRUE at `7299bbeb` (16:49) and FALSE at `4b573fb0` (20:54) — the same day. ▶ `node checks/claims-producer-does-not-decide-partition.mjs --all --tiles`. ⭐ A06 is Check-C hygiene again — the "NOTHING IS BROKEN FOR LS" line below is no longer contradicted. ⚠️ Jacob's Park Place eye report (2026-08-11) **STANDS**; cause not established, and it is **not A06's**.
  - ### ⛔⛔ RETRACTED 2026-09-06 — **"A06 IS CHECK-C HYGIENE, NOT A MAP DEFECT" IS FALSE, AND JACOB'S EYE IS WHAT FALSIFIED IT.**
    *(The struck line read: "NOTHING IS BROKEN FOR LS BY THIS… the 42 tiles are correct today and byte-identical; A06 is Check-C hygiene, not a map defect. Do not let the red dot read as a broken map." It is kept here only as the claim being corrected.)*
    ⭐ **Jacob, on the render, 2026-09-06: *"this is chains."*** He was pointing at the ped ribbon
    stopping mid-block and never wrapping the corner, against bare asphalt.
    ⭐⭐ **THE MECHANISM, AND IT IS A10'S OWN SENTENCE READ THE OTHER WAY ROUND.** `RIBBONS §1`'s
    slice-2 acceptance says that on a **partitioned** tile *"a gap is not constructible"* — the arcs
    close on the same inward bisector, step-over and step-back are one cut. ⇒ **On an UNSTAMPED tile
    none of that runs.** It keeps the pre-A10 construction, which re-strokes an AREA from the run's
    polyline — chain-world — and there a gap is not merely possible, it is the expected failure.
    **That is the visible defect, and it is A06's population.** Check-C hygiene and the map defect
    are the same tiles.
    ⛔⛔ **TWO PROBES, TWO NUMBERS — DO NOT MERGE THEM. Say which you ran.**
    | probe | LS result |
    |---|---|
    | `node scratch/claims-unpainted-arcs.mjs` | **89 stamped · 12 unstamped** ("no partition to drop from") |
    | `node scratch/claims-ia-source-stamp.mjs` | **91 stamped · 10 refused** — 7 `carve:median-divided` · 2 `carve:noncontiguous-arcs` · 1 `carve:small` |
    ⛔ **Re-run both; neither number is quotable.** They disagree by two and nobody has reconciled the
    denominators — `POLYGON-FIRST §5` RULE 3 (one invariant, one defect) applies before anyone sizes
    work off either.
    ⚠️⚠️ **AND THE REFUSAL REASONS CONTRADICT THIS TICKET'S OWN 2026-08-13 CORRECTION.** The line
    above says *"the producer does NOT decide the partition; tile 99 is stamped and resolves 30
    spans"* — yet **all ten refusals name a `carve:` reason.** Both cannot be the whole story. ⛔ Do
    not resolve it by picking; that cross-doc seam is the richest one we have
    (`project_find_the_code_error_by_finding_the_thinking_error`). **Measure which tiles refuse and
    why, before scoping.**
    ⭐ **What this does NOT change:** the 42 tiles' CURB is still correct and byte-identical, and A03
    still changed only where the curb is computed. What was wrong was inferring from that that
    nothing the operator sees is affected. **The curb is fine and the RIBBON on it is not.**
  - ⛔ **"FREEZE `aFill`" IS THE WRONG TARGET — do not re-derive this dead end.** It was A06's original framing and it is wrong twice over. (1) `aFill` **moves with authoring** (the stroke differs on 8 of 101 tiles between the authored and bare-defaults state, −2114 m² net, up to −769 m² on one tile), so freezing the polygon would bake the operator's widths into geometry and a width drag could no longer move the curb — `CLAUDE.md` Layer 0 q3, in artifact form. (2) It is also unnecessary: see the polyline row below.
  - ⭐⭐ **THE STROKE'S POLYLINE IS ALREADY CHAIN-FREE (Jacob, verified in code).** `groupRuns(tile)` takes only `{ring, edges}` and builds `run.poly` **purely from ring vertices** (`tileGround.js:1074-1076`) — and the ring is already frozen. ⛔ An earlier objection that "freezing the run polylines = freezing the chain" was **provenance-by-name and is retracted**; the test is **lexical scope**, which is the standard `buildCurbRings` was held to.
  - **What `aFill` still needs, per term — the trace that defines the work:**

    | term | verdict | |
    |---|---|---|
    | `run.poly` | ✅ **already chain-free** | built from the frozen ring |
    | widths (`runMeasure`) | ✅ **reduces** | A03's proven pattern: frozen `baseHW` + authored scalar applied in the builder |
    | `jTrimmed(run)` | ✅ **reduces** | touches `streetsOrig` only for **skelId — a NAME**, already frozen in `edges[]`; the trims are scalars keyed `skelId\|side\|coord` |
    | `roundTips` | ✅ **already frozen** | carried in `shapeTiles` |
    | `splitRunAtStations` | ⚠️ **needs ONE new frozen fact** | reads `streetsOrig[].points` to locate run ends in **chain-index space** and decide direction (`fwd`) — an orientation flag, mintable at freeze time |
    | the clip arrays | 🔴 **the real work** | see below |

  - 🔴 **The clip arrays are the weight.** The `*ClipFor(tileRing)` accessors are *already* chain-free by signature (ring + bbox filter over precomputed polygons) — but **the polygons themselves move with authoring**, measured authored-vs-defaults: `tClip` **2541 → 2021 m² (a 20% swing), 5 tiles** · `jClip` 6497 → 6476, 6 tiles · `cCut` 11915 → 11908, 3 tiles · `mergeClip` **invariant, 0 tiles**. ⇒ They **cannot be frozen as polygons** (same Layer 0 q3 objection, now measured rather than asserted); they must be **re-derived from frozen facts + authored scalars**, which means reducing the **junction/through construction itself** (E3.2: 64 pairs / 124 windows / 34 aprons · THRU: ~90 windows).
  - ⭐ **Scoping relief:** `aFill` reaches the curb only through `legacyBlock` + the cul-de-sac keyhole, so this is needed **only for the 42** — the 59 offset tiles never touch it.
  - **∴ corrected scope:** freeze `splitRunAtStations`' orientation fact → reduce the junction/through construction to frozen facts + authored scalars. **Bigger than the A03 split, smaller than "freeze the carve," and a genuine path to Check C green rather than a dead end.** ⛔ Byte-identical gate unchanged, both states: `scratch/a03-curb-identity.mjs`. **M–L** · `POLYGON-FIRST §3` · `_handoffs/HANDOFF-A06-legacy-carve-chain-free.md` (⚠️ its §2/§3 predate this correction — the "freeze `aFill`" framing there is superseded by this entry).
- **A07 · ✅ CLOSED 2026-08-04 (`0464c136`) — THE SHAPE PRODUCER NOW DISCLOSES.** Every tile carries `producer: 'offset'|'carve'` + `producerReason` in `shape.json`; the bake prints the split per pour; the tool shows it. **No curb moved** — `a03-curb-identity.mjs --against a07base` is identical on iA/block/curb/asphalt/sidewalk/fillets in **both** the authored and bare-defaults state, and `scratch/a07-producer-disclosure.mjs` proves the only artifact delta is the two new keys. ⭐⭐ **THE FINDING — measured live on all six poured scenes:** the carved share runs **7% (altadena) → 42% (LS) → 56% (centrum) → 75% (ksi-y-m-yn)**. **The invariant's truth is a property of the town's block geometry, not of the kit**, and LS sits mid-range — so no single scene could ever have revealed it. On the first non-US pour **three quarters of the map is built by the producer the docs never mentioned.** ⭐ **Degeneracy = 0 everywhere, MEASURED for the first time** (`POLYGON-FIRST §3` asserted it without the branch ever being run) — and it is now loud the day it stops being 0. ⭐ LS's live split (30 median-divided · 10 small · 2 median-loop = **42**) reconciles all three numbers in the record: §3's 42 was right, the artifact-derived 41 was the floor, Tier 1's 30 was the area term. ⏳ **Remaining, and it wants Jacob's eye:** the SPATIAL surface — tinting carved tiles in the overlay so "which blocks" is answerable on the map, not just as a count. Data is in the artifact; small. ✅ **`shape.json` on disk is now stamped — 101/101, carve 42 · offset 59** *(the "pre-A07 until a re-bake" caveat that stood here went stale when the re-bake landed; re-derive with the command in A06, don't quote this)*.

  - ### ⭐⭐ THE RELATION RULING (2026-08-04) — **still governs A06, which is open.** Three claims were live; two survive.
    | claim | verdict |
    |---|---|
    | **A07 is a PREREQUISITE FOR A06** | ✅ **TRUE, and it is the governing one.** A06's definition of done is *"these tiles now take the offset path."* **That is a per-tile producer statement, and A07 is what makes it measurable.** Without the stamp, A06 is verified by hand, once, on LS — and cannot be re-verified anywhere else. **A07 is A06's test instrument.** |
    | **A07 is a PREREQUISITE FOR the "d" bulge diagnosis** | ✅ **TRUE**, and it is a *different* dependency on a *different* thing: `SURVEY §6` names the bulge's mechanism as the asphalt-union carve, but on a large non-median tile the offset built it. **You cannot diagnose the bulge without knowing which producer built the bulging tile.** Same instrument, second customer. |
    | ~~**A07 is a SIBLING of A06 — "do these together"**~~ | ⛔ **FALSE — DELETED.** It reads the two as equal-standing and co-scheduled, which hides that one is the other's instrument, and "together" in practice means doing A06 **without** the instrument. *(This was A07's own text until today.)* |
  - **And A07 TRAVELS; A06 does not.** A06 makes *LS's* 41 tiles offset-built. On town #2 a different set fails the same gate, with nobody who has ever looked at it — the operator reads "concentric offset," sees a plausible curb, and cannot learn otherwise. `CLAUDE.md` Layer 0: the deliverable is the check that catches the class in a town nobody has inspected. ⭐ **The stamp also OUTLIVES A06** — after A06 closes, it is the regression guard that says the tiles stayed on the offset path.
  - ✅ **A06's scope was RE-CHECKED against the corrected prebake fact and DOES NOT MOVE** (2026-08-04). A06/A03 were scoped while *"prebake is authoring-blind — zero reads"* was believed; that is false (`derive.js` reads `clean/overlay.json`, 52 authored LS streets — only `design.json`/`blockCustoms` is unread). **But every load-bearing A06 measurement is an authored-vs-bare-defaults delta on the `blockCustoms` channel** — `aFill` 8/101 tiles, `tClip` 2541→2021 m², `jClip`, `cCut` — and that channel *is* genuinely unread by prebake. **So the "freeze `aFill`" retraction stands and the corrected scope stands.** ⭐ The one thing the correction does open: a fact depending only on **chain measure** (which prebake reads) is now prebake-freezable, and `splitRunAtStations`' orientation flag is pure geometry — so that "one new frozen fact" can be minted at **prebake**, not only at the Survey-exit freeze. *(Recorded so nobody re-checks this: the answer is "no change, for a reason.")*

- **A05 · ⛔⛔ THE CURB CHECK MEASURES THE OPERATOR'S AUTHORING AND CALLS IT DAMAGE** *(NEW 2026-07-31; **A04 "collapsed curb rings" was WITHDRAWN into this item the same day — it was never a defect, see below**)* — `litmus-curb-parallel.mjs:77` runs `blockCustoms: null`, i.e. **authoring OFF**, so it compares an authored curb against un-authored widths: Mississippi is parallel **to the centimetre** at its authored 8.70 m and was reported as a **3.13 m bow** against the corridor's 11.83 m. ⭐⭐ **Layer-0 shape, not a mere bug (`CLAUDE.md` Layer 0 q3): it fails WORST on the most heavily authored town — LS, the mould — and would look CLEAN on a fresh pour.** Blind exactly where the map is most worked-on.
  - ⛔ **WITHDRAWN — "collapsed curb rings, 28 of 92 tiles."** Filed, then retracted hours later: it measured **`iA` AREA**, and `Block = iA = tile − the authored roadway` (`SURVEY §3` step 5), while the asphalt-edge handle *"strokes the pavement half-width outward; **the block follows**"* (`SURVEY §4`). **So it was measuring the width edits themselves.** Area cannot separate (a) an authored wide street, (b) a genuinely narrow historical block — the reason the tools exist — or (c) a graph face that is legitimately all roadway (`SURVEY §3` step 4 documents a capacity guard for exactly that). Not with a better threshold: the median ring-share climbs 1% → 34% → 45% → 86% purely with block size, so the metric mostly encodes size. **No evidence of a real defect survives it.**
  - **The one honest test, and the whole of this ticket: DISTANCE, not area — is the curb at the *authored* half-width from its centerline — run WITH the scene's `blockCustoms` loaded.** By construction it cannot fire on authoring. Also fix `:86`, which skips tiles it cannot measure (a total failure printed as a modest bow): emit **`UNMEASURABLE`** as its own failing class, never folded into a magnitude.
  - **Then, and only then, re-ask whether anything is actually wrong.** (`tile 37` was the one case that still looked odd; treat it as a lead, not a finding.)
  - ⭐ **Carry the DUAL-STATE GATE pattern from A03's harness** (`scratch/a03-curb-identity.mjs`): run **both** the authored state and bare defaults, and **record which state produced each result.** "`UNMEASURABLE` as its own class" only means something if the check knows which state it ran in — otherwise it repeats the original fault in a new place.
  - ⛔ **Retracted before it could enter this ticket:** an A03-era report of *"76 of 78 Check-A violations on `side=left`"* is **an artifact of the instrument, not an asymmetry in the map** — the raycast only ever hits from the tile's interior side, and the face walk's orientation makes that "left" almost everywhere. Do not treat side-skew as a finding. **S** · rides A03's landing · `POLYGON-FIRST §2`/`§5` Rules 1–3.
- **A01 · ⛔⛔ RE-TITLED 2026-08-06 — THE PIPELINE *IS* REPRODUCIBLE. THE COMMITTED ARTIFACT IS STALE.** *(Filed 2026-07-31 as "the pipeline does not reproduce its own committed output"; that premise is **FALSE** and was never tested — nobody ran it twice.)*
  - ⭐⭐ **MEASURED 2026-08-06, in the isolated `pipeline-repro` worktree:**
    **(1)** `pipeline.js --scene=lafayette-square` run **twice** on unchanged inputs → **byte-identical** (it even reports `[unchanged]` via `writeIfChanged`).
    **(2)** Today's run vs the **2026-07-31** fresh derivation → **byte-identical across 6 days.**
    **(3)** `--skip-elevation` is not a confound — it sets `b.elev` on buildings only; streets are identical either way.
    ⇒ **The pipeline is deterministic and stable. It is not the defect.**
  - ⭐⭐ **THE REAL DEFECT, AND IT IS WORSE THAN NONDETERMINISM: `cartograph/data/lafayette-square/clean/map.json` DOES NOT CORRESPOND TO ANY RUN OF THE CURRENT CODE — and nothing anywhere says so.** The committed artifact was produced by *different code* (or hand-touched), and drifted silently. ⛔ **The operator cannot tell whether the map they are looking at is what the kit would build today.** That is `CLAUDE.md` Layer 0 exactly: on town #2 there is no known-good file to notice it against, and the artifact renders perfectly either way.
  - ⚠️ **AND IT INVERTS THE 7/31 STORY.** The conclusion drawn that day was *"re-deriving produced a worse map, so the pipeline is untrustworthy."* Since the pipeline is deterministic, the fresh derivation **is** what today's code builds — so the committed artifact is a map **the current code can no longer produce.** Either the code regressed after it was made, or it carries work no code path reproduces. ⛔ **Nobody has looked, and the "worse" verdict is the compromised one (wrong scene / flag-off bake / stale cache).** That question — *what changed between that artifact and this code* — is now the ticket.
  - ✅ **NOT the wrong-scene artifact** (Jacob asked; checked). Both artifacts are unambiguously `lafayette-square` and share a fingerprint — **209 streets · 101 tiles · 173 faces · 52 medians · 50 caps**; staging is 116 tiles off a 304-street/507-junction skeleton, nothing like it.
  - ⭐⭐ **WHAT ACTUALLY DIVERGES — re-run it, don't quote me:**
    `node scratch/repro-a01-artifact-diff.mjs <committed map.json> <fresh map.json>`
    Of the guard's 7 count fields **only ONE moves** (junction nodes) — while **57 of 209 streets come back with different points.** ⛔ **That is the ticket: `promote-ribbons.js`'s clobber guard compares COUNTS, and a same-count different-geometry run passes it silently.**
  - ### ⭐⭐ RESIZED 2026-08-11 (agent Quire) — **"57 STREETS MOVED" WAS A MISREAD OF THE AGGREGATE. ALMOST ALL OF IT IS DENSIFICATION, AND THE REAL LIST IS THREE STREETS.**
    ⛔ **The probe's own comment warns that a densified street cannot be compared index-to-index; three messages were built on top of the number before anyone classified it.** Classified: **1 is pure resampling · 56 "moved" — but `motorway-4` goes 19→222 points, `motorway-link-15` 10→121, `south-18th-street-4` 5→63, with ENDPOINT SHIFT 0.00 m.** That is a coarse polyline against a finely-sampled curve: **`CURVE_FIT` landed *after* the shipped map was poured.** ⇒ a re-pour gives the same neighbourhood with better curves. **Only THREE genuinely shift: `waverly-place-2` (6.50 m) · `motorway-link-33` (6.68 m) · `motorway-link-39` (6.30 m)** — two are freeway ramps, and `waverly-place` is already a canon-flagged awkward case.
    - ⭐ **Ruled out, each by its own run:** nondeterminism (twice → byte-identical) · the 2026-08-06 overlay reset (re-run against `overlay.json.pre-reset` — the measure divergence collapses 23→2, so **21 of 23 were the reset, correctly**; ⛔ reporting those would have been Layer 0 q3) · code drift (same divergence inside the July-31 `pipeline-repro` worktree) · `CURVE_FIT` (tested both ways; no effect on `map.json`).
    - ⭐⭐ **AND `886cdb46` (08-02) ALREADY CLOSED THIS ONCE — *"the pipeline now reproduces its own committed frame"* — BUT IT VERIFIED THE SKELETON (217 streets), NOT `map.json` (209).** The stage was declared fixed while **the stage after it stayed broken.** *(`feedback_it_already_exists_find_what_broke_it`: the fix exists; find what it did not cover.)*
    - ### ✅✅ THE THREE ARE MEASURED, AND THEY INVERT THE TICKET — **THE SHIPPED MAP IS THE WRONG ONE. THIS BOARD'S 2026-08-06 RE-TITLE WAS RIGHT AND NOW HAS ITS MECHANISM.**
      **The skeleton is byte-identical (`[unchanged]`), so it is the arbiter. Distance from it:** `waverly-place-2` **shipped 6.50 m → fresh 0.05 m** · `motorway-link-33` **8.96 → 0.04** · `motorway-link-39` **6.35 → 0.06**. ⭐ **The fresh build agrees with the skeleton to within 5 cm; the shipped map is 6–9 m off it.** All three are **end-of-street defects at junctions/caps** — `motorway-link-33` carries three vertices the skeleton does not contain and loses its final point; `-39` drops a vertex and ends 6 m adrift; `waverly-place-2`'s loop has its last point pulled 6.5 m sideways. **That is the class that has been worked on since.**
      ⇒ **We are not shipping a good map we cannot recreate. We are shipping a STALE map carrying three defects the current code no longer produces**, plus curves drawn with fewer points. **A re-pour is an improvement, and it is judgeable.**
      - ✅ **THIS UNBLOCKS `A0`** — the claim that a re-pour moves 57 streets under any A0 verdict is **retired**: the delta is one benign class (densification, endpoint shift 0.00 m) plus three streets that get **less** wrong.
      - ### ✅ MEASURED 2026-09-06 — **THE RE-POUR MOVES NOTHING. STOP TREATING THE PROMOTE AS A BLOCKER.**
        ⛔ This bullet read *"the promote is STILL THE DANGEROUS STEP"* and it is **evicted as ROT** — the retraction was already two paragraphs above it, and the alarm outlived it and cost a session: it was raised as a blocker against a re-pour Jacob had already settled ("we've been over the false alarm of the promote repeatedly").
        **Run on LS:** `skeleton.js` → `[unchanged]`; `pipeline.js` → **every layer the map is made of byte-identical** to the committed artifact (streets · tiles · faces · junctionMap · medians · corridors · paths · alleys · intersections · junctions · nameTransitions). The only delta is one **ADDED** key, `protopolygon`, which is additive and cannot move the map — ⭐ which also settles, by measurement, that ①'s ungated prebake mint (`derive.js:4866`) does not touch the blocks.
        ▶ **`node checks/claims-repour-changes-nothing.mjs <scene>`** — ⛔ re-run it; the digits are not repeated here on purpose.
      - ⚠️ **WHAT SURVIVES, and it is the only thing to carry forward:** `promote-ribbons.js` writes **`src/data/ribbons.json`** — LS's *shared default* path, the palimpsest root (`EXTENT-DESIGN §2.1`) — and **its guard compares COUNTS**, so a same-count different-GEOMETRY pour would pass silently. That is a real hole in the *guard*; it is **not** evidence that a pour changes anything. ⇒ diff the layers, never the counts — which is what the check above does.
  - ⛔⛔ **DO NOT QUOTE THE OLD NUMBERS — THEY DO NOT REPRODUCE.** The record said *"233 vs 228 nodes; 75 vs 71 asphalt rings"* and *"23 of 209 streets came back with operator-measured widths replaced by skeleton seeds (mississippi-avenue right 11.83 → 4.74)"*. Against the surviving artifacts today the run gives **233 → 231** and **2** streets with a changed `measure`, neither of them Mississippi, and **not** in the "authoring replaced by seed" direction. ⚠️ **This arc has now over-swung four times; treat the old figures as unreproducible rather than as false** — nobody can say which run they came from. **The reproducible, kit-general statement is the geometry divergence + the blind guard above.**
  - ⛔ **"The fresh derivation was worse on the eye" does not stand as evidence** — same compromised session as A0 (`7b5b87a3`'s body: *"I verified the artifact I produced instead of the artifact the operator was looking at — wrong scene… then a flag-off bake, then a stale browser cache"*). **Nobody has yet seen the fresh derivation correctly scened.** Do not cite "worse" as a reason not to re-derive; equally do not treat re-deriving as safe.
  - ⚠️ **A06 waits on this** — its instrument needs a re-bake, and this is the ticket that says re-baking is not yet trustworthy. **S/M.**
- **A10 · ✅ CURED 2026-08-11 (`07e65753`, agent Quire) — THE PED BAND IS PAINTED FROM THE PARTITION. ⏳ EYE GIVEN AND POSITIVE; FULL DoD NOT YET MET.** *(The full 2026-08-08→11 working-out — the blocked brief, the mint sizing, the dead miter-clamp routing, the width-seam debate, the keyhole hypothesis and its measurement — is retired to **`cartograph/_archive/A10-cure-journey-2026-08-11.md`**. Read it before re-opening anything here.)*
  - **The ruling it was built to** *(Jacob, ratified 2026-08-08 — settled, do not re-derive)*: **the ring is ALREADY partitioned, one owner per edge. PAINT FROM THE PARTITION — never re-stroke a run's polyline into an area. "Ends exist only because we make areas out of lines that have ends."**
  - **The construction, in one sentence:** on a stamped tile, cut `iA` at every fillet's two tangent points and at every ownership change the `iaEdge` stamp reports outside a fillet; each owner sweeps its own arc inward; adjacent arcs close on the **same inward bisector**, so step-over and step-back are one cut and ⭐ **a gap is not constructible.** No trim, no polyline stroke, no new key, and none of the closed verb set (**clamp · wrap · re-key · snap**).
  - ▶ **Numbers: `node scratch/claims-band-reaches-lu.mjs`. ⛔ Re-derive, never quote** — this figure moved twice in two days (a re-freeze, then the cure). Gates: `--inert` · `claims-ia-source-stamp` · `claims-ring-partition` (only *"0 double-covered"* is live) · `a03-curb-identity --against`.
  - ⏳ **WHAT REMAINS, and it is a short named list:**
    - **The 2 `keyhole-splice` tiles — Park Place + St. Vincent Court**, ~95% of the residual defect. ✅ **MEASURED RECOVERABLE** (`629bebf0`, `2157788c`): the splice replaces geometry only *inside* the bulb discs, **100% of their defect area lies outside**, zero labelled vertices outside a disc are destroyed, and their carry to `iA` is **no weaker than an ordinary stamped tile's** (edge carry 97.8% vs 95.8%). ### ✅✅ LANDED + EYE-CONFIRMED 2026-08-11 (`b72215aa`) — **THE KEYHOLE SCOPE IS CLOSED.**
      The stamp now carries through the splice **per vertex** (exact coordinate match, no tolerance); `bandSpans` drops a span with no owned vertex, so an unowned arc is never painted and never inherits a neighbour's identity by majority. **LS 57 → 59 stamped · 156 unowned labels · staging 68→69 · centrum 250→251.** Gates: `claims-ia-source-stamp` green 8/8, mutation-verified 6/6 · `a03-curb-identity` run **with and without the change on the same tree** — block/curb/asphalt/fillets/`iA` **identical**, only `sidewalk` moves ⇒ **no curb moved, no SHAPE eye-gate owed.**
      - ⭐⭐ **THE EYE, on Jacob's own render after his re-freeze: *"the leg is better!"*** Coordinate-keyed, so it survives a tile renumber: the **Carroll frontage end `(649.22, 147.07)` = 332.1 m² is GONE**, and the **bulb end `(803.19, 182.59)` = 40.9 m²** with it. That tile went **374.5 → 101.6 m².** ⛔ **Do NOT credit the cure with the whole-map 435.8 → 317.4** — the operator authored 46 tiles' geometry in between, so the two runs are not the same map.
      - ⛔ **NO NEW ARTIFACT KEY.** An `iaEdgeUnowned` count was written and then removed: it is derivable by counting `null`s in `iaEdge`, a stored restatement of its own source goes stale, and it turned the artifact-identity gate red — keeping the frozen shape at exactly two stamp keys is what lets that gate stay strict.
      - ⚠️ **The residue MOVED rather than vanished, and that is the trade the ruling made:** what is left on both circle tiles sits **inside the discs** — the unowned arcs, never painted. **That ground is `A06`'s, not this ticket's.**
      ### ✅✅ RULED 2026-08-11 (Jacob) — **THE CURE CONSUMES ARCS, SO THERE IS NO TOKEN TO SPELL.**
      Walk the owned arcs and paint each; an arc with no owner is simply never walked. ⛔ No sentinel, no new key, no fourth instance of `project_a_sentinel_is_not_a_value`. ⭐ It costs nothing to honour: `bandSpans`/`ringRunOwners` (`tileGround.js:1639`) **already consume the stamp as arcs.**
      - ⭐⭐ **EYE-CONFIRMED, PREDICTION-FIRST — the first time this arc an instrument predicted the eye instead of the eye correcting the instrument.** Scene: `lafayette-square`, Jacob's own render. The probe said the **Carroll × Dillon corner sits on the shared edge of tile 34 (stamped) and tile 35 (unstamped), both at 0 m, so it must be broken on ONE SIDE ONLY.** Jacob, unprompted: *"Carroll x Dillon are broken on one side only."* **A corner is a slice on EACH tile's own ring — that is why the asymmetry exists.**
      - ⛔ **THE PAIRING ON THIS LINE WAS WRONG; MEASURED, the two discs are ringed at a uniform ~8 m by `park-place-0/1/2` (disc `772.3, 97.4` R 17.17) and `saint-vincent-avenue-0/2` (disc `-409.2, -160.1` R 17.29).** NOT **St. Vincent Court** — that chain is 228 m away and is not involved. 59 offset tiles · 57 stamped · these 2 refused, map-wide.
      - ⭐⭐ **THE HARM IS WHOLE-PERIMETER, WHICH IS WHY IT PRESENTS AS SEVERAL UNRELATED BUGS.** `:3918` nulls **all 44 arcs** because the splice mints vertices *inside the disc*. Tile 35 carries **10 runs**; Carroll's frontage is **130 m from the circle** and loses its owner anyway. Jacob's three reports — a missing sidewalk stretch, a cap not matching its gap, a half-broken corner — are **one tile losing its partition.** ⭐ Only ~4 of the 10 runs show a defect end, because the fallback still paints a plain straight run: **it drops at the corner TAKEOVER and at the ends** — the "corners and legs" signature.
      - ▶ **CONSTRUCTION, fully determined:** carry the labels through the splice by **exact coordinate match** (⛔ no tolerance; the probe proves it — 0 pre-splice vertices outside the discs destroyed, and the 8 unmatched-and-outside sit **0.04–0.61 mm** from a disc boundary = genuine minted welds that MUST refuse). ⚠️ **The shape validator (`:4201`) must move with it** — it asserts every label is `Number.isInteger(v) && v >= 0` and today discards the whole tile if one is not.
      - ⛔⛔ **NOT LANDED — IT NEEDS A RE-FREEZE, WHICH IS A STANDUP.** `iaEdge` comes from the SHAPE pass and the FILL reads the frozen artifact, so the cure cannot execute on tiles 26/35 until `saveShapeFreeze` re-writes them. **Gates: `a03-curb-identity --against` byte-identical · `claims-ia-source-stamp` mutation-verified · the A10 figure must FALL · eye on this frame, scene recorded.**
    - **~25 m² of genuine residue on cured tiles**, nearly all one region on the Lafayette / Mississippi / South 18th face.
    - **`partitionDump`** — the named-refusal channel for a tile whose partition can't be established — **fires 0 times on all 8 states. Unproven, not passed:** no check points at it.
  - ⛔⛔ **DO NOT EYE-GATE THIS ON THE KENNETT / SOUTH 18th / DOLMAN / CARROLL / TRUMAN FRAME.** Two failures share it: the band capping while the road continues is A10's; **the ROAD ITSELF bulbing into a cul-de-sac is `A0`, upstream — and the band wrapping a bulb there is INNOCENT.** A verdict taken on that frame reads as a failure of this work and restarts the symptom argument that cost 2026-08-08.
  - ⚠️ **THE OTHER SIX SCENES ARE UNMOVED, AND THAT IS NOT EVIDENCE THE CURE TRAVELS.** Their committed `shape.json` predates the stamp (0 stamped tiles), so **the cure never executes there.** Only LS was re-frozen. ⭐ **The re-freeze is the operator's own gesture — open Survey and leave it** (`saveShapeFreeze`, autosaved on Survey-exit); verified 2026-08-10 to write **the same geometry plus labels** (0 tiles' `ring` moved; the only `iA` moves were the authored width drag). ⛔ Do not re-pour a non-LS scene to "see if it travels" — Jacob's scope ruling.
  - **Cost, named not hidden:** whole-map FILL pass **+26%** on LS. The whole-map rebuild is already the `D6d` item.
  - ⏳ **Canon debt, owed and unpaid:** `SECTION §7`'s *"deviation / the cure"* block now describes **built** code, and its four decline modes no longer gate the takeover on a partitioned tile.
- **A15 · ⭕⭐⭐⭐ HYPOTHESIS (Jacob, 2026-08-08) — "WE HAVE BEEN CHASING CHAIN-BASED PROBLEMS THIS WHOLE TIME." ⛔ THIS IS A HYPOTHESIS WITH A TEST ATTACHED, NOT A FINDING. Do not cite it as fact; run the census.**
  - **The claim, in Jacob's words:** *"If this turns out to be the issue we will have been chasing **chain based** problems this whole time, in spite of my best efforts to steer it otherwise. There must be tons of vestigial wiring in there… I think there is a real chance this fixes it and drags a substantial chunk of defunct code with it. So much of the journey with this app has been about perf; there's no way this hasn't been palimpsesting in."*
  - ### ⭐⭐⭐ THE CLAIM IS ABOUT A **DICHOTOMY OF CONCEPTS**, NOT STRAY READS. *(Jacob's clarification, 2026-08-08 — read this before the lexical material below, which is the smaller half.)*
    > *"I was talking about the several long passes we have taken such as **corner couplers**; **there are no 'couplers' in a curb-centric tile arrangement** — but we spent many 10s of hours writing around that dichotomy."*

    ⭐⭐ **A COUPLER IS A CHAIN-WORLD NOUN. It presumes two ends that meet and must be JOINED.** A tile has a **closed ring**; a corner is a **SLICE of a continuous band**, not a join between two things. **So "coupler" has no referent in the model we actually run** — and tens of hours went into building the join anyway, at a seam the geometry does not have.
    - ⭐⭐ **This is the same insight as the ratified sentence, one level up:** *"an inset of a continuous polygon cannot have an end"* and *"ends exist only because we make areas out of lines that have ends."* **Chain-world thinks in JOINS; tile-world thinks in SLICES of one continuous thing.** Every coupler, wrap, snap and stitch is a join written where there is no seam.
    - **The tell is in the names of the reverted work itself** — the walk-ordinal **coupler** · the mouth **wrap** · the band-neck **clamp** · `SPUR_OUTLINE` · and in memory, `project_dead_end_cap_is_an_end_coupler`. ⛔ **A10's own entry already says the FILL-patch class is closed *"do not clamp, wrap, re-key or snap"* — that list is exactly the chain-world verb set.** The rule was written down without the reason being named; **this is the reason.**
    - ▶ **∴ CENSUS ⓪, and it is the cheapest and probably the highest-yield: A VOCABULARY AUDIT.** Grep the FILL/Section surface for chain-world nouns and verbs — **coupler · join · seam · stitch · wrap · snap · weld · bridge · connect · pair** — and for each, ask the one question: **does its referent exist in a closed-ring tile model?** Where the answer is no, the construction is not a bug to fix but a **concept with nothing under it.** ⭐ Unlike ① and ②, this needs no instrument and can be run in an hour.
      ▶ **First probe, `src/lib/tileGround.js` alone** *(reproduce, don't quote: `grep -ioc '<word>' src/lib/tileGround.js`)* — **`seam` ×42 · `wrap` ×39 · `snap` ×23 · `coupler` ×12 · `weld` ×6 · `bridge` ×2**, and a **live named construction, `capCouplers`**. ⛔ **These counts are the SURFACE TO AUDIT, not a defect count** — some uses are legitimate (a `wrap` in the array sense, a `snap` to a grid). **The audit is per-site and asks only: does this noun's referent exist in a closed-ring tile model?**
  - ⭐⭐ **THE SMALLER, LEXICAL HALF — the wall was enforced on the OBVIOUS form of a chain and is blind to the DISGUISED one.** Nobody reaches back for chain *vertices*; that half held. What survived is **chains as DECISIONS** — *"past the Wall the chain survives not as geometry but as a **cutting instruction** (`skelId`/`side`/`segOrd`, `cornerAt = skelId a !== b`, band ends = each run's ends) — worse, because it's invisible"* (agent Sill). A `skelId` string does not look like a centerline, so it passes every *"is this chains again?"* reading. Same fault, several addresses: A10's five cut predicates (**none reads the curb**) · A03's 42 tiles still routing through a chain-derived `aFill` · A06's real work being *"reduce the junction construction to frozen facts"* · `A0`'s face walk dropping the stamp · `project_polygon_must_ask_the_stamp`. ⭐ And the four reverted attempts — band-neck clamp · walk-ordinal coupler · mouth wrap · `SPUR_OUTLINE` — were **each a patch on the output of a chain decision**, which is the *"in spite of my best efforts"* part, legible in the record rather than only in recollection.
  - ### ⭐⭐⭐ CONFIRMED AND SHARPENED 2026-08-09 — "NODES = CHAINS. THIS IS A STINK." (Jacob: *"YES INDEED this is the exact vestigial wiring to which I was referring."*)
    **THE ENTIRE POST-WALL IDENTITY SYSTEM IS CHAIN-KEYED — including the partition the agreed sentence calls already-correct.** Traced in source 2026-08-09:

    | post-Wall identity | keyed on | verify |
    |---|---|---|
    | `st.nodeKinds` (the "stamp") | **chain endpoint coincidence + chain TANGENTS + chain POINT-ORDER** | `derive.js:3902` — `junctionMap` is built from `ribbonStreets`, `ptOf(s,'start'\|'end')`, `outward()`, `rightDir()` |
    | `groupRuns`'s ring partition | `edges[i].streetIdx` + `.side` | `tileGround.js:1060` |
    | `cornerAt` | ⛔ **not `skelId`** — `streetKey = throughId \|\| roadId \|\| skelId \|\| name`, and the NAME outvotes `roadId` | grep `const streetKey =` in `tileGround.js`; **regression, cure exists** — `RIBBONS §3.3` |
    | `blockCustoms` | `skelId \| side \| segOrd` | the authoring key |

    ⛔⛔ **∴ "THE POLYGON MUST ASK THE STAMP" PROPOSES TO CURE CHAIN-LEAKAGE BY IMPORTING MORE CHAIN STATE.** A **node** is literally *"where two chain ends coincide,"* and its *kind* is derived from chain tangents. **That is the stink**, and it is why `13b5120c` measured green on plain blocks and was **nearly invisible** on the canonical dead-end — it added chain identity where the chain already had an answer, and had nothing to add where it didn't.
    - ⭐ **THE DISTINCTION THAT KEEPS THIS FROM OVER-SWINGING — not all of it is rot.** **AUTHORING must be street-keyed**: the operator says *"Mississippi's left side, 2 m treelawn"* and thinks in streets. **That is the product, and it stays.** The defect is that **GEOMETRIC PARTITION is COMPUTED FROM chain facts** instead of being intrinsic to the ring with the label riding along as an attribute.
    - ⭐⭐ **THE TELL, AND IT IS FALSIFIABLE: EVERY FAILURE CLASS IS A PLACE WHERE THE CHAIN'S ANSWER IS DEGENERATE WHILE THE POLYGON'S GEOMETRY IS PERFECTLY WELL-DEFINED.**
      · **The RIM** — no chain at all (`skelId: null`, **34 of 34, set-identical** with the no-asphalt set). Ring edge fine; label absent ⇒ the run was **dropped**.
      · **The FOLD** — both legs are the **same chain, same `segOrd`**, differing only by `side`, and `fe.side`/`run.side` were **inverted**. Ring is a good closed loop; label **doubled**.
      · **The DEAD-END MOUTH** — `cornerAt` fires only when two **different** streets meet, and a doubled-back spur's second pass is `spur → spur`. Ring position unambiguous; **no corner minted** because the chain cannot name one.
      ⇒ **three separate arcs, three reverts, one shape: the geometry always knew; the chain label didn't.** ⭐ **It predicts that any future failure sits where a chain answer is degenerate — which is how to test it.**
    - ⭐ **AND IT EXPLAINS THE TWO DEAD ATTEMPTS.** `(b-i)` (nearest-fe match) and the **walk-ordinal coupler** both tried to **RECOVER the chain label** — by proximity, then by walk order — **at exactly the places the chain has no label to recover.** ⛔ Do not try a third recovery.
    - ✅ **ANSWERED 2026-08-09 — the ruling lives at `A10` ③, not here.** *"What partitions the ring, if not the chain?"* → **the ring edge itself, carried forward as an index.** ⛔ **The standing warning stands: two agents invented an answer at this seam and lost the work.** Anything past the ruled construction is a standup, not a brief.
  - ⛔ **SECOND-ORDER, BUT DO NOT DROP IT: THE WIRING IS CURRENTLY LOAD-BEARING, BECAUSE THE POLYGON CANNOT ANSWER.** Measured by agent Verge, 2026-08-08, every scene: **the partition lives on `tile.ring`; the band is concentric to `tile.iA`; and NOTHING CONNECTS THEM.** `iA = filletRings(…)` densifies the ring **1.5×–8.9×** and **carries no owner stamp — 0 tiles carry iA-side ownership on any scene** (LS 1324 ring-edges → 8323 iA-verts · altadena 17594 → 85800 · centrum 5663 → 29722 · hipointe-demun 17672 → 26609 · toy 85 → 758). ⇒ **`sectionPassTile` re-strokes each run's polyline into an area because that is the ONLY route it has from a ring-side owner to an iA-side arc.** The agreed sentence names the round trip as the defect; **this is the reason the round trip exists.** ⭐ **So the chain is not left-over wiring — it is a SUBSTITUTE for an identity the frozen polygon was never given.** Rip it out first and nothing can answer at all.
  - ▶ **∴ THE CENSUS IS TWO MEASUREMENTS, NOT ONE — and running only the first is how this gets over-sized.**
    **① The chain-reach census (sizes the surface).** Use A03's proven standard — **lexical scope, pass/fail, no judgment**: `buildCurbRings` was held to *"no streets/runs/measures/ribbons in lexical scope."* Apply it to every post-Wall function and classify each: **chain-free · reads a chain-DERIVED frozen fact · reads chains outright.** Doubles as the regression guard afterwards.
    **② The stamp census (says which of ① are FORCED).** For each frozen artifact, does it carry identity, or must the reader re-derive it? ⭐ **Without ②, ① over-counts** — a chain read that exists only because `iA` has no stamp is **not** removable code, it is a symptom of the missing stamp.
  - ⚠️ **SIZE IT BY MEASUREMENT, NOT CONVICTION — this project has been burned on exactly this.** `C4` was sized as a **3,371-line** deletion with a per-frame perf drag; it is **1,846 lines**, the figure-ground geometry was **already deleted at T4**, and the perf drag **had already been memoized away**. The lesson is not *"disbelieve it"* — it is *"do not size it by conviction."*
  - ⚠️ **THE PERF CLAIM IS ARCHITECTURALLY SOUND AND EMPIRICALLY UNMEASURED.** `ORIENTATION §"Why there are bakes at all"` is explicit that **a freeze is *"a PERFORMANCE move as much as a correctness one"*** — so chain re-derivation after the wall is paying a cost the design decided not to pay. ⛔ **But whether it is *measurable on this map* is a separate question from whether it is architecturally wrong. Get the profile before calling it a perf win** (`feedback_a_metric_can_be_a_bad_proxy_for_the_symptom`).
  - ⛔ **SEQUENCING — do not cut code in the same motion as the cure.** The **removal freeze** holds (`SHOW-BIBLE §"Where it stands"`): nothing classified for removal is cut until v1 ships, in **one** window. Dead code that is *provably* dead is cheap to leave standing; a removal sweep entangled with a keystone fix destroys the ability to say which change broke what — which is `_archive/RIBBONS-history §7`'s most repeated lesson. **Cure → census → cut, as three acts.**
  - **∴ the real dependency, and it is already on this board:** ③'s blocker was **`project_polygon_must_ask_the_stamp` at its exact site** — ✅ **RULED 2026-08-09, in flight; the construction and its scope live at `A10` ③.** **M–L for the census; the stamp is its own arc.** → `A10`, `A0`, `project_polygon_must_ask_the_stamp`.

- **A09 · ⭐⭐ THE INTAKE FETCHES EVERY OSM NODE TAG AND THROWS IT AWAY — the cap-chooser is a stopgap for data we already paid for** *(NEW 2026-08-06)* — `fetch.js` asks Overpass for `node["highway"]`, then `ingestElements` does `nodes[el.id] = [el.lon, el.lat]` and **discards `el.tags`**. Measured live in the LS bbox: **763 `street_lamp` · 489 `crossing` · 96 `stop` · 26 `traffic_signals` · 23 `bus_stop` · 20 `turning_circle` · 17 `give_way`** — all dropped. `turning_circle` appears **nowhere in the codebase**.
  - ⭐ **13 of the 17 placeable turning circles land 0.0 m — exact vertex coincidence — on a skeleton degree-1 tip**, and they are the very chains the dead-end class is written about (`whittemore-place`, `rutger-street-0`, `st-vincent-court-1`, `albion-place`, `hickory-street-1`, `waverly-place-1`, …). **The cap DEFAULT is derivable today; nobody ever wired it.**
  - ⛔ **What made this survive: `INTAKE §Cul-de-sacs` closed the question on 2026-06-15 with "the tagging is sparse" — never measured, and false** — two days after `POLYGON-FIRST §5` recorded Jacob ruling the opposite (data-derivable; the chooser is a STOPGAP; the curations are data-defects to automate). **The corpus contradicted itself and the untested half won**, so 40 caps were hand-authored instead — **22 of them onto nodes where the street continues** (`checks/claims-cap-vs-skeleton.mjs`).
  - ⭐⭐ **SECOND CUSTOMER, 2026-08-11 — IT IS THE ONLY ARBITER WE HAVE FOR "SEVERED vs FRAGMENTED".** `carroll-street-1` · `caroline-street-0` · `henrietta-street-0` each carry a turning circle **and each is flagged by the same-name fragment test as a possible false dead end.** The tag calls all three REAL — exactly how Jacob ruled Carroll (*"severed by Truman"*). ⛔ A same-name test cannot tell the two apart; **this tag can, and it is the typing `OSM-FORENSICS §1.3` ⑤ has asked for since June.** ⇒ A09 is no longer only a cap-default nicety; it is what lets `A0`'s population be sized without the operator's eye per tip.
  - ⭐ **The selector STAYS. This is a DEFAULT bug, not a UI one.** Jacob, 2026-08-06: the cap's asymmetric editing + sidewalk/treelawn swapping are *"a logical end-state of the tool"* — not needed for LS, but intended. ⛔ **Do not "simplify" the cap machinery away.** Derive the default from the tag; leave every override first-class.
  - ⚠️ **`traffic_signals` (26) is already-planned work** — `RIBBONS §1` names signal hardware as instanced assets from `highway=traffic_signals`. It will hit this same wall. *(Lamps are unaffected — `bake-lamps.js` has its own fetch.)* **S/M** · probes: `checks/claims-cap-vs-skeleton.mjs`.
- **A0 · ⭐⭐ CLOSE THE POLYGON AT DEAD ENDS** *(NEW 2026-07-25 — takes priority over A1; A1's remainder is a symptom of it)*
  - ### ✅ THE "FALSE DEAD END" RESIZE IS **CLOSED WITHOUT MOVING THIS TICKET** — 2026-08-11, and the answer went the other way
    This slot held, for one day, *"some of these tips are fragment seams the skeleton should weld, so A0's size is unknown."* **Measured and ruled: no.** ⛔ **A same-name test cannot separate a SEVERED street from a FRAGMENTED one** — both keep the name on both sides — so it produces a shortlist, never a verdict. Its own worked example was a correct map: **Jacob, on Carroll — *"severed by Truman, cul de sac one side, butts up to Truman on the other"*, and *"already correct in the survey tool."*** `OSM-FORENSICS §1.3` had listed Carroll among **genuine** severances since June. ▶ `node checks/claims-false-deadend-census.mjs` (12 `CANDIDATE` on LS) — ⛔ **a shortlist for the eye; do NOT subtract it from this ticket.**
    - ⭐⭐ **AND JACOB'S SECOND SENTENCE IS THE ONE THAT MATTERS FOR THIS TICKET: *"it's only the corner naming and the ribbons reading the labels."*** Frame ✅ · Survey ✅ · curb ✅ (`README §START HERE`). ⇒ **the live defect is the unnamed corner at the mouth, which is `A15`'s tell** — `cornerAt` (`tileGround.js:245`) needs two **different** streets and a doubled-back spur's second pass is `spur → spur`, so no corner mints though the ring position is unambiguous. **That is `A10` ③'s territory, not a frame job.**
    - ▶ **The one derivable thing this arc DID surface, and it is small: `A09`.** `highway=turning_circle` typing is what `OSM-FORENSICS §1.3` ⑤ asks for and it is already measured — **`carroll-street-1` · `caroline-street-0` · `henrietta-street-0` all carry one**, i.e. the tag answers three census candidates the way Jacob did. Fetched at `fetch.js:97`, dropped at `:123`.
    - ⚠️ **The weld itself is NOT dead, it is re-scoped:** 35 of 113 named streets emit >1 chain and the within-name dog-legs are real, so weld **for frame quality, never as a dead-end cure, and never across a severance** (`SKELETON §5b-bis`).
  - ⭐⭐ **TWO DIFFERENT FAILURES SHARE THE CANONICAL FRAME — Kennett / South 18th / Dolman / Carroll / Truman.** The band capping while the road continues is **`A10`**'s; **the ROAD ITSELF bulbing into a cul-de-sac is this ticket's**, and the band wrapping that bulb is **innocent**. ⛔ **Do not eye-gate an `A10` fix on a frame containing these** — it reads as an A10 failure and restarts the symptom argument that cost 2026-08-08. ⚠️ **Amended 2026-08-11: at CARROLL the bulb is CORRECT — the street really is severed by Truman and Survey draws it right.** Do not assume every bulb in this frame is a defect; ask per tip.
  - **The mechanism.** The face freeze never closes a polygon at a degree-1 chain: `extractFaces` walks a spur out and back over the same vertices, so **all 50 LS dead-end tips are zero-width slits**, and the missing-mouth-corner test fires on **9 of 50**. The 40 that look right are held by a FILL-layer mouth-wrap snap. A leg with no interior on one side **has nothing to click** — that IS `A1`'s "Δ=0.0 / unresponsive legs", the leg-flip swap and the mouth co-claim, **all one upstream fault.** ⛔ **Not fixable in Section**: a side-free walk-ordinal key was built, gated and retired for re-deriving identity from a chain trace after the Wall. ⛔ **Counts here have been quoted at four different values — re-derive, never quote** (`POLYGON-FIRST §2.1`, `PIPELINE-CLAIMS C4`/`C5`).
  - **Construction (Jacob):** the SSoT radius as the **outer polygon**, everything inside **punched out** — blocks = boundary − stroked roads (precedent: V2's `differenceRings([stencil], asphaltSharp)`). Also lands the data-wall rule **POLYGONS ONLY by the Section tools**, and plausibly subsumes the junction/mouth co-claim. ⚠️ **Re-founds the tile substrate** (`RIBBONS §1`) — **spike + standup before build**; risks are identity attribution and width-dependent topology.
  - ⛔⛔ **STATUS: UNDECIDED.** Tried and reverted 2026-07-31 (`7b5b87a3`), **but the "worse" verdict was taken on the wrong scene and is not safe to lean on.** ⛔ Do not cite the revert as proof the construction fails; ⛔ equally, this is not licence to re-land it. **The right act is to look again, correctly.** `SPUR_OUTLINE` is gone from source; the construction lives at `152e7734`. **L** · → **`_archive/A0-deadend-history-2026-08-11.md`** (the full working-out: the revert forensic, the counter history, `SPUR_OUTLINE` as flown, the one-day false-dead-end resize) · `_handoffs/HANDOFF-deadend-face-resolution.md` · `PREBAKE §4.0` · `PIPELINE §5 (the Wall)`.
- **A1 · Flippable cul-de-sac LEGS** — ⭐ **NAMED INSTANCE 2026-08-11: where the END CAP MEETS THE LEG at Carroll's east tip.** Tile 35 is `offset` and **stamped**, nearest A10 residue **84 m away** ⇒ ⛔ not a band/partition defect; it is the **cap wrap meeting the leg strip**. Eye-reported after A10 ③ landed. *(⚠️ superseded in root-cause by A0 — do not chase the leg tail in Section)* — the dead-end **cap** flip + dip-in slope **LANDED & eye-confirmed 2026-07-18** (`5a0eecc8`→`330004ec`): toggle-swap cap, semicircle swap, legs bend so the sidewalk stays contiguous. The live remainder is the **legs**: a leg material flip currently renders **Δ=0.0** — the terminal-leg custom isn't reaching the strip, and/or the new crossing-band transition zone overrides it. M · → `HANDOFF-dead-end-cap-flip.md` (status block + next-task). *(To ship the caps: a real prod bake regenerates `shape.json` + `design.json` — the current re-bake/test-flips are uncommitted working-copy only.)*
- **A2 · Through-node T-artifact re-fix** — ✅ **SOLUTION LANDED (terminal-node sweep, 2026-07-21).** Replaced the failed `thruTNode` marker (which keyed the wrong run — side-street on split-skelId at Mackay, coord-miss at Kennett) with the **identity+terminal sweep**: group roads by identity (name + near-continuous), a node is TERMINAL for identity X iff it's a degree-1 tip of X's graph, else THROUGH; corners mint only on terminals; a 0-tip identity is a ring (no corner). Stamped in the frame, frozen, read post-Wall — no inference, no coord-match. The removal pass excises the guessers it subsumes. → `docs/briefs/BRIEF-terminal-node-sweep.md` (spec), `scratch/THRUNODE-GATE-FINDINGS.md`.
- **A2b · ✅ LANDED `cd062388`, eye-confirmed 2026-07-22** — hold the through-road's curb edge straight across a divided-into-through-road node; the terminating road corners INTO it. A CONSTRUCTION OVERLAY, never a frame edit (a frame straighten was tried and reverted the night before — it dead-ended a carriageway). ⚠️ **Its reach is the live caveat, not its correctness: the overlay never travels to ①** — `0 of 343` chains carry `strokePoints` in `ribbons.json` — so it reaches no corner. Mechanism, four rules and the constraint on widening it: **`SKELETON.md §5h`** (the whole thread is in that one section).
- **A3 · Freeze the curb in prebake** *(structural — closes the centerline leak)* — build curb+junction ONCE from the frozen frame; Survey consumes it; only the active element re-strokes → Check C green, chains can't leak. **Rebuild-gated.** L · → `HANDOFF-freeze-the-curb-in-the-first-bake.md` (D6b/c).
  - ### ⭐⭐⭐ MEASURED 2026-08-21 — **THE CURB IS NOT FROZEN. IT IS RE-DERIVED FROM THE CHAINS ON EVERY PASS, ALWAYS.**
    `tileGround.js:4186` reads `tile.curbFacts || freezeCurbEdgeFacts({ runs, streetsOrig, measures, … })`. **`tile.curbFacts` is written NOWHERE** — no producer in `src/` or `cartograph/`, and **0 of 101 tiles carry it** (`node -e "const t=require('./public/baked/lafayette-square/shape.json').tiles;console.log(t.filter(x=>x.curbFacts).length,'of',t.length)"`). ⇒ **the `||` fallback is the only path that has ever run.**
    - ⭐ `shape.json` carries the curb's **RESULT** — `iA` on 93/101 tiles, `iaEdge` binding every curb vertex to its owning ring edge, `runs` carrying `skelId · side · segOrd`. **The artifact has the shape; the pipeline still rebuilds it from the chains anyway.**
    - ⭐⭐ **THIS IS JACOB'S SENTENCE IN CODE** *(2026-08-21: "the polygons are the curbs" · "if we were properly polygonized we wouldn't be having these conversations")*. Every click has to re-resolve identity through a chain ordinal, because there is no frozen curb polygon to address. ⛔ **A17 and the other symptoms are downstream of this** — work them as their own tickets and they come back.
    - ⚠️ **A03 built the chain-free HALF** (`buildCurbRings`) and split the producer — **but nothing freezes the facts between them**, so chain-freedom is structural in the signature and never realised at runtime. **The gap is the freeze, not the builder.**
  - ⛔⛔ **A3 IS GATED ON THE OFFSET BEING ROBUST FIRST — `WALL §1`/`§5` + `POLYGON-FIRST §3`'s "D6b+D6c gated on D6a proving out."** **Freezing wrong data is worse than not freezing: A3 over a self-intersecting offset would LAMINATE the mint into the artifact every consumer trusts unconditionally.** ⭐ **∴ `A4`'s robustness half comes FIRST** *(its "strictly downstream of A3" line was backwards — corrected 2026-08-09)*.
  - ⭐⭐ **A10 ③ ROUTED HERE 2026-08-09 — this ticket owns the "minted vertex" class, MEASURED for the first time: 193 tiles across 7 scenes, 14–36% of every town's offset tiles.** `SURVEY §6` — **the curb is the last unfrozen polygon**; the silhouette is re-stroked every build and **self-intersects**, so a boolean cleans it up and mints vertices with no source, which is what blocks ③'s stamp on 43% of LS's defect area. ⛔ **Jacob's constraint (2026-08-09): *"the curb CAN move when the user authors it; same with the corner radius"* — `SURVEY §4`'s primary gesture. Whatever is frozen must still follow the drag ⇒ frozen base + authored scalar in the builder (A03's proven pattern), never a frozen `aFill`** (A06's ruling, same reason). Evidence: `scratch/fold-mint-forensic.mjs`, `A10` ③.
- **A4 · Robust bezier offset → curves ON → re-bake HPDM** *(this IS "HPDM chunky")* — `CURVE_FIT` is built + eye-approved but **OFF**; `STREET_SMOOTH` is pinned 0 because the offset isn't robust on tight bends (turning it on sprays needles). ⛔⛔ ~~**Strictly downstream of A3.**~~ **THAT ORDERING IS BACKWARDS AND WAS THE WHOLE BLOCKAGE — corrected 2026-08-09.** `WALL §1` — *"freezing WRONG data is worse than not freezing at all; it launders garbage into authority"* ⇒ **A3 would FREEZE the self-intersection.** `WALL §5` orders it *"fix the band-fold thorns → **then** freeze"*, and `POLYGON-FIRST §3` says **"D6b+D6c are gated on D6a proving out."** ⭐ **A4's robustness half is UPSTREAM of A3, not downstream** *(the `CURVE_FIT`/curves-ON half stays downstream — they were one line and are two jobs)*. L · → `HANDOFF-curve-primitive-skeleton.md`, `smoothCenterline.js:150`.
  - ⭐⭐ **THE SITE IS CONFIRMED AND QUANTIFIED** *(2026-08-09, agent Kerf, `b378a450` · `scratch/fold-branch-forensic.mjs`)* — of 536 crossings on the 193 minting tiles, **69.8% of crossing endpoints are averaged-normal vertices** (miter 16.0 · bevel 11.8 · cap 2.3). `POLYGON-FIRST §3` named the right place two months ago.
  - ### ⛔⛔ BUT ITS PRESCRIPTION — *"add the missing miter clamp"* — IS **PROVABLY DEAD, AND BOZ BRIEFED IT ANYWAY.** The agent refused the edit and proved it instead.
    **The clamp measures distance from the apex; in that branch the distance is BOUNDED BY CONSTRUCTION.** Emitted point is exactly `(A.d+B.d)/2` from `ring[i]`; `lim = 2.5·max(A.d,B.d,0.5)+1`. **`(A.d+B.d)/2 ≤ max(A.d,B.d) ≤ M < 2.5M+1` ⇒ `dist < lim` ALWAYS, any town, any width, forever.** Measured: **0 of 27,241 averaged-normal vertices, 7 scenes; max ratio 0.389.** ⇒ **not a missing call — the WRONG QUANTITY.** ⛔ **Landing it would have been dead code that looks like a robustness fix, with `A3` freezing on top — `WALL §1` committed while quoting `WALL §1`.**
    - ⭐⭐⭐ **THE FINDING THAT EXPLAINS THE TWO MONTHS: 27.8% of crossing endpoints are miter/bevel vertices — they went THROUGH the existing clamp and still crossed.** ⇒ **both branches are PER-VERTEX LOCAL tests; offset self-intersection is a GLOBAL property of the walk. No local test can decide it**, so no different constant would have saved it. **"Add a clamp" is closed as a class.**
    - ### ✅ RULED 2026-08-09 (Jacob): **STOP PREDICTING FOLDS — DETECT THEM.** *"Speed isn't a real concern, that's the entire point of the datawall and bakes; these are clumsy datasets which we take pains to simplify at every step."* ⭐ **That is `ORIENTATION §"Why there are bakes at all"` applied — cheap per-vertex prediction is the thing a freeze exists so we don't have to do.** ⚠️ It also runs live in Survey, but for **one tile** under the operator's hand; whole-map cost is paid once at the freeze.
      **AND THE EVIDENCE ARRIVED AT THE SAME ANSWER INDEPENDENTLY** *(agent Kerf, `64fa9341` · `scratch/score-nearest-chain-predicate.mjs`; re-run by Boz, all figures reproduce)*. Three predictors were scored against the ground-truth 193 (genuine default; ⭐ comparability holds because the mint set is **measured** unchanged under those customs, not assumed):
      | predictor | caught | false positives |
      |---|---|---|
      | vertex ≥165° reversal *(what `dropFoldSpurs` uses today)* | 92 / 193 | — |
      | segment sign against source edge | 149 / 193 | — |
      | **nearest-chain / wrong-side** *(Jacob's "a loop is tangent to chains")* | **183 / 193** | ⛔ **209 / 964** |
      - ⛔⛔ **"BEATS 149 ⇒ ARGUMENT OVER" WAS TOO STRONG — AND THE THING THAT KILLED IT IS THE SECOND NUMBER.** Boz predicted the false positives would be *legitimately narrow blocks* — a broader-but-valid signal, possibly acceptable. **Measured: of the 210 control tiles that trip it, exactly ONE self-intersects. 209 have a perfectly simple offset.** ⇒ **it fires where nothing is wrong. Precision 46.7%, and the false-positive population is LARGER than the target population.** As a gate it would flag a fifth of every town's blocks as broken. ⭐ **Recall alone would have shipped it; insisting on the second number is what caught it.**
      - ⚠️ **AND THE SIGN HALF ADDS NOTHING — Boz prized it as "pure sign, no epsilon."** `wrong-side` is a **strict subset** of `nearest-chain` (either = nearest alone = 183), contributes **zero incremental recall**, and carries **150 of the false positives on its own.** If anything is built here it is **one** predicate, not two.
      - ⭐⭐⭐ **THE COMPARISON EXPOSED ITS OWN ANSWER: to score the approximate predicate you must compute the ground truth — with a direct segment-pair crossing test.** ⛔ **Its "193/193, zero false positives" is DEFINITIONAL, not an achievement** — the 193 were *defined* as tiles whose `W` self-intersects. **The news is the COST: 18 ms for every offset tile of all 7 scenes** — 1,157 tiles, 7.30M segment pairs, **brute force, no spatial index** — **and NO CONSTANT, NO EPSILON, nothing to tune.** ⇒ **the exact test is cheaper to justify than the approximate one is to tune.**
      - ✅ **Computable with no new key:** `buildCurbRings` insets `tile.ring` by the half-width, so **`tile.ring` IS the chain and the curb is its inset**; "nearest chain" is an argmin over ring edges and A10 ③'s stamp already names each vertex's own. **No identity dragged past the Wall.**
      - ⚠️ **DIRECT DETECTION IS NEEDED EITHER WAY:** the predictor's **10 misses all genuinely self-intersect**, and of "the 44" it sees 34. *(That set reads 43 under HPDM/staging customs — per-tile `W` shifts slightly even though the mint SET does not, so ⛔ sub-counts are not quotable across authoring states.)*
      - ### ✅ THE REPAIR IS RULED 2026-08-09 (Jacob): **"TAKE THE REPAIR IN-HOUSE."**
        ⭐⭐ **THE FOLD IS NOT THE DEFECT.** The defect is that we hand the folded ring to **Clipper**, which repairs it **anonymously** and returns points with no source — that is the mint, and the mint is what breaks the stamp, which blocks A10 ③. **We detect the crossing ourselves (18 ms, whole map, no constant) and repair it ourselves, so the LABELS SURVIVE THE REPAIR.** ⇒ the 193 stop minting **not because they stop folding**, but because nothing anonymous touches them.
        - ⭐⭐ **THE ACCEPTANCE CRITERION IS THE STRONGEST ONE AVAILABLE, AND IT SHOULD BE STATED AS THE TARGET: OUR REPAIR SHOULD PRODUCE THE GEOMETRY CLIPPER ALREADY PRODUCES, PLUS LABELS.** Then `a03` stays byte-identical, `claims-band-reaches-lu` stays at its exact figure, **and the only thing that changes is that 193 tiles now stamp.** ⛔ **Any geometric divergence is a FINDING to report and an eye-gate to owe, never a thing to accept quietly** — the curb is `SURVEY §4`'s primary gesture.
        - ⛔⛔ **NOT EVERY SELF-INTERSECTION IS AN ERROR — the guard that must not be skipped.** An inward offset of a block that pinches in the middle **legitimately splits into two rings**, and Clipper returns two. **The repair must preserve that split, never "fix" it into one.** *(Related: A07's `degenerate:*` class and the capacity guard, which are the loud-failure path for the genuine cases.)*
        - **∴ the sequence, all four now ruled or measured:** detect directly *(18 ms, no constant)* → repair in-house *(labels survive)* → the stamp goes total → **③'s cure becomes buildable, and `A3`'s freeze becomes legitimate instead of a lamination of the defect** (`WALL §1`).
        - ### ✅✅ LANDED `1d945f3e` — **MINTED REFUSALS 193 → 0, AND THE CURB DID NOT MOVE.** *(agent Kerf; every gate re-run by Boz, all reproduce.)*
          **Stamped:** LS **44→57** · altadena **555→643** · centrum **209→250** · HPDM **79→106** · staging **43→66** *(toy/ksi unchanged — neither had a minting tile)*. **`clipper-minted-vertex` is gone from every scene's refusal list; `relabelThroughClipper` is deleted from source and `grid-collision` dies with it.** `a03` **byte-identical both states, artifact included** ⇒ ⛔ **no eye-gate owed, the curb did not move.** A10 **RED and unmoved on all 8 — LS 28/70/2958.0** (a moved figure would have meant part of ③ got built).
          - ⭐⭐ **THE CONSTRUCTION IS NARROWER THAN WHAT WAS RULED, AND IT IS BETTER — the agent flagged it rather than smuggling it.** The ruling said *"repair it ourselves."* **He did not write a boolean.** The defect was never that Clipper repairs the fold — **it is that Clipper repairs it ANONYMOUSLY** — so the repair stays where it is and **only the provenance comes in-house, via Clipper's own `SetZ`/`ZFillFunction` channel**: a surviving vertex inherits its Z from the endpoint it coincides with, and only a genuine intersection reaches the fill. **Three things a hand-rolled boolean would have had to EARN, and this gets for free:** geometry byte-identical **by construction** (the Z channel writes `.Z`, never `.X`/`.Y` — verified in `clipper.js`) · ⛔ **the split guard becomes unfalsifiable by accident** — a pinched block still returns two rings because the boolean is untouched (**33 stamped tiles return >1 ring, 33/33 carry a full per-ring label array**) · A07's `degenerate:*` gate and the capacity guard untouched. **No constant, no epsilon.**
          - **The one convention, since it is a choice:** *a crossing point is owned by the arc that LEAVES it* — the same rule the stamp already uses — **resolved on the output walk where "leaves" is well-defined, never inside `ZFillFunction`, where Clipper offers only scanbeam order.** ⭐ *"An implementation detail must not become geometry."* *(Labels ride as `label+1`: `Z=0` is Clipper's unset sentinel, so ring vertex 0 would otherwise read as unset.)*
          - ⚠️⚠️ **AN OPEN DEBT, DECLARED BY THE AGENT — `claims-ia-source-stamp` IS WEAKER THAN IT WAS.** Its **contiguity and sequence** assertions were true only while non-folding tiles alone were stamped; **carrying labels through a repaired fold makes them legitimately false** (verified per-tile before relaxing: LS **tile 42**'s repaired ring pinches, revisiting one coordinate at output index 0 and 187, so one source edge genuinely owns arcs **8.94 m** apart). **Still asserted, and it is what ③ needs: every `iA` vertex carries exactly ONE in-range source edge — no vertex unowned, none owned twice, green on 8/8.** Multi-arc cases are now **reported with counts** (LS 51 simple / 6 non-monotone / 1 multi-arc · altadena 625/53/6 · centrum 237/19/2 · HPDM 94/17/0 · staging 56/8/3). ⛔ **COST: the ORDER mutant is retired and nothing catches it now** *(shape/range/silent/empty still mutation-verify)*. ⭐ **Boz's suggestion, not a ruling: the agent's own manual tile-42 check is mechanizable — assert that a multi-arc owner COINCIDES with the ring revisiting a coordinate.** That would restore a real order gate without re-asserting the false invariant.
          - ### ✅✅ MEASURED `ce52f39e` — **THE REFUSAL SET IS STABLE UNDER A WIDTH DRAG. ③ HAS NO WIDTH-DEPENDENT SEAM.**
            **Control first, because a perfectly flat result is exactly when to doubt the instrument: the curb moved on 74–81 of 101 tiles at every step. The dial bites.** Across `pavementHW` **2 · 4 · 5 · 6 · 7 · 8 · 10 · 14 m** on LS: **offset 59 · carve 42 · `degenerate:*` 0 · keyhole 2 · stamped 57 — unchanged at every width.**
            1. **Producer flips: none, anywhere in 2–14 m.** The `ringArea`/`isMedianTile` reasoning is **confirmed, not assumed**.
            2. ⭐ **`degenerate:*` stays 0 across the whole bound — the door nobody had swept, and the one Boz missed when first calling the objection dead.** ⚠️ **Stated as it is: this is LS.** A07 measured the carved share **7%→75% by town**, so a carve-heavy town has more tiles near that gate ⇒ **this does not transfer.**
            3. **`keyhole-splice` unmoved at every width** — checked rather than reasoned, because the disk radius is built from a half-width.
            4. **At LS's real working range (5–8 m): 0 flips, 0 degenerate, keyhole 2** — reported **separately** from the 2–14 m bound so the two can never be merged. **Both readings agree, which is the strongest form of this answer.**
            - ⭐⭐ **THE MECHANISM, AND IT IS WHAT CHANGED:** the same sweep moved the mint set **9 → 27 tiles** *before* `1d945f3e`. **Folds still move with width — we never tried to stop them — but they no longer move the REFUSAL set, because provenance now survives the repair. The width-dependence lived in the mint, and the mint is gone.**
            - ⇒ **③'s scope is a FIXED SET.** Of LS's 28 defect tiles the 10 minted now stamp, leaving the **2 keyhole (494.9 m²)** refused at every width.
              - ### ✅✅ AND THE KEYHOLE REFUSAL IS RECOVERABLE — MEASURED 2026-08-10 (agents Quoin `629bebf0`, Tenon `2157788c`). ⛔ **BOZ'S "LEAVE THEM BROKEN" READ IS WITHDRAWN; ③'s REACH IS ~100% OF LS'S DEFECT AREA, NOT ~83%.**
                ⭐ **Jacob's recall is what opened it** — *"the keyholes are loop streets, we had those managed, find out before we decide it's done."* They are the **Park Place / Saint Vincent** turning-circle bulbs (`README` files them under **Loop streets**), and their **curb is eye-confirmed good** (`_archive/CULDESAC-KEYHOLE-FORENSIC-2026-06-16.md`, *"the cul-de-sacs look great"*, `b11cf6b`). **Only the ped band is broken — and the refusal is a blanket whole-tile drop, not a geometric impossibility:** `tileGround.js:3676` nulls every label, while the splice `(base − discs) ∪ (keyhole ∩ discs)` replaces geometry **only inside the discs**.
                ▶ **Re-derive, don't quote: `node scratch/claims-keyhole-splice-survival.mjs` · `node scratch/claims-keyhole-carry-to-ia.mjs`.** ⛔ **AS OF 2026-09-03 THE SECOND ONE CANNOT RUN** — it self-reports `INSTRUMENT ANCHOR DRIFTED — pattern matched 0 time(s), expected exactly 1`, i.e. the source pattern it pins itself to has moved. ⭐ **So the 100.0% below is currently UNREPRODUCIBLE, and this section instructs you not to quote it — re-anchor the instrument before relying on that number.** (The check failing loudly rather than answering from a drifted anchor is it working; the defect is that nobody re-anchored it.) Both assert nothing and gate nothing — they exist for this ruling. Neither modifies `src/`; `--inert` green on 8, and the whole-artifact `iA` hash is identical with the instrument off and on.
                - ⭐⭐ **THE DECIDING NUMBER: 100.0% of the 494.9 m² lies OUTSIDE the bulb discs, and 100.0% is reachable from an OWNED `iA` arc.** The wholesale refusal discards ownership of **exactly the ground that is broken.** *(Exclusion zones 1700.2 + 606.0 m² are printed on purpose, so a 100% cannot come from an empty zone.)*
                - **Survival is exact, never a tolerance:** of the 94 pre-splice labelled vertices outside every disc, **ZERO were destroyed.** The 8 unmatched-and-outside sit **0.04–0.61 mm** from a disc boundary — genuinely minted welds. ⛔ **They must refuse; a disc-boundary test to rescue them is the forbidden proximity recovery** (the receipt is `tileGround.js:3578`'s own *"collided 93× and orphaned 77×"*).
                - ⭐⭐ **THE UNIT IS THE EDGE, NOT THE VERTEX — and it is why the carry looks worse than it is.** `filletRing` mints an arc at corner `i` carrying ring vertex `i`'s label, so **a dissolved coordinate is not a lost owner.** Per vertex 73/94 reach `iA` (all 21 losses inside `filletRing`); **per source EDGE, 45/46 still own an arc.**
                - ✅ **CONTROL, and it is the answer that matters: the keyhole tiles are ORDINARY.** vs the 57 tiles that stamp today — vertex carry **70.3% vs 73.2%**, edge carry **97.8% vs 95.8%**. Mid-distribution on both. **There is no keyhole-specific carry loss**, and the one lost edge is the fillet-inset swallow the stamp probe already reports-but-does-not-gate.
                - ⚠️ **THREE LS tiles take the splice, not two.** Tile 99 carried no labels *before* it (`carve:small`) ⇒ the keyhole is not what refused it and a per-vertex keyhole rule has nothing to recover there. Named, not dropped. **The class travels: altadena 1, staging 3.**
                - ▶ **STILL OWED, and it is a STANDUP not a brief — the cure's shape.** A per-vertex refusal needs a spelling for *"this vertex has no owner."* ⛔ **A `-1` in the `iaEdge` slot is the forbidden shape** — the failure marker sitting in the field where answers live, which is how `classify.js`'s `unknown` hijacked 108 Altadena faces (`project_a_sentinel_is_not_a_value`: **reuse the invariant, don't build a fourth instance**; split the *legitimate* keyhole weld → **record** from the *genuine* unowned arc → **loud**). ⭐ **Boz's read, and it is a judgment about an unbuilt cure, not a measurement: a cure that consumes ARCS may never need the token at all**, since 100% of the defect area is reachable from owned arcs.
          - ### ⛔⛔ ③'s BUILD IS BLOCKED ON A **RE-FREEZE**, AND BOZ'S BRIEF CONTAINED THE CONTRADICTION. *(Agent Kerf stopped before building, 2026-08-09; all three legs verified by Boz.)*
            **`iaEdge` is produced by the SHAPE pass. The FILL reads the FROZEN ARTIFACT. And every committed `shape.json` predates the stamp: `iaEdge` on 0 tiles, all 7 scenes.** *(Verify: `node -e "const t=require('./public/baked/<scene>/shape.json').tiles; console.log(t.length, t.filter(x=>x.iaEdge).length)"`.)*
            - ⛔ **THE GATE CANNOT SEE THE CURE.** `claims-band-reaches-lu.mjs` reads `public/baked/<scene>/shape.json` and walks `bake.tiles` — **disk**. A cure keyed on `st.iaEdge` finds it `undefined` on every tile, falls through to today's painting **everywhere**, and prints **28 / 70 / 2958.0 — unchanged.** ⭐⭐ **That reads as "the cure did nothing." It would actually mean the cure never executed once** — a plausible-looking success, the single worst outcome the doctrine names. **Boz's brief demanded both *"the number must FALL"* and *"nothing re-baked."* They cannot both hold.**
            - ⛔⛔ **AND THE SECOND HALF IS WORSE: TWO DIFFERENT MAPS DEPENDING ON WHICH TOOL YOU ARE IN.** `BlockGeometryV2Debug.jsx:532` — **Section renders from the frozen `shape.json`, explicitly *"NOT from a live re-run"*; Survey renders live off `_shapeArtifact`.** ⇒ with ③ landed and no re-freeze: **Survey shows the cured band, Section shows the broken one — and Section is the tool where this defect is seen and authored.** ⭐ **That is the 2026-07-31 failure exactly** (*"I verified the artifact I produced instead of the artifact the operator was looking at"*): the eye-gate would run on Section, see no change, and **restart the symptom argument.**
            - ⚠️ **THIS WAS ALREADY ON THE BOARD AND THE BRIEF DROPPED HALF OF IT.** ③'s option (a) reads *"stamp ownership through `filletRings` in the shape pass — **a frozen-artifact change ⇒ re-bake ⇒ re-founds the substrate ⇒ STANDUP, never an agent's unilateral call**."* `1d945f3e` did the stamp; **the re-bake half was always part of option (a) and is still owed.**
            - ▶ **THE UNBLOCK — NARROWER THAN A POUR, AND IT IS A STANDUP.** `shape.json` has two writers: **(1)** `cartograph/bake-ground.js:1036`, the full pour — heavy, and Jacob's standing ruling forbids re-pouring non-LS scenes; **(2)** `src/cartograph/api.js:350 saveShapeFreeze` — the **Survey-exit freeze, decoupled from the heavy slab bake**, WYSIWYG off the live Survey build (called at `useCartographStore.js:1824`). **(2) looks like the intended path**, but it is still a frozen-artifact change on the substrate everything downstream trusts. ⛔ **The agent correctly refused to take it.**
              - ⭐ **The argument that makes it LOW-RISK in the one way that matters: `a03-curb-identity` proves the geometry is byte-identical on both states, so a re-freeze writes THE SAME GEOMETRY PLUS LABELS.** This is not `WALL §1`'s "freezing wrong data" — nothing about the shape changes. ⚠️ **The one question to answer first: does `saveShapeFreeze` write the same artifact the BAKE path writes?** If the two writers differ, re-freezing via (2) changes more than the labels. **Measure before, not after.**
              - ⭐ **It also improves `A01`'s complaint rather than worsening it** — a committed artifact that no run of current code reproduces. A re-freeze makes LS's `shape.json` correspond to today's code.
              - **Kerf's read, and Boz concurs: re-freeze LS only via (2) → verify `iaEdge` lands on disk → confirm the gate can see the cure → then build ③, LS-only, which is where the eye-gate happens anyway.** The other six stay stale and unstamped and ③ simply does not fire there — **correct, and they get it at their next pour.**
            - ⚠️ **Unrelated, found while verifying: `public/baked/toy/shape.json` has NO `tiles` key at all**, so toy is silently absent from any census keyed on it. Small; file it.
          - ### ✅ THE ORDER GATE IS RESTORED AS A **SPLIT ASSERTION**, NOT A SKIP — `be3e93d5`
            ⭐⭐ **The branch a tile takes is decided by the CHECK's own crossing test on the pre-union walk — the repair path offers no opinion about itself.** *(That was the design constraint: a repairer that declares its own exemptions lets the exempt population grow invisibly.)* **SIMPLE** (never self-crossed) → contiguity + sequence **asserted strictly, as before**. **REPAIRED** → a source edge owning >1 arc **must coincide with the repaired ring genuinely revisiting a coordinate** — exact equality on Clipper's grid, **no tolerance**. Populations print every run: LS **44 simple / 14 repaired (1 split arc)** · altadena 555/129 (6) · centrum 209/49 (2) · HPDM 79/32 (0) · staging 43/24 (3) · toy 10/0 · ksi 19/0.
            - ⭐⭐ **A SUBTLETY FOUND BY MEASURING BEFORE ASSERTING, WORTH KEEPING: THE PINCH IS TESTED AT THE UNION OUTPUT, NOT ON `iA`.** The obvious site is `iA` and **it is wrong** — `filletRing` runs *after* the repair and can **dissolve the pinch vertex into arc points**, so **2 of 12 multi-arc rings** (altadena 313 ring 3, staging 59 ring 0) carry no exact repeat by the time they reach `iA`. **Asserting there would have failed two honest tiles and invited exactly the tolerance that is forbidden.**
            - ✅ **Mutation-verified 6/6 on both branches — REPRODUCED BY BOZ 2026-08-09.** ▶ **`for m in shape range silent empty order split; do node scratch/claims-ia-source-stamp.mjs --mutate $m; done`** — each must print `MUTATION-VERIFIED`. *(⛔ `--selftest` is `claims-corner-delta`'s flag and does nothing here — it silently runs the normal path, which is how an unverified claim sat on this board for an afternoon.)* **`order` is aimed at a SIMPLE tile; `split` manufactures a split arc on a repaired tile whose union output has no pinch.**
            - ✅ **Two false claims in the check's own OUTPUT — excised.** The verdict line claimed contiguity + ring order for the **REPAIRED** population the same run reports three lines above; it now states what is actually asserted, per branch. And its baseline message said *"gitignored"* for a file that is **tracked** *(and tracked is right — 396 bytes, survives a fresh clone, which is what this ticket complains `a10-post-rim` does **not** do)*. ⚠️ **Both were reported done by the agent but existed only in his context — the tree was clean and the strings were still live**, so they would have read as authoritative the next morning.
    - ⚠️ **BOZ'S BRIEF WAS WRONG IN ITS FRAMING TOO, and the agent asked for it to be carried back: it said "this MOVES THE CURB, an eye-gate is owed."** Nothing moved — **no geometry changed, `a03` is byte-identical by construction, and NO eye-gate is owed.** ⭐ The `A0` bulb caveat (a frame containing the Kennett / S-18th / Dolman / Carroll cul-de-sacs will read as broken for another reason) **stands for whenever a real geometry change does reach Jacob's eye.**
  - ⭐⭐ **"SPRAYS NEEDLES" IS NOW MEASURED, AND IT IS NOT ONLY NEEDLES** *(2026-08-09, agent Kerf)* — of 169 locally self-crossing offsets: **86 NEEDLE** (a ≥165° reversal — the class `dropFoldSpurs` can delete) and **83 LOOP** (self-crossing with **no reversal anywhere** — ⛔ **`dropFoldSpurs` removes reversal TIPS, so it is not mistuned, it is the WRONG INSTRUMENT**), plus **24 global-only**. ⭐ **And the pin is CIRCULAR: the fold-strip inside `offsetRingVariable` runs only when `smooth > 0`, and `smooth` is pinned 0 *because the offset isn't robust* — the cleanup is disabled by the condition it exists to handle.** ⛔ **So "turn `STREET_SMOOTH` on and let the strip clean it" cannot work for the LOOP half.** This is also what blocks `A10` ③ on 43% of LS's defect area.
- **A5 · band-fold G12 thorn clamp** — the local capacity clamp; built but stranded on `8e1e414`, unlanded. Eye-gate + canon fold. S–M · → `HANDOFF-band-fold-fix.md`.
- ⛔ **A6 · junction-construction geometry — CLOSED 2026-09-05, SUPERSEDED BY THE GROUT** (Jacob: *"E3 is superseded; fold it in and close the campaign"*). Thicken→collide→trim→assemble was polygonize-then-offset under construction vocabulary; the grout gets the junction without constructing it. ⭐ **An `L`-sized item retired without being built.** → `cartograph/RIBBONS.md §1`.
- **A7 · Fortify corner identity + the ADA-regime trigger** — ⭐⭐⭐ **THE DEFECT IS IN THE *PAINT* OF A CORRECTLY-IDENTIFIED CORNER. "Corner identity" is the wrong title for what remains — measured 2026-08-11 against Jacob's own marker.**
  - ▶ **THE SITE IS PINNED, NOT INFERRED: `marker_strokes.json` stroke #4, centroid `(501.4, 124.7)`, a 9.7 × 9.8 m circle** — Jacob circled the **SE corner of Dolman × Carroll** with the Marker tool. *(⛔ Two earlier attempts to locate this from street names were WRONG. Use the marker.)*
  - ✅ **What the polygon says at that mark, 1.1 m away: the corner span has EVERYTHING it should.** `carroll-street-1/right/1` · len 12 · **fillet r=4.50** · owned · its own span · on stamped tile 10 · depths `tl 1.5 / sw 1.5`. ⇒ identified ✓ filleted ✓ owned ✓ sliced ✓.
  - ⛔⛔ **THREE HYPOTHESES ARE DEAD — do not re-derive them.** **(1)** *"the corner has no fillet"* — it has one, 1.1 m from the mark. **(2)** *"the 18° per-vertex `FILLET_TURN_TOL` swallows densified corners"* — architecturally it **is** a local test on a global property (the same shape as `A4`'s dead miter clamp), but the census finds **exactly 1 corner-sized unfilleted turn on LS and it is the RIM** (`skelId: null`, `A15`'s known class). **Latent, not the bug.** **(3)** *"it is the keyhole partition"* — tile 10 is stamped and the A10 residue at that corner is **0.002 m²**.
  - ⚠️ **AND THE CENSUS ONLY RAN ON LS.** hipointe-demun / altadena / centrum report 0 because their committed `shape.json` **predates the stamp — 0 partitioned tiles**, so `bandSpans` never ran. ⛔ That is *not measured*, not *clean*; closing this class kit-general needs those scenes re-frozen.
  - ### ⛔⛔ THE DEEPER DEFECT, FOUND BY BREAKING IT 2026-08-11 — **`cornerT` IS KEYED BY NODE COORDINATE, AND A TILE'S RING CAN VISIT ONE NODE SEVERAL TIMES.**
    **LS tile 10 visits Dolman × Carroll at ring indices 6, 8 AND 10.** Only two tiles meet that crossing (10 and 19), so the four quadrants the operator sees are **tile 19 (one) and tile 10 (three, wrapping)**. Keyed by coordinate, **tile 10's three geometrically distinct corners collapse into ONE `cornerT` entry** — and with `legs` capped at 2, a leg from one quadrant takes the slot that shapes another.
    - ⭐⭐ **A CORNER IS A RING POSITION. Keying it by the coordinate two runs happen to share is NODE-THINKING — `A15`'s stink one level in**, in the FILL's own corner table. *(`A15`'s tell: the geometry knew — three separate ring positions — and the chain-shaped key could not tell them apart.)*
    - ### ⚠️ THE RE-KEY WAS BUILT 2026-08-11 AND REVERTED. **Its measured effect: 500 → 550 corner instances (50 collapsed corners separated) and the impossible `legs: 3` entry → 0.** ⛔ `no-fillet-in-range` doubled 34 → 71 and **THE CAUSE IS NOT ESTABLISHED** — the explanation first written here (*"a three-way tie at the tolerance"*) was **measured FALSE the same hour**: the real tolerances are 15–20 m, so a 7.75 m apex passes easily. **Do not build on the doubling until someone measures why it doubles.**
    - ▶ **HOW IT WAS FOUND, and the method is the point: a fix that fed the pool MORE legs made the pre-existing collapse visible.** Jacob's eye, immediately: ***"that only ruined the opposite corner."*** ⛔ **Reverted the same hour** (the leg-drain half only). **Re-land only on a per-corner-INSTANCE key.**
  - ### ✅ LANDED AND KEPT — **`cMin` VOTES ONLY AMONG LEGS THAT CARRY CONCRETE** *(the half that is independent of the keying)*
    **Jacob's EITHER-rule, in one expression** — a sidewalk-less leg no longer drags `cMin` to 0 and paints the pad LU; it simply does not vote, and all-LU happens only when **neither** leg carries concrete. ⭐ **It closed a class nobody had connected: `built-empty-concrete` 6 → 0** — those corners built a pad whose concrete came out EMPTY, *the exact failure the code's own comment at `:1760` feared and had tried to prevent by suppressing the leg.* **BUILT 331 → 337.** ✅ SHAPE proven untouched by differential (`iAAll·block·curb·asphalt·fillets` byte-identical; only `sidewalk` moves). ⭐ No re-freeze — `sectionPass` runs live off the frozen shape.
  - ### ⏳ STILL OPEN — the marked corner is NOT fixed
    **The bug, still live: one flag is doing two jobs.** The five predicates at `tileGround.js:1766` (`noPed · tipped · through · isNameTransition · isThruNode`) legitimately stop an end from **MINTING** a corner — that is `A2`'s cure for the false corner a through-run bids against a stem. They were **also** stopping it from being a **LEG** of a corner another run minted, which was never intended: the corner then has `legs.length === 1`, the whole Idea A block is skipped, and **no ramp is laid — which IS the operator's hard step, on the suppressed side only.**
    - ▶ **MEASURED AT JACOB'S MARKED CORNER, and it is `through`, not `noPed`:** `dolman-street-1|left|3` and `|4` both suppressed by **`through`**, both `pavementHW 5.49` — **a leg dropped where the curb is perfectly good**, while the same corner on the neighbouring tile kept both legs and looked right. *(Jacob: "where is there no curb? there is curb everywhere we're looking." He was right — `noPed` is the RIM and is nowhere near this.)* **Tile 10 now reads `legs 2`.**
    - ▶ **AND JACOB'S EITHER-RULE CLOSED A SECOND CLASS NOBODY HAD CONNECTED: `built-empty-concrete` 6 → 0.** Those corners built a pad whose **concrete came out EMPTY**, because a zero-depth leg dragged `cMin` to 0 — *the exact failure the code's own comment at `:1760` feared, happening anyway on the un-suppressed path.* `cMin` now votes only among legs with `conD > 0`; all-LU only when **neither** leg carries concrete.
    - **Net on LS: pads BUILT 331 → 337 · `legs=2` 298 → 306 · `legs=1` 32 → 30 · `built-empty-concrete` 6 → 0.** Declines unchanged at 163 — ⛔ **those are `A06`'s** (see below).
    - ✅ **SHAPE PROVEN UNTOUCHED BY DIFFERENTIAL, not asserted:** `a03-curb-identity` run with and without the change on the same tree — `iAAll · block · curb · asphalt · fillets` **byte-identical between the two**; only `sidewalk` moves. All five hunks sit inside `sectionPassTile` (1422–2335), the FILL pass. ⭐ **No re-freeze needed** — `sectionPass` runs live off the frozen shape, so a reload shows it.
    - ⏳ **REMAINING on this ticket: the 30 `noPed` one-legged corners** (the rim — genuinely no curb, so no ramp to build against; needs its own ruling) and **`no-fillet-in-range` 31 on partitioned tiles.**
  - ### ⭐⭐⭐ THE GOVERNING RULE (Jacob, 2026-08-11) — **THE CORNER MIRRORS ITS LEGS.** ⛔ Not *"always a concrete pad"*, which was this ticket's phrasing an hour earlier and is wrong: **if the legs have no sidewalk there are TWO treelawn/LUs, and the corner must be two LUs too.** *"The corners have to be responsive to the legs."*
    - ⭐⭐ **THIS IS `A15`'s "SLICE, NOT JOIN" APPLIED TO MATERIALS, not a new rule.** A corner is a **slice of a continuous band**; a slice **inherits** the band's arrangement. Deciding the corner's material independently of its legs treats it as a separate object — the chain-world error in one more place.
    - ✅ **HALF BUILT ALREADY, and in the right direction:** *"Idea A"* (`tileGround.js:2027`) makes the corner **depth**-responsive — `cMin = min(legA.conD, legB.conD)`, a concentric arc at the shallow depth, the deeper leg ramped on its own straight run. **The principle is the construction's own design; it just stops at depth.**
    - 🔴 **WHERE IT DOES NOT FOLLOW: the MATERIAL.** `let concrete = pad` — the pad starts concrete unconditionally and `luInner` is only carved *inside* it at `cMin`. Nothing asks whether the legs carry a sidewalk at all.
    - ### ⛔⛔ TWO CORRECTIONS FROM JACOB THE SAME HOUR, AND THEY SPLIT THE CURE IN TWO
      **(a) THE TEST IS *EITHER*, NOT *BOTH*.** *"A block might have TL|SW on one face and no sidewalk on the side (all LU). **We can build the corner there**; ALL LU only happens if there are no sidewalks from either side."* ⇒ **concrete pad iff AT LEAST ONE adjacent leg carries a sidewalk; two LUs only when NEITHER does.** *(He has one near his house — this is a real arrangement, not a hypothetical.)*
      **(b) ⭐⭐ ALL-LU IS A *MATERIAL* STATE, NEVER AN ABSENCE.** *"That just means the mono-width ribbon is set to all LU… not 'not there'."* `SECTION §3.3`: **two strips always, EQUAL width — swap, not collapse.** ⛔ **Do NOT model a sidewalk-less leg as zero depth** — that encodes an authoring state as missing geometry, which is `[[project_a_sentinel_is_not_a_value]]` / `[[feedback_partition_artifact_vs_authored_zero]]` in a new place. *(A draft of this cure was heading exactly there.)*
      ⇒ **THE CURE HAS TWO INDEPENDENT HALVES:** **GEOMETRY — unconditional** (the corner slice is always built, mono-width, full depth; no material state may suppress it) · **MATERIAL — leg-responsive** (the *either* rule above). ⭐ **And (b) sharpens the `bandRem` finding rather than replacing it: if the ribbon is sacrosanct and always full width, a corner ALWAYS has band to paint, so `bandRem-empty` is never a legitimate "nothing here" — it is the leftover model reporting that the legs ate the corner's own ground.**
    - ⚠️ **MODE 4 IS A CORRECTNESS CONSTRAINT ON THE CURE, NOT THE POPULATION** *(Jacob: "there is almost always a sidewalk", and by (a) it takes BOTH legs bare to reach all-LU — rarer still)*. ⛔ **Do not scope A7 around it** — the visible defect is the DECLINE RATE below. Mode 4 is what the cure must not get wrong, not what to go hunting.
    - ▶ **THE ONE MEASUREMENT THAT SCOPES MODE 4, unrun:** does an all-LU leg already drive `conD` → 0? If yes, `cMin = 0` and the pad may collapse to LU on its own, leaving only mixed cases broken. If no, **every open-field corner is painting a concrete disc.**
  - ### ⛔⛔ THE DECLINE POPULATION IS **A06's**, NOT THIS TICKET'S — measured 2026-08-11, `node checks/claims-corner-decline-vs-partition.mjs`
    **`tile-gate:bandRem-empty` fires 101 times on LS and ZERO of them are on a partitioned tile.** All 101 (plus the 21 `+no-fillets`) sit on the **42 carve tiles**. ⇒ ⛔ **the proposed cure "stop gating on `bandRem` where the corner owns its arc" would have changed NOTHING** — it never fires there. *(That was Boz's hypothesis; it is dead.)*
    | | tiles | pads built | declined | rate |
    |---|---|---|---|---|
    | **partitioned** | 59 | 315 | **43** | **12%** |
    | **carve (unpartitioned)** | 42 | 16 | **120** | **88%** |
    ⭐⭐ **42 carve tiles produce 120 of LS's 163 declined corners.** `A06` is therefore **not Check-C hygiene — it is where corners die**, and it now owns the largest share of this defect.
  - ▶ **AND THE PAD IS DECLINED 17–63% BY TOWN — MEASURED, `node checks/claims-corner-takeover.mjs`:** altadena 605/3609 (16.8%) · **LS 163/500 (32.6%)** · centrum 624/1819 (34.3%) · hipointe-demun 406/879 (46.2%) · **ksi-y-m-yn 149/237 (62.9%)**. ⭐ **Worse on the towns nobody has inspected — the kit's signature tell.** The declines are already NAMED, which makes this scopeable rather than exploratory; on LS: **`tile-gate:bandRem-empty` 101** · `+no-fillets` 21 · `no-fillet-in-range` 34 · `empty-pad` 7 · `built-empty-concrete` 6. ⛔ **The dominant one is not about the corner at all — `bandRem-empty` is a WHOLE-TILE condition suppressing every pad on that tile.** ⇒ start there; it is the largest single decline on **every** town in the table, so it is the one that travels.
  - ▶ **The other branch: how a fillet-carrying span is PAINTED** — `SECTION §6.9`.4's all-SW slice tangent-to-tangent (*"both legs stop at tA/tB; the corner ribbon takes over"*). The eye shows a **step/notch in the band at the corner**, not a missing corner. **M** · was: ⭐⭐ **NAMED, MEASURED INSTANCE 2026-08-11: DOLMAN × CARROLL, and it is PROVEN not to be a partition defect.** Both bounding tiles (19, 10) are `producer: offset` and **stamped**, and the A10 residue at that corner is **0.002 m² — none**: the band paints, the corner is still wrong ⇒ the defect is the **corner CONSTRUCTION**, which is this ticket. *(It is the corner Jacob opened the whole arc on; it survived two months because it was never the class being fixed.)* — **some real corners aren't getting ADA treatment.** A dedicated pass on *what is a corner* (identity, not angle — `feedback_walker_corner_detection_is_identity_not_angle`) and *when/how to force the ADA regime* on one. Jacob held it to see if the through-node + cap fixes would shake it out; they didn't fully → its own pass. The **inverse of A2** (through-node = a FALSE corner wrongly firing ADA; this = REAL corners NOT firing) — same root question, corner identity. M–L · → `SECTION` canon (the governing rule) · `RIBBONS §1/§4`.
- **A8 · Sidewalk proximity-join** — when two sidewalk runs come within proximity and leave a **thin treelawn sliver** between them (seen at a cul-de-sac bulb ↔ leg), **join them into one contiguous sidewalk** instead of leaving the gap. Consistent with the frontage-contiguity governing rule, applied *between* runs. Likely **FILL/Section** (the inward ped stroke arrangement) — ⚠️ confirm the layer (FILL join vs a curb-proximity SHAPE thing) with the canon open before touching. M · → `SECTION` canon · *(surfaced 2026-07-18 from the cul-de-sac render)*.
- **A9 · Pair-free edge-anchor (fix overlap-holes on divided/frontage roads)** — where two same-material road strokes run **parallel and overlap** (divided carriageways, frontage roads), the bake's `ringsToHoledPolys` (`bake-ground.js:307`) reads the overlap as a **hole** (winding-sign classifies an overlapping opposite-wound ring as a void → punched-out asphalt; the nested-donut islands). ⭐ **Fix at source, not the bake union:** don't let them overlap — **project the ribbon from an edge, not the centerline** (the inner-edge anchor, `RIBBONS §3.1`). The anchor mode EXISTS but is **hard-coupled to a detected pair** (`derive.js:3554`; the Survey dropdown greys out "Inner-edge (no paired chain detected)") → **useless for frontage roads** (no mate). Generalize the anchor from `{Center, Inner-edge-needs-pair}` to **`{Center, Left-edge, Right-edge}`** — pair-free, operator-chosen side, kit auto-suggests from the nearest parallel same-material neighbor; the offset already does one-sided projection. ⚠️ `innerSign` is documented UNRELIABLE (`tileGround.js:742`) — a second, smaller leak. M · → `docs/briefs/BRIEF-pair-free-edge-anchor.md` (draft) · `RIBBONS §3.1`. *(surfaced 2026-07-21, HPDM divided/frontage overlap-holes)*
- *(Gated sibling: **D3 corner-identity freeze at prebake** — the false-corner cure is live; doctrine wants it decided once upstream. M · → `PREBAKE §5`. Do with A3, don't cleanup-refactor.)*

## Column B — tree impostor editing + placement  ·  *this IS "works on a phone" (trees are the OOM/perf culprit)*
> Column B makes the phone light. The instrument that *proves* it's light (the measurement regime) is
> the confidence-half, parked in the Horizon (**H1**) — pull it forward when you want publish-and-know.
>
> **▶ STATUS 2026-07-22 — BOTH impostors are landed and in the player; what's left is WEIGHT.**
> `hero-impostor-foundation` (21 commits, Phases 0–2) merged to trunk (`3e809a56`). The impostor is now
> the **foundation**: every tree paints, the tallest `heroGeomFraction` (0.15) keeps mesh as anchors, and
> the hero-pan **cull is retired** (it was dropping 69% of placements onto bare ground). Overhead/Browse
> landed 2026-07-10. **The player-load gap is closed** — the crux was indeed slab-carry: `heroImpostorBySpecies`
> is browser-authored and must be *carried*, never re-derived (it had to be grafted by hand through the merge).
> **B1/B2/B3 are done; the remainder is B6 + the two open defects below.** Full as-built + open work:
> **`arborist/ARCHITECTURE.md §"Tree-render reality at LS"` — the SSoT; don't restate render state on this board.**

- [x] **B1 · Salon hero-impostor editorial surface** *(2026-07-22)* — `HeroImpostorBaker` on Bake→Slab, Hero-view eye-gate in the Salon, baked `heroImpostorBySpecies`, `bake-look` carry. `Matrix4` aliasing prereq fixed (`5a06ed5d`). ✅ **It pays for itself as of 2026-08-29** — the pool ships KTX2/ETC1S, encoded by the pour's last step. ⛔ The old warning here read *"B6 is now a prerequisite"*; **B6 is placement/census correctness** and never was the compression item, so that pointer sent readers to the wrong row for a month. The remaining compression job is the master ATLAS (`arborist/BACKLOG`), which is excluded by design and needs coverage-preserving mips fed to the encoder. ▶ `node scratch/claims-every-declared-page-ships.mjs`
- [x] **B2 · Overhead-impostor chain** *(2026-07-10, `a4458f4a`)* — 3-slice snapshot, runtime camera-height select, `overheadBySpecies` carried by `bake-look`. ⚠️ **Open defect:** 3 of 30 LS bands baked **blank** and shipped undetected (platanus canopy+branch, linden canopy); the baker now refuses blank bands, and the platanus needs a Salon composition so it has a `barkDetailBySpecies` record at all.
- [x] **B3 · impostor LOD render** *(superseded/absorbed)* — the capture-architecture question is answered (in-browser GPU RTT, both hemispheres); the whole-tree octahedral cross is **killed, not parked**; `occlusion→cull` is **withdrawn** (culling is no longer how density is managed).
- **B4 · density-impostor-swap** — HPDM's mesh trees to impostor. **Largely absorbed by the foundation** (the height split is scene-generic, so HPDM inherits it) — what remains is the **canopy-dims join fix** + confirming HPDM's own bake. ⛔ **AND IT IS NOT ONLY HPDM (2026-08-27):** every look with no baked `heroImpostorBySpecies` boots foundation-OFF, falls to the legacy prominence path and **culls onto ground that already carries a contact-shadow ring for every placement** — *"circles without trees"* (Jacob). The runtime now warns; ▶ **`node checks/claims-every-shadowed-placement-renders.mjs`** names which looks, and **staging was one of them.** ⛔ Don't quote counts from this board — run it. Doctrine + the tell: `arborist/ARCHITECTURE.md §Tree-render reality at LS`. Re-scope before dispatching; the per-scene `promThreshold` it was built around no longer exists. S · → `HANDOFF-density-impostor-swap.md`.
- **B5 · atlasKind `/stem/` classifier bug** — 11.3% of placements render **black** (foliage samples the bark atlas). Needs standup + a re-bake (stale atlas blanks the whole grove). M · → `HANDOFF-tree-spokes-and-census.md §3f`, `atlas-kind-classifier.js:52`. *(⚠️ Revised 2026-07-22: this no longer claims to also clear the overhead blank-bands. Those were measured — the platanus blanks because it has **no `barkDetailBySpecies` record at all** (never Salon-composed), which collapses `uBarkTileScale` to (0,0); a different bug from `/stem/` misclassification. `scratch/overhead-band-coverage.mjs` is the meter.)*
- **B6 · Placement / census correctness** — **the tree-spokes overhead artifact still UNIDENTIFIED** (next step is a scene-graph probe in Browse, not more theory — 9 theories already ruled out). ✅ *Closed 2026-07-22:* the **census-well union** (LS was baking 729 park-only placements while its own 2,635 + 3,376 street-tree wells sat unread — two entry points had drifted) and the **allowed-zone bypass** (the bare CLI placed with no zone test at all; it now resolves through `treeBakeInputsForMap` and refuses to run unzoned). LS: 729 → **4,992 placed**, 892 forbidden-surface drops, 1,405 surveyed trees nudged onto legal ground. ▶ **Still open:** LS's `neighborhood_boundary.json` **exists** but carries **no `polygon` key**, so tree membership falls back to the disc (`bake-trees.js` prints `⚠️ no boundary-street polygon` — that is `membership.hasPolygon`, not a missing file). **The work is authoring a polygon INTO the file that is already there, not creating one.** *(This line said "no `neighborhood_boundary.json`" — wrong diagnosis, corrected 2026-08-04.)* S + M · → `HANDOFF-grove-neighborhood-axis.md`, `HANDOFF-tree-spokes-and-census.md`, [[project_tree_census_wells_must_union_all]].
- **B7 · 23a merged-mesh segmentation** — split "group-shot" merged-mesh forest GLBs into individual trees (the real root of the rows-of-trees / lost-willow artifact). Now dispatchable (gauntlet landed). M–L · → `HANDOFF-23a-merged-mesh-segmentation.md`.
- **B8 · Trees "demander" force-render** — explicit `invalidate()` when tree assets finish loading so trees appear unconditionally (async-load race on the on-demand loop). M · → `arborist/ARCHITECTURE.md §"Tree-render reality at LS"`, BACKLOG NOW.

## Close-out — Supabase / Cary security  ·  *"just be done with it"*
- ~~**S1 · Apply migration `009`**~~ ✅ **CLOSED 2026-08-24 (Wren).** Applied to prod. F-1/F-7 verified shut (anon → 401), F-8 by DDL. ⚠️ `db push` alone was a no-op — remote migration history was empty; needed `migration repair 001…008` first (now tracked). ▶ **Owed: re-run the Advisor in the dashboard** (not reachable from the CLI) + the Auth-console lints. Check: `node scratch/claims-cary-anon-exposure.mjs`.
- **S2 · Service-role auth + webhook signatures** — ✅ **F-2, F-4, F-5-Twilio, F-9, F-10 CLOSED 2026-08-24 (Wren)** — deployed/applied and verified live, including F-5's positive path (a real inbound text landed in the SMS Inbox, so a genuine signed Twilio request passes the new check). **F-3 fixed in source** (caller auth + atomic claim vs double-charge) but the function stays undeployed and un-runnable by design — its bare imports need an import map, and making a money endpoint deployable is its own decision. F-5-Stripe/Checkr still open (`cary/stripe/webhooks.js` has no HTTP entrypoint at all). ▶ **Owed by Jacob: enable anonymous sign-ins** (dashboard) or the requester flow stays failed-closed. ⭐ **Not the design conversation it was filed as:** couriers already hold a real Supabase auth session (`useCary.js:59-69`) and `courier_profiles.id = auth.uid()` throughout `002_rls_policies.sql`, so the fix is server-side only — assert the caller JWT's `sub` against the body's `courier_id`. ⭐ **Ordered by LIVE exposure** (`SECURITY.md §5`): F-2 (`onboarding` ACTIVE) → F-5-Twilio + F-9 (`sms-webhook` ACTIVE) → F-4 → F-3 + F-5-Stripe/Checkr (**not deployed**). M · → `SECURITY.md` F-2…F-5.
- **S3 · Remaining Supabase findings** — ✅ F-9, F-10 closed 2026-08-24. Open: **F-14 (new — `courier_profiles` hands every column, incl. license plate + Stripe account id, to the anon key for any active courier; latent only because there are none yet)** · F-6 IDOR (narrowed — no longer covers `requests`) · F-11 cron fail-open · F-12 `serve.js` command-injection (dev) · ~~**F-13 `contact-sms`**~~ ✅ **capped + origin-locked 2026-08-24** (global daily ceiling, per-IP/device windows, salted-IP key, fails closed) — it was never a LOW: the Ward's public *Text us* button reaches it with no key at all and every call spends money. F-13 stays open for `sms-inbox`/`sms-reply`/`web-messages` · the Auth-dashboard lints (OTP expiry / leaked-password / redirect URLs / **anonymous sign-ins, now required**). S–M each · → `SECURITY.md`.
- *(Sibling, distinct arc — the **LS Apps-Script** side: admin-token ship-blocker + no rate-limiting, both 🔴. Ships-blocks the LS app, not Supabase. → `ls/STATUS.md §security`. Tracked in the Horizon LS-app cluster **H7**.)*

## Quick independent wins — cheap, public-visible, no column dependency
- ~~**Lamp census restore**~~ **LANDED `87fe26fb` (2026-07-18)** — scene-homed the raw OSM file + allowlisted + re-baked: 80 fallback → **536 real OSM lamps** (641 clipped to hood). Render path was already live. Pending final eye-confirm; retire `HANDOFF-lamp-census-restore.md` once confirmed.
- **Ground foundation-crack (mechanism B)** — on a slope the ground falls below the flat foundation top and exposes a concrete band. Cure = a `bake-terrain.js` footprint-flatten + apron sharing one `footprintMeanElevation()` helper with `bake-buildings.js`. Rebuild-gated. *(Mechanism A — the tessellation split — is already fixed.)* M · → `HANDOFF-ground-foundation-crack.md`, `scratch/GROUND-BUILDING-CRACK-FINDINGS.md`.
- **Forest Park census free win** — read `FORESTRY_TREES/MapServer/4` (4,297 trees, richer schema) we currently ignore; best species prior for the HPDM census gap. "Costs one integer." S · → `HANDOFF-tree-spokes-and-census.md:33`.
- **Milky Way re-enable** — the renderer is **present and named**: `src/components/CelestialBodies.jsx` (1369 lines) — `MilkyWaySphere` at `:363`, `MILKYWAY_FLAT_DEFAULTS` imported `:19`, `MILKYWAY_DEFAULT_CHANNEL` `:37`, resolved through `resolveGroupAtMinute` `:405`, mounted `:486`. Channel plumbing is live (`skyLightChannels.js` → `bake-scene.js`). *(2026-07-18 claimed the file no longer existed and sized this M for a locate-the-renderer investigation; re-verified 2026-08-04 — nothing is missing.)* The real task is re-enable + view across TOD + report what beautiful costs. S · → talk-out brief. · **building-dissolve UI sliders** · **landmark-label nudge**. S each · → `ls/STATUS.md`, `ls/BACKLOG.md`.
- **A22 · The two-centers model is RULED and does not reach disk — the disc centroid cannot move after commit.** `R10`/`D4`: the frame origin is frozen forever, the **hood centre is a value, not a frame**, so it is free to roam — that is the whole mechanism by which a hood re-centres without a re-pour. As built: `CenterHandle` exists and is gated `!committed`; the disc-centre memo returns the origin once committed; and **`rescopeMap` never puts `center` in its body** although `/rescope` destructures it and logs *"frame origin untouched"*. ⛔ **Un-gating the handle alone makes it worse** — it would drag, appear to save, and be discarded on Bake. Both halves, or the model stays a doc. S · → `EXTENT-DESIGN §3.3` (D4).
- *…tiny aesthetic bugs land here as they surface.*

---

# THE CLEANUP TAIL — process-cleanup ahead of human-developer engagement
> Code legibility + pristine docs + the kit-productization that makes the whole thing hand-off-able to
> a human developer. This is where the horizon's "make it legible" subset lives.

- **C1 · blank-app / Universal Reader** — Ph4 (`branding.copy` bundle + build-time `index.html` inject) + **the zero-hardcode gate** (grep the reader for LS literals → zero) + the per-instance content sidecar (off `src/data/buildings`) + the module manifest. The legibility spine for handoff. L · → `HANDOFF-blank-app-instance-decoupling.md`, `plans/front-front-end-and-productization.md`, `SLAB-CONTRACT §C2`, `NEIGHBORHOOD-INPUTS §5.1`.
- **C2 · HiPointe backend-tenancy — deploy + verify** — per-look Apps Script backend; code shipped on `sheet-tenancy`, Jacob runs `clasp push && clasp deploy` + the 5-step eye-gate (no cross-tenant leak). S · → `HANDOFF-neighborhood-backend-tenancy.md`, `_archive/DEPLOY-CHECKLIST-backend-tenancy-DIARY-2026-09-13.md`.
- **C3 · The correctness-detector campaign** — the real kit deliverable: one automatic check per bug-class → CI gate → onboarding loop (run-suite → fix-class → green). This is what lets a human work town #2..N without re-inspecting everything. L · → BACKLOG kit-correctness track, `POLYGON-FIRST §5`, `scratch/correctness-detector.mjs`.
- **C4 · figure-ground dead-path excision — RESIZED 2026-08-02, it is ~half what this said.** `buildBlockGeometryV2.js` is **1,846 lines, not 3,371** (`wc -l`); the figure-ground *geometry* was **already deleted at T4** (`4044bca1`, −1,900 lines — README §construction model). ⛔ **Do not "delete the file."** What remains is **load-bearing**: the frontage-edge identity builder (`feCustomKey` = the `blockCustoms` key) **plus the bake's boolean ops** — `differenceRings`/`intersectRings`, imported by `tileGround.js:35` and `buildPathRibbons.js:29`, along with `pickLuFromHash`, `blockKeyFromRing`, `resolveChainSegmentation`. The real work is unchanged and is **T3**: migrate the fe identity onto the tile `runs` (same `[skelId, side, segOrd]` triple, `tileGround.js:935`) **proving key parity first, or authored customs silently orphan** — then extract the boolean helpers to their own module and the vestigial name dies. ⚠️ The **per-frame perf drag this item used to justify is gone** — memoized and gated off outside Survey/Measure (`DESIGNER-LOAD-FORENSIC` repair #1). M (was L) · → `HANDOFF-tile-T3-authoring.md`, `ACCORDANCE-REVIEW` AP-4/AP-7, `plans/clean-for-handoff.md` W2. *(`DOC-CODE-COHERENCE` C3/C4/B4 and `BAKE §5` still carry the stale "delete the 3,371-line builder" instruction — Tier 2.)*
- **C5 · Doc-pristineness to zero** — drive `DOC-CODE-COHERENCE` corpse-lies to ✅ (C5–C13, D1–D8); codify the doc-currency mechanism into `BOZ §4`; physical-archive the parked material (RUNTIME-DELTA, the misfiled cartograph sky-ADRs in `meteorologist/NOTES`); reconcile the three May-12 "marriage-leap" plans (partly OBE). M · → `cartograph/DOC-CODE-COHERENCE.md`, `plans/clean-for-handoff.md`.
- **C6 · Retire landed HANDOFFs.** ✅ **Settled policy (Jacob, 2026-07-21):** every root `HANDOFF-*.md`/`BRIEF-*.md` except the currently-active briefs lives in the local gitignored **`_handoffs/`** folder — out of git, kept on disk, history intact. The Diary `cartograph/_archive/` holds the tracked retirement batches. ⛔ **Do not re-adopt the old "keep done-stamped handoffs at root as detail-homes" policy** — that is what produced the root clutter this replaced. **Still pending:** `ls-statistical-planting` (**NOT done** — its "retire once landed" clause is future-tense); `thrunode-gate-fix` / `grove-neighborhood-axis` (live A2/B6 detail-homes — archive when their lines land). S · → `ACCORDANCE-REVIEW.md` detritus survey.
- **C7 · LS pre-public cleanout** — mode-conditional `rollupOptions.input` (drops the ~4.5 MB cartograph chunk from prod), `copyPublicDir:false` allow-list, deployment-ID audit gate, two verified-orphan deletions. M · → `plans/pre_public_cleanout.md`.
- **C8 · render-pipeline-install Ph4/5** — the SCENE tree onto the one manifest + fold render-conformance/preview-measurement in as installer capabilities (makes the render path legible + parity structural). M · → `HANDOFF-render-pipeline-install.md`.
- **C10 · The CSS + accessibility pass — PAIR THEM, they touch the same files.** *(Jacob 2026-07-20 / 07-21; parked in the old `docs/agents/BOZ.md §1` for months (now `cartograph/_archive/BOZ-full-2026-08-06.md`) where it read as process rather than work — rescued to the board 2026-08-06 when BOZ was cut.)* (a) **Extract inline `style={{}}` onto the token system** — `src/tokens/design.css` is the SSOT (`:root`-scoped, portal-safe); `cartograph.css` layers `--carto-*` **scoped to `.cartograph`**, so anything portalling to `<body>` must use the project tokens or every `var()` collapses to an unreadable panel. Consolidating is what makes the suite read as one product instead of a pile of operator panels. (b) **A real a11y pass, never yet run:** focus order + visible rings, keyboard operation of every control (the Materials source buttons and the Extent pen especially), contrast in both standard and `.accessible` mode, `aria` on dialogs/expandable rows, `prefers-reduced-motion`. The app has `.accessible`; the helpers do not honour it. ⛔ Do (b) WITH (a) — an a11y fix written against inline styles is redone once the styles move. M · → `src/tokens/design.css`.
- **C9 · Detritus-survey code-legibility items** *(2026-07-18 survey — record in `ACCORDANCE-REVIEW.md`; Tier 1 archive sweep already done)*. Headline: the *source* is disciplined (7 `TODO/HACK` markers repo-wide, near-zero commented code) — the sediment is docs + `scratch/` + 2–3 god-files. Three tracked, none urgent:
  - **(a) Decompose `cartograph/derive.js#deriveLayers()`** — a single **~3,465-line function** (L1009→~4474, 75% of the 4,607-line file) — into named phases like its siblings (`detectDividedStreets`, `correctStreetWidths`, …). A deliberate SHAPE-core refactor, **eye-gated, NOT a delete**. L. *(Distinct from C4's `buildBlockGeometryV2.js` excision — different file.)*
  - **(b) De-barnacle the patch-over-prose docs** — `arborist/FEATURES.md` (40 dated + 31 `Brief` tags, incl. "read X below as Y" clauses) + `arborist/ARCHITECTURE.md` (713 ln) + `meteorologist/NOTES.md` (957 ln): pull the dated decision-log to `_archive`, rewrite the "read-as" clauses to plain current-state. Finishes the FEATURES voice sweep. M · folds with **C5**.
  - **(c) `scratch/` sediment triage** — 1,048 files (~459 images); archive/delete the pre-July render-dumps + stale `.md`. It's git-tracked, so cut deliberately. S.

---

# THE HORIZON — approaching, not far
> Bumped below the near-term push, but the horizon is coming up fast — these won't stay here long, and
> several are already part-planned or part-superseded. Re-tier into the push as a column empties.

- **H1 · Publish-confidence / mobile-measurement** *(the confidence-half of Column B's phone goal)* — `preview-measurement` (**real per-tier device numbers = the hard gate**; today `phone-hi==phone-lo`, both guessed) · render-conformance Ph4–7 · mobile-profile B/C/E · phone-slice frustum culling (trees + buildings) · hero-motion smoothness (spatial Arch-bounce → catmullRom cusp/FOV) · the 7 Browse-shot looks · heroTier wrong-camera-target (~1200 m, unblocks all tree-LOD). L · → `HANDOFF-preview-measurement.md`, `HANDOFF-render-conformance.md`, `HANDOFF-mobile-profile.md`, BACKLOG NOW.
- **H2 · Altadena / CDP pour** — ground-refine-cdp scale blocker (26.3M tris/88 min → per-hood knob) · the Stage landscape-upload flow (sets `landscape.source`, unbuilt) · mountain radial cut + shading pass · fix the wrong (LS) lamps. L · → `HANDOFF-altadena-pour.md`, `HANDOFF-altadena-mountain-hero.md`, `HANDOFF-ground-refine-cdp.md`.
- **H3 · HiPointe productization** — building-position-from-slab (dead clicks / missing pins) · content-join re-join (24 stale listings) · city-county-divide render · HPDM lamp coord bug + trees eye-gate. L · → respective HANDOFFs.
- **H4 · Meteorologist** — the **Tuner** (the `capture-render` primitive T-1 gates everything) · cloud-specialist morphology renderer · 8 weather-wiring gaps (W-1…W-8) · sky-pivot seasonal deviations · Preview Studio · wind-field · audiologist. L · → `meteorologist/TUNER.md`, `HANDOFF-cloud-specialist.md`, `meteorologist/STATUS.md`.
- **H5 · Streaming-slab spatial chunking** *(Jacob, 2026-07-17)* — chunk the monolithic merged-mesh slab at coarse region granularity + stream just-in-time along the fixed hero pan → attacks mobile OOM. **Composes with Column B** (impostor placeholder first, geometry streams in). L · → BACKLOG Front B, `SLAB-CONTRACT`.
- **H6 · Arborist depth** — leaf-base library gaps (~6) · phenology (season/color ramp + annual-cycle editor) · STAGE-2 Sugar-Maple vertical slice · full dossiers · green-light readiness gate · night-emissive foliage · birch-bare render. L · → `arborist/BACKLOG.md`, `arborist/README.md`.
- **H7 · LS app features** — Cary order pipeline + POS (L) · generic place-card templates · honorary-townie endpoint · TOD-aware OG preview · **LS Apps-Script security ship-blockers (🔴)** · ~12 completeness gaps (event edit/delete, community-stats hydration, courier-count stub…). S–L · → `ls/BACKLOG.md`, `ls/STATUS.md`.
- **H8 · Kit intake** — custom street-data import (generalize `survey.json`) · the chassis-tagging gauntlet (the manual 0/241 pass — surface built, tagging not) · productionize the scene-generic content-intake stage · ⛔ **grow/shrink the bb from the frozen centre** — ruled and measured (a re-fetch renumbered **zero** of 13,427→8,460 footprints) but **the gesture is UNBUILT and is routinely recalled as shipped**; until it lands a hard fetch has **no give**, so err generous on the envelope (`EXTENT-DESIGN §7.1`). S–L · → `SKELETON.md`, `[[project_chassis_tagging_gauntlet]]`, `NEIGHBORHOOD-INPUTS §11`.
