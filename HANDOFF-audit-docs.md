# Handoff — Documentation Officer (the Show Bible)

> **You ARE the dispatched agent.** Name yourself; sign your commits with it. Read
> `AUDIT-MATRIX.md` and `plans/front-front-end-and-productization.md` first. You don't audit code
> yourself — you **synthesize** the pathologists' reports and interview them.

## Mandate

Aggregate the four domain audit matrices (`scratch/audit-cartograph.md`, `audit-ls-app.md`,
`audit-arborist.md`, and the cloud specialist's meteorologist output — in flight) into the
**master matrix** + the **Show Bible**. **New paperwork** — the old docs (cartograph quintet,
`ls/` trinity, arborist/meteorologist docs) become *pruned source material*, not things you
overwrite.

## The Bible's three jobs — drives its shape (layered + modular)

1. **Marketing** — "you can do X." Per-app, **extractable, standalone-marketable** (cartograph /
   arborist / meteorologist each sellable à la carte). Doubles as the dead-code detector: a
   capability statement that reads as nonsense is a flag back to the pathologist.
2. **Fundraising** — "here are all the moving pieces." The master package / map.
3. **Engineering** — what each thing is, how you reach it, how to fix it, how to troubleshoot it —
   **including the deploy path and what's behind any login/password** (sourced from the LS App audit).

**Structure:** the master package **sits above**; per-app product sheets are **extractable**; the
**Slab gets its own FORMAT spec** (it's a portable product — render-vs-content boundary); the **API
gets a developer-docs layer**, seeded from the endpoint inventory in the LS App report.

## Productization capture (the campaign's forward yield)

Maintain, from the matrices' columns, three living lists that feed the horizons in
`plans/front-front-end-and-productization.md`:
- **future-setting** candidates (de-hardwiring) → the intake/settings spec seed.
- **slab-field** gaps (slab-completeness) → the format spec.
- **api-route** candidates (endpoint inventory) → the API surface.
- plus the **blocked-work ledger** (what releases when each knot is untied).

## Sequencing + collaboration

You aggregate **as the pathologists deliver** — don't wait for all to finish. Interview them on
ambiguous classifications. You're the connective tissue: the three pathologists + the cloud
specialist fortify each other through you.

## Deliverable

The **master matrix**, the **Show Bible v1** (layered/modular per above), and the three
productization lists + blocked-work ledger. Flag conflicts between domains' findings for Boz.
