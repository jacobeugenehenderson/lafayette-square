# Cartograph Backlog

> **STATE + forward-work only** (`BOZ.md` doc architecture). No diary (→ `NOTES.md` + git), no how-it-works (→ Reference: `SKELETON` / `PREBAKE` / `SURVEY` / `RIBBONS` / `ARCHITECTURE` / `PIPELINE`). When an arc completes: *fact* → Reference, *why* → ARCHITECTURE decisions, *narrative* → NOTES — it leaves here.
>
> *Reshaped 2026-06-05 to the polygon-first program. The pre-reshape backlog (DONE-narratives, the superseded C5/wall-move/palimpsest thread, the prong-4 red herring) is in git history; the deeper pre-prune blob at `git show 4bf8af2:cartograph/BACKLOG.md`. ★IP NACTO/AASHTO pad + ADA-radius decisions still carried for ARCHITECTURE's Decisions section.*

---

## 🔥 NOW — the polygon-first program

**The committed arc (Jacob, 2026-06-05):** **(1) fortify the Skeleton + its Prebake → (2) polygonize Survey (all authoring tools connected + rendering live) → (3) a DataWall with prepared artifacts behind it.** The false corner / thorns are **symptoms** of Survey not being polygon-first — the corner-patch is **KILLED** (`_archive/handoffs/`). Cure = move the chain→polygon conversion into **prebake** + freeze (wall → ~P3). Doctrine: `SURVEY.md §5.1` · `PREBAKE.md §5`. Trunk: `cartograph-looks-pass-ab`.

> 🔄 **PIVOT (2026-06-07, Jacob).** Survey fortification reached good-enough — the osm2streets grounding + intersection-everywhere (`9c275ce`) cured the marks/artifact family (corners, spurs, the perpendicular-join). **Survey fortification is PAUSED** (residuals deferred, not abandoned — below). **Focus → finish the (3) DataWall** (`HANDOFF-wall-phase-d.md`, now freezing GOOD data) → then **RIBBONS / Section** (first thing tomorrow). Standard road model is canon: `OSM2STREETS-GROUNDING.md`.
>
> **Deferred Survey residuals** (revisit after the wall/ribbons; `[[feedback_accept_the_long_tail]]`):
> - **(a) intersection-everywhere FINISH** — places it made *much better but not totally fixed* (the smoothed-but-not-perfect corners/junctions; includes the E3.2-window marks #0 missY ~2m, #6 park-4 ~2.7m, the Mackay-join 8m). **Same fix, incomplete at some nodes — NOT a new class.** (Phase-next: fillet gate → `cornersAdjacent`, apron-everywhere + leg-completeness, perimeter through-steps, edge-collision window sizing.)
> - **(b) the curve-and-cut class** — a genuinely **NEW, separate** class: a block edge **curving in then cutting** near a junction; recurs map-wide; was optimistically folded into intersection-everywhere but it's distinct.

| Stage | Status |
|---|---|
| intake | ⏭️ deferred (LS has `osm.json`); Provincetown = first run |
| **skeleton** | centerlines polygon-ready ✅; remaining = fortification (name-logic doglegs) |
| **prebake** | ⚠️ **thin two-source compile** — the polygon-ization + freeze (the wall-move) is the live target |
| **survey** | tile construction live (LS, unflagged) but **NOT yet polygon-first**; authoring mid-migration (T3) |
| WALL | at `sectionPass` today; target = the prebake→survey boundary (~P3) |
| section | ~70% (`sectionPass` chain-incapable by closure) |
| bake → 3D | working; LS visual correctness gated on the above |

**Live work (the program, in dependency order):**
- **Doc campaign — the on-ramp (in flight).** Lean the corpus to rebuild-precision; front-half spec = `SKELETON`+`PREBAKE`+`SURVEY` (done/drafted, indexed). Remaining passes: `RIBBONS` lean to the tile model (coherence **D1**), `FEATURES` retire figure-ground-as-live (**D3**). Method: `[[feedback_docs_effluvium_buried_the_answer]]`. Ledger: **`DOC-CODE-COHERENCE.md`**.
- **(2) Prebake polygon-ization + freeze → `HANDOFF-prebake-d2-face-freeze.md` (D2, drafted 2026-06-07, dispatch-ready).** Move `extractFaces` upstream, freeze the **tile topology** (`tiles[]`, L1 only) into `ribbons.json`, consume frozen tiles in `tileGround` — **byte-identical**; this moves the Wall to ~P3 + unlocks D5 perf. `PREBAKE.md §5`. ⓘ The false-corner **cure is already LIVE** (intersection-everywhere `9c275ce`); D3 (corner identity at prebake, L2) is **deferred to §HARDENING** — not part of D2. C5 (two-source seam) = D4, later.
- **(3) Polygonize Survey / connect authoring (T3)** — migrate handles / corner-R / cap-selector / strip-swap / translucency off the dead figure-ground onto tiles; **activated-block live redraw** (perf, the sticky-tools fix). `HANDOFF-tile-T3-authoring.md` · `HANDOFF-survey-section-tool-design.md` · `SURVEY.md §4.1`.
- **⭐ The Section build (its SSOT is now `SECTION.md`, 2026-06-07).** Section = ped FILL, ~70% built; the real arc = **freeze the silhouette, author the FILL *live*** off the frozen `iA` (`SECTION.md §4`). Ordered backlog: `SECTION-CENSUS §6` (wire authoring onto tiles → **resolve D1 = the live-FILL/WYSIWYG decision, Jacob's call** → fix the 3 `sectionPass` FILL defects → strip-swap/caps → delete figure-ground → rename). Pairs with D2 (Ashlar, prebake freeze) + D5 (activated-block perf).
  - ⚠️ **Known-broken, deliberately left to molder (Jacob 2026-06-07 — not authoring this session, so no revert).** Wall Phase-D (`ef460d1`) froze the **FILL** as well as the silhouette in the Measure tool (`tileGeos`→null when `sectionGeos` present), so FILL authoring there is a frozen snapshot — **move a handle, nothing re-strokes.** The freeze→open *mechanism* is correct; it just got ahead of the live-FILL build. **Fix when the Section build starts:** either revert the `if (sectionGeos) return null` gate, or (better) re-host `sectionOpen` so it freezes the silhouette and strokes the FILL live. `SECTION.md §7`.
- **Skeleton fortification — name-logic doglegs** — weld same-name fragments + straighten within-name kinks. `HANDOFF-name-logic-skeleton-pass.md`.
- **Band-fold thorns** — local capacity clamp (thin-tile / T-mouth `iW` fold; ~100 instances). Distinct thorn-class; re-confirm scope once prebake-freeze lands. `HANDOFF-band-fold-fix.md` (+ `HANDOFF-junction-band-thorns-FINDINGS.md`).
- **⭐ Perpendicular-join protrusion (RE-CLASSED 2026-06-05, Jacob's eye).** The "bulge at the base of some T intersections" + **Benton's stem-joint protrusion** are ONE class: *a path meeting another perpendicularly → a spurious polygon protrusion opposite the meeting.* **NOT (just) band-fold** — a **junction-construction** artifact, same family as the false corner + the fold-at-join (the face-walk/fillet manufacturing spurious geometry at a junction). → folds into **E3** (broaden it to "clean every perpendicular junction," not just divided transitions). ⚠️ Visually-unified by the operator's eye; **the E3 spike must confirm the shared *code* root** (face-walk vs fillet vs band-fold) before assuming one fix lands all (the false-corner-saga discipline). Benton's full bless waits on this (the guard fixed only the collapse).
- **T4 — delete figure-ground** (`buildBlockGeometryV2` / `cornersAtIx`; coherence **C3/C4**) — after T3 migrates authoring off it.
- **Corpse-lie excision** — the code-side of every landed truth; via briefs at the code phase. `DOC-CODE-COHERENCE.md`.

## NEXT
- **⭐ "Revert to Data" buttons — Survey + Section (Jacob 2026-06-08).** The operator-facing version of the datum-repair: instead of a forensic dropping deviating overrides, a button **strips the authoring overlay back to the base.** The clean inverse of "always populate best-effort, then override" (`SECTION.md §8`). Scoped (edge / block / chain / whole-neighborhood):
  - **Survey · "Revert to Data"** → the **sterile Skeleton**: discard the SHAPE overrides (per-fe `pavementHW`, corner-R, caps) → the skeleton's surveyed base widths. The skeleton/`survey.json` is the truth ([[project_skeleton_is_the_first_bake]]).
  - **Section · "Revert to Data"** → the **calculated best-effort**: discard the FILL overrides (treelawn Y/N, ped depths, strip materials) → the gleaned treelawn-Y/N × ADA defaults + default strip ordering (`SECTION.md §3.1`).
  Mechanism partly exists (`MeasurePanel` reset/wipe-customs); generalize + surface as a first-class per-tool control. *(Motivation: deviating overrides cause artifacts — the no-mouth doglegs were exactly this; reverting to data cured them. Give the operator the button, not the forensic.)*
- **Survey · Section · Stage rename** — decided; rides with T3 (⚠️ don't rename code until implemented).
- **Boundary-trio** — circle-SSOT, crop-to-circle-last, bake-time stencil-cull. `HANDOFF-boundary-trio.md`.
- **Asphalt-as-ribbon** — Survey-owns-SHAPE capability. `HANDOFF-asphalt-as-ribbon.md`.
- **Dead-end typology** — reframed as Survey-side authoring. `HANDOFF-dead-end-typology.md`.
- **Slab-content** — alleys / footways / overlays / highway-class asphalt.
- **Story-pass address-maps** — Meteorologist + Arborist (`[[project_story_pass_campaign]]`).

## 🛡 HARDENING — correct on the eye, not yet correct-by-construction
> **Distinct from the deferred long-tail** (those are still *wrong* on the operator's eye — open defects). These items are **already correct** but the fix lives at the wrong layer / isn't frozen-by-construction = robustness debt, not bugs. The goal: make the architecture *guarantee* what a live fix currently *achieves*. Promote deliberately (a layer-relocation can regress the working fix — gate it), not as routine cleanup. Lens: `[[feedback_accept_the_long_tail]]` (defer ≠ abandon).

- **D3 — corner identity at divided transitions → freeze at prebake.** The false-corner cure is **LIVE + working** (intersection-everywhere `9c275ce`: `dedupeRing` + `MIN_CORNER_LEG` + corners from leg-adjacency at every node — clean on Jacob's eye, at the *construction* layer in `tileGround`/`derive`). Doctrine (`PREBAKE.md §5`, `PREBAKE-POLYGONIZATION-PLAN.md §2`) wants the decision made **once upstream as frozen topology**: detect each transition node via `phase.spineAt*`, drop the taper segment, pair cross-street curb × carriageway straight-body, stamp `cornerIdentity{legs}` — so no per-build code can ever re-manufacture the needle. ⚠️ **Relocating re-derives corners → risks regressing the working live fix**; do it deliberately, gated on the 24 enumerated transition nodes (the Mercator spike prints them), not as cleanup. Superseded-as-NOW by the live fix; kept here for correct-by-construction completeness.

## 🖥 RUNTIME / GPU-PERF THREAD (separate from the pipeline; surfaced 2026-06-07 — the Preview GPU emulator pegged the **ground layer**)
- **⭐ Adaptive terrain refinement — the tri-cut.** The ground bake is **1.23M tris** *not* from geometry (it's simple) but from `bake-ground.js triangulateAndRefine` (`:489`) subdividing EVERY ground polygon to a uniform `maxEdge` grid so the flat slab drapes over terrain — **over-refined on flat blocks** (the two biggest land-use floods alone = ~1.0M tris). **Fix: curvature-adaptive refinement** (subdivide only where terrain bends) → 1.23M → a fraction, no visible change, helps mobile + desktop + download at once. Elevates parked `HANDOFF-tile-feature-ledger.md` P1/P3. ⚠️ **NOT the grass shader** (grass = a material/fragment uniform, 0 geometry); **NOT wall/bake-staleness** (bake is current as of `bf16612`).
- **Mobile policy ("what's mobile") — `HANDOFF-mobile-profile.md`** (restored 2026-06-07; had been retired to git at `9bfcb66`). **SETTLED architecture** (Boz#2 cold-review): split the ~20 `IS_MOBILE` hardwires into **two axes / two homes** — *inclusion* (which layers ship to mobile) → slab/`design.json`, Stage-authored, per-Look; *global quality* (`dpr`/AA/shadows/post) → `INSTANCE.mobileQuality`, product-wide, **synchronous**; the shader-baked tail stays code-side. Device *sensing* → one source; *policy* → the two homes; seed with today's hardwires (no day-one change). **Near-dispatch-ready, was parked behind the in-flight pipeline arcs → now UNBLOCKED.** Needs Jacob's nod on §2.
- **Preview measurement (Vernier) — Phases 0–2 LANDED** (`GpuMonitor` window-dilution fix · non-destructive `.visible` gating · Buildings-toggle convention · **Ground+AO → "Ground" de-conflation** = why the emulator labels it "Ground"). **Open:** the §6 next-arc + **per-environment budgets** — today the budget is hardcoded-mobile (`17ms / 200 draws / 1M tris`), **no Desktop variant**, and the Phone/Desktop toggle is **cosmetic** (same scene/assets, no perf isolation). Sibling: `HANDOFF-render-conformance.md` (Stage↔Preview↔Production parity, `IS_MOBILE`).

## LATER / PARKED
- **Onboarding / Intake** — Provincetown first run; designed, no code. `[[project_intake_onboard_process]]` · `[[project_real_tiny_town_spike_surface]]`.
- **⭐ Custom street data — import + manage (KIT)** — operator-measured per-street width/ROW/lanes is the **base-width source** (priority: custom → OSM → AASHTO; baked into the skeleton so the frame is born accurate, operator only tweaks). LS already has it: `raw/survey.json` (68 streets, 61 measured). The kit needs: **(a)** import it at onboarding *if the place has it*; **(b)** a canonical home/format; **(c)** a way to view/manage/edit it (it's operator authoring — existing tools, no new control class). Generalizes the LS `survey.json` into the Intake/Onboard flow. Docs: `SKELETON.md` (seed width-sourcing). *(The LS base-load fix — survey.json wins + propagates through divided splits — is **E1** in the divided-corridor/width build, NOW; this backlog item is the kit generalization, LATER.)*
- **Tile-bake footprint (P1) / curve-fineness (P3)** — parked; profile before cutting (`HANDOFF-tile-feature-ledger` E-rows).
- **Hosted bake service w/ auth** (v2+).
- **Hero keyframe FOV regression · TAA post-pass** — not urgent.
- **Doc-debt** — stale `BakedBuildings` refs in `ls/ARCHITECTURE.md` / `ls/reference/INVENTORY-DATA.md` / `RUNTIME-DELTA.md`.

## ⏸ AWAITING JACOB (QC / eye)
- **Grade-sep** faces-filtered + stroked-as-asphalt — live re-bake eye.
- **Buildings bake L1.3** · **Hero-tree LOD** · **Render-conformance Phases 4–7** (cold-review gate).

## 🏛 STANDING CAMPAIGNS
- **The living Process / doc-architecture** (`BOZ.md`) — now active as the doc campaign + the `DOC-CODE-COHERENCE` discipline.
- **Forensic audit + productization → Show Bible** — `HANDOFF-audit-{cartograph,arborist,ls-app,docs}`.

## 🛠 LIVE HANDOFFS (the dispatch layer — index)

| HANDOFF | what | bucket |
|---|---|---|
| skeleton-tile-hygiene | ✅ DONE (Lye, merged `06c394b`) — C1/C2/C9/C10/C11 excised; the writeFileSync drift gremlin killed via `io.js {touch:false}` | done |
| prebake-polygonization-scope | ✅ DONE (Mercator) → `PREBAKE-POLYGONIZATION-PLAN.md` (D1–D5 decomposition + validated spike; false corner is partly a DATA bug) | done |
| d1-carriageway-measure-hygiene | ✅ DONE (Gunter, `bbc3401`) — divided per-side measure hygiene | done |
| divided-corridor-model-scope | ✅ DONE (Alidade) → `DIVIDED-CORRIDOR-PLAN.md` (chains=inner-edge, constructed median, fold-at-join, rip-out ledger) | done |
| e1-custom-width-base | ✅ DONE (Lye, `2afeb0e`) — custom-width base baked into skeleton; major corner + many improvements (Jacob's eye) | done |
| benton-loop-guard | ✅ DONE (Stadia, `1bd711b`) — bad-data guard; both loops at sane widths (collapse fixed; Waverly pills gone). Role cross-sections = L.3 (later). | done |
| e2-constructed-median | ✅ DONE (Esplanade, `2d98861`) — constructed median (40 median + 60 merge); crossings-open rule; isMedianFacing/G3a retired; new `'median'` LU; Truman drop-off + island-pill cured; 43k m² reclaimed. **Shipped imperfect (Jacob 2026-06-06) — REFINE post-E3** (Truman = perfect; others OK; likely self-improve as E3 touches the transition regions. Open: 53 m² Park-Ave piece, edge wobble. The E3 spike checks whether any sit at the transition noses E3 builds on). **Feature-authoring** (insert an "S"-shaped median, turn pocket, etc.) **does NOT exist yet** — future T3 authoring (derived base + author-the-exceptions; not blocking). | done (refine later) |
| e3-junction-cure-scope | ✅ DONE (Voussoir, `c197bb5`) → `JUNCTION-CURE-PLAN.md`. **VERDICT: ONE root** — the junction silhouette is never constructed (emergent butt-capped strokes; trigger = width discontinuity, 53 instances); fold + corner + perpendicular-protrusion all covered; band-fold + doglegs ruled out; de-taper already beats production vs the 8 marks. | done |
| e3.1-junction-map | ✅ DONE (Alidade, `2d5e329`) — `ribbons.junctionMap` 86 nodes (126 continuity pairs, 43 windows, corner identities, 50 aprons); geometry-neutral (both gates byte-identical). **Corrections:** Truman×Lafayette is a perpendicular T (corridor-terminus), NOT a continuation; the real continuation is Truman↔S-18th. **Finding:** `innerSign` vote-flips on snaking corridors (s18-6) — D1's measure canon uses the global vote → **E3.4 datum check**. | done |
| e3.2-detaper-construction | ✅ DONE (Trammel, `b853217`) — 0 folds at all 24 nodes (was 18/24); Lafayette/S-Jefferson/Benton-stem/Truman↔Grattan/deg-6 cured. "Much better, many visible improvements" (Jacob). | done |
| e3.3-fillet-identities | ✅ DONE (Chamfer, `6a4f611`) — corners fixed BY CONSTRUCTION (keep-out quadrant at identified-line P, then fillet); Mississippi×Lafayette 1.76→1.39m, stub fillets gone, 5/103 tiles (all at stamped nodes), no regress. Subtle visible change (corner curls gone at park corners). Pending Jacob's eye. | done |
| e3.4-datum-repair / **junction-datum-repair** | ⛔ **REVERTED once (Stadia, `stadia-e34-datums`) — its tileGround corner-gate/G9 surgery regressed the 18th corridor.** ⭐ **ROOT NOW PINNED (Sextant forensic, 2026-06-08, `scratch/JUNCTION-FINISH-FORENSIC.md`):** the no-mouth-side park-perimeter doglegs = a **per-fe `pavementHW` datum step** on a *continuous* avenue, rendered by `[THRU]` (`tileGround.js:1788`) as a sharp Z-jog sized just under the 18° fillet tol. NOT junction-construction (nodes are `kind:"plain"`), NOT fillet, NOT skeleton-off-chord. **Fix = reconcile colinear through-fe widths UPSTREAM (seed/prebake) so a continuous avenue carries one width → THRU self-gates (`:1773`); NO tileGround surgery** (that's what regressed 18th). Steps: Vail 0.66 / Mackay 2.20 / Albion 0.60 m (+ unmarked worst park-avenue-1 3.36 m); Kennett/Waverly `dw=0`=separate centerline-kink class. **Dispatch-ready: `HANDOFF-junction-datum-repair.md` (FRESH/worktree; Boz may do it).** | **dispatch-ready (surgical, datum-only)** |
| osm2streets-grounding | ✅ DONE (Macadam, `8fcb8bf`) → `OSM2STREETS-GROUNDING.md`. **E3 = the trim-back algorithm rediscovered by hand** (intersection-everywhere + trim; generalizes to plain Ts). **18th = geometric divided-detection where standard is data+topology** (ramp+service mis-paired). "Loop street" NOT a standard concept (authoring-card name only). Recommendation: **port 2 methods + align vocab** (don't adopt). innerSign should be face-adjacency (kills the foot-vote bug). | done |
| divided-detection-data-first (the 18th complex) | ✅ DONE (Telford, `870a1fd`) — data-first gates; divided pairs 22→19; 18th fabrication killed (ramp+service), real corridors intact; class-flattening fixed. `innerSign`=face-adjacency punted to intersection-everywhere (avoid 2nd face walk). Jacob fixed the stray 30m manually. | done |
| intersection-everywhere ⭐ = "the initial thing" | ✅ DONE (Tresaguet, `9c275ce`) — **the marks were TWO drawing bugs** (data right, as Jacob said): (1) `filletRing` minting corners from Clipper quantization micro-edges → `dedupeRing`+`MIN_CORNER_LEG`; (2) one-width-per-run → through-node split + per-fe width + trim + blend. Handle-truth: #1/#2/#5 → 0.00, #7 18.3→3.7, Benton 2.8→0.9. junctionMap → every node (86→238, 689 corner pairs by leg-adjacency); innerSign=face-adjacency (s18-6/truman-1 flipped); ~3.5k m² bites recovered; no median regression. **Pending Jacob's eye.** | done |
| e3.2-window-quality (residual follow-up) | marks **#0** (missY ~2m), **#6** (park-4 ~2.7m), the **Mackay-join 8m** spot — all at E3.2-constructed nodes (the window/X quality class). Phase-next: fillet gate → `cornersAdjacent`, apron-everywhere + leg-completeness, perimeter through-steps, edge-collision window sizing (the seam). | NEXT |
| Jacob's marks = ONE class (corrected 2026-06-06; Boz had mis-split them into "datums/loops" — WRONG) | **All marks are the perpendicular-join artifact** — a chain meeting a straight run, artifact on the OPPOSITE side. E3 (the junction cure) fixed only the divided/mapped junctions and **left the ~84 plain cross-nodes untouched** — the marks sit at those **plain perpendicular Ts**, so the cure never reached them. **Real work: extend the junction cure to the plain Ts.** (NOT datums, NOT loops.) | open — the real follow-up |
| 18th = a mis-detected LOOP STREET (Jacob 2026-06-06) | U/horseshoe dead-end, **interior = regular block (not median)**, **crosses a name-shift**. Skeleton mis-paired its parallel legs as DIVIDED → E1 weird-width + E2 median-split the block. **Fix in SKELETON:** detect the loop (across the name-shift, via `continuesAs`/collinear) + take precedence over divided detection + interior = normal block (sidewalks both sides). `LOOP-STREETS.md`. | open — skeleton fix |
| build sequence (D+E, in the plans) | **E1 ✅ · D1 ✅ · E2 ✅ · E3.1 ✅ · E3/junction-cure ✅ LIVE** (intersection-everywhere `9c275ce` cured corners/folds/perp-joins at the *construction* layer). **NOW → D2** (prebake face-freeze, dispatch-ready). **D3** (corner identity → prebake) **moved to §HARDENING** (correctness already live; relocation is deliberate, not routine). Then **D4** (LU at prebake + retire C5 — also a §HARDENING candidate) · **D5** (perf, the freeze payoff). Parallel: brief F (north-void). | live |
| **wall-phase-d** | the Wall as a real freeze — Section opens the frozen Survey data | ✅ **MECHANISM LANDED (Hadrian, `ef460d1`)** — §5(a) checkpoint; `sectionOpen` chain-free, Section ← `shape.json`. §5(b) "correct data" stays open on the prebake cure. Handoff retired. |
| **prebake-d2-face-freeze** | D2 — move `extractFaces` to prebake, freeze `tiles[]` (L1 topology only) into `ribbons.json`, byte-identical; moves Wall to ~P3 | ✅ **DONE (Ashlar, `55e5b8e`+`dee5c20`, byte-identical) — awaiting land** |
| **section-per-edge-fill** | the per-edge FILL — per-edge depth + divider + bent corner (`cw+max-adjacent`) + handle alignment. Seed = `SECTION.md §3.3`. Foundation (①a+swap) landed `531db59`; **LANDED (Metcalf, `b5c7e98`) — handles re-anchored to frozen iA (`fix(section)`); residual = the no-mouth dogleg → junction-datum-repair below.** Corner-disk → tangent-slice (Plumb's Fix B) still open. | **landed; corner-disk fix open** |
| **junction-datum-repair** | the no-mouth-side T-junction doglegs — reconcile per-fe `pavementHW` datum steps upstream (Sextant root). `HANDOFF-junction-datum-repair.md`. | **dispatch-ready (FRESH/worktree; Boz may do it)** |
| **section-forensic / junction-finish-forensic** | ✅ DONE (Plumb → `SECTION-FORENSIC.md`; Sextant → `JUNCTION-FINISH-FORENSIC.md`) — corner=disk-primitive + handle=anchor-mismatch; no-mouth dogleg=THRU width-datum step | done |
| pipeline-reconception | tile re-pour umbrella (T1–T4) | NOW (context) |
| tile-feature-ledger | re-pour definition-of-done | NOW (reference) |
| tile-T3-authoring · survey-section-tool-design | polygonize Survey / authoring migration | NOW |
| name-logic-skeleton-pass | skeleton fortification (welds + kink-straighten) | NOW |
| band-fold-fix · junction-band-thorns-FINDINGS | thin-tile thorn clamp | NOW |
| boundary-trio | circle-SSOT + crop-to-circle | NEXT |
| asphalt-as-ribbon · dead-end-typology | Survey-side authoring capabilities | NEXT |
| render-conformance | env parity (DRAFT) | AWAITING (cold-review) |
| buildings-bake · tree-hero-lod | slab/3D | AWAITING (QC) |
| audit-{cartograph,arborist,ls-app,docs} | forensic pathology | STANDING |
| toy-to-stage-bake · cloud-specialist · dead-end-spike-prune | parked | LATER |
