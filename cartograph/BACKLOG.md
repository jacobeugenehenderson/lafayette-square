# Cartograph Backlog

> **STATE + forward-work only** (`BOZ.md` doc architecture). No diary (→ `NOTES.md` + git), no how-it-works (→ Reference: `SKELETON` / `PREBAKE` / `SURVEY` / `SECTION` / `RIBBONS` / `ARCHITECTURE` / `PIPELINE` / `STAGE` / `PREVIEW` / `BAKE`). **When an arc lands it LEAVES here:** *fact* → Reference, *why* → `ARCHITECTURE` Decisions, *narrative* → `NOTES`, *dispatch brief* → `_archive/handoffs/` (dated). Landed work is NOT logged here.
>
> *Rewritten 2026-06-22 (the doc-pristineness campaign): the NOW reframed from the SHAPE closeout to the publish-confidence frontier; landed narratives moved out (the pre-DataWall closeout, the tri-cut, the Section build, the code-landed ledger → `_archive/` + Reference); the HANDOFF index reconciled to the 15 live briefs (27→15 prune, wave 1+2). Deep pre-rewrite history: git + `NOTES.md`.*

Active branch `curb-offset-draw` (off trunk `cartograph-looks-pass-ab`). Production (`main`) is intentionally pre-slab + stale; the marriage-leap (merge → deploy) waits until the whole platform is up on staging (LS-side gate — `ls/BACKLOG.md`).

---

## 🔥 NOW — the publish-confidence frontier (the look, mobile-viable, measured)

> The active front. The map renders; the LOOK is authored. The work is making the deployed runtime render **identically to Stage on every device** and letting the operator **publish-and-know** — the STAGE/PREVIEW→PRODUCTION conformance + the mobile-viable render path. ⚠️ **These arcs all converge on `Scene.jsx` / `PostProcessing.jsx` / `PreviewApp.jsx` — serialize them** (surface to Boz before editing those). The single biggest trust-gate is **real per-device numbers** (below) — without them every "✅ ships" is false confidence.

- **Post-FX follow-ups** — DoF + bloom *landed* 2026-06-26/27 (the landed state LEFT here per doc-doctrine → `ARCHITECTURE.md` Decisions + `OPERATIONS.md` knobs + `FEATURES.md`; arc/lessons `NOTES.md` + `_archive/HANDOFF-real-dof-2026-06-27.md`). Forward work, smallest-first: (a) bloom **"blend" slider** — ⏸️ **PARKED 2026-06-27 (approach failed).** Tried a subtraction-lerp `max(fine − coarse·(1−blend), 0)` to mix broad bloom back in — but it keys on **brightness**, and the Arch body + the open sky are *both* uniform-bright with the sky dominating → it washed the **whole scene**, reintroducing the exact absolute-brightness wash the band-pass exists to prevent (`CustomBloom.jsx` header). Reverted (uncommitted). **A screen-space bloom can't isolate "Arch body" from "sky" by brightness** — body-glow needs to select the *geometry*: **depth-gate the broad bloom to exclude the sky dome** (far/max depth) or localize to the hero-pocket (reuse the DoF anchor). Revisit as a real arc, not a slider. *(Separately: bloom **intensity ceiling** opened 2→3 the same day so the glow can blow out on purpose.)* (b) the **Stage timeline-shift-on-edit quirk** — ✅ **FIXED 2026-06-27 (eye-pending):** the keyframe write now targets the **explicit stamped slot** (`createGroupChannelActions` `set${cap}(key,value,slotId)` + `TodChannel.onFieldChange` passes `editTarget`; the playhead is keyed by slot id whose time is already stamped, so the write needs no clock) → editing no longer jogs the playhead; the scrub-on-edit retired. Hosts threaded (StageApp ArchLight/Lantern · CartographPost · CartographSkyLight · CartographSurfaces). ⏳ **Meteorologist parity follow-on:** Teacup's `setCloudParam` (separate store) still infers slot from the playhead → same edit-while-scrubbed-away edge; give it the explicit-slot arg too. *(NOT a blocker — Jacob, 2026-06-27.)* (c) **bloom TOD-curve tuning** (band-pass contrast runs SMALLER than absolute brightness → `threshold` wants LOWER values across the day); (d) **Phase 4** — the LoD cover (leaf taper *within the one tree program*, gated by the blur zone; arborist-side) + **Phase 5** — day/night budget (DoF/Halo day-dominant → off the night budget, twilight cross-fade). **✅ DoF is now per-slot / TOD-animatable** (`4ca9ab5f`, 2026-06-28 — the mount gate is animated-aware: a `dof` keyframe in one TOD slot no longer forces the effect on across the whole day-cycle; `dof` promoted alongside lantern/ambient/exposure/halo/mist/warmth/dirMoon/fill). *(The Preview post-FX fork: the DoF **driver** half was unified 2026-06-27 — `src/components/dofDriver.js`, one `applyDofFrame` called by BOTH `PostProcessing` + `PreviewPostFx`; the silently-wrong URL-param fork excised, `?dofDebug` kept. A tactical precursor to the structural retire below — the shared driver is exactly what the installer reuses.)*
- **⭐ `HANDOFF-render-pipeline-install.md` — the v1 render-pipeline install method (NEW, design draft 2026-06-27).** The FOUNDATION for parity: one pipeline **manifest** → one **installer** (parameterized by mode — production / Stage / Preview-with-`inspect`) → inspection (toggles + cost gauges) as a wrapper, **not a fork**. **Retires `PreviewPostFx`** (the post-FX fork that drifted — Preview's DoF driver was silently wrong). Makes `render-conformance`'s "Preview == Production" *structural*; absorbs `preview-measurement`'s toggle/gauge + the per-platform inclusion-manifest as installer capabilities. ⛔ **Standup on the manifest shape + the 4 open decisions BEFORE any code** (high blast radius: `Scene.jsx` / `PostProcessing.jsx` / `PreviewApp.jsx`). **⭐ Manifest-framing correction (Jacob, 2026-06-27):** for pyramid-backed effects (bloom/DoF/far-blur) the per-platform manifest is **resolution-bracket / degree, NOT on/off inclusion — everything ships to every surface** (`preview-equals-pyramid-tier-ladder`). **B (immediate follow-on to the DoF-driver unify):** the concrete fork the installer must convert is production's `PostProcessing.jsx` `IS_MOBILE` branch, which still **hard-drops the whole pyramid stack** (no DoF/bloom/AO/aerial on mobile) — replace the *drop* with a low **bracket position**, not exclusion. ⚠️ mobile runs smooth *because* it's stripped today (Jacob: "runs beautifully" 2026-06-27) → re-adding effects at low res must stay inside the device budget (eye + device gated, not blind). **Suite-wide doc fix owed:** `PREVIEW.md §0.2/§3` say "which channels ship to desktop vs mobile" (on/off) — correct to degree/resolution for pyramid-backed effects, exception-flag any genuine on/off survivors. **Carry-through note:** `dof` is now TOD-animatable per-slot (`4ca9ab5f`, landed ahead of this arc) — when the manifest/installer is built, dof's per-slot animated behaviour must survive the install (don't regress it back to a static mount).
- **`HANDOFF-preview-measurement.md` — the publish-confidence instrument.** Device-profile SSoT landed (`c1cc244`; `phone-hi`=iPhone 16 Pro Max ceiling / `phone-lo`=Galaxy A54 floor / `desktop`). ⛔ **GATED on two decisions:** the desktop-budget doctrine, and **real per-tier phone numbers** (today phone-hi==phone-lo, both guessed). Then: re-aim gauges to %-of-budget, the virtual-device emulator (workload path = render-conformance Ph4 + supersample strain), the thermal / memory / transition-spike gauges, and the **per-platform channel-listing** (the inclusion manifest, Preview-authored).
- **⭐ Trees don't reliably render — need a "demander" that forces the trees in no matter what (Jacob, 2026-06-27, IMPORTANT).** Symptom: the InstancedTrees sometimes never draw (suspected async-load race — geometry/atlas resolves *after* the last requested frame, so on an on-demand loop nothing re-renders the trees until an interaction nudges a frame). Fix direction: an explicit `invalidate()` / forced demand-frame when the tree assets finish loading (or a guaranteed mount + render request), so trees appear unconditionally. ⛔ **Route the tree-render canon BEFORE coding** (`BATON-tree-render-next.md`, `InstancedTrees.jsx`, the frameloop/demand + atlas-cache history — memory `bloom-foliage-freeze-shared-pyramid`, the Grove context-loss/stale-atlas threads). Its own arc; flagged here so it isn't lost.
- **`HANDOFF-render-conformance.md` — env parity.** Ph1–3 landed (desktop-LOG, camera-reads-slab, dedup). Open: Ph4 (virtual phone = mobile path → folded into preview-measurement §2a), Ph5 (mobile policy → 3 homes), Ph6 (parity cleanups), Ph7 (self-serve bake/test loop). Cold-review gate before dispatch.
- **`HANDOFF-mobile-profile.md` — what's mobile.** Sensing consolidated (one `isMobile.js`). Open: Phase B (`INSTANCE.mobileQuality`, sync), Phase C/E (the per-Look inclusion channel, now **Preview-authored** — reversal B). Lands after the measurement regime + the Scene.jsx work.
- **`HANDOFF-channel-variant-cascade.md` — per-shot + per-platform channel VALUES.** A sparse override cascade keyed `{shot?, platform?}`, most-specific-wins (base → shot → platform → TOD curve). Same mechanism for "Hero ≠ Browse lighting" and "brighter neon on mobile." **Highest convergence → lands LAST; design-doc first.**
- **`HANDOFF-authoring-session-hardening.md`.** Ph1–3 landed (restore-where-left, the settle-gate, Bake & Preview). Open: Ph4 (one camera home `useCartographCamera` + de-dup CameraRig — touches the conformance/DoF files, serialize), Ph5 (doc fortification).
- **`HANDOFF-hero-camera-authoring-mode.md` — Hero camera authoring/runtime modes.** BUILT (`0141c60f`), **eye-gate pending**: click a Hero keyframe dot → pause + jump + free orbit to reposition (Hero Lock kept — orbit pivots the subject); **Save keyframe** → re-lock, stay paused. Default = runtime, playing. No keyframe-model / bake / `SLAB-CONTRACT` change. On eye-confirm: retire the HANDOFF → `NOTES` (already drafted there).
- **`HANDOFF-section-perf.md`.** #1 block-local `sectionGeos` + #2 figure-ground gate + toggle-cost all landed; **drag-fluidity eye-check pending** (Jacob). #3 dead per-tick overlays = cheap cleanup.
- **Lamp-placement authoring (queued).** Lamp positions are hardwired to `street_lamps.json`; the `lantern`/pool channels (landed) author the *look*, not the *placement*. Queue an add/move/remove affordance → re-bake.

**Authored 2026-06-28 (Wren):** the full **7-slot TOD day-cycle look** for lafayette-square + its artistic arc (`bc9a74ea`, `986a4f8f`) — lantern/ambient/exposure/halo/mist/dof/warmth/dirMoon/fill all promoted to TOD-animated channels.

**Landed 2026-06-22 → Reference (not logged here):** the ground-contact trio (lamp pools + contact shadows + trunk-base blend, baked into the ground), the `lantern` channel, arch foot-uplights (`archLight`) — `STAGE.md`, `SLAB-CONTRACT §3.1/§3.2`, `BAKE §2`, `FEATURES`. The Focus/DoF + camera-SSOT + Halo + on-pan-flicker (2026-06-21) — `STAGE.md`/`PREVIEW.md`.

---

## 📐 the kit-correctness track — drive `# curated` → 0 (automate the eye)

> The SHAPE campaign: make the skeleton produce correct street geometry **automatically** so LS's **35 `source:'curated'` hand-fixes** can be deleted and the auto-pipeline still holds. This is **kit-forward correctness** (town #2…N), distinct from the v1-render frontier above. **The KIT INVARIANT** (`SKELETON §6` · `INTAKE §6.1`): hand-authored SHAPE is a *defect*, never a fix. The data question is CLOSED (no external street source helps — `INTAKE §5.1`); the lever is the skeleton's *interpretation*. North star: *Survey shows the perfected map straight from the skeleton.*

- **⭐⭐ The correctness detector IS the deliverable** (not the 35 fixes) — `POLYGON-FIRST §5`, harness `scratch/correctness-detector.mjs`. 8 invariants seeded; **`junction-band` is the recall instrument** (24/25 structurally-visible curated defects; the defect is pervasive ~60 junctions), **`width-step` the flagship** (the datum-step class). The 35 are mostly **topological** (loops/cul-de-sacs + seam datum-steps), not jagged curves — so the suite's centre of gravity is graph-level. Findings: `SIEVE-DETECTOR` · `LOOM-TOPO` · `THROAT-JUNCTION-FINDINGS`. The detector flags the class everywhere → CI gate → onboarding loop (run-suite → fix-class → green).
- **The open construction (the bug classes still to kill generally):**
  - **`HANDOFF-curve-primitive-skeleton.md` — curves as bezier primitives.** Ph1 (centerline beziers) + Ph2.1 (robust curve curb) landed; the junction-curb bump RESOLVED. ▶ **v2: loops + divided carriageways as closed/paired beziers** (the cul-de-sac faceting + Benton/Waverly/divided medians) — Benton-gated; add the `curve-faceting` detector. *(Absorbs the old round-skeleton-corners / concentric-curb / vector-curve framings.)*
  - **`HANDOFF-junction-construction.md` — intersection-everywhere.** Construct the junction polygon at EVERY node (trim-back + corners-by-clockwise-adjacency = the osm2streets standard). The junction MAP spans every node + corner-pairs are frozen; only the GEOMETRY (apron + edge-collision trim) is divided-only. Probe derivation-first per class before constructing. *(Absorbs the perpendicular-join protrusion + the divided-junction termination.)*
  - **`HANDOFF-band-fold-fix.md` — the G12 thorn clamp.** ⚠️ **NOT landed** (`8e1e414` stranded; `thinTile` orphaned to `bandJoin`, no depth clamp). The LOCAL capacity clamp (engage on partial-degeneracy without over-clamping). Two subclasses, both open. *(Absorbs the junction-band-thorns forensic.)*
  - **`HANDOFF-freeze-the-curb-in-the-first-bake.md` — freeze the curb geometry (the last unfrozen polygon).** D2 froze the tile *topology*; the curb is still re-stroked live (the "d" bulge — its divided-transition instance landed `fd38c70`, but the program is the freeze). Emit the clean parallel-offset curb once in prebake, consume it; only the element under the operator's hand re-strokes. ⚠️ **Rebuild-gated → PARKED.** Phase 2 (block-local edit loop) is the FILL-perf sibling of section-perf #1.
- **Skeleton fortification — name-aware weld** (`SKELETON §5b-bis`): weld 35 same-name fragments + straighten within-name kinks (St-Vincent/Papin artifacts vs Park-Place real bends). Partly addressed by the `roadId`/`continuesAs` union.
- **`HANDOFF-tile-T3-authoring.md` — polygonize Survey / migrate authoring onto tiles.** Move handles / corner-R / cap-selector / strip-swap / translucency off the dead figure-ground; activated-block live redraw (the sticky-tools fix); then **T4 deletes figure-ground**. *(Absorbs the design spine + the injective corner-handle slice.)* The **Survey · Section · Stage rename** rides this (don't rename code until implemented).
- **`HANDOFF-tile-feature-ledger.md`** — the tile re-pour definition-of-done ledger (tracking matrix; the construction side is ✅, T3/T4/slab-content remain).
- **Deferred SHAPE residuals** (`[[feedback_accept_the_long_tail]]`): the intersection-everywhere finish (better-but-not-perfect nodes — = the junction work) · the curve-and-cut class (a block edge curving-then-cutting near a junction).

**Landed → `_archive/handoffs/` + Reference (not logged here):** the pre-DataWall closeout (surface-and-wire G1/G3/G4, edge-of-map perimeter faces, divided-"d" curb, cul-de-sac keyhole, the walked-face median, intersection-everywhere `9c275ce`, the Section build, loops/dead-ends/cap-wrap/handle-float/alleys). The honest "done" test (code-landed ≠ eye-verified) lives in `DOC-CODE-COHERENCE.md`.

---

## 🛡 HARDENING — correct on the eye, not yet correct-by-construction
- **D3 — corner identity at divided transitions → freeze at prebake.** The false-corner cure is LIVE (`9c275ce`); doctrine wants the decision made once upstream as frozen topology (`PREBAKE §5`). Sibling of the curb-freeze. ⚠️ Relocating re-derives corners → gate it; don't cleanup-refactor.

## ⏭ NEXT
- **⭐ "Revert to Data" buttons — Survey + Section.** Strip the authoring overlay back to base (Survey → skeleton surveyed widths; Section → the gleaned treelawn-Y/N × ADA calc). Mechanism partly exists; generalize + surface per-tool. `SECTION §8`.
- **`HANDOFF-boundary-trio.md`** — circle-SSOT, crop-to-circle-last, bake-time stencil-cull (the edge-of-map face-walk half landed; the cull/SSOT + the S1 elbow→fade residual remain here).
- **Slab-content remaining** — overlays + highway-class asphalt (alleys/footways landed `735f02c`/`543bdeb`).
- **Story-pass address-maps** — Meteorologist + Arborist (`[[project_story_pass_campaign]]`).

## 🖥 GPU-perf gotchas (live)
- **⚠️ Bake-ordering footgun:** a ground GEOMETRY re-bake (`bake-ground.js`) silently drops the AO `lightmap` → flat-lit slab. **Always run `bake-ground-ao.js` after** (the GUI `/bake` chains them; only manual CLI bakes hit this). Outstanding: make `bake-ground.js` preserve the lightmap or warn.
- **tri-cut follow-ups** (tri-cut LANDED, fact in `OPERATIONS §Ground tri-budget`): (1) import the `V_EXAG` SSoT into `bake-ground.js` (hardcoded `1.5` would silently facet a future exaggeration bump); (2) expose `GROUND_REFINE_TOL_M` as an `INSTANCE.mobileQuality` tier → `mobile-profile`.

## ⏸ LATER / PARKED
- **⚠️ Sky-colour overrides are keyed by CLOCK HOUR (seasonal grid), not by TOD slot.** A sunset/dusk colour authored for one season won't track the slot in other seasons (the slot's clock time shifts with the season, the override doesn't follow). Future: **slot-keyed or per-season sky-colour authoring**. The Sky Builder is the current live tool. *(Known limitation surfaced 2026-06-28 while authoring the 7-slot TOD day — `bc9a74ea`/`986a4f8f`.)*
- **Onboarding / Intake** — Provincetown first run; designed, no code. `[[project_intake_onboard_process]]`.
- **⭐ Custom street data — import + manage (KIT)** — generalize LS's `survey.json` (operator-measured widths, the base-width source: custom → OSM → AASHTO) into the Intake/Onboard flow. `SKELETON.md`.
- **Tile-bake footprint (P1) / curve-fineness (P3)** — profile before cutting (`tile-feature-ledger` E-rows).
- **Hosted bake service w/ auth** (v2+). · **Hero keyframe FOV regression · TAA post-pass** — not urgent.

## ⏸ AWAITING JACOB (QC / eye)
- **Grade-sep** faces-filtered + stroked-as-asphalt — live re-bake eye. · **Hero-tree LOD** → now the real-DoF cover. · **Render-conformance Ph4–7** — cold-review gate.
- **Section drag-fluidity** (section-perf) · **divided-"d" curb + perimeter faces** (eye-pending the Section/Stage re-bake).

## 🏛 STANDING CAMPAIGNS
- **⭐⭐ The correctness suite — DEFEAT hand-fixing** (the kit's correctness backbone) — see the kit-correctness track above; doctrine `POLYGON-FIRST §5`.
- **⭐ The doc-pristineness campaign (ACTIVE — pristine docs by v1).** The living Process (`BOZ.md`) + `DOC-CODE-COHERENCE`. Done: the HANDOFF prune (27→15). Open: the ls/ + cross-domain accord sweep (ls/STATUS + ls/BACKLOG stalest); the STATUS keep-or-fold call; codify the doc-currency mechanism (dated `verified` headers + currency-by-pointer) into `BOZ §3`; the gitignore-`_archive`/`git mv` archiving gotcha.
- **Slider ranges → 0 "Extreme"** (touch-as-you-go, `scratch/AUDIT-slider-ranges.md`). New channels (DoF/Focus/lantern/archLight) born to it.
- **Forensic audit + productization → Show Bible** — LANDED (`AUDIT-MATRIX.md` master matrix + `SHOW-BIBLE.md`). Remaining: execute the removal queue (🧊 **post-v1 only**); route Tier-1/2/4 into `plans/`; re-audit the held ribbons/corners zone; build out `OPERATIONS` as T3 lands.

## 🛠 LIVE HANDOFFS — the 15 live briefs (dispatch layer)

> Honest labels (`DOC-CODE-COHERENCE §Landed-forensic`): `landed` = ancestor-of-HEAD + code-path-live · `eye-pending` = code-landed, unconfirmed on the lit app · `⚠️ NOT landed` = claimed but absent. The 12 landed/OBE/folded briefs are in `_archive/handoffs/` (dated).

| Brief | Arc | Status |
|---|---|---|
| **real-dof** | the look + mobile-viable DoF cover | Focus channel landed; mobile-viable gather + selector = NEXT |
| **preview-measurement** | publish-confidence instrument | device-profile SSoT landed; **real per-tier numbers = the gate** |
| **render-conformance** | Stage↔Preview↔Production parity | Ph1–3 landed; Ph4–7 open (cold-review) |
| **mobile-profile** | what ships to mobile (3 homes) | sensing done; B/C/E open (Preview-authored inclusion) |
| **channel-variant-cascade** | per-shot + per-platform channel values | design-doc first; **lands LAST** (highest convergence) |
| **authoring-session-hardening** | restore-where-left · safe bake · one camera home | Ph1–3 landed; Ph4 (camera) + Ph5 (docs) open |
| **section-perf** | the FILL re-stroke perf | #1/#2/toggle landed; drag-fluidity eye-pending |
| **curve-primitive-skeleton** | curves as beziers | Ph1/2.1 landed; **v2 (loops/divided) open** |
| **junction-construction** | intersection-everywhere | metadata spans every node; geometry divided-only |
| **band-fold-fix** | G12 thorn clamp | ⚠️ **NOT landed** (`8e1e414` stranded) |
| **freeze-the-curb-in-the-first-bake** | freeze the curb polygon | "d" instance landed; the freeze program PARKED (rebuild-gated) |
| **boundary-trio** | circle-SSOT · crop-last · cull | post-tile-arc; edge-of-map face-walk half landed |
| **tile-T3-authoring** | authoring → Survey (off figure-ground) | open; gates T4 (delete figure-ground) + the rename |
| **tile-feature-ledger** | re-pour definition-of-done ledger | tracking; construction side ✅, T3/T4/slab-content open |
| **cloud-specialist** | cloud morphology renderer | TABLED (flagship IP; the agentic Tuner loop is the design — `meteorologist/TUNER.md`) |
