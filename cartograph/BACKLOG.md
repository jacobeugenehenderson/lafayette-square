# Cartograph Backlog

> **This doc is STATE + forward-work only** — *where we are* and *what's next*. One kind per doc (see `BOZ.md` → doc architecture): no diary here (that's `NOTES.md` + git), no how-it-works (that's the Reference layer: README / FEATURES / ARCHITECTURE / PIPELINE / RIBBONS). When an arc completes, its *fact* updates Reference, its *why* updates ARCHITECTURE's decisions, its *narrative* flows to NOTES — it does **not** stay here.
>
> **Ribbons / corners / curbs / intersections / block geometry:** every such entry is governed by `cartograph/RIBBONS.md` (§1 regime + §3.9 emitters + §6 failure modes) and `cartograph/PIPELINE.md` (the address-map). Read before working.
>
> *Pruned from a 4,567-line chronological diary on 2026-05-31. The full pre-prune history — including the ★IP NACTO/AASHTO pad-geometry + ADA corner-radius decisions — is preserved at `git show 4bf8af2:cartograph/BACKLOG.md`. Salvage of those decisions into ARCHITECTURE's Decisions section + diary into NOTES is deferred to a later consolidation pass (see §Triage).*

---

## 🔥 NOW — the active front

- ✅ **RESOLVED — Measure handles broken on toy after the V1.6 reset** (`dac2301`, 2026-05-31). Root cause: the store hydrated `centerlineData.measure` through a third-tier fallback (`rb?.measure`) keyed off a **hard-coded LS ribbon import**; once V1.6 correctly stripped `chain.measure` from the toy overlay, toy chains (`HW*`/`VW*`) found no match → `undefined` measure → `sideBoundaries()` rendered no handles. The baseline was right; the fallback was scene-blind. Fix = scene-aware fixture in `_loadCenterlines`. Diagnosis + lesson in NOTES (2026-05-31). Also landed: hundredth-precision corners slider (`43e7034`). **Toy authoring restored → wall-arc unblocked.**

- 🔀 **THE TILE RE-POUR — the active front (built through 2026-06-01).** The stroke pivot crystallized into the **TILE model**: *tiles = faces of the centerline graph; the centerlines are the grout; paint strips INWARD per tile (`asphalt|curb|treelawn|sidewalk|LU`); round the strips, not the tile.* No figure-ground, no asphalt/block distinction, no complement. Spike-validated (toy), then **built through T2-finish: LS now runs tiles, UNFLAGGED — the construction side is done** (per-edge asymmetric widths, divided-road medians, concentric ADA corners — *the 13-month corner, on Jacob's eye* — dead-end caps honoring `capEnds`, exterior streets roaded, per-LU colour + matching treelawn, strip-material swap-ready; figure-ground dead-in-place). **Umbrella: `HANDOFF-pipeline-reconception.md` · live point-cloud / definition-of-done: `HANDOFF-tile-feature-ledger.md` · program memory `[[project_reconceived_pipeline_program]]` · narrative: NOTES 2026-06-01.**
  - **NEXT: T3 — authoring → Survey/Section onto tiles** (`HANDOFF-tile-T3-authoring.md`; design spine `HANDOFF-survey-section-tool-design.md`). Migrates the whole authoring channel (handles / corner-R / cap-selector / strip-swap / Measure translucency) off the dead figure-ground path; fixes "A2 no work" + lifts the figure-ground perf drag.
  - **Docket after T3:** T4 (delete figure-ground + the RIBBONS/PIPELINE/FEATURES tile rewrite + honest-close §6.8–6.10) → boundary-trio (circle-SSOT, kill the vestigial bbox; `HANDOFF-boundary-trio.md`, refreshed) → slab-content (alleys/paths/overlays/highway-class) → polish (footprint P1 / curve-fineness P3) → rebake + migrate to LS → Arborist forensics / Provincetown intake / Meteorologist / LS-app.
  - ⤵️ *The figure-ground framing below — the 'geometry wall-arc / C5 cutover / stroke-construction' bullet — is **subsumed** by the tile re-pour; prune at the next consolidation pass.*
- 🎯 **The geometry wall-arc — (the prior incremental framing; now reframed under the stroke pivot above)** (the through-line; `[[project_two_bakes_two_walls]]`, `[[project_skeleton_is_the_first_bake]]`, `[[project_ribbon_three_representations]]`, PIPELINE §Wall):
  1. **C5 cutover** — ✅ *flip landed, deliberately descoped* (`567a854`, Transit, 2026-05-31). LS now runs the mono-width `emitBlockRingBands` (3 gate sites → keeper; `blockRounded` is produced upstream of the emitter so the flip stands through the wall-move). **Eyeball verdict (Jacob's eye): LS visually 0% — but diagnosed, not mysterious.** The break is the *pre-wall* architecture, data-verified: LS `blockCustoms` is a **two-regime graveyard** (legacy integer/per-side keys + V2 coordinate/per-fe keys) that **never got V1.6's cleanup**, compounded by **blockKey rounded-vs-sharp drift** on the customs write→read round-trip (`[[feedback_block_key_rounded_vs_sharp_diverges]]`). Both die in the wall-move (H4). ⚠️ The C5 corner "catalog" was built from a **self-authored rasterizer**, NOT the production render — operator-contradicted, optimism void (`[[feedback_proxy_render_is_not_the_operator_eye]]`); see `HANDOFF-ls-bespoke-corners.md` (banner). **Deferred to the wall-move:** Commit 3 (delete `silhouetteStraightEmitter` + `buildFrontageBandsV2` + dead `buildFrontageBands`/`chainPavementRing`) → W4; LS visual correctness → W5 (post-wall re-bake + production-path eval). **Decision: stop polishing LS pre-wall — it's the doomed intermediate.**
  2. **Chain-consumer census** — **DONE 2026-05-31 (Plumb): `HANDOFF-chain-consumer-census.md`** — 33 sites across 3 paths. P9/P12/P13 chain-free; keeper P10 reads chains in *one* fallback; dependency concentrated in P4–P8 producers + 2 metadata leaks + the P11 fillet residual (H1). 6 HARD residuals (H1 fillet / H2 asphalt identity / H3 dead-end caps / H4 id scheme / H5 R-coupled corner geom / H6 segmentMeasures gap).
  3. **Wall-move** — **scoping brief DRAFTED: `HANDOFF-wall-move.md`** (toy-first; 7-artifact First Bake; W1 metadata-freeze → W2 id-scheme/H4 → W3 geometry-freeze → W4 eliminate → W5 LS-bring-across). Success metric = **WYSIWYG** (render conforms to authoring tools — the three-representations collapse, `[[project_ribbon_three_representations]]`). **✅ ALL 5 DESIGN DECISIONS MADE (2026-06-01) — cleared to decompose into dispatchable sub-briefs.** Q2 id-scheme = chain-anchored `(skelId,side,segOrd)`; Q4 LS-customs = clean-slate; Q1 artifact = in-memory-freeze + persist-to-slab; **Q3 dead-ends = INCLUDE NOW** (absorbs `HANDOFF-dead-end-typology`; W3+); **Q5 rename = RIDES WITH** (discrete WR step, W3→W5). **Publish-critical spine = W1 (identity; storage landed `6b83798`) + W2 (metadata) → LS RENDERS** (the census-confirmed 0% fix); dead-ends + rename ride W3+ and do NOT gate it. Sequence: **W1 → W2 → W3 (+dead-ends) → W4 eliminate → WR rename → W5 LS-bring-across.** Block-independence verified — re-pour is block-local. **➡️ NEXT: dispatch W1 (`HANDOFF-wall-W1-identity.md` — REFRESHED 2026-06-01 to a FINISH brief; warm → Vesalius).** Verified: the keystone mechanism already LANDED (`6b83798` + `src/lib/feCustomKey.js` = `blockCustoms[skelId][side][segOrd]`, segOrds→min-segOrd, all read/write sites unified incl. `buildChainBandsLive`). Toy clean `{}`. **Remaining W1 = (a) clean-slate LS** (NOT done — 4 graveyard keys still in `lafayette-square/design.json`; the prime suspect for "live application BROKEN" + the census-confirmed 0% fix) **+ (b) verify the WYSIWYG outcome** (toy + LS edits render, operator's eye). W1 is ~90% there → **LS-renders is close.**
- 🆕 **Onboarding / Intake — the front door + the protoslab** (Jacob design session 2026-05-31; *conversation/scoping, no code yet*. First real run = **Provincetown** as a new Look, `[[project_real_tiny_town_spike_surface]]` · `[[project_intake_onboard_process]]`). Onboarding is a **card stack** of setup tasks; the foundational card *is the map*. Settled design:
  - **Circle-primary input.** Operator authors **(center, radius)**; `bbox` (enclosing square of the *outer* feather), `lonToMeters`/`latToMeters` (from latitude — a correctness fix, Provincetown ≠ LS), tile `zoom`, and the `fade`/`streetFade` bands all **derive**. One source generates both the fetch bbox *and* `neighborhood_boundary.json` (today authored independently → drift). Kills the edit-JSON-rebake-eyeball boundary loop — which has no editor; `boundary.js` consumers re-clip at runtime ~12× in `MapLayers.jsx` + 2 fade shaders.
  - **Surface.** Place-name geocode (Nominatim, keyless) = entry; an interactive **2D top-down aerial picker** (same ArcGIS source as `AerialTiles`, lower zoom) = containment refinement. Feather bands drawn read-only.
  - **Controls.** Extract the Salon's Autodesk-style gizmo (`TreeGizmo`, `SpecimenViewport.jsx:386`, callback-driven) into a shared **`FloorGizmo`**: **center = floor drag, radius = the repurposed rotator-ring handle** (the flat ring *is* the boundary circle; `radius = distance(cursor, center)`). **No rotation, ever** — rotation's *meaning* retired (world-coord-locked; realign = camera), only the ring *handle* reused. `[[project_no_map_rotation_world_locked]]`, corroborated by ARCHITECTURE §7 (compass frame, no rotation). Chrome adopts shared style tokens; Salon's inline styles untouched this pass.
  - **Source = OSM** (decided 2026-05-31; we're invested in the OSM workflow). Onboarding *plumbing* is **source-pluggable at the protoslab wall** (`skeleton.js` source swappable; nothing past P1 cares), so the seam stays open as cost-free future option-value. **Overture assessed and dropped:** for a small town its transportation ≈ OSM-derived + conflated → no meaningful gain over OSM to justify the GeoParquet ingestion build. (Open-consortium, NOT the Google evil-empire model — so not excluded on principle, just not worth it.)
  - **Output = the protoslab.** Finishing the card freezes the First Bake's **container** — geography + boundary + **all** raw OSM layers + the street skeleton spine — and unlocks Survey. Same artifact the wall-move calls the "7-artifact First Bake." Three bakes now, not two: `[[project_two_bakes_two_walls]]` (amended).
  - **Move-the-map = trivial** via **decoupling the fixed coordinate origin from the movable boundary circle**: resize/nudge within fetched extent = re-clip, no shape re-bake; only *growing past extent* needs a re-fetch — ⚠️ `fetch.js` is **one-time/destructive** (README points at `memory/feedback_never_refetch_data.md`, **which is missing — doc-debt, below**), so grow-re-fetch must preserve operator overlay.
  - **Banked payoff:** clip-at-bake retires the runtime boundary stencil → fold into wall-move ledger.
  - **Post-confirm flow (DESIGNED).** One **Confirm**, progressed (not a wizard): derive → write geography + `neighborhood_boundary.json` → fetch (Overture) → skeleton (P1) → freeze protoslab → land in QC. Runs on the backend via a new scene-parametric `serve.js` endpoint (mirrors `/looks/:id/bake`); `skeleton.js`/fetch made scene+bbox-parametric. Operator only *watches* the fetch (counts + failure/retry); rest is black-box. **Re-confirm is extent-aware** (inside fetched bbox → re-clip only, no re-fetch, via `writeIfChanged` dirty-skip; past extent → re-fetch+re-skeleton). Same shape as the in-flight "Toy reset → skeleton bake" item. Aerial needs no fetch (runtime Esri tiles).
  - **QC seam = gate A + comparison oracle (DECIDED).** Explicit "looks sane?" checkpoint (OSM/Overture + skeleton over the aerial, inside the circle) → Proceed→Survey / Adjust-circle / flag. Given as gate; teeth come from a **comparison oracle** (corporate map as *reference*, never baked): **visual** = toggle an Esri `World_Street_Map` tile layer (keyless, same `AerialTiles` machinery) now-ish; **geometric auto-diff** = Overture (open, holdable) later. Proprietary providers (Google/HERE) = display-reference only, never imported (terms).
  - **Open beats (before HANDOFF):** scene-slug source (folder + Look key, must stay scene-parametric per ARCHITECTURE) · all-themes-vs-streets-only scope fork (above) · elevation/DEM as a third onboarding layer (FEATURES data-flow: ~453 MB USGS 3DEP per place + `bake-terrain` clip).
- ✅ **The Osteopathologist — skeleton-frame forensics — DONE 2026-06-01 (Vesalius, `cartograph/OSM-FORENSICS.md` + `scratch/vesalius-*.cjs`).** **VERDICT: the frame is strictly poorer than the raw OSM — the marrow is already there; the First Bake throws it away. "OSM or us?" = 100% us.** ① **79/79 interior T-junctions present in raw at 0.00 m, deleted by junction-blind `simplify()`** (`skeleton.js:375`, devTol 0.2 m collapses the locally-straight through-vertex; `EPS=1e-3` endpoint-only matching) — **Boz-verified: mechanism confirmed.** ② **"Stop dropping it" is most of the fix** — lanes/surface/maxspeed/median-width present-and-discarded at P1; only `width` (0% OSM) + sidewalk/treelawn need seeding, standards seed them — **Boz-verified: lanes=242, width=0 exact; maxspeed≈176; ⚠ surface 261(V) vs 1416(Boz, all-highway) → reconcile denominator, finding stands.** ③ **Extractability PROVEN** (re-runnable prototype): recovers all 338 junctions, types every node (141 T/84 cross/103 dead-end), offset-safe at 33% reduction vs prod's junction-destroying 48%, passes north-star — *(Boz trusts-not-re-run; scripts present).* ④ corner-saga = chains-root, one disease, §Wall confirmed. ⑤ **Dolman→18th = canonical regression case**; fixing it deletes two `derive.js` hacks (West-18th densify, LaSalle magic-coord) = frame-thinness scars. **Clean-slate-vs-migrate input → lean clean-slate but ENRICH-FRAME-FIRST** (migrate a small exception set onto a rich frame, not a large compensation layer onto a thin one). [U] AASHTO paywalled→DOT-secondary; PROWAG/NACTO primary. **➡️ NEXT (the first *forward* brief): P1 frame-enrichment pass — validate vs Dolman→18th, the 79 interior-Ts (must hit 0), and the north-star.** This is the publish path. — *Original brief context follows:*
- ⛓️ *(forensics brief, now satisfied)* **OSM data-forensics** (Jacob, 2026-05-31; read-only; `HANDOFF-osteopathologist.md`).
- ✅⚠️ **P1 frame-enrichment — DONE (Vesalius, 2026-06-01; `cartograph/OSM-FORENSICS-EVAL.md`; staged-not-committed).** Frame is now correct + banked: **79 deleted Ts → 0, typed nodes, caps-as-fact, tags/median/seed carried, Dolman→18th understood, 2 `derive.js` hacks removed, chain bakes clean.** Code surgical (`skeleton.js` +211, `derive.js` +38); A/B backup intact (1.19→1.35 MB). **BUT ZERO visible render change — and *that is the finding*:** Boz-verified the render's blocks+intersections consume **raw osm via `pipeline.js`, NOT `skeleton.json`** (skeleton read only by derive.js/migrate/serve). **We enriched a bypassed sidecar.** → exposed the PALIMPSEST (below). Frame work = keep/commit, do NOT revert; it's the foundation + the probe that found the subfloor. `[[project_the_palimpsest_code_path_multiplicity]]`.
- 🆕🔬 **Render-path census — THE NEW PRIORITY (read-only; before any more building)** (Jacob's palimpsest insight, 2026-06-01). The meta-problem is **code-path multiplicity** — dead/stale/parallel/bypassed paths from many iterations; fixes land on non-live code. Map **THE single live path raw-data→pixels for LS** + inventory the carpet/linoleum to pull up (skeleton-bypass, dual emitters, customs regimes, two-step skeleton/pipeline, dead funcs, vestigial bbox). Same playbook as Plumb's chain-census + Vesalius (works). **Sequenced AHEAD of Layer-2 + the boundary-trio** (both would build on the palimpsest); the census likely reframes them + the wall-move as ONE program: *route the render through a single source.* This is the publish path. **✅ DONE 2026-06-01 (Vesalius, `cartograph/RENDER-PATH-CENSUS.md`).** Verdict (Boz-verified): **(1) the 0% cause is the `blockCustoms` two-regime graveyard + blockKey drift — NOT the frame or emitter** (vindicates "stop polishing LS pre-wall"). **(2) Correction: the skeleton DRIVES block shape** (`ribbons.streets`←`skelStreets`, `derive.js` L2314); only intersections+faces bypass to raw osm (L1056) — "bypassed sidecar" was half-wrong (Boz over-concluded last turn; corrected). **(3) The render emitter already collapsed to one path** (`emitBlockRingBands` both surfaces; legacy per-leg parallel-dead) — collapse ~80% done at the emitter, 0% at the data source. **(4) P1 was invisible** = colinear verts + enriched fields dropped at the `derive.js` L2909 serializer (Boz-verified) + broken customs downstream. **`derive.js` is the palimpsest's spine** (two-source: raw-OSM faces/ix + skeleton ribbons). **COLLAPSE PLAN (priority): ① wall-move H4 stable-id customs FIRST (the only 0% fix; = the id-scheme + clean-slate-vs-migrate decisions; Vesalius lean = clean-slate-but-enrich-frame-first) → ② Layer-2 faces+intersections on-frame (P1 becomes visible) + cheap serializer-widen → ③ boundary-trio + dead-code sweep (W4).** Layer-2 + wall-move + boundary-trio = ONE move (one frozen id-stable skeleton-sourced artifact at Survey-exit). **➡️ NEXT: Jacob's 5 wall-move design decisions (the gate on `HANDOFF-wall-move.md`) — H4 + clean-slate-vs-migrate are load-bearing.** Turn the proven prototype into the real enriched First Bake → swap into LS → measure the off-the-shelf delta. Sequenced lowest-risk-first: ① junction-aware `simplify` (79 interior Ts → 0) ② node typing (cap-as-fact) ③ carry lanes/surface/maxspeed/median + standards-seed width & treelawn/sidewalk (NACTO-by-class R) ④ Dolman→18th resolved in-frame, 2 `derive.js` hacks deleted. **Honest calibration:** upstream of the wall-move — makes the frame correct + shrinks the customs-need (clean-slate-but-enrich-first), does NOT flip LS's downstream-0% to 100%; eval at Survey/Designer render + frame metrics. Frame-only A/B (preserve old `skeleton.json`).
- ⏭️ **Boundary trio — the fast-follow after frame-enrichment** (Jacob, 2026-06-01; **brief drafted, dispatch-ready: `HANDOFF-boundary-trio.md`**; dispatch AFTER the frame A/B lands — shares the LS bake). The unifying idea: **the circle is the only real boundary; bbox derives from it, everything crops to it.** (a) **circle-SSOT, bbox derived** as the enclosing square of the outer feather (retire the vestigial independently-authored bbox → kills the drift class); (b) **build on the full extent, crop to the circle LAST** (early truncation breaks edge junctions — same disease at the boundary; "the map keeps going" past the soft crop → correct edge geometry); (c) **systematic bake-time stencil-cull** (`faceInBoundary`/`pointInBoundary`/`clipPolylineToBoundary` exist) replacing the **manual out-of-neighborhood toggles** (vestigial-UX retirement; smaller slab). OUT/flagged: the full runtime-stencil retirement (MapLayers ~12 `pointInBoundary` + fade shaders) = the banked clip-at-bake payoff → wall-move. Kept OUT of the frame brief to preserve a clean A/B. **⭐ PUBLISH-CRITICAL-PATH (reframed 2026-06-01):** LS renders at **0% and the marrow is why** — better bones are the *prerequisite* to a publishable map, not a foundation nicety. The customs-graveyard / wall-move pain are *symptoms of a frame too thin*. **This is where Jacob's multi-month "journey back" ends:** symptom → substrate (polygons+wall solve it) → **frame (the skeleton)** = the first artifact *we* author, the root of our own interpretation. The step further back (data *source*) was tested tonight and found ≈OSM — empty — which is how we *know* it's the floor. Forward from here. BEFORE designing skeleton refinement, characterize the raw material. **#1 deliverable = the ATTRIBUTE TRIAGE** (the real prize, bigger than geometry-cleaning). The semantics we want — width, lanes, oneway, dead-ends, divided/median, surface, turn restrictions, … "etc forever" — aren't infinite; sort every field into four buckets on real OSM: **(a) Kept** — `skeleton.js` already carries `{name, highway-class, oneway, divided-classification}`; **(b) In OSM but IGNORED** — `width`/`lanes` are *never even read* (only `name`+`oneway` are), divided carriageways are *collapsed to a centerline so median width is thrown away* → seedable (est. width from `lanes`×~3.5 m where `width` absent); **(c) Topologically derivable** — dead-ends, connectivity, junction type → compute + emit, don't ask anyone (the stuck "dead-end typology" HANDOFF lives here); **(d) Genuinely residual** — where OSM is missing/wrong → operator fortifies, and THIS is the aerial's real narrow job (measuring backdrop, not data oracle). Key fact: the loss is at P1, **not** the spigot — `fetch.js` stores `way.tags` wholesale, so the protoslab container still holds full semantics; they're recoverable whenever we thread them. **Triage answers "how do we tell the system effectively": for most of the tail we don't — ingest or derive; only the residual needs authoring.** Then the geometry/topology prongs — **"better bones":** **(2) what transforms make it useful** — the two named failures are **intersection consolidation** ("680 nodes in one IX" → one logical intersection node; cluster coincident nodes) and **over-noding** ("3 M segments in a chain" → Douglas-Peucker/Visvalingam simplification), plus weld + topology repair; characterized not built. Forensic q isn't "does it snap" (it does — EPS `ptsEqual`, `snap.js`, `rejoin-splits.js`, welder) but **why current snapping misses the blobs** (EPS radius / cluster span / dual-carriageway multi-node junctions / over-noded imports). **(3) connect the dots** — assemble elements into a connected graph, *by what* (algorithm / AI-inference / operator) — **survey prior art, don't reinvent:** `osm2streets` (A/B Street) has an explicit "intersection consolidation" pass; routing-graph builders (OSRM/Valhalla/GraphHopper) cluster+simplify to build clean graphs; JOSM validator rules. **⭐ Strategic upside:** corners are derived *at* IXs (`cornersAtIx`, P7) — a 680-node IX dooms corner derivation, so clean intersection bones may be the **upstream lever that relieves the 13-month corner saga** (PIPELINE §Wall: chains-root-problem + corner-confusion = one disease, one cure). Possibly the highest-leverage item on the board. **Deliverable = a characterization report** that makes the refinement brief + the onboarding QC-gate's "what to flag" obvious — *evidence before excision* (`[[project_forensic_audit_and_productization_campaign]]`, `[[feedback_geometry_briefs_need_artifact_inspection]]`). The §Wall standing-debt attack: make P1 the clean bake (`[[project_skeleton_is_the_first_bake]]`). ⚠️ **Read-only forensics + recommendation ONLY** — building refinement is the 13-month chains-swamp; the report scopes the *next* brief. Fixture: LS's existing `osm.json` now; PTown's once onboarding fetches it. **Oracle = the aerial** (license-clean independent ground truth, already rendered) + operator's eye; Google/proprietary maps are *operator-in-another-tab* reference only — **our code never ingests them** (ToS: no drawing their geometry, no building a dataset from their content).
- **Toy reset → skeleton bake, deploy editing tools from that state** (Jacob, in-flight; two-button reset shipped `ea7c754`, post-V1.6 baseline `8a1d9d1`; design HANDOFF retired to git `2854aa3`).
- **Toy → Stage bake path** doesn't carry through (HANDOFF-toy-to-stage-bake). Reframed as Slab-Players: fold into render-conformance Phase 6 (Preview-inherits-Stage) rather than patching the path — unless it blocks the operator now, then patch tactically.

## NEXT

- **Grade-separated roads — faces filtered + stroked as asphalt** ✅ LANDED (Theodolite, 2026-06-02). `buildTileGround` excludes `gradeSeparated` streets from the face graph (kills the degenerate interchange triangles/slivers/false-blocks) and strokes them as flat asphalt strips off `measure.pavementHW` in the same pass — they paint like alleys, no bare render. A/B verified at the I-44 corridor: interchange degenerates clear, highway/ramps render, ordinary blocks/junctions unaffected. Wall held (sectionPass 0 chain refs). Pending Jacob's eye on the live re-bake.
- **Survey · Section · Stage rename** — *decided, not yet built.* Tools: Survey = hardscape shape (footprints + corners + curb + polygon-shaper); Section (was "Measure") = ped-zone chop; Stage = look. Rides with the wall-move arc. ⚠️ Do NOT rename code/FEATURES/MeasurePanel until implemented (stale-label rule). (`[[project_two_bakes_two_walls]]`.)
- **Story-Pass address-maps for Meteorologist + Arborist** — same template as PIPELINE.md (`[[project_story_pass_campaign]]`).
- **Doc-consolidation Process** (the living Process, `BOZ.md`): P3 RIBBONS §5/§6/§7 freshness+archive (other Boz owns) · P4 NOTES (4,369-line diary) + this BACKLOG ✅ done · P5 archive completed HANDOFFs · P6 helper docs. ARCHITECTURE grows a Decisions section.
- **Asphalt-as-ribbon** (HANDOFF-asphalt-as-ribbon) — authored outer-extent for asphalt. Reframed as Survey-owns-SHAPE; likely a Survey capability post-wall-move, not an emit-side addition. Destination set; timing by urgency.
- **Dead-end / Spike typology** (HANDOFF-dead-end-typology) — endpoint types (Spike / Stub-with-cap / Stub-no-cap). Chain-consumer-census territory; reframe as "what Survey-side authoring do chain-endpoints become?"
- **Hero keyframe FOV regression** (not urgent) — per-keyframe FOV slider stopped surfacing in Stage Hero UX; model intact (`StageApp.jsx:840-849`), loss is UI. Diff against Détente's `3ebf510`.
- **TAA post-pass** (not urgent) — temporal AA on the production Canvas for the live-3D↔slab seam. `[[project_zfighting_known_cosmetic]]`.
- **Render-conformance open phases** (HANDOFF-render-conformance): REDEPLOY so deployed app picks up the camera/LOG fixes · Phase 3 dedup the 6 `IS_MOBILE` regexes + browse-altitude dup · Phases 4–7 (BLOCKED on Jacob's cold-review) · Phase 6 Stage↔Preview↔Production parity (Preview-inherits-Stage) · measurement empirical meter-verify.

## LATER / PARKED

- **Hosted bake service with auth** (v2+, post-v1) — per-operator publish without git write access. The kit's multi-operator horizon.
- **Non-building landmarks** — own treatment (not the building path).
- **Slab completeness** — ongoing: anything authored-but-not-baked is invisible to the deployed product. Audit gaps as they surface (FEATURES "the slab carries the full authored product").
- **Doc-debt sweep** — stale `BakedBuildings` refs in `ls/ARCHITECTURE.md`, `ls/reference/INVENTORY-DATA.md §A`, `RUNTIME-DELTA.md`.

## ⏸ AWAITING JACOB (QC / confirm gates)

- **Buildings bake L1.3** — production-app visual confirm (HANDOFF-buildings-bake; slab→v2 cutover shipped).
- **Hero-tree LOD/impostors** — Phase A classification-overlay QC; at the A→B seam (HANDOFF-tree-hero-lod).
- **Render-conformance Phases 4–7** — gated on Jacob's cold-review of HANDOFF-render-conformance (DRAFT).

## 🏛 STANDING CAMPAIGNS (multi-session)

- **The living Process / doc-architecture** — this conversation, codified in `BOZ.md`. Three doc kinds (Reference / State / Diary); content flows downstream as it ages; one pass per session paired with active work.
- **Forensic audit + productization → Show Bible** (`[[project_forensic_audit_and_productization_campaign]]`) — HANDOFF-audit-{cartograph,arborist,ls-app,docs}. Pathologists per domain → rip-up/clean-out → capability statement.

## 🛠 IN-FLIGHT HANDOFFS (the detail layer — index)

| HANDOFF | what | status |
|---|---|---|
| ls-migration | C5 cutover conversation-starter | live → NOW |
| toy-to-stage-bake | bake path Toy→Stage | live → NOW |
| asphalt-as-ribbon | authored asphalt extent | live → NEXT |
| dead-end-typology | endpoint typology | live → NEXT |
| divided-pair-station-overlap | ✅ LANDED (Groma `8ffd795` → converged `8392b3e`, Theodolite) — dropped the offset #5/#6 pair, 28→27 medians | done |
| truman-forensics | ✅ DONE (Galen, `TRUMAN-FORENSICS.md`) — 8 defects, 6 systemic. **ROOT = D1: `weldChains` won't fuse a carriageway's own colinear continuation → carriageways shatter into staggered fragments.** Median is emergent + free after D1; `medians[]` ring is vestigial decoy. **Docket: D1 weld (skeleton/Groma) → D6 oneway-serialize → D3/D8 median-tag → D4 vestigial-cleanup → D5 dead-end → D7 TBD.** | done → D1 docket |
| divided-carriageway-longitudinal-weld (D1) | THE keystone: direction-strict tail-to-head same-heading never-flip weld so each carriageway = one continuous chain → median falls out free. Systemic all divided roads. | dispatch-ready (WARM→Groma) |
| dead-end-spike-prune | prune pendant edges from `extractFaces` + stroke stubs as asphalt (the grade-sep pattern) — kills the dead-end "big triangle" slack | dispatch-ready (WARM→Theodolite) |
| section-census | ✅ DONE (Stratum, `f652053`, `SECTION-CENSUS.md`) — Section is ~70% built (sectionPass exists + chain-incapable by closure = deep wall already satisfied). Remaining = wire + **D1 decision** + 3 FILL fixes. **Section build docket** = census §"ordered backlog" 1–7; **#3 (ADA/point-ramp/G12 guard) is schema-independent → startable now.** | done → docket |
| render-conformance | env parity (DRAFT) | cold-review gate |
| audit-{cartograph,arborist,ls-app,docs} | forensic pathology | campaign |
| **retired 2026-05-31 (`2854aa3`, in git)** | block-independence · degenerate-W-flood · pass2-customs-hardening · ribbon-corners · measure-authoring-redesign · toy-reset-to-defaults | done — recoverable via git |
| **still tracked, pending P5 retire** | neon · neon-roof-depth · hero-keyframes · preview-measurement · mobile-profile · sky-and-light · buildings-bake · tree-hero-lod · cloud-specialist | retire to outcome+commit refs |

## §Triage (carried, not lost)

The pre-prune BACKLOG (`git show 4bf8af2:cartograph/BACKLOG.md`, 4,567 lines) held ~184 done/superseded markers and 11 session-end pins (pure diary) plus a few buried live items I classified from memory + HANDOFF state, not a full line-by-line read. **If a live item isn't above, it's in that git blob.** Two deliberate carries for a later pass: (1) the **★IP NACTO/AASHTO pad-geometry + ADA corner-radius decisions** → ARCHITECTURE Decisions section; (2) the **diary/session-pins** → NOTES or left in git. Neither deleted — both addressed when the consolidation reaches them.
