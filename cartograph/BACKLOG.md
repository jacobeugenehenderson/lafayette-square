# Cartograph Backlog

> **STATE + forward-work only** (`BOZ.md` doc architecture). No diary (→ `NOTES.md` + git), no how-it-works (→ Reference: `SKELETON` / `PREBAKE` / `SURVEY` / `RIBBONS` / `ARCHITECTURE` / `PIPELINE`). When an arc completes: *fact* → Reference, *why* → ARCHITECTURE decisions, *narrative* → NOTES — it leaves here.
>
> *Reshaped 2026-06-05 to the polygon-first program. The pre-reshape backlog (DONE-narratives, the superseded C5/wall-move/palimpsest thread, the prong-4 red herring) is in git history; the deeper pre-prune blob at `git show 4bf8af2:cartograph/BACKLOG.md`. ★IP NACTO/AASHTO pad + ADA-radius decisions still carried for ARCHITECTURE's Decisions section.*

---

## 🔥 NOW — the SHAPE campaign: drive `# curated` → 0

> ⭐ **CURRENT (2026-06-13). Until the map is *finished*, all remaining work is *actually* Skeleton work** (Jacob) — the LOOK is authored (Stage), but every open *SHAPE* bug routes to the **skeleton's interpretation**, never a downstream patch (the whole-day lesson). The work is the **SHAPE campaign**: make the skeleton produce correct street geometry **automatically**, so the **35 `source:'curated'` hand-fixes can be DELETED** and the auto-pipeline still holds. ⚠️ **They are NOT at 0 yet** — the 35 are still in `centerlines.json` propping the map up (and some, like West 18th, render wrong even *with* the hand-fix). **Metric vs. mechanism:** the **35 = the METRIC** (the live defect count / scoreboard); the **6 tasks = the MECHANISM** (the bug *classes*; the 35 streets are distributed across them). So **"→ 0" is a three-step, not a target-list:** (1) solve the 6 classes **generally** (kit-invariant — fix the *class*, never the street); (2) flip each curated chain back to `osm` (**remove the hand-fix**); (3) **verify on Jacob's eye** the auto-output matches. ⚠️ **A hand-fix that can't be removed after the 6 = a 7th class we missed** — so *"remove the 35 and see what breaks"* is itself the final forensic + the proof the campaign worked. The **KIT INVARIANT** (`SKELETON §6` · `INTAKE §6.1`): hand-authored SHAPE is a defect, never a fix. **The data question is CLOSED** — we use our inputs to high effect (`INTAKE §6`), and **no external street source helps** (the County centerlines were probed + rejected — coarser geometry, no divided/ROW; `INTAKE §5.1`); the lever is the skeleton's *interpretation*, not acquisition. **The six visible tasks** (Jacob), each a **class** of bug to kill *generally* (never a per-street `if`): **(1) general arc/loop rounding** → `HANDOFF-round-skeleton-corners.md` (first; West-18th corners) · **(2) highways + frontage roads** · **(3) the continuous side of a T** (the plain-T extension, ↓ HANDOFFs) · **(4) divided roads + medians, once** (incl. the pinch) · **(5) polygon overlap = include/add** · **(6) the 2 botched circular cul-de-sacs**. ⭐ **Different pathologies want different SKELETON tools** — curves/loops (1) → centerline **corner-round** (`round-skeleton-corners`); **junctions (3/4/5) → intersection construction** (the *"intersections everywhere"* / osm2streets trim-back — **PARKED, NOT DEAD**: the archived brief mis-aimed it at a *curve* + an unconfirmed symptom, but it is very likely the right tool for the genuine junctions — **revisit when (3)/(4) come up**, `_archive/handoffs/…polygon-first-junction-construction-SUPERSEDED-2026-06-13.md` + `OSM2STREETS-GROUNDING §4.2`); cul-de-sacs (6) → cap typology. North star: *Survey shows the perfected map straight from the skeleton* (`README §⭐ START HERE`). Active branch `curb-offset-draw`; trunk `cartograph-looks-pass-ab`.
>
> *(Trail — now in `NOTES`/git: the "polygon-first program" framing, the 2026-06-12 "intersection-everywhere junction construction" reframe (`HANDOFF-polygon-first-junction-construction.md`, **archived** `_archive/handoffs/…SUPERSEDED-2026-06-13.md` — it was the wrong task), and the 2026-06-07 Survey-pause pivot. The polygon-first **infrastructure** below (D2 freeze ✅, curb-freeze, the Section FILL tail) **still stands** — it's the *machinery*; the six tasks are the *targets*.)*

**The polygon-first infrastructure (still live, the machinery under the campaign):** fortify the Skeleton + Prebake → polygonize Survey → a DataWall with prepared artifacts behind it. Doctrine: `SURVEY.md §5.1` · `PREBAKE.md §5`.

**Deferred SHAPE residuals** (not-totally-fixed nodes; `[[feedback_accept_the_long_tail]]` — defer ≠ abandon):
- **(a) intersection-everywhere FINISH** — nodes the `9c275ce` cure made *better but not perfect* (E3.2-window marks #0 ~2 m, #6 park-4 ~2.7 m, the Mackay-join 8 m). Same fix, incomplete — **not a new class.** Phase-next: fillet gate → `cornersAdjacent`, apron-everywhere + leg-completeness, perimeter through-steps, edge-collision window sizing. *(= task (3), the continuous side of a T.)*
- **(b) the curve-and-cut class** — a **distinct** class: a block edge **curving in then cutting** near a junction; recurs map-wide.

| Stage | Status |
|---|---|
| intake | ⏭️ deferred (LS has `osm.json`); Provincetown = first run |
| **skeleton** | centerlines polygon-ready ✅; remaining = fortification (name-logic doglegs) |
| **prebake** | ⚠️ **thin two-source compile** — the polygon-ization + freeze (the wall-move) is the live target. Topology frozen (D2 ✅); **curb geometry NOT yet frozen — the live leak** (the "d" bulge; `PREBAKE §4.1`, curb-freeze item below) |
| **survey** | tile construction live (LS, unflagged) but **NOT yet polygon-first**; authoring mid-migration (T3) |
| WALL | at `sectionPass` today; target = the prebake→survey boundary (~P3) |
| section | ~70% (`sectionPass` chain-incapable by closure) |
| bake → 3D | working; LS visual correctness gated on the above |

**Live work (the program, in dependency order):**
- **Doc campaign — the on-ramp (in flight).** Lean the corpus to rebuild-precision; front-half spec = `SKELETON`+`PREBAKE`+`SURVEY` (done/drafted, indexed). Remaining passes: `RIBBONS` lean to the tile model (coherence **D1**), `FEATURES` retire figure-ground-as-live (**D3**). Method: `[[feedback_docs_effluvium_buried_the_answer]]`. Ledger: **`DOC-CODE-COHERENCE.md`**.
- **(2) Prebake polygon-ization + freeze → `HANDOFF-prebake-d2-face-freeze.md` (D2, drafted 2026-06-07, dispatch-ready).** Move `extractFaces` upstream, freeze the **tile topology** (`tiles[]`, L1 only) into `ribbons.json`, consume frozen tiles in `tileGround` — **byte-identical**; this moves the Wall to ~P3 + unlocks D5 perf. `PREBAKE.md §5`. ⓘ The false-corner **cure is already LIVE** (intersection-everywhere `9c275ce`); D3 (corner identity at prebake, L2) is **deferred to §HARDENING** — not part of D2. C5 (two-source seam) = D4, later.
- **⭐⭐ (2b) Freeze the CURB geometry — the last unfrozen polygon (`HANDOFF-freeze-the-curb-in-the-first-bake.md` · `PREBAKE.md §4.1`/`§5` · `SKELETON.md §5f`).** D2 freezes the tile *topology*; the **curb geometry is still re-stroked live every frame** by `buildTileGround` (`iA = tile.ring − asphalt-union`), and that union **bows** the curb at divided transitions (the **"d" bulge** — still wrong on Jacob's eye; only *Section* consumes a frozen curb, *Survey* re-strokes). Destination: emit the curb **once** in prebake as the clean parallel offset (`chain ⊕ halfWidth`, corners = offset-intersections), freeze it; Survey **+** Section *consume* it; only the element under the operator's hand re-strokes live, re-freezing on commit (the authoring loop — `HANDOFF` "Authoring integration"). **This is the move that ends the whole divided-transition defect class** — the bulge, the corner saga, the patches are all symptoms of the curb not being a frozen polygon. ⚠️ **Rebuild-gated → PARKED** (Jacob 2026-06-09: anything requiring a rebuild waits; the artifact regen `fc42a51` parks with it). A **labeled stopgap** (additive, transition-gated, no corner-cut surgery) may straighten the live Survey curb on Jacob's eye meanwhile — symptom relief, *not* the fix. **Sibling to D3** (§HARDENING): D3 freezes the corner *identity*, this freezes the *curb geometry* — both dissolve the transition when Survey goes polygon-first.
- **(3) Polygonize Survey / connect authoring (T3)** — migrate handles / corner-R / cap-selector / strip-swap / translucency off the dead figure-ground onto tiles; **activated-block live redraw** (perf, the sticky-tools fix). `HANDOFF-tile-T3-authoring.md` · `HANDOFF-survey-section-tool-design.md` · `SURVEY.md §4.1`.
- **Corner-registration gap — ~77 Survey corners get no handle (Caliper forensic, `scratch/SURVEY-CONSTRUCTION-FORENSIC.md`).** ⚠️ **The "136 + knocks-on-into-Section" framing is KILLED.** Real gap = **~77** (not 136 — the harness `turnAt` miscounted 58 reflex verts; true convex corners 544, fillets 560). Root = **non-injective re-keying**: achieved fillets (built on the *block* ring) snap to the nearest *tile-ring* node via `nearestCornerVertexIndex` (`tileGround.js:2006`), which **collides (93 overwrites) + orphans (77)** on big divided-avenue/junction blocks (~65 = the bug) + sliver-collapse (~12, mostly correct). **SURVEY-HANDLE-ONLY** — `sectionPass` reads `cornerFillets` **0×** (verified); sole consumer `CornerEditHandles.jsx:234` falls back to `c.Q` (off the real curb arc). **NOT coupled to Section** (that defect is independently Plumb's disk-primitive, Fix B). **Fix:** make `tileGround.js:2004-2008` injective. Distinct root from the marks ↓.
- **⭐ The 4 "SVG-overdraw/tension" marks = the BAND-FOLD class (Caliper — known, not new).** All four are inward-offset **self-intersections of `iA`** folding at a local neck (`offsetRings(iA,−δ)`: ped `iW` `:653`, curb `:2043`, asphalt `:2042`) → opaque blob. The known `RIBBONS §6.3` SELFINT / `HANDOFF-band-fold-fix.md` Roots A+B. **#1/#2 = iW T-mouth fold · #3 = curb needle · #0 = asphalt acute-junction wedge.** ▶ **Fix = `HANDOFF-band-fold-fix.md` Option A (local capacity clamp), extended from ped `iW` to curb (`Cacc`) + asphalt (`Aacc`)** — Caliper's highest-leverage: kills all 4 marks + the ~49 §6.3 residuals. Distinct root from the handle gap (keying vs geometry); they only co-locate.
- **⭐ The Section build — BUILT (SSOT `SECTION.md` v0.3, 2026-06-10).** Section = ped FILL; the per-edge FILL, bent-slice corner, dead-end caps, live material+depth overrides, and the Revert UI all landed (**freeze the silhouette, author the FILL *live*** off the frozen `iA`, `SECTION.md §4`). What remains is the **FILL tail** (`SECTION.md §7`). **Landed 2026-06-11/12 (`curb-offset-draw`):** ✅ **loops** — the emergent enclosed-face median via the `extractFaces` endpoint-weld + the §2 body (`e8cc310`/`ed250b3`, `LOOP-STREETS.md`); ✅ **dead-ends** render clean woven (the pendant-prune **reverted**, `dd4ddb6` — it deleted tile-sourced curbs); ✅ **cap-wrap G8** (round fat-pad + blunt circle-cut, `f908143`); ✅ **handle float** capped (`rayHitCurb`, `646b8b1`, `SURVEY §4`); ✅ **alleys/footways** un-stranded from the dead V2 path (bake + render, `735f02c`/`543bdeb`, `DOC-CODE-COHERENCE` C13/C14). **Open NOW = the PED-BAND JUNCTION-CONSTRUCTION FAMILY** (`SECTION §7`, `273fe23`) — the weird-street FILL mess (Dolman/18th/Carroll): per-edge width-steps + treelawn-Y/N ordering-flips + no constructed junction ped-silhouette; same family as the Albion notch / **G12** (two subclasses — self-int fix STRANDED on `8e1e414`, band-neck = the orphaned `thinTile`→`cap` wiring; `SECTION-CAP-CLAMP-FORENSIC.md`). Plus: perf/D6d · delete figure-ground (T4) · rename Measure→Section. The pre-build census is archived (`_archive/SECTION-CENSUS-2026-06-03.md`); pairs with D2 (prebake freeze) + D5 (activated-block perf).
  - ⚠️ **Known-broken, deliberately left to molder (Jacob 2026-06-07 — not authoring this session, so no revert).** Wall Phase-D (`ef460d1`) froze the **FILL** as well as the silhouette in the Measure tool (`tileGeos`→null when `sectionGeos` present), so FILL authoring there is a frozen snapshot — **move a handle, nothing re-strokes.** The freeze→open *mechanism* is correct; it just got ahead of the live-FILL build. **Fix when the Section build starts:** either revert the `if (sectionGeos) return null` gate, or (better) re-host `sectionOpen` so it freezes the silhouette and strokes the FILL live. `SECTION.md §7`.
- **Skeleton fortification — name-logic doglegs** — weld same-name fragments + straighten within-name kinks. `HANDOFF-name-logic-skeleton-pass.md`.
- **Band-fold thorns** — local capacity clamp (thin-tile / T-mouth `iW` fold; ~100 instances). Distinct thorn-class; re-confirm scope once prebake-freeze lands. `HANDOFF-band-fold-fix.md` (+ `HANDOFF-junction-band-thorns-FINDINGS.md`).
- **⭐ Perpendicular-join protrusion (RE-CLASSED 2026-06-05, Jacob's eye).** The "bulge at the base of some T intersections" + **Benton's stem-joint protrusion** are ONE class: *a path meeting another perpendicularly → a spurious polygon protrusion opposite the meeting.* **NOT (just) band-fold** — a **junction-construction** artifact, same family as the false corner + the fold-at-join (the face-walk/fillet manufacturing spurious geometry at a junction). → folds into **E3** (broaden it to "clean every perpendicular junction," not just divided transitions). ⚠️ Visually-unified by the operator's eye; **the E3 spike must confirm the shared *code* root** (face-walk vs fillet vs band-fold) before assuming one fix lands all (the false-corner-saga discipline). Benton's full bless waits on this (the guard fixed only the collapse).
- **T4 — delete figure-ground** (`buildBlockGeometryV2` / `cornersAtIx`; coherence **C3/C4**) — after T3 migrates authoring off it.
- **Corpse-lie excision** — the code-side of every landed truth; via briefs at the code phase. `DOC-CODE-COHERENCE.md`.

## NEXT
- **⭐ "Revert to Data" buttons — Survey + Section (Jacob 2026-06-08).** The operator-facing version of the datum-repair: instead of a forensic dropping deviating overrides, a button **strips the authoring overlay back to the base.** The clean inverse of "always populate best-effort, then override" (`SECTION.md §8`). Scoped (edge / block / chain / whole-neighborhood):
  - **Survey · "Revert to Data"** → the **sterile Skeleton**: discard the SHAPE overrides (per-fe `pavementHW`, corner-R, caps) → the skeleton's surveyed base widths. The skeleton/`survey.json` is the truth ([[project_skeleton_is_the_first_bake]]).
  - **Section · "Revert to Data"** → the **calculated best-effort**: discard the FILL overrides (treelawn Y/N, ped depths, strip materials) → the gleaned treelawn-Y/N × ADA defaults + default strip ordering (`SECTION.md §3.1`).
  Mechanism partly exists (`MeasurePanel` reset/wipe-customs); generalize + surface as a first-class per-tool control. *(Motivation: deviating overrides cause artifacts — the no-mouth doglegs were exactly this; reverting to data cured them. Give the operator the button, not the forensic.)*
- **Survey · Section · Stage rename** — decided; rides with T3 (⚠️ don't rename code until implemented).
- **Boundary-trio** — circle-SSOT, crop-to-circle-last, bake-time stencil-cull. `HANDOFF-boundary-trio.md`.
- **Asphalt-as-ribbon** — Survey-owns-SHAPE capability. `HANDOFF-asphalt-as-ribbon.md`.
- **Dead-end typology** — reframed as Survey-side authoring. `HANDOFF-dead-end-typology.md`.
- **Slab-content** — ✅ **alleys / footways now render + bake (2026-06-12, `735f02c`/`543bdeb`)** — they were stranded in the dead V2 path on both the bake (`buildTileBakeShape` never called `buildPathRibbons`) and the render (path meshes only in the non-tile `return`); `DOC-CODE-COHERENCE` C13/C14. `path`/`cycleway`/`steps` follow when their `layerVis` toggle is on + re-bake. Remaining: overlays / highway-class asphalt.
- **Story-pass address-maps** — Meteorologist + Arborist (`[[project_story_pass_campaign]]`).

## 🛡 HARDENING — correct on the eye, not yet correct-by-construction
> **Distinct from the deferred long-tail** (those are still *wrong* on the operator's eye — open defects). These items are **already correct** but the fix lives at the wrong layer / isn't frozen-by-construction = robustness debt, not bugs. The goal: make the architecture *guarantee* what a live fix currently *achieves*. Promote deliberately (a layer-relocation can regress the working fix — gate it), not as routine cleanup. Lens: `[[feedback_accept_the_long_tail]]` (defer ≠ abandon).

- **D3 — corner identity at divided transitions → freeze at prebake.** The false-corner cure is **LIVE + working** (intersection-everywhere `9c275ce`: `dedupeRing` + `MIN_CORNER_LEG` + corners from leg-adjacency at every node — clean on Jacob's eye, at the *construction* layer in `tileGround`/`derive`). Doctrine (`PREBAKE.md §5`, `PREBAKE-POLYGONIZATION-PLAN.md §2`) wants the decision made **once upstream as frozen topology**: detect each transition node via `phase.spineAt*`, drop the taper segment, pair cross-street curb × carriageway straight-body, stamp `cornerIdentity{legs}` — so no per-build code can ever re-manufacture the needle. ⚠️ **Relocating re-derives corners → risks regressing the working live fix**; do it deliberately, gated on the 24 enumerated transition nodes (the Mercator spike prints them), not as cleanup. Superseded-as-NOW by the live fix; kept here for correct-by-construction completeness.

## 🖥 RUNTIME / GPU-PERF THREAD (separate from the pipeline; surfaced 2026-06-07 — the Preview GPU emulator pegged the **ground layer**)
- **⭐ Adaptive terrain refinement — the tri-cut.** The ground bake is **1.23M tris** *not* from geometry (it's simple) but from `bake-ground.js triangulateAndRefine` (`:489`) subdividing EVERY ground polygon to a uniform `maxEdge` grid so the flat slab drapes over terrain — **over-refined on flat blocks** (the two biggest land-use floods alone = ~1.0M tris). **Fix: curvature-adaptive refinement** (subdivide only where terrain bends) → 1.23M → a fraction, no visible change, helps mobile + desktop + download at once. Elevates parked `HANDOFF-tile-feature-ledger.md` P1/P3. ⚠️ **NOT the grass shader** (grass = a material/fragment uniform, 0 geometry); **NOT wall/bake-staleness** (bake is current as of `bf16612`).
- **Mobile policy ("what's mobile") — `HANDOFF-mobile-profile.md`** (restored 2026-06-07; had been retired to git at `9bfcb66`). **SETTLED architecture** (Boz#2 cold-review): split the ~20 `IS_MOBILE` hardwires into **two axes / two homes** — *inclusion* (which layers ship to mobile) → slab/`design.json`, Stage-authored, per-Look; *global quality* (`dpr`/AA/shadows/post) → `INSTANCE.mobileQuality`, product-wide, **synchronous**; the shader-baked tail stays code-side. Device *sensing* → one source; *policy* → the two homes; seed with today's hardwires (no day-one change). **Near-dispatch-ready, was parked behind the in-flight pipeline arcs → now UNBLOCKED.** Needs Jacob's nod on §2.
- **Preview measurement (Vernier) — Phases 0–2 LANDED** (`GpuMonitor` window-dilution fix · non-destructive `.visible` gating · Buildings-toggle convention · **Ground+AO → "Ground" de-conflation** = why the emulator labels it "Ground"). **Open:** the §6 next-arc + **per-environment budgets** — today the budget is hardcoded-mobile (`17ms / 200 draws / 1M tris`), **no Desktop variant**, and the Phone/Desktop toggle is **cosmetic** (same scene/assets, no perf isolation). Sibling: `HANDOFF-render-conformance.md` (Stage↔Preview↔Production parity, `IS_MOBILE`).

## LATER / PARKED
- **Onboarding / Intake** — Provincetown first run; designed, no code. `[[project_intake_onboard_process]]` · `[[project_real_tiny_town_spike_surface]]`.
- **⭐ Custom street data — import + manage (KIT)** — operator-measured per-street width/ROW/lanes is the **base-width source** (priority: custom → OSM → AASHTO; baked into the skeleton so the frame is born accurate, operator only tweaks). LS already has it: `raw/survey.json` (68 streets, 61 measured). The kit needs: **(a)** import it at onboarding *if the place has it*; **(b)** a canonical home/format; **(c)** a way to view/manage/edit it (it's operator authoring — existing tools, no new control class). Generalizes the LS `survey.json` into the Intake/Onboard flow. Docs: `SKELETON.md` (seed width-sourcing). *(The LS base-load fix — survey.json wins + propagates through divided splits — is **E1** in the divided-corridor/width build, NOW; this backlog item is the kit generalization, LATER.)*
- **Tile-bake footprint (P1) / curve-fineness (P3)** — parked; profile before cutting (`HANDOFF-tile-feature-ledger` E-rows).
- **Hosted bake service w/ auth** (v2+).
- **Hero keyframe FOV regression · TAA post-pass** — not urgent.
- **Doc-debt** — stale `BakedBuildings` refs in `ls/ARCHITECTURE.md` / `ls/reference/INVENTORY-DATA.md` / `RUNTIME-DELTA.md`.

## ⏸ AWAITING JACOB (QC / eye)
- **Grade-sep** faces-filtered + stroked-as-asphalt — live re-bake eye.
- **Buildings bake L1.3** · **Hero-tree LOD** · **Render-conformance Phases 4–7** (cold-review gate).

## 🏛 STANDING CAMPAIGNS
- **⭐⭐ The correctness suite — DEFEAT hand-fixing (the kit's correctness backbone, Jacob 2026-06-13).** Automate what the operator's eye does: a comprehensive invariant suite (one RED-until-true check per bug-class) that flags bad streets, so the kit never needs a hand-fix on *any* town. Validated against LS's 35 curated streets (the labeled set); enforced as a CI gate; the onboarding loop = run-suite → fix-flagged-classes → green → ship. **The six SHAPE tasks make LS's suite green; the *suite* defeats town #2…N.** Doctrine + first-pass scope: **`POLYGON-FIRST.md §5`**.
- **The living Process / doc-architecture** (`BOZ.md`) — now active as the doc campaign + the `DOC-CODE-COHERENCE` discipline.
- **Forensic audit + productization → Show Bible** — `HANDOFF-audit-{cartograph,arborist,ls-app,docs}`.

## 🛠 LIVE HANDOFFS (the dispatch layer — OPEN work; landed → the summary below)

> **Labels are honest now (`DOC-CODE-COHERENCE` §Landed-forensic):** `code-landed` = ancestor-of-HEAD + code-path-live · `awaiting-eye` = code-landed but unconfirmed on the lit app · `⚠️ NOT landed` = claimed but absent. Never a bare "done."

| HANDOFF | what | bucket |
|---|---|---|
| **correctness-detector** *(first pass — the system move)* | run the invariant suite (curb∥chain · iA self-int · corner-guard · **max-turn** · width-step · face-closure) on the **35 curated streets' RAW OSM geometry** → confirm it flags them (labeled-set validation: high recall on the 35, low false-positive on the grid). Seed of the kit's anti-hand-fix system. Read-only. | **NOW** — scope `POLYGON-FIRST §5` |
| **round-skeleton-corners** | **task (1)** — round West-18th's (& any) jagged centerline corners in the skeleton; the first SHAPE piece | **NOW** |
| **the plain-T extension** *(needs brief)* | **task (3)** — the old `e3.2-window-quality` marks (#0 ~2 m, #6 park-4 ~2.7 m, Mackay-join 8 m) + the perpendicular-join residuals sit at the **~84 plain cross-nodes** the junction cure never reached; extend intersection construction to them (= *"intersections everywhere"* revisited — **parked, not dead**) | NOW |
| **prebake-d2-face-freeze** | D2 — `extractFaces` → prebake, freeze `tiles[]` into `ribbons.json`; moved the Wall toward ~P3 | ✅ **code-landed (folded-live)** — `55e5b8e`/`dee5c20` orphaned, but `tilesFromFrozen` + 103 frozen tiles are live in HEAD (forensic 2026-06-13; "awaiting land" was stale) |
| **section-per-edge-fill** | per-edge FILL — depth + divider + bent corner + handle alignment (`b5c7e98`/`531db59`) | **code-landed; corner-disk → tangent-slice (Plumb Fix B) open** |
| **the plain-T continuous side** *(needs brief)* | tile-T3 / pipeline context (`pipeline-reconception` · `tile-feature-ledger`) | NOW — context |
| tile-T3-authoring · survey-section-tool-design | polygonize Survey / migrate authoring off figure-ground | NOW |
| name-logic-skeleton-pass | skeleton fortification (welds + kink-straighten) | NOW |
| **band-fold-fix · junction-band-thorns-FINDINGS** | thin-tile / T-mouth thorn clamp (the 4 marks + ~49 residuals) | **NOW — ⚠️ NOT landed** (`8e1e414` stranded; `thinTile` orphaned to `bandJoin`, **no depth clamp** — forensic 2026-06-13) |
| freeze-the-curb-in-the-first-bake | (2b) freeze the curb geometry — the last unfrozen polygon (the "d" bulge) | PARKED (rebuild-gated) |
| mobile-profile | two-axis mobile policy (inclusion → slab; quality → `INSTANCE`) | unblocked; needs nod |
| boundary-trio · asphalt-as-ribbon · dead-end-typology | Survey-side authoring capabilities | NEXT |
| render-conformance | env parity (Stage↔Preview↔Production) | AWAITING (cold-review) |
| buildings-bake · tree-hero-lod | slab / 3D | AWAITING (QC) |
| audit-{cartograph,arborist,ls-app,docs} | forensic pathology → Show Bible | STANDING |
| toy-to-stage-bake · cloud-specialist · dead-end-spike-prune | parked | LATER |

> **✅ code-landed (→ git; the D+E build sequence + the wall + the junction cure):** skeleton-tile-hygiene · d1-measure-hygiene · e1-custom-width · e2-constructed-median · e3.1 junction-map · e3.2-detaper · e3.4/junction-datum-repair (`8452c31`) · divided-detection-data-first (`870a1fd`) · **intersection-everywhere (`9c275ce`)** · wall-phase-d (`ef460d1`) · prebake-d2-face-freeze (folded) · loops (`e8cc310`/`ed250b3`) · dead-ends (`dd4ddb6`) · cap-wrap (`f908143`) · handle-float (`646b8b1`) · alleys (`735f02c`/`543bdeb`). **⚠️ AWAITING YOUR EYE** (code-landed, NOT visually confirmed — the BACKLOG's own words): **e3.3-fillet** · **intersection-everywhere**. Scope/plan docs (no code): prebake-polygonization-scope · divided-corridor-model-scope · e3-junction-cure-scope · osm2streets-grounding · section-forensic. *(Forensic provenance + the three-tier "done" test: `DOC-CODE-COHERENCE.md` §Landed-claim forensic.)*
