# A10 ③ — the cure's journey (2026-08-08 → 2026-08-11)

**Diary / retired narrative.** The LIVE state of A10 is on `ROADMAP.md`; this is the working-out that
got there, kept per `CLAUDE.md §PRUNE AS YOU GO` (superseded content **migrates**, it is never deleted).
Retired 2026-08-11 when ③ landed (`07e65753`) and the board's blocked-and-sizing narrative — ~130 lines
of it — became history. ⛔ **Retired for CURRENCY, not TRUTH** (`feedback_it_already_exists_find_what_broke_it`):
the measurements here are good and the dead ends are worth not re-walking. **Do not quote a number from
here as current — re-derive it.**

**What this record is actually good for:** the refusals. Three separate agents refused an instructed
edit and proved it dead instead — the miter clamp (`b378a450`), the ③ brief's contradictory gate
(Kerf), the keyhole's "leave it broken" ruling (Quoin/Tenon). That is the band working.

---

- **A10 · ⭐⭐⭐ THE BAND IS BUILT WHOLE, THEN CUT BY CHAIN PREDICATES — the broken sidewalk's root** *(⭐ this is the FRONT — Jacob: "Survey works as expected; it is the Measure/Section customs tool that doesn't," and **all curbs work**)* — the ped band stops short of **some** corners. Some, not all, which no curb-derived explanation produces.

  - ### ⭐⭐⭐ THE AGREED SENTENCE — ratified by Jacob 2026-08-08 (agent Plumb). ⛔ THE SYMPTOM IS SETTLED. DO NOT RE-OPEN IT.
    > **The ring is already partitioned correctly, one owner per edge. The band must be painted FROM THAT PARTITION — each owner painting its own arc of the inset — not by re-stroking each run's polyline into an area and intersecting. Ends exist only because we make areas out of lines that have ends.**

    **Jacob's contention, verbatim:** *"This couldn't be happening if we were using the curb-made polygons, because those themselves continue."* **An inset of a continuous polygon cannot have an end.** If the band caps, either it isn't insetting the polygon or the polygon isn't continuous — **there is no third option.**
    - ⛔⛔ **THE AGREEMENT IS THE EXPENSIVE PART — protect it.** Jacob: *"this is extremely contentious because it became broken because we never seem to agree on what the symptom/problem is in the first place."* A day went into measuring corner-decline counts — **one layer too low** — and a single screenshot settled it in one exchange. ⛔ **Do not re-derive the symptom from numbers.** D1's decline census is still valid *evidence*; it is **not** the symptom definition, and it no longer defines done.
    - ⛔⛔ **TWO FAILURES SHARE EVERY FRAME — TWO TICKETS, NOT ONE.** Eye evidence: LS local, the **Kennett / South 18th / Dolman / Carroll / Truman** complex (the canonical complex the canon already names).
      **(1) The band caps while the road CONTINUES** — Kennett's asphalt merges straight into South 18th, the sidewalk curls into a U and stops short. ⇒ **this ticket.**
      **(2) The ROAD ITSELF caps while the map continues** — South 18th and Dolman bulb into cul-de-sacs short of Lafayette Avenue; Carroll does it at Truman Parkway. **The band is faithfully wrapping a bulb — innocent.** ⇒ **`A0`, upstream.**
      ⛔ **Fix only (1) and the frame still looks broken and the symptom argument restarts.** That restart is the expensive thing this entry exists to prevent.
    - ⛔⛔ **NEVER CITE A CENTERLINE AS EVIDENCE HERE.** Jacob, correcting the agent directly: *"we must never even KNOW about centerlines — they were discarded at the datawall to prevent exactly this."* **The admissible evidence is the asphalt/curb polygon.** ⭐ **And the code commits the same error structurally, which is why this keeps recurring:** past the Wall the chain survives not as geometry but as a **cutting instruction** — `skelId/side/segOrd`, `cornerAt = skelId a !== b`, band ends = each run's ends. **Worse than a visible leak, because it is invisible.**
    - ### ⛔ CODE ANCHORS — CITE THE SYMBOL, RE-DERIVE THE LINE. Every number below drifted between 2026-08-07 and 2026-08-08.
      **Verified in source 2026-08-08** (`grep -n` the symbol; do not trust the number on sight):

      ⭐⭐ **THE TABLE OF LINE NUMBERS IS GONE — IT DRIFTED THREE TIMES IN ONE DAY (2026-08-09) AND WAS RE-DERIVED BY HAND EACH TIME.** It is a check wearing prose (`CLAUDE.md §PRUNE AS YOU GO`.1). **Run this instead — it cannot go stale, because it reads the source:**

      ```
      grep -n "function sectionPassTile\|const fullBand = \|bandRem = differenceRings\|const noPed = \|luExtra.push(\|let luRemainder = \|function groupRuns\|const cornerAt = (i)\|function offsetRingVariable\|function filletRing\|const legacyBlock\|function relabelThroughClipper" src/lib/tileGround.js
      ```

      **What survives here is the JUDGMENT the numbers were carrying, which a grep cannot give you:**
      · `fullBand` is the whole ribbon, cw → WB · `bandRem` on a **single-run** tile is the **authored-shallower residual — legitimate, and it does NOT pass through `luExtra`** · `luRemainder` is ⛔ **where the unclaimed band lands, the defect's destination** · `groupRuns` is the ring partition, one owner per edge · `cornerAt` is `skelId a !== b` · `offsetRingVariable`/`filletRing`/`legacyBlock`/`relabelThroughClipper` are the four steps the ③ stamp rides.

      ⛔⛔ **THE GREP TRAP — DO NOT SEARCH FOR `if (aBase <= 1e-6) continue`.** Verge's ① rewrote that site; it **no longer exists in live source** and survives only in `scratch/tg-instr.mjs`, the deliberately frozen forensic fixture. **Searching for it lands in a fixture that reads as live code** — it has now caught two people. The live symbol is `const noPed`.
    - ⚠️ **Eye-gate caveat, recorded per `A0`'s rule:** the verdict came from **Jacob's screenshot of his own running local server**. The agent did **not** independently confirm the scene or the build on screen. *(Jacob was the one looking, so this is a weaker loop than 7/31's — but it is still an unclosed one.)*
  - ⛔ **This is the V1 keystone working as DESIGNED, with one step not honoured — the construction is not new and the corner doctrine is settled. Read the design record before proposing anything:** `_archive/RIBBONS-figureground-emitter-2026-06-15.md` (`emitOneBlockRingBands`, 2026-05-29: *"three inward Clipper offsets… 2 strips + sector slicing; the corner is the `fullBand` slice"* — the same construction, the same names, as `sectionPassTile` builds today) · `_archive/RIBBONS-history-2026-06-12.md §6.9` (the AASHTO/ADA corner doctrine) **and `§7`** (the 13-month corner saga: every construction tried, shipped or reverted, with its lesson).
  - **The mechanism.** `fullBand` is one continuous ribbon, Clipper-offset from the frozen `iA` (`tileGround.js:1301`); every strip boundary in the FILL is a concentric offset of `iA`. The per-run sector (`:1450`) does the designed job — **sector slicing for material tags** — deciding which *arc* carries this group's depths and materials. **It carries a label, not a geometry**, so "give the FILL a continuous ring" is not the cure: it has one.
  - ⭐⭐ **The deviation: the corner takeover is CONDITIONAL.** Doctrine `§6.9`.4 — *"both legs stop at tA/tB; **the corner ribbon takes over**; legs resume"* — so the pull-back (`:1416`) is intended. But the takeover is gated on `bandRem.length && cornerT.size && fillets.length` (`:1566`) and per corner on **finding a fillet near it** (`:1575`). **Step over is unconditional; step back is not.** Band the legs released and the corner did not claim falls to `luRemainder` (`:1642`) and **renders as land use** — the gap is ribbon *painted green*, not absent geometry, which is why it is intermittent while the curb is uniformly correct.
  - **Five predicates decide the cuts; none reads the curb** — `cornerAt` = `skelId a !== b` (`:245`), plus `tipped ‖ through ‖ isNameTransition ‖ isThruNode` (`:1416`). `isThruNode` keys the wrong run on a split-skelId through-street (Mackay) and misses Kennett on a node-coord mismatch. Canonical complex: Dolman ↔ West 18th ↔ South 18th / Carroll / Kennett.
  - ⛔⛔ **THE FILL-PATCH CLASS IS CLOSED — do not clamp, wrap, re-key or snap.** The band-neck clamp, the walk-ordinal coupler, the mouth wrap and `SPUR_OUTLINE` were each built and reverted; each treats the output of a wrong decision.
  - ⭐⭐ **THE PARTITION ALREADY EXISTS — the cure needs NO NEW KEY** (measured 2026-08-07). `groupRuns` (`:1060`) walks the tile's `edges[]`, groups consecutive edges sharing `(streetIdx, side)` and consumes the whole ring ⇒ **every ring edge has exactly one owning run, carrying `skelId/side/segOrd` — the `blockCustoms` key.** ▶ **`node scratch/claims-ring-partition.mjs`** (read-only, all baked scenes): **0 double-covered edges on every scene**; off-ring run vertices sit on dead-end tiles → A0's mouth slit, not a blocker here. ⇒ **the defect is a ROUND TRIP** — the FILL takes the run's polyline back out, re-strokes an *area*, trims it, intersects it with the band. A partition cannot leave a gap; an area mask can. **No re-key, no parity gate** (what killed the walk-ordinal coupler, what T3 is gated on). ⚠️ **Not yet established:** that per-arc painting is byte-identical where today is correct — that is the build's gate (dual-state, `scratch/a03-curb-identity.mjs` pattern).
  - ⭐ **NEW LEAD from the same probe — 4 tiles whose runs leave the ring with NO dead-end tip:** altadena 45/159/501, centrum 234. **LS has zero** — invisible on the mould. Undiagnosed; a lead, not a finding.
  - ✅ **D1 LANDED 2026-08-07 (`scratch/claims-corner-takeover.mjs`, agent Jamb) — the declining corners are NAMED, all 7 scenes, both states.** Runs the real `sectionPassTile` under `CORNER_DUMP=1` (which **README documented and `src/` did not contain**; created under the documented name, proven inert — output byte-identical armed and disarmed). **7,632 bids · 5,489 pads built · 2,143 declined (28.1%)**; decline rate spans **11.1% (toy) → 62.9% (Księży Młyn)**, LS **36.0%**, mid-range again.
    - ⭐⭐ **ANSWERS THE PORTABILITY THESIS: no town carries a decline mode LS lacks** — all four modes fire on LS, so the cure can be designed on the mould. ⚠️ **But the MIX is not portable:** `bandRem-empty` is **6.6%** of altadena's bids and **53.6%** of Księży Młyn's (LS 20.2%). **A cure tuned to LS's dominant gate is tuned wrong for both extremes** — handle all four structurally. *(So "the other six mostly fall out" holds for the METHOD and not for the tuning.)*
    - **A fourth gate the brief missed — `empty-pad` (`:1583`), 331 corners map-wide and toy's dominant mode.** Two gates are **dead**: `zero-depth` (`:1569`) and the pure no-fillets tile gate fired **0 times in 7,632 bids**. ⛔ Not load-bearing; don't design around them.
    - ▶ **D2 (the classified-delta gate) is next. ⭐⭐ ITS FIXTURE PROBLEM IS SOLVED — the historic authored state is ON DISK AND STILL RESOLVES** *(measured 2026-08-08)*. **Two authoring channels, and A10 lives in the second one:** `clean/overlay.json` (Survey — widths/caps) vs `design.json`'s **`blockCustoms` (Section — the ped cross-section)**. ⛔ **Production LS carries ZERO `blockCustoms`, and that is DELIBERATE, not a gap** — Jacob reverted it to genuine default on 2026-08-06 (`2481ffad`, *"16 streets / 79 segs / 279 fields → 0"*) **because the problem must be fixed underneath before customs are layered back on.** ⇒ a dual-state run on LS today is defaults-vs-defaults, and only `lafayette-square-staging` (19 keys / 51 live slots) + `hipointe-demun` (6 / 12) carry live Section authoring.
      - ⭐ **The fixture: `public/looks/lafayette-square/design.json.pre-reset`** (git-tracked, beside its `clean/overlay.json.pre-reset`). **It is PARTIAL: 48 of 79 slots resolve against LS's current bake; 31 are stale, renumbered by a re-skeleton.** *(Reproduce by keying the `skelId|side|segOrd` **triple** against the bake's `runs[]`. Cross-check that the method is right: the same code reproduces staging's **51 of 90** exactly.)*
        ⛔⛔ **THIS LINE SAID "all 16 of its streets resolve, 0 stale" ON 2026-08-08 AND THAT WAS WRONG — the error is worth more than the number.** Street-presence is a **proxy** for a **triple-level** question: the key is `skelId|side|segOrd`, so a street can be entirely present while most of its slots point at segment ordinals the re-skeleton renumbered. **That is `POLYGON-FIRST §5` RULE 1b — *measure the definition, not a proxy that correlates with it* — committed by the coordinator in the same session that cited the rule.** ⭐ Note the shape: the proxy answered **16/16, a clean pass**; the definition answers **48/79, a partial**. **A proxy fails toward "fine."**
      - ⛔ **HPDM IS INERT FOR A10 — it carries ZERO Section authoring.** Its slots hold only `pavementHW` + `terminal` — **Survey** fields, consumed upstream and already frozen into `iA`; replaying each alone moves the FILL not at all. *(This entry called it "live Section authoring (6/12)" — wrong; slot **presence** was read as **Section** authoring. Verify: list the field names inside each scene's `blockCustoms`.)*
        - ⭐⭐ **BUT "INERT" IS A FILL VERDICT AND THIS LINE HID ITS BEST USE** *(Kerf, 2026-08-09)*: **the very property that makes HPDM useless for a FILL question makes it the PUREST SINGLE-DIAL PROBE for a SHAPE question** — its authoring is `pavementHW` and nothing else, so a SHAPE result cannot be confounded by a second channel. ⛔ **Do not read "inert for A10" as "not useful."** It was the cleanest of the three same-bake pairs in the mint-under-authoring measurement.
      - ⇒ ⛔⛔ **THERE IS NO CROSS-TOWN SECTION-AUTHORING STATE AVAILABLE TODAY.** Only **LS pre-reset (48/79)** and **`lafayette-square-staging` (51/90)** — **both partial, and both Lafayette Square.** ⭐ **State that in any result rather than implying portability the evidence does not have**; the cure is still designed to be kit-general (D1 proved no town carries a decline mode LS lacks), but **today it can only be *tested* on the mould.**
      - ⭐ **∴ the build order is settled by the reset itself, not by argument: get the partition right at GENUINE DEFAULT first** (which is what town #2 pours into), **then replay the pre-reset fixture to prove the cure does not break authoring.**
    - ✅ **D2 BUILT 2026-08-08 (agent Plumb) — `scratch/claims-corner-delta.mjs`.** `served` / `unchanged` / `regression`, per corner **and** per tile; **refuses to call a decline-count drop a success.** `--selftest` replays 3 mutants and tampers a baseline copy to fire 3 loud classes — **all six pass, and the false-cure mutant "fixes" 325 corners and still FAILS.** ⚠️⚠️ **NOT YET FIT TO GATE THE CURE: its `served` bucket is defined around corner declines, which the agreed sentence supersedes.** ⛔ **Restate its invariant as "the band has no ends" before it gates anything.**
    - 🟡 **`scratch/claims-decline-fate.mjs`** — splits declines into `no-band` / `hole` / `mispaint`. **Taxonomy sound; ⛔ COUNTS NOT QUOTABLE** — they moved twice under the agent (a probe disc too small to reach the band at wide junctions). *(Agent Plumb, self-reported, and correctly: "4 holes on LS" then "23" after fixing the probe.)*
    - ✅ **①+② LANDED 2026-08-08 (`431ff1b8`, agent Sill) — THE RED-UNTIL-TRUE INVARIANT EXISTS AND IS RED.** `node scratch/claims-band-reaches-lu.mjs` (exit 1) — *"no part of `fullBand` reaches `luRemainder`, except the authored-shallower residual."* **Stated on the OUTPUT, not the decision** — that is why it sits at the right layer and the decline census did not. Reports **WHERE, per tile, as coordinates in the tile frame**; ⛔ **no centerline is read, named or reported in either file.** It instruments a *copy* of `tileGround.js` in `scratch/.band-probe/` via six **declared** edits, each asserted to match exactly once so drift **aborts loudly** instead of silently measuring an unapplied instrument; `--inert` proves the FILL is byte-identical through clean and instrumented modules on all 8 states. D2's `served` is restated to **"a band end that existed no longer exists"**, by geometric overlap, never a proximity tolerance. ⭐ **The gate's own teeth, re-verified by the coordinator: the false-cure mutant removes 5 band ends while moving 225 tiles — the old decline-based definition would have called that a cure.** ⭐ It also **refuses to baseline a stale fixture silently**: `--save` stops on the 48/79 partial and makes it the operator's call, then keeps printing the slot counts in every later report.
      - ⭐ **THE ARRIVALS AT `luRemainder` ARE FOUR, NOT TWO** *(the brief said two; Sill corrected it, and one correction is a Layer 0 q3 near-miss by the instrument itself)*: **(1)** `luExtra` authored-shallower residual — **legitimate** · **(2)** `bandRem` on a **single-run** tile (`:1338`) — **also legitimate, and it never passes through `luExtra`**, so an invariant excluding only `luExtra` would have scored the operator's own gesture as a defect · **(3)** band the legs released that no corner claimed — ⛔ **the defect, this ticket** · **(4)** **NO-PED**, below.
    - ⛔ **THE `NO-PED` ARRIVAL — IDENTIFIED 2026-08-08, AND IT IS *NOT* A SIDEWALK DEFECT. RULING OWED.** A run with `aBase <= 1e-6` (`:1230`) is **skipped**, so its arc of the annulus is never claimed and falls to `luRemainder`.
      - ⭐⭐ **WHAT THESE RUNS ACTUALLY ARE, measured on LS's committed `shape.json` (a read, no probe):** **34 of 584 runs**, and **the no-ped set and the `skelId == null` set are SET-IDENTICAL — 34 and 34, from two independent properties.** They carry **no `skelId`, no `roadId`, no `throughId`, no `anchor`, no measure.** ⭐⭐ **And every vertex of all 34 sits at radius 892.000 — exactly the disc radius** (`neighborhood_boundary.json`: centre `[-15,-15]`, `radius 892`). ⇒ **They are not block edges at all. They are the DATA-WALL CLIP — the line where the neighborhood disc cut the tile.** ⛔ **The code comment at `:1230` says "median-facing / no street"; on LS it is 100% "no street" and ZERO median-facing.** *(Cross-check: 30 of 101 LS tiles are `isMedian`, and median tiles are handled separately one line up by `pedOff`, which keeps the run at zero depth rather than dropping it.)*
      - ⛔⛔ **THE "≈6× BIGGER THAN A10" FRAMING WAS WRONG — retracted the same day it was written.** `claims-band-reaches-lu.mjs:241` does `if (row.noPed > 0) { out.noPedArea = out.defectArea; … return out }` — **any tile carrying even ONE no-ped run has its ENTIRE defect area booked to NO-PED and its A10/A0 classification skipped.** So **11,127 m² is a whole-tile UPPER bound that mixes in whatever A10 defects those tiles also have**, and **A10's 1,888 m² is a FLOOR that excludes ~23 tiles outright.** The two are not comparable. ⚠️ **This is the very failure Sill fixed for A0** (*"my first cut sent a whole tile to A0 the moment it carried one tip — which hides A10 defects, i.e. fails toward fine"*) — **the same pattern survives one class over.** ⭐ It is *defensible as conservative* (it refuses to attribute rather than guessing, and labels itself `UNATTRIBUTABLE`), **but its number may not be used as a size comparison.**
      - ⭐ **The honest statement: `NO-PED` BLOCKS MEASUREMENT on ~23 of 101 LS tiles.** That — not size — is why it wants settling before the cure: **the cure cannot be verified on a quarter of the map.**
      - ⚠️ **Likely LOW visual stakes, unconfirmed — needs the eye, not more arithmetic.** The rim sits at **892 = `fade.outer`**, the far end of the face-fill fade band (`fade {inner: 758, outer: 892}`), i.e. where ground faces have faded out. **If that holds, `NO-PED` is invisible geometry and a measurement problem only.** ⛔ Do not treat that as established — it is an inference from two numbers, and `feedback_proxy_render_is_not_the_operator_eye` applies.
      - ### ✅ RULED 2026-08-08 (Jacob) — ⛔ `NO-PED` IS **NOT A SEPARATE TICKET**. IT FOLDS INTO A10. ONE CURE.
        **The premise the three proposed options all got wrong: the rim is not an absence.** `ARCHITECTURE §"The compound shape"` — **the neighborhood is ONE closed shape whose outer contour is the circle**, and the streets and LU fills **close against it**. ⇒ a rim block's ring is **continuous**, so **A10's ratified sentence already covers it, unchanged**: *an inset of a continuous polygon cannot have an end.* The band ends at the rim for **exactly the A10 reason** — the code asks *"which street is this edge?"*, gets nothing, and drops the edge. **Ends exist because we make areas out of lines that have ends.**
        ⛔ **The option Boz recommended — "declare the rim arc as land use" — would have RATIFIED the defect**, writing down that the edge of the operator's map is a hole. Recorded because it was one message from being canon.
        - ⭐ **NOT AN AESTHETIC QUESTION, BY RULING:** *"the rim edge, as far as the user knows, is blended to transparent by the time we would see this issue, so we need to build it to be the most foolproof / guaranteed / easy version."* ⛔ **Do not tune it, do not eye-gate it.** ⚠️ **The eye CANNOT witness this** ⇒ **the invariant is the only gate that will ever exist here**, so it must **COUNT** rim tiles, ⛔ **never exclude them as `UNATTRIBUTABLE`** — which is what it does today (`:241`). **Changing that is part of the cure, not a nicety:** un-excluded, those ~23 LS tiles are how we learn the cure worked there.
        - ⭐⭐ **THE FOOLPROOF FORM IS ALREADY IN THE FUNCTION, ONE LINE UP.** A **median** tile has no ped realm either and handles it by **keeping the run in the partition at zero depth** (`pedOff`); the rim case **drops the run** (`if (aBase <= 1e-6) continue`, `:1230`). **Make the rim behave like the median: the run stays, owns its arc, carries zero ped depth**, and the LU fill closes against the outer contour as the compound shape says it should. **No new key · no threshold · no tuning · existing pattern in the same function.**
        - ⭐ **Kit-general, and LS is the MILD case:** altadena reports **38 NO-PED tiles / 67,563 m²** — the share tracks how tightly the disc is drawn, so **town #2 gets more of this, not less.**
    - ✅ **①+② LANDED 2026-08-08 (agent Verge, `073ea0c5` + `1a66dbd7`). ③ IS NOT BUILT — a STRUCTURAL BLOCKER, handed over rather than papered with a heuristic.**
      - ✅ **① the rim run stays in the partition** — it now behaves like a median run (`pedOff`) and owns its arc at zero ped depth. **A10 area moved by EXACTLY 0.00 m² on all 8 states** — it lands no cure and claims none. 215/1868 tiles moved, **every one carrying a no-asphalt run**; all others byte-identical. Envelope fingerprint **unmoved**; corner set unmoved; `--inert` re-proven on all 8 states after re-anchoring the probe. ⭐ **Two wrong first cuts, both caught by the gates and both worth keeping:** a zero-depth leg dragged `cMin` to 0 so the **whole corner pad painted LU — 393 corners, i.e. the fix manufactured this ticket's own symptom**; cure = **a no-asphalt run bids NO corner** (*"there is no curb ramp where there is no curb"* — structurally a tip). And suppressing the **trim** as well let the rim's slab reach into its neighbours' arcs and **steal 215 corner pads**; it trims like any other leg.
      - ✅ **② the invariant COUNTS rim tiles** — the whole-tile `noPed` skip is gone from the classifier **and from D2's two matching skips**. ⭐⭐ **AND A SECOND CHANGE THAT WAS NOT IN THE BATON, WHICH IS THE MORE IMPORTANT ONE: a zero-total group also satisfies `g.total < TLmax + SWmax`, so the rim arcs rode `luExtra` — i.e. THE RIM WAS WEARING THE OPERATOR'S AUTHORING AS COVER.** LS at **genuine default** reported *"authored-shallower residual EXCLUDED: **10,696 m²**"* **with zero `blockCustoms`**, flatly contradicting the sentence the file prints one line above it. **Layer 0 q3 committed by the instrument, worst on the state town #2 pours into.** Now split onto its own line; every genuine-default state reads **0.00** again, and ⭐ **the two authored states reproduce their pre-change figures exactly (47.01 / 47.88) — which is what proves the split is RIGHT rather than merely zero.**
      - ⭐ **THE NEW BASELINE ③ MUST DRIVE TO ZERO — larger than the old figure because it now includes ground the gate could not see, NOT because anything regressed** *(re-derive, don't quote: `node scratch/claims-band-reaches-lu.mjs`; baseline `a10-post-rim`)*: **LS 28 tiles / 70 ends / 2958.0 m²** · pre-reset 28/70/2856.3 · toy 1/3/0.7 · HPDM 64/152/6841.1 · altadena 239/561/20972.4 · ksi 8/13/404.0 · centrum 127/220/8569.4 · staging 34/76/3669.6.
      - ### ✅ ③'s BLOCKER IS RULED — 2026-08-09 (Jacob). **THE STAMP IS A SOURCE RING-EDGE *INDEX*, NEVER A STREET NAME.**
        The partition lives on `tile.ring`, the band is concentric to `tile.iA`, **and nothing connects them** — `filletRings` densifies 1.5–8.9× and **0 tiles carry iA-side ownership on any scene**. **The ruling:** carry, through to `iA`, **which ring edge produced each piece**. The street label stays an attribute of the ring edge, where authoring already lives. ⭐ **This is what keeps the cure from committing `A15`'s stink** — stamping `skelId|side|segOrd` onto `iA` would cure chain-leak by importing more chain state; an edge index is geometry, not chain. *(The rejected sibling — projecting ring owners onto `iA` by proximity — stays ⛔ forbidden: a tuned knob deciding ownership.)*
        - ### ✅ THE STAMP IS BUILT AND LANDED — `52008afa`, 2026-08-09 (agent Kerf). ⛔ **THE CURE IS NOT.**
          `iA` carries **`iaEdge`** — per vertex, the index of the `tile.ring` vertex whose emission produced it; `shape.json` picks it up automatically. The carry rides `offsetRingVariable → dropFoldSpurs → dedupeRing/filletRing →` the post-fillet needle strip **by index**, and across the Clipper booleans it re-attaches by **exact integer match on Clipper's own `1/SCALE` grid** (`toClipper` rounds, so a surviving vertex returns bit-for-bit) — ⭐ **not a tolerance.** Gates: A10 **unmoved** at LS 28/70/2958.0 m² and every scene at its recorded figure · corner-delta served 0 · `--inert` green on 8 · ring-partition's 0 double-covered holds.
          ▶ **Probe: `node scratch/claims-ia-source-stamp.mjs`** — per scene, stamped/refused **with every refusal named**. **Stamped: LS 44/101 · toy 10/12 · altadena 555/694 · centrum 209/571 · HPDM 79/196 · ksi 19/77 · staging 43/116.**
        - ### ⛔⛔ MY SCOPING OF THIS TICKET WAS WRONG, AND IT FAILED TOWARD "FINE" — corrected 2026-08-09 by Kerf's measurement, **before** anything was planned on top of it.
          **This board said** *"offset is vertex-indexed end to end, so the carry is free … all 28 LS defect tiles are offset ⇒ offset-only covers 100% of the defect."* **The first clause is false, and the conclusion dies with it.** `offsetRingVariable` **ends in `unionRings([W])`** — a Clipper boolean, *the very operation this ticket correctly names as provenance-destroying for the carve path.* Where the offset self-intersects, the union **mints vertices with no source**. The cul-de-sac keyhole splice is a second boolean on the same path. ⭐⭐ **∴ THE DIVIDING LINE IS NOT THE PRODUCER — IT IS WHETHER A BOOLEAN MINTED VERTICES, AND THE OFFSET PATH RUNS ONE AT ITS END.** *(Boz read the emission loop and stopped before the return. `BOZ.md §0`: a wrong claim from Boz costs more because the context makes it sound like recall.)*
          | LS A10 defect, by stamp | tiles | area |
          |---|---|---|
          | ✅ stamped — ③ can be built and verified here | 16 | **1,678.1 m² · 56.7%** |
          | ⛔ unstamped — 10 `clipper-minted-vertex` + 2 `keyhole-splice` | 12 | **1,279.9 m² · 43.3%** |
          **The 28/28-are-offset split is still true; it just never carried the conclusion, because being an offset tile is not sufficient for the carry.** ⇒ **③ CANNOT DRIVE THE INVARIANT TO ZERO AS SCOPED.** *(Re-derive, don't quote: defect tiles from `claims-band-reaches-lu.mjs --rows 40`, stamp status from `claims-ia-source-stamp.mjs`. **Verify the indexing first** — Kerf did, 0 ring mismatches.)*
          - ⛔ **AND `A06` DOES NOT CLOSE THIS HALF.** A06 moves **carve** tiles onto the offset path; it does nothing about a self-intersecting offset's union minting vertices. **This board read as though A06 makes the stamp total. It does not.** ⭐ **A06 still helps — it converts the carve refusals — but the minted half belongs to `A3`/`A4`, NOT to ③ (see the routing below).**
          - ### ⛔ SIZED 2026-08-09 (agent Kerf, `4c096fcc` · `scratch/fold-mint-forensic.mjs`) — **NOT A PATCH ADJACENT TO THE STAMP. THREE CONSTRUCTIONS, AND ONLY 45% IS THE KNOWN FOLD.**
            **The refusal tells the truth: 193 minting tiles across 7 scenes, and NEITHER = 0** — every one has a genuinely self-intersecting `W`. Nothing else mints.
            | class | tiles | can the existing machinery see it? |
            |---|---|---|
            | **NEEDLE** — a ≥165° reversal is present | 86 | ✅ yes — **this is the known fold class** |
            | **LOOP** — self-crossing with **no reversal anywhere** | 83 | ⛔ **structurally blind** |
            | **GLOBAL only** — distant parts of `W` colliding | 24 | ⛔ no |
            *(37 carry both local and global. Crossing index gap: 2 ×137, 3 ×32.)*
            - ⭐⭐ **THE LOOP HALF IS THE FINDING.** A crossing at gap ≥3 is three-plus vertices each turning ~90° — **a small loop with no reversal tip to delete.** `dropFoldSpurs` removes reversal tips; **there is nothing for it to remove. It is not mistuned, it is the wrong instrument.**
            - ⭐⭐ **AND FOR THE HALF IT *CAN* SEE, THE MACHINERY IS IN THE WRONG PLACE — ordering, not opinion.** The strip runs twice and **neither run protects provenance**: inside `offsetRingVariable` it is gated to `clean` mode (`smooth > 0 || tileIsCurved`, and **`STREET_SMOOTH` is pinned 0**) ⇒ it runs on **26 of 193, all HPDM**, and never on the other 167 · at the call site the `ringHasSpur` pre-scan runs on **the output of `unionRings`** ⇒ **it fires after the boolean has already minted the vertices.** It cleans the geometry; it cannot restore the source.
            - ✅ **THE CAPACITY GUARDS ARE NOT THE CAUSE** — **1 of 193** tiles has offset depth ≥ the ring's inscribed reach. This is **not** "the block is too small for the road." ⚠️ **With its limit stated by the agent:** that measure is the largest inscribed circle, so it answers *"is the whole tile swallowed"*, not *"is there a local pinch"* — the 24 global-only cases plausibly live in pinches it cannot see. ⛔ **Do not read ratio < 1 as "the depth is fine here."**
            - ⭐⭐ **ALL THREE REDUCE TO ONE SENTENCE: the offset emits an INVALID POLYGON and a boolean cleans it up.** ⇒ **any cure that keeps the boolean keeps the mint; a cure that drops it must make `W` non-self-intersecting BY CONSTRUCTION.** **Honest size: large, and partly unknowable without building it.**
            - ### ⛔⛔ ROUTING CORRECTION — **THE MINTED CLASS IS NOT A NEW QUESTION. IT IS `A4`'s NON-ROBUST OFFSET AND `A3`'s UNFROZEN CURB, AND BOTH ARE ALREADY ON THIS BOARD.** *(Jacob: "read the Survey docs." Boz had sized it from source without routing — the failure `CLAUDE.md`'s gate exists to prevent.)*
              **`SURVEY §5.1`: "The topology is frozen; the SHAPE is not"** — the silhouette is re-stroked and re-filleted from chain measures **on every build**. **`SURVEY §6`: "the curb is the LAST UNFROZEN POLYGON"**, a correct curb is `chain ⊕ halfWidth` — *"a pure function of the skeleton, so it belongs **frozen in prebake**, and that freeze is the unfinished half."*
              ⭐⭐ **AND `A4` ALREADY SAYS THE MECHANISM IN ITS OWN WORDS: `STREET_SMOOTH` is pinned 0 *"because the offset isn't robust on tight bends — turning it on SPRAYS NEEDLES."* Kerf's 86 NEEDLE / 83 LOOP is that sentence MEASURED, for the first time.** ⇒ ⛔ **Do not open a third construction for the minted half. `A3` (freeze the curb in prebake) is its ruled cure and `A4` is strictly downstream of it.**
              - ⭐ **THE CIRCULARITY, worth keeping because it is invisible from either ticket alone:** the fold-strip inside `offsetRingVariable` runs only when `smooth > 0` — **and `smooth` is pinned 0 *because the offset isn't robust*. The cleanup is disabled by the condition it exists to handle.**
              - ⭐⭐ **JACOB'S CONSTRAINT ON `A3`, ruled 2026-08-09: *"The curb CAN move when the user authors it; same with the corner radius."*** That is **`SURVEY §4` verbatim** — the asphalt-edge drag *"strokes per-side pavement half-width outward; **the block (curb line) follows**"*, and corner-R is a handle **on the achieved curb arc**. ⇒ **curb movement is the tool's PRIMARY GESTURE, not a permission to grant.** ⛔ **So whatever A3 freezes must still follow the drag** — which is A03's proven pattern (**frozen base + authored scalar applied in the builder**) and exactly why A06 rules that freezing `aFill` is wrong: *it moves with authoring.*
              - ### ⛔⛔ MEASURED 2026-08-09 (agent Kerf, `18320791`) — **THE MINT SET IS A FUNCTION OF AUTHORED WIDTH, NOT A PROPERTY OF THE TILE. ∴ THE BIFURCATED CURE IS DEAD.**
                **Which dial, from the code path — and it REFINES Jacob's ruling rather than contradicting it.** ⭐ **`pavementHW` is the ONLY authored scalar that reaches the mint decision:** `blockCustoms[skelId][side][segOrd].pavementHW → authoredHW → buildCurbRings.depthAt → seg[i].d → W → unionRings`. ⭐⭐ **Corner radius provably CANNOT** — `cornerRfn`'s only consumers are `filletRing`, which runs **after** `buildCurbRings` returned and **after the union that mints**. **The curb does move under the radius dial (Jacob is right about the gesture) — but the mint/refuse decision is already taken. Once refused, refused.** *(Cap style reaches `capAt` and so reaches `W`, but it is frozen in `ribbons.json`, not a live dial; `capFlip` in `blockCustoms` is read only inside `sectionPass` — a FILL-side material flip, not a shape-side cap style.)*
                ⛔ **THE FIXTURE COMPARISON RETURNED ZERO FLIPS AND KERF REFUSED TO REPORT IT AS STABILITY — correctly.** LS default vs pre-reset (48/79 slots) moved the curb on 7/101 tiles, **of which exactly ONE is a minting tile**; HPDM 8/196 and staging 14/116, both 0 flips. **The fixture never sampled the boundary. A zero from an untested sample is the fails-toward-fine shape.** ⭐ So he bounded the dial directly instead — `pavementHW` set map-wide over every resolvable triple:
                | `pavementHW` | 2 m | 5 m | **default** | 6 m | 8 m | 14 m |
                |---|---|---|---|---|---|---|
                | **minting tiles** | 9 | 12 | **13** | 15 | 18 | 27 |
                **Monotone, in the direction the geometry predicts — a deeper inward offset folds more.** *(A sweep bounds the dial; it is not a claim about real authoring.)*
                - ⛔⛔ **AND THE CROSSING TILES CARRY THE BROKEN BAND, AT AN ORDINARY DRAG.** LS default half-widths run **~5.2–8.4 m**, so these are inside the working range: **5 m → tile 70 STOPS minting** (an A10 defect tile becomes stampable) · **6 m → tiles 11, 56 START** (11 is a defect tile) · **8 m → +5, four of them defect tiles** · **14 m → +14, eight defect tiles.** ⇒ **a ONE-METRE width drag from default crosses the boundary in both directions, on exactly the blocks whose band is already broken.** ⚠️ *(The A10 defect set itself moves under authoring; the default-state set is the reference because it is the operator's starting point.)*
                - ⭐⭐ **∴ A CURE THAT PAINTS FROM THE PARTITION WHERE STAMPED AND RE-STROKES WHERE NOT HAS A WIDTH-DEPENDENT SEAM — under `SURVEY §4.1` the strip drag is the PRIMARY GESTURE.** The operator nudges a width by a metre and the block silently changes which construction draws its sidewalk. ⛔ **Boz recommended exactly this bifurcation on the morning of 2026-08-09; it is withdrawn on this measurement.** **③'s cure needs the stamp to be TOTAL, and the stamp cannot be total while the offset self-intersects.** *(That step is a judgment, not a measurement — a cure shape needing no per-tile stamp would escape it, and nobody has found one.)*
              - ### ⛔⛔ AND THE ORDERING BOZ WROTE HERE — *"③ is downstream of `A3`"* — WAS **BACKWARDS**. Corrected 2026-08-09 on `WALL.md` + `POLYGON-FIRST §3`, both of which already said so. *(Jacob: "read the docs about the datawall." Fourth routing correction in one day, same failure: reasoning forward from a measurement instead of reading the canon that owns the question.)*
                ⛔⛔ **`WALL §1`: "FREEZING *WRONG* DATA IS WORSE THAN NOT FREEZING AT ALL — it launders garbage into authority."** ⇒ **A3 cannot be the cure for a self-intersecting offset, because A3 would FREEZE the self-intersection** and every consumer downstream would then trust it. **`WALL §5` states the order outright: *"fortify Prebake → **fix the band-fold thorns** → freeze → open in Section → operator confirms."*** The thorns come **before** the freeze, and `POLYGON-FIRST §3` sequences it identically: **"D6b+D6c are the architecture move, GATED ON D6a PROVING OUT."**
                - ⛔⛔ **THE SITE IS DIAGNOSED (averaged-normal vertices, 69.8% of crossing endpoints) BUT *"ADD THE MISSING MITER CLAMP"* IS **PROVABLY DEAD** — `b378a450`/`8217b225`, 2026-08-09. ⭐ **"Add a clamp" is closed AS A CLASS: both branches are per-vertex LOCAL tests and self-intersection is a GLOBAL property of the walk**, so no constant saves it — 27.8% of crossing endpoints went *through* the existing clamp and still crossed. **The live entry, with the algebra, the coverage numbers and the named-not-built sign test, is `A4` — do not restate it here.**
                  ⚠️⚠️ **THIS SLOT CARRIED THE DEAD PRESCRIPTION FOR A DAY AFTER IT WAS EXCISED, AND IT COST A WRONG EXECUTIVE READ (2026-08-10).** `8217b225` corrected `A4` and `POLYGON-FIRST §3` and **left this second copy standing** — still instructing the reader to *"clamp the averaged-normal branch and re-run the mint census."* Boz read this copy, not `A4`, and told Jacob the clamp was **"the single highest-leverage unbuilt thing on the board."** ⭐⭐ **Jacob caught it from memory — *"I feel like we've done these steps already."*** **The fact had two homes and the retraction reached one** (`CLAUDE.md`: one home per fact, every other mention is a **link, never a copy**; `feedback_a_retraction_must_fix_the_steps_not_the_signage`). ⛔ **When you retract something, grep the corpus for the CLAIM, not just the doc you were reading.**
                - ⭐⭐ **∴ THE REAL ORDER: make the offset produce a VALID polygon (D6a's unfinished half) → then the stamp is total → then ③'s cure → and A3's freeze becomes legitimate rather than a lamination of the defect.** ⛔ **`A4` carries this work in its TITLE ("Robust bezier offset") but its body says *"strictly downstream of A3"* — that inversion is on the board and is plausibly why nobody has done it.**
          - ⭐ **The keyhole pair — 494.9 m², 17% of the defect — is no longer a hypothesis. ✅ MEASURED 2026-08-10; the ruling and the numbers are below** (*"AND THE KEYHOLE REFUSAL IS RECOVERABLE"*).
          - ### ⛔ NEGATIVE RESULT, RECORDED SO IT IS NOT RE-CHASED (Jacob asked it 2026-08-09; measured, not reasoned): **"IS THE UNSTAMPED HALF THE DEAD ENDS?" — THE KEYHOLE 2 YES, THE MINTED 10 NO.**
            The **keyhole** pair is dead-end **by definition** — that splice *is* the cul-de-sac (punch the bulb disk, weld a morphologically-closed keyhole back in; **the weld is the second Clipper boolean**). The **minted 10 carry dead-end features at 40% — against 50% for the 16 defect tiles that DID stamp.** ⇒ **dead-endness does not separate stampable from unstampable, and is not the explanation for that 26% of the defect.** ⛔ Do not spend a session on it.
            - ⭐ **What the same table DOES show, and it is a confirmation rather than news:** a tile with a dead-end tip/mouth carries an A10 defect at **50% vs 10%** for tiles with none — **5×**. That is **exactly what the agreed sentence already says** (*A10 and `A0` are two failures sharing every frame*), seen in a table. ⛔ **It does NOT say dead ends cause the A10 defect. Same tiles, two problems, still two tickets.** ⚠️ 101 tiles, and *"2 keyhole tiles"* is not a rate.
            - ⚠️ **Method note, because the first cut was wrong:** using `cap` as the dead-end marker returns **100% for every group including the whole map** — `cap` is a per-tile depth scalar, present everywhere. **The uniform answer is what exposed it.** Discriminate on `mouths`/`roundTips`/`bluntTips`. *(`feedback_a_metric_can_be_a_bad_proxy_for_the_symptom`.)*
        - ⭐⭐ **PORTABILITY IS NOW MEASURABLE WITHOUT A RE-BAKE — this board's "the other five cannot be cross-tabbed at all" is RETIRED.** That was true of the *committed artifacts*; `claims-ia-source-stamp.mjs` runs the **shape pass live**, so producer/stamp questions are answerable on all 8 states with nothing re-poured. ⛔ **Jacob's don't-re-bake ruling is untouched and still governs.**
          ⛔⛔ **AND THE "IT VARIES WILDLY BY TOWN" READ WAS A DENOMINATOR ERROR — MINE, corrected by Kerf 2026-08-09.** This board said *"`clipper-minted-vertex` dominates altadena (88) and is 0 on ksi — a class that goes from zero to dominant between towns has a cause worth naming."* **Normalised to the only tiles that CAN mint (offset tiles), it is flat:** staging **25/69 = 36%** · HPDM **27/106 = 26%** · **LS 13/59 = 22%** · centrum **40/251 = 16%** · altadena **88/643 = 14%**. ⭐ **Altadena is the LOW end; its 88 is a big denominator, not a local class.** ⇒ **14–36% of offset tiles everywhere there is a real sample — kit-general, not town-specific.** The two zeros are **small samples, not absence** (at 20%, P(0 of 19) ≈ 1.4%, P(0 of 10) ≈ 11%; and 46 of ksi's 77 tiles are `carve:small`, so few reach the offset path at all). ⚠️ **LS 22% vs staging 36% is ONE neighbourhood at two bakes** ⇒ no single figure is a town's property. ⭐⭐ **The rule this keeps re-teaching: a raw count across scenes of wildly different size is not a cross-town finding. Normalise to the population that could have produced it, or the biggest town always looks like the problem.** *(Second instance in one day — cf. the `cap` bad-proxy note above.)*
        - ⭐ **THE PROXIMITY RECEIPT IS AT THIS EXACT SITE, ALREADY PAID FOR.** `tileGround.js:3578-3594` puts corner identity back onto the fillet output by **nearest-apex greedy matching**, and its own comment records the predecessor *"collided 93× and orphaned 77×."* **That is proximity recovery at this seam with the bill attached** — the argument for carrying the index forward instead of matching after the fact. ⭐ Retiring that matcher onto carried provenance is a real prize; ⛔ **out of scope for the stamp — do not absorb it.**
        - ⛔ **Still true, and it governs the gate:** a frozen-artifact change ⇒ **re-bake ⇒ re-founds the substrate.** The gate is **`a03-curb-identity.mjs`, never the clobber guard** — `A01` proves that guard compares counts and passed 57 reshaped streets at up to 6.7 m. ⭐ **And `claims-band-reaches-lu.mjs` must stay RED at the SAME numbers: if the stamp moves the defect figure, part of the cure got built, which is a fail and not a bonus.**
          ✅ **`a03` NOW STRIPS DECLARED ARTIFACT KEYS — LANDED `b773bd13`, 2026-08-09** *(surfaced by Kerf, who hit it and correctly refused to edit the gate himself; ruled by Jacob)*. Its contract is identity — no curb may move — but when a change's deliverable **is** a new artifact key, `artifact` cannot not move, and **a gate that reads correct work as failure trains people to wave it through.** `ARTIFACT_NEW_KEYS` names each addition with its commit; ⛔ **anything UNDECLARED still moves the hash and still fails**, and ⭐ **a declared key that is no longer emitted exits 2** — a strip list that outlives its key is a permanent blind spot. `artifactRaw` is printed but not gated, so the addition stays visible. **Both behaviours mutation-tested.** ⚠️ **This board previously said "byte-identical" with no footnote** — that phrasing would have read ③'s correct result as a failure.
        - ⛔ **DO NOT RE-BAKE THE OTHER SCENES TO "SEE IF IT TRAVELS."** Proposed by Boz and **withdrawn the same day on Jacob's scope ruling** — altadena/ksi/centrum are deliberately unfinished, nobody is looking at them, and re-pouring a non-LS scene is the operation with a damage history. **Unproven-and-said-so beats measured-at-that-price.**
      - **Verge's corrections to this board, all verified:** ⛔ **`claims-ring-partition.mjs` EXITS 1 today, not green** — on 3 pre-existing unexplained off-ring tiles (altadena 45/159/501, centrum 234). **Only *"0 double-covered edges"* is the live gate**, and that holds; the baton implied the tool passes. · **`zero-depth` is NOT permanently dead** — D1 recorded 0 firings in 7,632 bids; Verge's first cut fired it once. It is dead **by construction** now, not by luck — ⛔ don't design around its deadness in either direction. · **`built-empty-concrete` ("hollow") went 0 → 29** and survives ① — corners whose takeover is honoured in name only, on rim tiles. Not a new class, but it was at zero before. · **`scratch/tg-instr.mjs` is a FROZEN pre-① copy** still carrying `if (aBase <= 1e-6) continue`; a grep for the old line lands there and reads as live. **Banner added 2026-08-08; ⛔ do not resync it — a forensic's fixture must stay frozen.**
      - ⚠️ **No eye gate was run, deliberately and correctly.** ① is invisible by ruling; eye-gating it would be tuning it. **The A10 street-facing fix that DOES want Jacob's eye is ③, which is not built.**
      - ### ⛔⛔ RUNNING THE GATES — read this BEFORE you touch anything. Two refusals look like failures and are not, and one baseline does not exist on a fresh clone.
        | invocation | what happens | read it as |
        |---|---|---|
        | `node scratch/claims-band-reaches-lu.mjs` | **exits 1** | ✅ **by design** — RED-until-true. Exit 0 means the cure landed. |
        | `node scratch/claims-band-reaches-lu.mjs --inert` | proves the instrumented copy yields byte-identical FILL | **re-run it after ANY `tileGround.js` edit** |
        | `claims-corner-delta.mjs --selftest` *(no name)* | **exits 2**, *"needs a baseline"* | ⚠️ **usage, not failure.** Two-step: `--save <name>` then `--selftest <name>` |
        | `claims-corner-delta.mjs --save <name>` | **STOPS** on LS pre-reset (48/79 slots) | ⚠️ **deliberate** — it refuses to baseline a stale fixture silently. Re-run with **`--accept-partial`** to proceed knowingly |
        | `node scratch/claims-ring-partition.mjs` | **exits 1** | ⚠️ on **3 pre-existing unexplained off-ring tiles** (altadena 45/159/501, centrum 234). **Only *"0 double-covered edges"* is the live gate** — that holds |
        ⛔⛔ **THE BASELINE IS NOT IN GIT.** `scratch/.d2-*.json` is **gitignored** (they are ~1.8 MB derivatives), so **`a10-post-rim` does not exist on a fresh clone** and will not survive a `scratch` clean. Regenerate: **`node scratch/claims-corner-delta.mjs --save a10-post-rim --accept-partial`**. ⭐⭐ **AND REGENERATE IT *BEFORE* YOU TOUCH THE CURE — a baseline captured from half-built code has the cure baked into it and will score its own defects as "unchanged."** *(`feedback_verify_the_baseline_before_comparing_to_it` — a baseline you can't verify isn't one.)*
        ⚠️ **`scratch/tg-instr.mjs` is a FROZEN pre-`073ea0c5` copy** of `tileGround` (a Survey forensic's fixture) and still contains `if (aBase <= 1e-6) continue`. **A grep for that line lands there and reads as live source.** It carries a banner; ⛔ do not resync it.
    - ▶ **NEXT:** ⭐ **③'s standup — the stamp** (`A15`, `project_polygon_must_ask_the_stamp`) · then build the cure, ⛔ no re-key, no parity gate · then **separately, `A0` for the bulbs.**
  - ⭐ **Cure: honour doctrine 4 — ring ownership is a PARTITION.** Every point of `fullBand` belongs to exactly one run or corner; a corner with no fillet still owns its arc (invariant 3: *the ADA pad is a band-slice, not predicated on the arc — works square OR round*). The one-owner path (`:1321`) is that partition already working where there is nothing to partition. ⛔ **Preserves:** the **mono-width envelope** (`cw + max(TL) + max(SW)` over the tile's edges, `:1286`) and the width adjustment riding it — deeper grows the whole block's band, shallower routes its residual to LU (`:1511`), so **the partition is purely along the ring, never into the envelope** · the bent corner as a band slice · the inside/outside swap + slope-joiner · **`jtMiter`, never `jtRound`**.
  - ⭐ **Clamps are already settled (2026-05-30) — don't re-open them.** `§6.9`.5's *"self-intersection is signal, not error"* governs **meaningful** degeneracy (authored R shrinking the pad — no clamp, the authored R is the design control). It does not govern **meaningless** degeneracy — `W` past the medial axis inverts the offset and floods the block interior — which needs a **topological capacity guard, not a doctrine clamp**; that is what `:1198`/`:3601` already are. Test: coherent-but-smaller output = regime 1, inverted/reversed-winding = regime 2. `[[feedback_no_corner_radius_clamps_in_emit]]`. **L** · → `SECTION §7`.
