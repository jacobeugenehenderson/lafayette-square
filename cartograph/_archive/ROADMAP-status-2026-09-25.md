# ROADMAP status block — 2026-09-25 (superseded 2026-09-26)

Moved out of `ROADMAP.md` by Boz on 2026-09-26. Retired for currency, not truth.

- **⏱ WHERE THE WORK STANDS — 2026-09-25** *(Boz. Every item is LANDED, IN FLIGHT with an owner, AWAITING JACOB, or PARKED with its home. ⛔ Nothing is pushed or deployed from this repo; Operations' Legal Phases 1–2 are deployed in `theward-operations`.)* The superseded 09-23/24 block: `cartograph/_archive/ROADMAP-H3-and-status-2026-09-24.md`.
  - ✅ **LANDED (the commit log holds the list; the canon holds the truth):** highways `H-3` · the new ② curb (① minus depth bands; width steps taper) and the ① containment label rule · ground F1–F3, terrain normals everywhere, anchors that know their heightfield · **Provincetown poured and baked end to end 2026-09-25** (radius 5290, datum = the sea, revetment + coast-distance written) · the sea as water, terrain + lidar in every pour, a pour that STOPS on a false map (`verifiedAbsent` is the way past), the revetment's three-outcome gate · the Bake: watches its own code, asks before a code-driven re-pour, shows every step, cancels by name, never killed on a clock · intake: scene named from the Name field, parcels by declaration, one OSM feature per id, `node scripts/bake-in-flight.mjs` · LS-fallback sites (`BRIEF-ls-bleed-excision`) · legal declared per installation · the surface lab, dunes as a land use, one surface table, the coast-distance channel · tree cards framed from the ground · Legal Ops Phases 1–2 (`BRIEF-legal-operations`) · **2026-09-25:** parcels vote land use only (`e5a46bd3`) · a face takes the land use of what COVERS it and a hole is not its outer (`dc0c264f`; ⚠️ LS's recreation→residential ×18 and parking→residential ×22 landed unverified by eye) · the coast trusts the lidar and every bake says how much shore it covers (`r-coast-trust-the-lidar`, `d324c27d`) · LS trees re-seated on today's terrain · ③ sky holes closed, the ped bands a partition (`26e9e682`; one huron island hole OPEN).
  - 🔧 **IN FLIGHT:** **Wellhead** — the radius has one home + Extent's Bake applies it, fails loudly and rolls back consistently; parcels feed land use only, `(b)` · **Plumb** — ② round-3 corners (the c4 acute tip; worktree), the first-round paint that ASKS what each frontage is (`SECTION §4`, `node scratch/walk-data-coverage.mjs`), ① Provincetown pier needle (`BRIEF-junction-shape-where-the-offset-crosses`) · **Gantry** (review) — Revetment's proposal that ink caps carry no leg's identity, kit-wide · **Marram** — the dune surface (`BRIEF-surface-lab`; blocked on `q-dry-sand-repose-angle` + `q-beach-band-width` `[U]`, and on sand faces reaching the land-use vote) · **Gantry** — the two branches above.
  - 👁️ **AWAITING JACOB:** the Grove re-capture (huron, LS) · water clarity (the Water Quality Portal as a source; unmeasured water = (a)?) · HPDM scratch pour (Gantry's window) · Legal Phase 3 · Plumb's two: ②'s one corner conditional (miter where the lines meet inside the corner, else the chord — RIBBONS records his "no conditional, the miter, full stop") and ③'s partition proposal (`SECTION`) · re-fetch huron + Provincetown (duplicates) · huron re-pour after `(b)`.
  - 🅿️ **PARKED, with homes:** Stage ⌥-drag — the fix is verified only on synthetic pointers; first move is to record one real stylus drag (`58117ff6`) · LS + HPDM `A19` pours, held for the roundabout brief · shore `natural=scree` = armour awaits Jacob (`q-shore-soft-tag-vs-measured-bank`) · research gaps → `node checks/claims-references-are-sound.mjs` (dispatch queue) · sidewalk default ruled, unbuilt (`r-sidewalk-default-rule`) · structures over water (piers, the Breakwater) unbuilt · the fetch cap bounds AREA, not what the fetch returns · Legal: OH/MA terms (`C1a`) · Provincetown's parcel well (MassGIS) awaits confirmation into `sources.json` · HPDM's 9 m unapplied radius draft, cause not established · the tree mask reads `map.json`'s undrawn legacy `pavement` layer (`BRIEF-ls-bleed-excision §3`) · the terrain-normals frame cost (4 extra terrain samples per ground vertex) is unmeasured against `H-9`'s budget · huron's lamps carry no `groundRaw`, so they sit on the smooth field (measured, not fixed) · **two place defects (Tern, 2026-09-25):** buildings MSBF lacks but OSM has (Provincetown's Crown & Anchor, Library, Red Inn, Visitor Center, the monument tower…) are simply absent from the slab while bake-content prints them "correctly absent"; and `buildBaseListings` / `applyListingOverrides` snap a listing to the nearest building (≤25 m / ≤60 m) with no report, so a card lands on the neighbour · **deploy plan owed before any push**.
