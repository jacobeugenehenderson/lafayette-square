# ROADMAP — H-3 and the 2026-09-23/24 status block, as they stood before the 2026-09-25 conformance

*Moved to the Diary 2026-09-25 by Boz. Retired for CURRENCY, not truth: H-3 is built and the status block was superseded. The live versions are in `ROADMAP.md` (H-3 and "WHERE THE WORK STANDS").*

- **H-3 · HIGHWAYS ARE NOT BUILT — they are a flat stroke beside ①, fed by a constant.** *(Talked through with Jacob 2026-09-23; the rulings are his.)* ▶ **BUILD brief: `docs/briefs/BRIEF-highway-build.md`** (`3145d68b`) — every value cited to `references/`, seven checks, acceptance = combing the highway teeth. The design review it came from is archived at `cartograph/_archive/BRIEF-highway-positive-object-SUPERSEDED-2026-09-23.md`. ⛔ **Huron's centre-of-map "highway withers into the grid" is NOT this item — it is `A19`** (US 6 continues as an unnamed at-grade `primary`, dropped to `paths[]`).
  - **Ruled:** scope = the `gradeSeparated` class first · *the roadway IS the positive object*, built as designed (alignment + typical section, lanes from the data), not by ①'s negative-space swap · **no authoring, no geometry access** — so a missing input **fails loudly** · no curb, no sidewalk on the highway.
  - ⭐ **Ruled — VERGE (2026-09-23):** land bounded **entirely** by highway (between carriageways, interchange infields) is **verge** — grass, no curb, no sidewalk, no LU choice. Land bounded **partly** by highway is a block with a **bare** highway-facing edge. ⚠️ **Unbuilt**, and it supersedes `PIPELINE §5`'s 2026-09-06 item for all-highway regions only (that item ended *holes*; a verge ends them too) — **amend `PIPELINE §5` when the build lands, not before.**
  - ⭐ **A THIRD SURFACE — the ramp-terminal concrete** *(Jacob, 2026-09-23: "never really gotten those streets right because it's all embedded in the concrete built environment that is neither street nor highway — the concrete the signal lights sit on")*. Paved island / channelisation: not street, highway, verge or block. OSM barely maps it (`area:highway=traffic_island`: LS 1 · HPDM 8 · huron 0), so it must be **derived** as the leftover between the highway object and the town's curbs at an at-grade terminal — ⛔ under the verge rule alone it would stay a *block*. Put to the plan brief as Q8 (by message). LS's South 18th stub sits in it.
  - ✅ **Ruled (Jacob, 2026-09-23) — how the third surface is detected:** a ramp-end region becomes junction residual (JR) by **frontage**, and frontage counts the **RAW intake footprints** (`raw/msbf.json`), ⛔ **never the shown/member set** — a building the operator hid at the hood's edge still fronts its street (Jacob: *"that doesn't mean they're suddenly sitting on concrete"*; Layer 0 q3). Its check swaps in the shown set and must flip a face. **No park guard** (dropped: `lu=park` is the phantom-park class and `underived` on most candidates). **Parcels** ("no parcel → road land") are **ruled to be MEASURED** on LS's 28 terminal faces + huron before adoption, framed as the pour declaring its frontage method per town. Every JR is listed by name in the pour.
  - ⛔ **NAIP SPIKE: DON'T BUILD (2026-09-23, `c567b771`).** Off-the-shelf models on NAIP cannot see the ramp-end concrete: the Chesapeake paved model called **0.0–0.6%** of known-paved Jefferson highway paved; the road model swung **11.8% → 54.4%** by flight; neither saw an island. Second day on zero-shot SAM **declined** (Jacob). ⇒ the third surface is **derived from geometry** (the plan's Q8), not from imagery. ▶ `scratch/naip-spike/`. ⛔ **The AASHTO Green Book is OUT — its terms forbid "the entry of AASHTO content into any AI tool"** (Jacob, 2026-09-23), so it cannot be what agents cite. The plan's freeway section values stay **[U], uncorroborated in the corpus**, until a **state DOT manual** (the road's own state) or FHWA's own text is cleared and read. ▶ **`references/registry.json`** (rules: `references/README.md`).
  - ✅ **Ruled (Jacob, 2026-09-24) — DIVIDED HIGHWAYS:** the divided-street pass (`derive.js` `innerEdgeAssign`) flattens a divided highway to a symmetric section anchored at its inner edge. **(B) now:** left untouched, each flattened highway printed by name (huron has none). **(A) AT LS'S POUR, not before:** one guard so highway-class chains skip `innerEdgeAssign` and its anchor stamp — measured blast radius LS 14 · HPDM 24 · altadena 2 chains. ⛔ LS's I-44 is drawn slightly wrong until (A) lands.
  - **LS's highways are a known source of visual bugs** — faked for now, ⛔ **owed this fix; the fake is not the finish.** Huron shows first; LS's highway defects are noted as the work passes, not chased.
  - ▶ **Next:** a graphics specialist hammers the plan brief → Jacob rules → a build brief. Nothing is off limits, but ① and the divided/median machinery are **not volunteered** unless the plan proves them germane. M · → `H-2`/`H-4` (water: the same "a boundary that is an object" shape).

- **⏱ WHERE THE 2026-09-23/24 WORK STANDS** *(Boz, end of the two-day push — every item is LANDED, IN FLIGHT with an owner, or PARKED with its home. Nothing is pushed or deployed.)*
  - ✅ **LANDED:** `A19` unnamed half (huron) `9042204a` · expressway = no ped realm `911d4103` · closed-loop stripe keeps its hole (the yellow disc) `b757717c` · the ground stretches, not breaks (`H-21`, huron re-baked) `2e7e114e` · revetment crest read one grid step landward `49ccd193`/`e869230c` · the research database `references/` (sources · questions · findings · rulings; `node checks/claims-references-are-sound.mjs`) · highway build steps 0–3 (`H-3`: ref + weld `98c620fc`, section from lanes `660c24cb`, H `4735926e`, one edge `9a569df1`) · listing identity sealed (`H-29` half) `8943d30d` + LS listings consolidated `367eed83` + hijacked LS links `eba2d554`.
  - 🔧 **IN FLIGHT (owner):** highway step 4 — verge / JR / gore classifier (**Gantry**; then Jacob's Bake + eye) · **Provincetown** — first MA pour, Jacob's hand; Gantry reports its highway lines.
  - 🅿️ **PARKED — Stage ⌥-drag (Hero/Street, `OrbitControlsShot` in `CartographApp.jsx`).** Code now: ⌥ is a clamp — the press is stopped in the window capture phase so OrbitControls never sees it, and a map-style grab slides camera + target across the horizontal plane through `target` (ground point under the pen stays under it; height/look held). ⌥ is read off the press OR from keydown. Verified only with SYNTHETIC pointers (mouse/pen/touch types). ⛔ **On Jacob's real stylus it still read as "tumble + zoom" and the cause was never observed:** an input recorder got **0 events** before the work was parked, so which events his tablet sends (pointerType, altKey, wheel/pinch) is unknown. ▶ First move for whoever resumes: record one real ⌥-stylus drag. Commits `fbea5045` · `4b6a7d00` · `ef5c90dc` · `76815c88`.
  - ⏸ **PARKED, with its home:**
    - **LS + HPDM A19 pours** — held for a **roundabout brief (OWED, unwritten)**; HPDM also re-bakes its 3 fence/wall discs and its pre-① `shape.json` then. → `A19`.
    - **Divided highways (A)** — skip `innerEdgeAssign` for highway chains, lands WITH LS's pour. → `H-3`.
    - **Forest Park Parkway** (HPDM, 41 `expressway=yes` at-grade ways) would lose its walk on HPDM's pour — **Jacob's ruling owed**.
    - **Shore:** Revetment's blanket "measured bank beats soft land use" was **DROPPED on NAIP evidence** (real beach at 906,−287); its narrower **"`natural=scree` at the shore = armour"** (49 of 124 disputed stations) **awaits Jacob**. ⚠️ `shore-armour.mjs` `containing()` (soft land use) has **no distance bound** — a big park can bare a shore from inside it; not yet biting. → `BRIEF-boulder-revetment.md`, `q-shore-soft-tag-vs-measured-bank`.
    - **Research gaps** no permitted source answers: island paint-vs-raised size · physical gore position · cul-de-sac island treatment (huron's tiny courts paint sidewalk). MassDOT *Roundabout Guidelines* + a clean re-save of PDDG Ch 3 would help. → `references/` dispatch queue.
    - **Sidewalk default** is RULED (`r-sidewalk-default-rule`) but the code still paints `ADA_SIDEWALK`; a small build after the highways (touches every town's ped band).
    - **Provenance** (`A23`) · the two pre-existing red gates (`A19`) · **48 pre-existing safe-suite reds** (reported by the listing work) — the suite is not a usable green signal today.
    - **Provincetown's bake died on the ground mesh — cause MEASURED (Gantry):** the whole 157 km² disc, Cape Cod Bay included, is ONE land-use face `beach` (the bay is not water — `H-2`), and with NO terrain (the Extent pour ran `--skip-elevation`) the soft fill refines to 64 m → 10.3M sliver triangles → `groundConformity.js` `Map` past 2^24. Shrinking the disc does not help until a re-pour. ⭐ Ruled (Jacob): **F3** no terrain → no refinement (derived) + **F2** conformity maps that cannot hit a size cap — both byte-identical on huron — **owner Gantry**; **F1** longest-edge bisection FILED (changes huron's mesh; own gates). **Assigned to Revetment:** why the bay (and 18 water features) never reach the ground as water, and why the pour gets no terrain — forensic first, kit fix, not a Provincetown one-off.
    - ✅ **Ruled (Jacob, 2026-09-24): terrain + lidar are part of every fetch/pour** — the Extent fetch acquires the town's DEM (National Map, 1 m lidar first) and the pour bakes it; no coverage → a LOUD failure, never a silent flat town. Until now terrain was an elective, hand-run step (huron's `elevation-sources.txt` was written by hand). **The sea is water too:** `coastline.mjs` only drew a water feature that was `clipped` (a lake cut by the fetch — huron's Erie), so an ocean mapped as open `natural=coastline` never drew; the fix closes coastline chains against the bb on the water side (Jacob's H-4 ruling applied to seas). Owner: **Revetment**. ⛔ Provincetown re-pours only after BOTH land (terrain turns refinement back on).
    - **NEW CLASS — structures over water** (Provincetown, 2026-09-24): piers (70 footway · 4 service · MacMillan Wharf, Fishermen's Wharf) and the stone **Breakwater** (`man_made=breakwater` + a footway on it). A path/road on a structure, water both sides — **the structure is the positive object** (deck at its width, water under it; the breakwater is STONE — reuse the revetment's rock). Today a service pier may bound blocks over water. Unbuilt; not scoped. → `H-2`/`H-4` (water), `H-3` (positive object).
    - **The fetch cap is in the wrong unit** (Jacob, 2026-09-24): `MAX_FETCH_KM2` bounds AREA, but load is what the fetch RETURNS — water carries no geometry (Provincetown's ZIP envelope 312 km², mostly bay). Raised 200 → 400 as the unblock; the kit fix is a bound on returned features/bytes (terrain is the one area-proportional part). ⛔ A coastal town's search envelope also includes water — size it to the land.
    - **Deploy:** nothing from these two days is pushed; `staging.yml` is gone and `deploy.yml` deploys `main`. A deploy plan (what ships together, LS verification after) is owed before any push. Phase 1/2 of the backend genericizing → the listing agent's handoff.


## H-4 sub-bullet retired 2026-09-25

  - ⛔⛔ **MY BRIEF WAS WRONG ABOUT THE SEAM AND STRAND SETTLED IT AGAINST ME.** I wrote that `mintProtopolygon`'s `boundary` param means ① already models a non-street boundary edge. It does not: `derive.js:5113` passes the **DISC**, and `tileGround.js:547` rules *"⭐⭐⭐ THE CIRCLE IS THE STENCIL, NOT A CHAIN"*, citing `RIBBONS §1`'s *"THE RIM BOUNDS, IT DOES NOT OWN."* ⚠️ Also: the **live** path passes no boundary at all; only prebake does.
